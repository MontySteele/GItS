# Dispatch — 2026-08-06(e): six replies execute

> **Lifecycle: REFERENCE once landed.** Replies verbatim from chat. Zero new
> design authority beyond the answers. House style applies: verbatim verdicts
> land in the ledger before anything else; strike queue rows with the verbatim
> reply attached; nothing inside a frozen record is rewritten (R101b); suite
> green at every boundary.

The dispatch as delivered, reproduced verbatim below the rule. It is the
authority for **R121** (`tier0/DECISIONS.md`), which routes each reply to the
track that executes it.

---

Dispatch — 2026-08-06(e): six replies execute

Lifecycle: REFERENCE once landed. Replies verbatim from chat.

Q18 — "agreed, countersigned, tto quarantined." The re-registration converts from DRAFT to the sprint's registration, pinned DRAFTER 14, predictions still blank by design. Execution order, strictly: (1) the tto columns join the RA-G1/RA-G2 quarantine under the same banner; (2) at sprint kickoff, predictions are authored design-side and appended to the registration as their own commit — before any measurement runs; (3) the sprint runs under D14; (4) blind-first grading; (5) staged D15 (EB-43) lands with its re-baseline; (6) the quarantine — now including tto — lifts on the graded read. No step reorders.

Q19 — "let's shield it, plus a note for any future sim work to take a look and figure out what went wrong (why did winrate go down, basically)." SHIELD executes: score_offer's core-advance bonus is made blind to the anchor arm's tags (instrumentation continues to read them); the ref_ironclad arm alone is re-measured under the same recipe; the quotable table's row is republished with a dated note. Mint the investigation row verbatim in the engineering backlog: why did tag-visible scoring lower the anchor's winrate (11.13% → 7.50%)? — a diagnosis question for future sim work, no deadline, no design authority. If the shielded re-measurement does not restore the archived ordering, stop and surface — that would mean the mover wasn't the tags.

Q20 — "agreed, MOVE-WITH-RESOLVER." Track R-B resumes: the 45 held ledger-cited files move to docs/archive/ verbatim-under-banner; registry/identifiers.md gains the old-path → new-path resolver table; the citation lint learns to resolve ledger paths through it (a stale path that resolves is green; one that doesn't is a failure). Ledger bytes untouched, per rail 1. Re-run the charter's acceptance count when done.

R59 — "superceded but flag it for future design discussions (SHOULD it be rarity limited to avoid serving up crap cards, or is it fine to offer commons?) - this might need empirical data during a future round on the companion cards." Sidecar: SUPERSEDED-BY:R116, scope limited to the slot-2 floor clause, [USER] verbatim in the evidence column. The design question is minted onto the S4-G10/M11 shop close-out agenda (its natural home): should slot 2 carry a rarity floor at all — with the note that the answer wants empirical data (Common offer/pick/skip rates) from a future companion-card measurement round, not a priori ruling.

R103 — "OPERATIVE-NARROWED." Sidecar updated with the narrowed scope as briefed: R103(b)'s gate still binds any future selector-recorded fanfare trace; the escrow strike stands unaffected (different question, different instrument). No re-litigation of R113.

R107 — "AMENDED-BY:R118." Sidecar updated, scope "fence target only." R90's banner and R118's re-point are now formally reconciled as amendment, zero behavioural difference.

Queue hygiene: the three DOUBT rows resolve and the G9-riding queue row closes (G9's sitting shrinks by one); Q18/Q19/Q20 strike verbatim; regenerate the status digest (CI fails stale); suite green at every boundary.

---

## Routing, recorded here so the tracks are legible

Nothing below adds to the dispatch; it names who executes what.

| Reply | Paper (this track) | Work (follow-up tracks) |
|---|---|---|
| `Q18` | Registration converts DRAFT → SIGNED/COUNTERSIGNED in place; step (1) executes — the `tto` columns join the existing `RA-G1`/`RA-G2` quarantine banner as a dated addendum | Steps (2)–(6) are future sprint steps: predictions at kickoff, the D14 run, blind-first grading, staged D15 (`EB-43`), the quarantine lift |
| `Q19` | The investigation row is minted verbatim as `EB-46`; the queue row strikes with the reply | The SHIELD code change, the `ref_ironclad` re-measurement and the republished table row are a separate track |
| `Q20` | The queue row strikes with the reply | The 45 file moves, the resolver table and the citation-lint change are Track R-B's resumption |
| `R59` | Sidecar → `SUPERSEDED-BY:R116` (slot-2 floor clause only); the design question minted on the `S4-G10`/`M11` shop close-out agenda with the reply verbatim | The empirical round (Common offer/pick/skip rates) is future measurement work, unscheduled |
| `R103` | Sidecar → `OPERATIVE-NARROWED` with the narrowed scope | — |
| `R107` | Sidecar → `AMENDED-BY:R118`, scope *"fence target only"* | — |

**Not done by this dispatch, stated so no one looks for it:** no number moved,
no measurement ran, no ledger byte inside a frozen entry was rewritten, and the
`S4-G10` shop close-out remains OPEN and [USER]-gated — the agenda gained an
item, not an answer.
