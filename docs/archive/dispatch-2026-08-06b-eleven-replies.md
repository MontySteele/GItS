> **MOVED 2026-08-06 — Clear the Stage, Track R-B resumption (R121 `Q20`, MOVE-WITH-RESOLVER; charter R119, rail 1).**
> Old path: `docs/dispatch-2026-08-06b-eleven-replies.md` — new path: `docs/archive/dispatch-2026-08-06b-eleven-replies.md`.
> Verbatim move: everything below this banner is byte-identical to the
> pre-move file. Live citers repointed in the move commit; ledger and other
> frozen citations keep the old path on purpose (rail 1: ledger bytes are
> never rewritten) and resolve through the moved-path resolver table,
> `docs/registry/identifiers.md` §17. Per-file map:
> `review/stage-clear/rb-move-manifest.tsv`.

# Dispatch — sitting batch 2026-08-06(b): eleven replies execute

> **Lifecycle: REFERENCE once landed.** Replies are [USER]'s, verbatim, from
> chat. Zero new design authority beyond the answers. House style: strike
> queue rows with the verbatim reply attached; suite green at every boundary;
> staged-never-landed for anything not answered here.

## §1 one-worders

**Q7 — "Yes."** Land the drafted YES form: six axes closed permanently as
informational (numbers stay publishable as description, never acceptance
targets); Fanfare axis survives as the whole of `S4-G5`/`B-G1`; re-point the
R107/F1 fence to the narrowed gate. **Unfenced by this stroke:** Furina
backlog items 1–3 and the fanfare STOP (`S4-G7`).

**Q11 — "Countersign."** Probe E (corpse detonation, scripted) converts from
paper to work. Run under its own registration: two tells, negative-control
arm, nine confounders, cost ceiling, stop-and-re-register tripwire. The
table check survives as fallback until the probe reports. `S4-G15` remains
open until either answers.

**Q9 — "Yes."** Mod-side kit-exclusion fix for Encore Performance's copy
pool lands; game now matches sheet and sim. Blast radius: one card's copy
pool; no sim number, no test flip expected — if one flips, stop and surface.

**Q10 — "Neither - leave it alone."** Verbatim rationale: *"I recognize that
card (it's from an event) - it's not possible to duplicate in normal play,
so this whole concern is a false positive."* Land the drafted NEITHER form
with this ecosystem fact attached as the reason. Consequences:
- `curse_poor_sleep` keeps both typings; a single stuck copy is accepted
  behaviour.
- Annotate `S13-X14` in the exploit ledger: multi-copy jam is a false
  positive per [USER] ecosystem ruling (verbatim above).
- **Verification task, surface-only:** check what X14's recipe actually uses
  to acquire multiple copies. If it reaches ten copies through normal-play
  acquisition (no scripted/contrived vector), that contradicts the
  false-positive premise — come back with the finding as a question; do not
  bury it and do not re-litigate on your own.
- BETA (status-beats-retain law) was declined, not deferred. If a future
  card is typed status+retain, that is a new row, not a re-ask.

**Q6 — "Repair."** Drop the `+` from `furina_salon_3_gala_bow_storm`'s
upgraded set; re-verify at the card's now-printed 0 cost; pin returns from
SKIP to a live verdict. X6 mechanism remains R111's watch item.

**Q5 — "Playtest."** The default stands confirmed: `OT-1` on the next Kokomi
protocol playtest is the fork's venue and its only accepted input. Strike
the POOL-REWORK alternative with the reply attached.

**Q16 — "Condition."** `SHOP_COMPANION_RARITY_ODDS` renormalizes over the
≥Uncommon pool; both engines; no new numbers stated. Land inside the v6
window with the rest of NC-10's completion, before the re-baseline sweep.

**Animation — "Let's do FREE-SPIKE and reconsider if the results
disappoint."** Path B (Skeleton2D) spike becomes a normal Code sprint:
Kokomi pilot, computed-weights-from-layer-masks idea attached, Path C
layered remains the shipped fallback throughout. **Record the reconsider
trigger verbatim** — disappointing spike results re-open the Spine licence
question ($379 Pro; Essential cannot author meshes) without a new sitting.
The "animation path session" leaves the design queue.

## §2 short picks

**10.2 — "Yes, and rider yes."** The roster-anchor v14 n=3000 table is
designated the quotable standing table — **as re-produced under v6**: the
designation names the recipe and its v6 re-run, not the archived v5 read.
Rider: `ref_ironclad`'s `archetype_package` gains `Card.archetypes` tags.
[USER]'s stated reason, on record: the tags are needed so the instrumentation
reads the anchor properly — confirmed correct; without them the
core-attainment/payoff-reach columns cannot see the anchor at all.

**10.7 — "Let's yes to both and see if it turns up on local."** (a)
Search-and-repair authorized for the payoff-reach/`RARITY_ODDS` registration
— sweep local worktrees first per [USER]'s note, then all branches again.
(b) `RA-G1`/`RA-G2` core-attainment columns are QUARANTINED until the
document is found or re-established. If truly lost: re-register **before**
any new number is read, never retro-fit a registration to existing reads.

**10.8 — ruled by principle.** Verbatim: *"This is a prior ruling that seems
to have gotten twisted. A character may be playable and still have a
companion card."* Execution: **amend R88** — strike the exclusivity clause
("reserved for a playable slot may not appear as a companion"); playability
and companion-card existence are compatible, full stop. Consequences to
record:
- The Neuvillette contradiction dissolves; R52 needs no amendment.
- The four Fontaine Burst-name reservations stop being rule-enforced; if a
  future card wants one of those names, that is a taste question then, not a
  rule violation now.
- R108's Itto addendum stands unchanged; its "released to the companion
  pool" justification is superseded by the cleaner principle (release was
  never needed). Dated note on the addendum, nothing re-decided.

## §3 paperwork trio — "correct, correct, repoint"

(1) S15 pin-table headline and per-module rows corrected to the counted 133.
(2) S8 gallery header corrected to 51. (3) `review/enemy-dossiers` branch
pointer repointed to `ec15028` + `28759f0`.

## Queue hygiene on landing

Strike every answered row with its verbatim reply; refresh
`known-identifiers.tsv` if anything new is minted; run the open-item
placement lint; suite green before and after. Version-stamp note: Q16 and
10.2's re-run both live inside the v6 window — nothing quotable until the
sweep is green.
