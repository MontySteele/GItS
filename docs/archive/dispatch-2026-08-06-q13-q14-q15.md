> **MOVED 2026-08-06 — Clear the Stage, Track R-B resumption (R121 `Q20`, MOVE-WITH-RESOLVER; charter R119, rail 1).**
> Old path: `docs/dispatch-2026-08-06-q13-q14-q15.md` — new path: `docs/archive/dispatch-2026-08-06-q13-q14-q15.md`.
> Verbatim move: everything below this banner is byte-identical to the
> pre-move file. Live citers repointed in the move commit; ledger and other
> frozen citations keep the old path on purpose (rail 1: ledger bytes are
> never rewritten) and resolve through the moved-path resolver table,
> `docs/registry/identifiers.md` §17. Per-file map:
> `review/stage-clear/rb-move-manifest.tsv`.

# Dispatch — three replies execute (Q13, Q14, Q15)

> **Lifecycle: REFERENCE once landed.** Drafted 2026-08-06 in chat; the replies
> below are [USER]'s, verbatim, from the same channel. Zero new design authority
> beyond the three answers. House style applies: strike queue rows with the
> verbatim reply attached, suite green at every boundary, one variable per
> measurement window.

**[USER], verbatim (2026-08-06):**
> *"14) Yes · 13) I'd say A · 15) Widen"*

---

## Item 1 — Q14: YES, the world stamp goes up

- `CONSTANTS_VERSION` **5 → 6**.
- Archive banners on every published number below the stamp, **where it is
  published** — nothing rewritten (R101b). The affected surfaces are the ones
  the batch itself named: all pre-batch combat numbers (Frozen rules changed)
  and the tier-0.5 shop maths in both engines (`NC-10`).
- **The rider is NOT answered by this reply and stays open.** Register it as
  the next Q row in `registry/user-queue.md`:
  - Ask (plain): inside the shop's new Uncommon-or-higher pool, do the
    existing `SHOP_COMPANION_RARITY_ODDS` simply renormalize over ≥Uncommon,
    or does [USER] state a fresh split?
  - Reply shape: **CONDITION / FRESH-SPLIT**.
  - Not to be built against until answered. (Chat's recommendation, carried
    into the agenda: CONDITION — it is the one-variable reading; a fresh
    split is a shop-tuning decision and deserves its own window.)

## Item 2 — Q13: α (minions only)

- Complete the stopped mod half of `NC-7`: in boss rooms, a creature carrying
  the game's `MinionPower` gets Frozen; every other creature in the room gets
  the Vulnerable substitution. Per the execution note, this is the only
  per-creature "secondary enemy" concept in the assembly — no roster file.
- **Sim alignment (parity, not design):** tier0 currently substitutes per the
  authored `is_boss` fact, under which boss-room helpers freeze. Under α the
  freezable set is *minion-flagged creatures only*. Align the sim predicate to
  α semantics so both engines answer the same question the same way, and add a
  parity vector for a boss room with (a) a minion and (b) a non-minion helper.
- **Required annotation on R116** (and mirror on the `NC-7` execution note in
  `review/parity-sweep/noncard-triage-memo.md`): the ruling's stated
  consequence — Kaiser Crab's second claw remaining freezable — is
  **overridden deliberately** by [USER]'s α selection, verbatim *"I'd say A"*,
  2026-08-06. Under α the second claw takes Vulnerable, not Frozen. This is a
  chosen reading, not a missed example.
- Chat flagged the R116 contradiction to [USER] before drafting this dispatch;
  the α selection stands with that consequence stated. No re-litigation.

## Item 3 — Q15: WIDEN

- The citation lint's sweep extends to `tools/*.py`.
- Repair the three occurrences in `tools/lint_role_tempo_coverage.py` to
  satisfy it. Blast radius per the queue row: comments and docstrings only —
  no behaviour, no number, no test.

---

## Ordering and the measurement window

All three items land inside the same v6 window, **before any re-measurement**:
the α scope change is itself a Frozen-rules change, so v6's meaning is
"Frozen unified + α boss-room scope + shop-slot spec" as one batch boundary.
Do not run the v6 re-baseline sweep, and do not quote any new combat or shop
number, until items 1 and 2 are both green. Item 3 is orthogonal and may land
in any order.

Queue hygiene on landing: strike Q13/Q14/Q15 with the verbatim reply, mint the
rider row, refresh `known-identifiers.tsv` if any new identifier is introduced,
and run the open-item placement lint.
