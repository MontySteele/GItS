# Countersign Package — Track B / Validation Gate (Seven Items)

**Filed verbatim, as signed.** House convention for a signed package (cf.
`docs/understudy-countersign-2026-08-04.md`,
`docs/axis-validity-countersign-2026-08-04.md`,
`docs/a2-gate-ratification-2026-07-27.md`): the text [USER] signed is committed
unedited, and the repo's own commentary lives elsewhere — the R-numbers in
`tier0/DECISIONS.md` (**R99** and **R100**), the execution record in
`docs/sprint-track-b-gate-log-2026-08-05.md`. Nothing below this heading was
written by Code.

---

# Countersign Package — Track B / Validation Gate (Seven Items)

Date: 2026-08-04 (late). One sitting closes the local agent's gate
package. Read, amend anything, sign at the bottom. R-numbers on landing.

---

**1. Build 0.2-289 — KEEP.** Package the zip for co-op partners; the
next table session runs it. Courtesy requirement: [USER] tells the
table the build writes local telemetry (their machines, their files,
read-only hook) before the session, not after.

**2. Punch Off crash — ROUTED to the animation stream, status
SUSPECTED-OURS.** "Signal already connected" with the animation-router
patch on the stack reads as our patch double-connecting on event-screen
re-entry; note that validation attempt #1 also crashed inside Punch
Off. Seed 8B97LMCL2F becomes the regression case. If it proves
game-side, the routing note flips and nothing is lost. Recorded as the
apparatus's first suspected mod-side catch — the validation headline
("zero attributable to the build") gains an asterisk with a date.

**3. Harness defects 13/14 — routing ACCEPTED.** Traversal-class per
debt #1, filed with reproduction, deliberately unfixed. Next traversal
pass owns them.

**4. Fanfare deck-intent — BOTH options, in order.**
(a) The one-line-per-session declaration ships now: a human session may
declare an archetype intent, and B2 cuts by it. Cheap, immediate.
(b) The substantive fix: an **archetype-committed draft arm** — a
flagged, seeded policy variant that drafts with declared-archetype
priority (Fanfare, Salon, others as needed). One variable per window:
the flag is the only difference from baseline soaks. This single
instrument serves two open questions at once:
  - N committed-Fanfare runs make the early-half Fanfare claim
    gradeable against a deck that actually is one.
  - N committed-Salon runs re-measure fill time with the
    bot-doesn't-build-salons confound removed. The current result
    (0/56 at cap, median peak 1 of 3) is recorded as AMBIGUOUS —
    design finding vs policy gap — and the R91/2b revisit stays open
    until the committed arm reports.
Scope note: this is a policy variant behind a flag, not a policy
change; baseline soak behavior is untouched.

**5. Win visibility — PATCH next pass.** Harmony-patch
EndCombatInternal so the human feed labels outcomes. Fight records
without outcomes cannot separate "survived the spike" from "died to
it," which is the demand curve's whole job. Same pass as item 6a.

**6. Encore visibility — ACCEPTED on bot feed; human hook extended.**
(a) Next pass (with item 5): the C#-side human hook logs Furina's
meters (Encore, Fanfare, Salon count) — CustomResource is directly
readable there. Since the Fanfare LATE-half claim requires Act 3 data,
and Act 3 data is human-feed by construction, this keeps every
registered prediction gradeable without the bridge fork.
(b) The bridge fork (exposing resources on the wire) is P1.5 work,
gated: required before any Furina-meter claim is graded from BOT feed,
not before.

**7. R98 stays MECHANICAL.** Pre-agreed condition met, struck as
agreed. This is the two-tier ledger discipline working; no upgrade.

---

## Signature

- [x] Items 1–3 — COUNTERSIGNED
- [x] Item 4 (a+b, committed-arm scope as written) — COUNTERSIGNED
- [x] Items 5–6 — COUNTERSIGNED
- [x] Item 7 — acknowledged

[USER], 2026-08-04 — signed as written, recorded from review
conversation. Ready for DECISIONS.md and hand-back.
