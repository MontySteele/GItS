# Furina Sheet Red-pen — Salon / Fanfare / Basics / Generic (chat structural pass)

**Date:** 2026-07-20. Scope per R22: the stable worlds only — the
Spotlight list is deliberately NOT red-penned (R16 re-authors it).
Verdict up front: **this is a good sheet.** The R8 conversions are
faithful (Melody of Many Waters as buffer is the law applied to her own
kit), the encore_cost grammar is consistent (playability gate, never
overdraw), no passive accrual exists anywhere, both heals are
Rare+Exhaust, and the theatrical naming has real Dodoco-grade moments
("Blocking Notes" is canon now). Flags below; items marked [USER] are
ecosystem-taste calls.

## Approved as measured
- **All five basics.** The starter lands A1 1.2 / A4 4.4 against
  declaration — the basics are doing exactly their declared job and are
  ratified as a set.
- **Salon commons/uncommons/rares** structurally sound; the
  member-upkeep greed curve (Crabaletta → Full Ensemble → Grand Gala)
  is legible and priced in Encore. Grand Salon's ceiling is what the
  ratified salon A2 band (7.6) already prices.
- **Fanfare threshold family** (Dramatic Entrance 6/9, Showstopper 5/12,
  Thunderous Ovation 3/7): internally consistent grammar — flat base +
  threshold kicker. Fine as a family.

## Flags

1. **Comment/number drift — mechanical sweep requested.**
   hearts_swelling's comment says "Eight points of deferred HP" over an
   `amount: 6` — a tuning-round relic. Ask Code to script a
   numbers-in-comments vs numbers-in-effects consistency check across
   the sheet (cheap lint, catches the whole class).
2. **[USER] commanding_gaze: Weak 1 to ALL enemies at common** (+2
   block, 1 cost). The sheet claims "Spooked parity" — verify against
   the actual base-game card. Mass Weak at common is a strong defensive
   effect if the parity claim is off; single-target or uncommon is the
   fallback. Your base-game call.
3. **[USER] the_sea_is_my_stage (the uncapper): is 4 self-damage
   "nasty"?** Kickoff §4 demands uncappers at nasty setup cost — but
   the blood is itself Fanfare flux, so the cost partially pays the
   archetype it serves. Self-subsidizing "nasty" isn't nasty. Options:
   raise to 6, or leave and let the fanfare-cap sweep re-run under R16
   decide. Your pick.
4. **suffering_for_art free-value note:** 0-cost, 2 self-damage → 3
   Encore = net +1 HP-equivalent plus double flux, for a card. Hot
   Hands precedent covers the shape, but Hot Hands costs 1. Watch in
   fanfare tuning; no change proposed now (it's the archetype's idle
   engine and the cap governs the ceiling).
5. **ebb_and_flow vs audience_participation near-overlap** (net +2
   Encore + draw vs +2 Encore + draw): ebb's spend-1 gate is a real
   early-turn constraint and rings flux both ways — keep both, but they
   are the closest twins on the sheet; if a slot is ever needed, one of
   these is it.
6. **warmup_act's "Crackle parity" comment will go stale** — R10's
   Crackle gains a Spark rider in its measurement window. Not a
   problem (different pools price differently); just update the comment
   when R10 lands so nobody reads a false comp.
7. **universal_revelry × uncapper ceiling:** at raised cap 45, it's
   9+15 AoE at 3 cost — earned (needs the rare uncapper AND sustained
   flux), inside rare-payoff range. Logged as the fanfare ceiling
   watch-item; re-check under the R16 world with the cap re-sweep.
8. **undercurrent (mass hydro at common):** correctly self-watchlisted;
   directive — add it explicitly to the hydro+cryo convergence cell
   alongside rain_of_roses and guest_neuvillette_judgment so the check
   covers all three mass-application sources in one measurement.
9. **house_call (5→8 conditional at 1 cost):** consistent with the
   threshold family and her A7 setup-tax identity; noted for [USER]
   taste only because 8-at-common-for-1 is the sheet's biggest
   conditional number.

## Naming/lore audit (user's v1.7 pass)
Talent/summon names pre-verified per the sheet header (Salon Solitaire
cast, Soloist's Solicitation, Let the People Rejoice). The authored
theatrical names read well and stay on register. Constellation names
are correctly NOT spent on commons — rares are where they'd go, and the
authored rares mostly use kit/identity names instead, which leaves the
constellation namespace free for upgrades per §3.6. Your pass is the
binding one.

## Status
With this red-pen relayed: basics + salon + fanfare + generic are
red-penned pending your [USER] items (2, 3, 9); Spotlight awaits the
R16 re-author; the fanfare-cap and base-mult sweeps re-run in the R16
world. Pass 2's full scope: R16 card work, the graded-encounter EP
instrument (R21), A6 v2 (R18), and the flags above.
