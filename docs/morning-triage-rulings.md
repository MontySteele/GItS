# Morning Triage — Rulings on Findings 21/22, Soak, and M6 Asks 1–4

From chat-Claude, having read triage-execution-report.md, tier05-m6-report.md, and the finding-21/22 + soak commits in full.

## C# findings — both fixes blessed, two lessons codified

**Finding 21 (elite/boss softlock):** root cause analysis is exemplary — a closed character type-switch throwing inside an async combat-end continuation is a nasty class of bug, and the response hit every beat: migrate to the mechanism that already carries the guards (CustomCharacterModel), keep the diagnostic finalizer as a WARN canary rather than a shadow fix, sweep the decompiler for the whole bug class, and confirm the other closed switches have safe fallbacks. The O4 achievement-dilemma retirement is the satisfying kind of resolution: the dilemma dissolved because we'd been off the intended mechanism.
**Finding 22 (reward-draw throw):** clamp-not-remove is right (Sealed Deck stays playable; the fix self-retires at C3 pool size). The self-check audit is the more important half — R3b's bound of 3 with a comment *conceding* the real max was 30 is exactly how a provable softlock passes review, and making rule labels attributable turns the self-check from decoration into instrumentation.
**Codified (add to DECISIONS as a standing rule):** *before building infrastructure or reasoning around a limitation, sweep BaseLib and the decompiled game for an existing solution.* Three hits in one night: the achievement guard, the character-model guards, and RunHistory. The pattern has earned rule status.

**Soak approach: blessed with enthusiasm.** Reading the game's own RunHistory instead of building a telemetry writer is better than what the queue specced — richer data (floor_added_to_deck is an independent check on M6's starter-exclusion, exactly as noted), exact-seed crash repro for free, and the missing-row softlock signature is genuinely elegant: the absence of evidence made into evidence, with the launcher count as the denominator. The scope caveat (AutoSlay validates crashes/coverage, not draft metrics) is correctly drawn and correctly enforced by default.

## Ruling: Burst acquisition → OPTION 1, as kit (sheet v0.5, principles v1.9, ships with this doc)
Your decomposition (5.8% = 79% × 71% × 10%) made this a one-round ruling — the pity null wasn't a failure, it was the experiment that proved the constraint lived elsewhere. Mechanics, precisely: **the Burst is not in the draftable pool and not in the starting deck. When the meter first fills in a combat, it is granted to hand** (Retain per v1.4 governs it there); casting empties the meter; refill re-grants. Genshin-faithful (bursts are kit, charged by play), kills the deck-dilution and draw problems in one move, and makes the burst-accelerator cards readable in 100% of runs. Consequences to execute: rare pool is now 14 draftable; reaction core redefinition (2 appliers + amp payoff — the Burst arrives by charging, so it leaves the assembly definition); re-measure reaction achievability and relevance (your ~56% projection is the expectation); demolition/spark also gain reliable Burst access — watch their bands, and if demolition re-breaks 97 the answer will need to be Burst-energy gain rates, not the splash cap again.

## Ruling: relevance (M6 ask 1 + the finding-22 refinement)
Your definition — **strict advances-the-plan, conditioned on a live plan** — is the right one, and the conditioning catch (unconditioned relevance punished the fastest assembler; 50.2% of demolition screens fell after completion) was a better version of my ruling than my ruling. The 60–70% claim is hereby *revised, not failed*: it was a faith-era number that conflated "advances the plan" with "worth engaging with." New acceptance: **strict-conditioned relevance ≥35% per archetype** — the anti-brick floor the claim was always spiritually about (roughly every other screen advancing your live plan is healthy drafting; every screen advancing it would be autopilot) — with loose "worth engaging" reported alongside as a secondary (expected 60–75, unenforced). Current reads: demolition 46.7 clear, spark 39.4 clear-ish, reaction 35.5 at the line pending its post-Burst re-measure.

## Ruling: finding 3 (assigned loses to adaptive) — three parts
1. **DRAFT_SKIP_THRESHOLD: retune authorized.** The frozen-M5 conflict resolves by versioning, not paralysis: bump a CONSTANTS_VERSION, archive the old snapshots (don't overwrite), re-run every *currently-load-bearing* comparison under the new constant. M5's raw numbers were already superseded by the post-compensator re-measures; what must stay comparable is the current grid, and it will be, to itself.
2. **The 14.5-point selection gap: add the discriminating experiment before any conclusion.** Run a **hybrid policy** — assigned's archetype scorer blended with adaptive's power+synergy term — at matched deck size. If hybrid closes most of the gap, explanation 2 (hand-set weights) wins and it's a tuning pass; if the gap persists, explanation 3 (truncated scaling) is effectively confirmed by elimination, since finding 1 already shows the archetypes pull when power-drafted. Cheap, decisive, worth running before anything else in this section.
3. **The upgrade gap gets solved by design, not by modelling.** The deep insight in your explanation 3 is that this isn't sim infidelity — *our sheet has no upgrade paths at all*. That's v1 design debt, and the actual mod needs upgrades anyway (rest-site smithing is core StS). So the M7-scope modelling question converts into scheduled design work: **the Upgrade Pass is the next chat-side design phase** (user + me, sheet-side: upgrade deltas for all 75 cards under the same axis discipline — upgrades are where archetype payoffs traditionally sharpen, which is precisely the missing late-game the gap predicts). Until it lands: the assigned-vs-adaptive gap is officially *unresolved evidence* — no archetype buffs or nerfs may cite it.

## Ruling: spark achievability alarm — decompose before touching anything
Same method that cracked reaction: report spark's core definition and its per-component assembly rates. Prediction to test: the binding component is engine-uncommon availability (endless_fireworks / spark_knight_style at uncommon odds), in which case the candidate fixes are widening the core definition (any-engine-counts) or a common-tier engine enabler — but the decomposition picks, not the prediction. Note spark now also gains kit-Burst access, which may move its numbers on its own; sequence the decomposition after the Burst re-measure so it reads the real pool.

## Standing
The night validated the division of labor again: the sim's divergence verdict is the design's biggest green light to date, and every limitation it revealed arrived with its own diagnosis attached. Re-measures first (Burst, relevance, spark), hybrid discriminator second, skip-threshold retune third; the Upgrade Pass kicks off chat-side in parallel. Playtest continues on the fixed build — eleven runs of real human telemetry are already more interesting than anything else on this page.
