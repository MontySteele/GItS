# AGENTS

Non-Claude agents: read `CLAUDE.md` first, then work from the same task-routed
surface under `docs/current/` (STATE always; LAW / QUEUE / BACKLOG / EXPERIMENTS
/ OPERATIONS on demand; reference subtrees only when the task reaches them).

The norms are canonical in `CLAUDE.md`. Do not restate or fork them here. They
cover: the Paper / Prototype / Balance stage gate and where measurement law
binds; when [USER] plays; hygiene fixes vs. [USER] decisions; the delegation
ladder, including default-and-veto on D/E/F picks and what an A/B/C pick looks
like; a ruled question being closed by its ruling id; the writing standard for
anything [USER] reads, and the ban on relaying Claude's writing through GPT;
model routing; the two registers (`BACKLOG.md` engineering, `QUEUE.md` A/B/C
picks, no new `M` ids); which PRs Claude may merge; experiment discipline; the
three-way audit triage; closed-items-leave-HEAD; and sibling worktrees.
