You are the DOCTRINE SEAT for a card game modification. You did not write the
proposal below, you are not being asked whether it is good, fun or balanced,
and you are NOT being asked to design anything.

THE PROTOCOL YOU ARE UNDER, verbatim from the repository's OPERATIONS.md:

    The seat's other job -- reading a slice proposal against the character
    charter before anything is built -- answers FOLLOWS or
    REQUIRES_MODIFICATION per arm and NAMES THE CLAUSE it ruled against. That
    is the whole output. It may not supply card text, a number, a mode or a
    rewritten row: a remedy it volunteers is DISCARDED, and Claude re-derives
    from the named clause.

So: name clauses, not remedies. If you find yourself writing a number, a card
face, or "you should instead...", stop and name the clause that forced it. Any
remedy you volunteer anyway will be recorded and marked DISCARDED, and it will
disqualify your model family from grading the row later, so it costs the
process something real.

WHY YOU: independence is by MODEL FAMILY, author against grader. Claude
authored the SPECIFICS of this proposal. You are the other family.

============================================================================
WHAT IS AND IS NOT IN SCOPE FOR YOU
============================================================================

THE DIRECTION IS SETTLED AND IT IS PARTLY YOURS. The architecture this packet
implements -- Salon and Companions as one cast, Spotlight directing it, Encore
paying for it, Fanfare merged into the Burst meter as the finale -- was
proposed by your own model family and accepted by the project owner. It is not
open. DO NOT RE-LITIGATE THE DIRECTION. Do not argue that Center Stage should
stay, that Fanfare should remain a separate meter, that the three archetypes
should change, or that a different architecture would be better. A verdict that
does any of those is out of scope and will be recorded as such.

WHAT YOU ARE GATING, and only this:

  G1  The E4 RULING TEXT in section 3 -- what it reopens, and each of the five
      things it explicitly RETIRES.
  G2  The LAW AMENDMENT WORDING in sections 3.1 and 3.2 -- as replacement text
      for the clauses quoted above each of them.
  G3  The ARCHITECTURE SPECIFICS in section 4 -- the shared performance hook,
      the two Featured outcomes (one per lane), the persistent Spotlight and
      its combat-start choice, the Encore-priced move, the merged meter's two
      legs and their rates, the DERIVED meter size and its arithmetic, Grand
      Performance, and the Encore display.
  G4  The PICK LIST in section 9 -- whether each pick is genuinely a call the
      packet cannot settle, and whether any pick smuggles in something already
      settled by LAW or by the charter, or omits something that should have
      been a pick and was instead assumed.

Answer FOLLOWS or REQUIRES_MODIFICATION for EACH of G1, G2, G3, G4, and then
OVERALL. For every REQUIRES_MODIFICATION, NAME THE CLAUSE -- quote or cite the
LAW line, the D-charter item, or the kickoff section it violates. If a clause
you would cite is one this packet itself proposes to retire or amend, say so
explicitly, because a retirement is not automatically a violation.

You may also, within G3, rule on whether the two Featured outcomes are
"meaningfully different" in the sense the direction requires -- that choosing
Salon versus Guest is not merely selecting which body produces meter. That
question is in scope because it is a doctrine question, not a taste question.

THE SANDBOX. You are read-only and previous runs of this seat found the command
policy on this machine rejects every file read. DO NOT TRY TO READ THE
REPOSITORY. Everything you need is pasted below verbatim and nothing has been
summarised for you.

============================================================================
GOVERNING LAW -- the D1-D9 design charter (R213/R217), verbatim
============================================================================

construction: each is a question a reader answers about a card, a package or a
pool, not a number a report computes.

1. **D1 — Character brief before pool construction.** No pool is built before
   its character has a short [USER]-owned brief: the player promise; two or
   three core verbs; one or two recurring tensions; the three archetype loops;
   the bridges among them; the intended weakness; what the starting relic and
   starter deck teach; and the failure modes to avoid, named. A tension
   sentence *summarises* the character — it does not mandate that every card
   serve one mechanic. Zhongli's brief is the entry gate of his deep dive.
2. **D2 — Player-controlled leverage.** Every persistent resource and every
   automatic engine must feed a decision the player can steer: timing,
   targeting, placement, acquisition, conversion, or forgoing. "Watch it rise
   until the number is large" is not a decision. The control must be reachable
   early and reliably — starter kit, starting relic, base system, or the
   ordinary pool — not only through a rare.
3. **D3 — Benefits carry binding prices.** Defence and engine advancement may
   share a card, but not both at full rate without a binding cost: energy or
   tempo, a below-rate half, mutually exclusive outcomes, target or timing
   awkwardness, a card or resource spent, identity position, a future draw or
   deck cost, or the loss of another action. The counterfactual test: remove
   the defence — is what remains still a full-rate play the player already
   wanted? If yes, the defence was a subsidy.
4. **D4 — Visible and live effects.** At the decision point the player can
   perceive and forecast the consequences that matter, through the card, a
   keyword, a persistent UI element or a character rule — not necessarily
   verbatim on every face. Text that cannot bind in the shipped world,
   invisible feeds and misleading calculated displays are defects. A rare
   intentional edge case is not removed for being rare.
5. **D5 — Simple surfaces, deep interactions.** Richness comes from
   interactions — between cards, enemies, energy, draw order, piles, targets
   and future turns — not from clauses on faces. Commons establish the verbs
   and stay concise. Any added line of text must alter a decision.
6. **D6 — Every card has a place.** Each card has one primary decision home:
   acquisition and build; combat (sequencing, targeting, holding, conversion,
   timing); teaching or utility, deliberately plain; or bridge. Plain cards are
   legal and necessary, and a pool of them is not a defect to be edited away.
7. **D7 — Mesh without preassembly.** Each pool carries linear signposts AND
   modular tools. No preassembled deck; no archetype written in a private
   language only its own cards speak; bridges exist so combinations arrive
   unexpectedly. Shared-verb and hook counts describe a pool — they are never
   acceptance bands.
8. **D8 — Distinct play patterns.** Archetypes differ in how turns and drafts
   unfold, not in the label on a bigger number. Damage may stay terminal; the
   route, cadence, constraint, targeting, transformation, control or economy
   must differ. One non-scalar payoff does not rescue an otherwise automatic
   loop.
9. **D9 — Shared layer and starting tutorial.** Companion packages connect to
   both character verbs and universal verbs; not every Companion card needs a
   hook, but every package needs a distinctive identity. The starting relic and
   starter deck introduce the central verbs and one recurring tension from
   fight one, with visible triggers and no invisible feed.

**Provisional through the Klee slice**, then reviewed. **Nothing here is a
numeric band and nothing here gates:** hook share, bridge %, payoff-role %,
scalar-payoff %, random-target %, Powers-per-universal-verb count, plain-card
%, word count and "turns with a named alternative" rate are descriptive only.
No subjective front-matter fields enter card YAML, and there is no waiver
mechanism. Decision closeness (R213 F) remains the only numeric design
falsifier, and it falsifies one way. (R217 — drafted by GPT, sharpened by
Claude, ratified by [USER].)

============================================================================
GOVERNING LAW -- Combat & reactions, including the clause section 3.1 replaces
============================================================================

  aura; off-element access comes only from companions or a co-op partner.
  (principles Pillar 2 / Guardrail 2)
- **Amplifiers are per-hit and consume the aura.** No reaction ever produces a
  persistent or compounding damage multiplier — this is the iron rule and the
  balance governor. (Whether a particular card's scaling that happens to get
  duplicated is *too strong* is a balance question, not a law.) (principles §2.2)
- **One aura per enemy (v1), 2 player-turns, refreshed by same-element hit.**
  Anemo/Geo leave no aura — they only trigger. (principles §2.1)
- **Canonical Frozen is a per-turn-decrementing, per-creature duration counter.**
  Non-boss Frozen = −50% next-action damage + Shatter (first Attack hit only,
  direct HP damage, cannot shatter the freeze it just applied). In boss rooms
  only creatures carrying `MinionPower` are Frozen; every other creature takes
  the Vulnerable substitution. (principles §2.2; R44; R116 NC-7 / R117 Q13)
- **Elemental application coexists with Artifact.** An Aura or a Bomb lands
  beside Artifact rather than consuming a charge — only an actual debuff
  reduces Artifact. Scope: Auras and Bombs coexist; Frozen and
  reaction-applied Vulnerable/Weak/Poison stay real debuffs and are negated
  as normal. Bomb's "first attack −25%" rider lands through Artifact under
  the same rule — ruled acceptable. ([USER] 2026-08-23)
- **Hard CC is payoff-tier only.** No reaction and no companion card produces an
  intent-skip/stun at repeatable-common economics; full stun is priced at or
  above base-game stun scarcity with per-combat diminishing returns. Companions
  never source hard CC (the `control_uptime` / `SUPPORT_CARRY` detector enforces
  it). (principles §2.2a, §4.3; R45)
- **Reaction credit — damage attribution and Burst energy — goes to the
  triggering player;** auras live on shared enemies so cross-player reactions
  need no special-casing. (principles §2.5)
- **Overload splash (to all enemies) and Electro-Charged (stacking DoT) bypass
  Block and are damage-pipeline-free** (no strength/vulnerable recursion).
  **Shatter** is separate: bonus damage on the first Attack hit against a Frozen
  enemy, which removes Frozen — it is not a DoT. (M1; principles §2.2)
- **Application cadence is a per-character dial** — catalyst-grade (every attack
  applies, low base numbers) vs. skill-grade (only Skill/Burst-tagged cards
  apply, higher base numbers). (principles §2.3)
- **Spotlight/empowerment boosts numbers only, never turn-economy effects.**
  (principles §2.2a, v1.10)


============================================================================
GOVERNING LAW -- Economy: companion pool, and the clause section 3.2 amends
============================================================================

  Commons. (principles §4.7; R59; R116 NC-10; R117/R118 Q16; [USER] 2026-08-10)
- **An empty companion slot is omitted, never faked, in both engines.** When the
  fallback ladder runs out (no drawable companion at any nation or band) the slot is
  not created; the mod does not substitute a base colorless card. Omission happens
  upstream of `MerchantCardEntry.Populate`, which has no no-card path.
  ([USER] 2026-08-10; R59, R60)
- **Gold price is the balance governor, never a stat nerf.** Companion pricing is
  50/75/150 by rarity; the ×1.15 colorless surcharge applies to `ColorlessCardPool`
  only, so companions are exempt. (principles §4.7; R61, R63)
- **Price, shelf composition, and shelf order JOINTLY govern the premium channel.**
  Price alone is not the governor: what is on the shelf (the rarity floor) and when
  the player is asked (companions resolve before the relic shelf, so they get first
  claim on the purse) move the same outcome, and a channel tuned on price alone is
  tuned on one of three levers. Amends the older "pricing is the governor" thesis,
  which stands as the pricing half of it. ([USER] 2026-08-10, S4-G10)
- **Neutral action-energy / draw / thin are legal enabler-grade companion utility**
  (one-shot, Exhaust-gated, costed) — the pool must let any character draft a
  kit-gap fix. **Burst-meter (`burst_energy`) generation stays character-kit-scoped**
  and must never be cheaply repeatable from companions. (principles §4.7, §2.4)
- **The Featured Banner governs 5-star availability:** each run rolls
  min(roster, slots) limited 5-stars per nation, without replacement, fixed for
  the run, per-player in co-op; 4-stars never gated; reward slot, shop slot 1,
  and sim are wired together. Standard-banner 5-stars carry `standard: true` so
  an off-banner floor is one flag, not a redesign. (principles §4.2, v1.8; R64)
- **An unreleased-nation character sits in their nation of operation until their
  home sheet ships** (Arlecchino→Fontaine, Childe→Liyue). (R65)
- **Artifacts are relic subcategories with 2-piece set bonuses** — no 4-piece
  sets, no main/substat rolls; themed to reaction styles, not characters
  (cross-character content). (principles §5)
- **Infinite cycling engines gate to Uncommon+** (a hand- and energy-neutral
  self-replacing card is a cycling engine whatever else it does). (R109 X2; R114
  FLAG-1)

## Character identity — Klee

- **Pyro, catalyst-grade:** every attack applies Pyro, base numbers run low;
  enables any reaction, triggers none alone. (klee §1)
- **Declared statline is load-bearing:** elite A1 (frontload) and A6
  (utility/AoE); weak A3 (block) and A4 (sustain ~0.5). **Her scaling never tops
  her frontload — A2 < A1 and A2 ≤ 4.0** (Klee-scoped, not mod-wide), scaling
  powers stack-capped. (klee §2, §9; R52 ask 3)
- **Bombs detonate at the start of Klee's turn,** early on any Attack-card hit;
  multiple bombs stack independently, each keeping its own damage. (klee §3)
- **Repeatable spark generation satisfies one of two limbs:** it sits at
  Uncommon+, or no sub-Rare card is simultaneously a spark source and a draw
  enabler — infinite sparks unreachable at Common (an unboundedness test, not a
  ban on Common spark-minting). X-cost attacks are exempt from spark-spend.
  (R109 X7 + [USER] annotation; R34)
- **The Burst (Sparks 'n' Splash) is kit, not loot:** never draftable, granted to
  hand on meter fill, casting empties the whole meter (overflow lost at cast),
  re-granted on refill, carries Retain. (principles §2.4, v1.9)
- **Detonation lifesteal patches A4 at relic rarity, not card rarity.** (klee §5)


============================================================================
GOVERNING LAW -- Character identity: Furina, verbatim and entire
============================================================================

## Character identity — Furina

- **Hydro, skill-grade support-protagonist:** modest numbers, scaling routes
  through others (elite A4 sustain + A6 utility; weak A1 + A7). (furina §1, §2)
- **Spotlight runs in exactly two modes** (the retired self-Spotlight model is
  not law): **CENTER STAGE** designates Furina — her cards generate Fanfare, no
  numeric bonus; **GUEST CAST** designates the Companion *category* — companion
  cards get the +50% multiplier and Spotlight texture, generate no Fanfare. The
  `character:` field, invalid-target rule, freely-movable persisting designation,
  inert duplicate selectors, per-player registry, and the per-turn Spotlighted-card
  cap stay live. (R41; principles §4.5 v1.14; furina §3)
- **Encore is an unbounded per-combat buffer** absorbing after Block and before
  HP (overdraw drains true HP); **Encore absorption credits A4, never A3.**
  (principles v1.10; furina §2, §4)
- **Fanfare is capped at %maxHP; generation is activity-based, never passive**
  *(Encore's per-turn-trickle ban carries the same Ancient carve-out: R127)*.
  Design invariant: **every point of damage past Block prints exactly 1 Fanfare.**
  Live legs: HP lost / Encore spent / Encore absorbed / Spotlighted card played.
  Cards use printed `Fanfare Cap +X` (raises cap) and `Fanfare +X` (full grant,
  rare POWER payoff only); cards raise a permanent floor (`gain_fanfare_floor`),
  not the cap. **`Fanfare Cap +X` is an AVAILABLE EXPLICIT VERB, not a rider
  every Power carries** — a card prints it when raising the ceiling is that
  card's job. The incidental carriers were removed along with the register
  lint that required one on every Power (`EB-118` §5.2, 2026-08-24; lint `R7`
  retired, `R6` untouched — the full grant is still a rare-POWER payoff).
  Fanfare is a global pool on Furina surviving Spotlight moves.
  (principles v1.12 RATIFIED; furina §4; R41; R114 FLAG-3; EB-118 §5.2)
- **Delete-test applies unmodified, no detector carve-outs:** deleting Furina's
  cards from a winning Spotlight deck must gut it; companions winning anyway is
  `SUPPORT_CARRY`. **Self-carry must not be the median-best plan** (Salon and
  Spotlight beat self-carry at median draft quality; self-carry owns the ceiling
  only on cracked-Rare draws). The Ethereal Spotlight selector is kit machinery
  and does not count toward A5. (furina §8, §2; R61)
- **Guest Star generators — four guardrails:** this-combat-only; generators
  Exhaust; equal-rarity (sub-Rare cannot create 5-star Rares); pull only from the
  shared companion pool + purpose-built Guest Star sets, never playable pools.
  Guest cameos are Furina-personal-pool only. (furina §9; principles §4.5; D2)
- **Fontaine Cryo-convergence is managed in kit, not roster exclusion:**
  Charlotte and Freminet each get one Cryo-applying card; Chevreuse is the
  authored Overload/Vaporize counterweight (buff other routes, never nerf
  freeze); Fontaine's zero 4-star Electro is scarce by construction, not a bug.
  Freminet is one applier + one defensive/trigger + one enabler — no fake-support
  reflavor of a DPS kit. (furina §10)


============================================================================
GOVERNING LAW -- the animation / funnel contract and the Burst indicator
============================================================================


## Art & visual layer

- **Ship pre-sized art, no runtime minification** — except where the layout pitch
  is a per-player stat, where bounded runtime fitting is allowed with the bound
  written down and asserted (Salon members capped at 0.5). (principles v1.13
  RATIFIED)
- **Visuals bind to funnels, never to values (Funnel Contract):** a number a kit
  redesign moves must not be able to break a display. Contracted binding points:
  Salon = 3 slot-index-keyed slots (duplicates legal); Encore-absorbs-before-HP;
  Spotlight-is-a-designation-event. A breach = stop-work on that track, flagged
  in the PR/commit; visuals never chase an unlanded kit change. (animation-sprint-2)
- **The centered-overhead creature-space slot is the cross-character Burst
  indicator;** gauge skins are unique per character. A mirror belongs above the
  animated node, never on it; **never write `Visuals.Scale`** (a sign flip inverts
  the hitbox — a gameplay bug in visual-bug clothing). A node a bridge depends on
  is a contract with a side-effect-free boot check. (animation-sprint-2 C4;
  klee-mod placement rules)
- **Art dedupe (`art_lint` L1):** effective pick = auto or shortlist rank 1 unless
  red-pen resolves; register-crossing reuse is legal; known collisions sit in a

============================================================================
GOVERNING LAW -- card-sheet material-edit rule and measurement authority
============================================================================

- **Ancient-tier pool gaps are fixed with real content, never option removal;**
  each character needs one Ancient card, gated by a deploy lint that fails on an
  empty ledger. (klee-mod Ancient ruling)
- **A material card-sheet edit is a world change and lands under a
  `CONSTANTS_VERSION` bump.** *"A card-sheet edit that materially changes the
  drafted or combat world — card additions or removals, cost changes,
  effect-number changes, rarity moves — is a world change. It lands under a
  `CONSTANTS_VERSION` bump like any other balance constant, and numbers are not
  comparable across it."* The sheets sit outside the `RT/D/P/C` stamp, so
  without this rule two worlds differing in whether a card exists at all are
  indistinguishable to a reader of the stamp. **Rename amendment:** a
  *cosmetic* rename is exempt from the bump **only when neither the card id nor
  the display name is read mechanically** — and the burden is on the renamer to
  establish that, because at least one relic reads both. `card_name_damage_bonus`
  matches a **substring** against the card id **OR** the display name
  (`tier0/engine/relics.py:385-398`); its one carrier is `strike_dummy`,
  substring `"strike"`, `+3` to attacks only, FROZEN. A rename that adds or
  removes such a substring on either field moves damage and is not cosmetic.
  (R179; M15 draft text ratified as written, amended on the rename clause)
  **Role/archetype amendment (RATIFIED 2026-08-24, R202):** a change to a card's
  `role` or `archetypes` is a material card-sheet edit because both fields are
  mechanically read by drafting. It requires a `CONSTANTS_VERSION` bump, and
  drafted-world numbers are not comparable across it.

## Design governance & measurement authority

- **The simulator's authority is relative deltas and structural findings, not
  absolute human-play winrates;** a ratification resting on an absolute winrate
  rests on the wrong number. (principles §7)
- **Seven-axis numbers are reportable, not load-bearing:** **all seven** axes are
  permanently CLOSED as reportable-only (never a gate, target, or justification
  to move a value). There is **no "Fanfare axis"** — R118's term names no axis;
  Fanfare is a Furina mechanic that falls under **scaling**, and the per-axis
  disposition therefore collapses to one disposition for all seven. (principles
  §7; D3; R90 Q7; R118; R138)
- **Every archetype passes aura-starvation / bricking checks in the sim before
  implementation;** test packages model plausible drafts, never monocultures.
  (principles Guardrail 4)
- **Ratified 1000-fight winrate bands change only by ruling, with archives;**
  small-n heuristic locks may be retuned to measured-noise reality only with a
  dated comment and disclosure. Authored Tier-0 25-card batteries are
  ceiling-saturated and keep only their matchup floors; Tier 0.5 owns the

============================================================================
THE CHARACTER CHARTER -- Furina kickoff v0.1, sections 1-9, verbatim
============================================================================


- **Element:** Hydro. **Cadence: Skill-grade** — only Skill/Burst-tagged
  cards apply Hydro; higher base numbers than catalyst-grade *within her
  low-statline identity*. Salon members are her repeatable application
  engine (see §5).
- **Companion appetite: Standard.** Spotlight is draft-gated high-ceiling,
  not her only plan; the self-Spotlight fallback means companion-poor runs
  are dimmer, never bricked (Pillar 4).
- **Burst:** kit-Burst per v1.9 (granted on meter fill, Retain, re-grant on
  refill; never in pool). Meter size and card design deferred to the sheet
  pass; her particle economy leans on Salon application and Encore spend.

## 2. Statline declaration (7-axis, declared before any card is written)

| Axis | Target | Rationale |
|---|---|---|
| A1 Frontload | **1.0–1.5** | The load-bearing weakness. Dreadful, by design. |
| A2 Scaling | ~3.0 | Fanfare gives her a curve; her scaling *identity* routes through others. Declared on starter+median per acceptance law — companion-dense decks measuring higher is the design working, not a violation. |
| A3 Block economy | ~2.5 | Encore is not Block; see harness note. |
| A4 Sustain | **4.3** | The healing metric's protagonist. Encore absorption + rare-tier true heals. |
| A5 Velocity | 3.7 | Encore-spend tempo, card-level velocity texture. Strong, deliberately sub-elite (heuristic: exactly two elite axes). *(Redpen 2026-07-20, ruling (a): selector cadence struck — the Ethereal selector is kit-delivery machinery and does NOT count toward A5.)* |
| A6 Utility | **4.2** | Hydro aura uptime (Klee's dream co-op partner), party buffs, debuff texture. |
| A7 Setup tax | ~2.0 | Second declared weakness. Punisher fights should genuinely hurt. |

**Harness note (Tier 0, binding):** Encore absorption credits **A4**
(chip-reduction), never A3. Without this accounting rule she grows a
phantom third elite axis and the declaration is unfalsifiable.

## 3. Signature subsystem: Spotlight (the one Ghostflames-scale system, §3.2)

**Subsystem budget ruling:** Spotlight is THE subsystem. Encore and Fanfare
are resource counters (cheap). Salon rides existing summon machinery
pending the check-if-solved audit (§5). Pneuma/Ousia is **pure flavor,
zero mechanics** (ratified). Nothing else novel ships in her v1.

### 3.1 Designation (two modes — R41, v1.14)

- Spotlight runs in **exactly two modes**. **CENTER STAGE** designates
  Furina herself: her own cards generate Fanfare when played and receive
  **no numeric Spotlight bonus**. **GUEST CAST** designates the
  **Companion category**, not one character: companion cards receive the
  empowerment multiplier and Spotlight texture, and generate no Fanfare
  (`review/active/red-pen-2026-07-26.md` R2(a); LAW "Character identity —
  Furina").
- Every card gains a **`character:` schema field** (shared schema change,
  not Furina-private — Columbina inherits it). Companion cards carry their
  character implicitly; Klee's cards tag Klee; Furina's tag Furina. Cards
  with no tag are **invalid Spotlight targets** (selector greys them out).
- **Delivery:** her starting relic adds an **Ethereal Spotlight selector**
  card to hand each turn. Applying it to a companion card designates
  Guest Cast; applying it to one of Furina's own cards designates Center
  Stage. In co-op, the
  selector may instead be passed to a teammate, who applies it to one of
  their own cards (first cross-player designation — Appendix A.4's
  engineering, arriving early).
- **Movable freely; persists until moved.** Designation lasts until
  re-aimed; moving costs only the (free) selector play. Commitment is
  rewarded through Fanfare throughput (§4), never enforced through rules —
  drafting a second companion or a 5-star splash must never feel like a
  pre-nerfed pick.
- **Global registry:** one live designation per Furina player at a
  time, tracked globally; duplicate selector cards are inert. Registry is
  per-player in co-op (two Furinas = two independent Spotlights).

### 3.2 Empowerment

- **Baseline (v1, boring on purpose):** under Guest Cast, companion cards
  get **+50% printed numbers** — damage, Block, element-application
  counts. Center Stage grants no numeric bonus; its payoff is Fanfare
  throughput (§4). Flat rate is the knob; texture lives in cards, not the
  baseline.
- **§2.2a guard — RATIFIED law:** Spotlight/empowerment boosts numbers
  only, never turn-economy effects. Canonical text in
  `docs/current/LAW.md` ("Combat & reactions"); the retired principles
  doc's §2.2a extension (v1.10) reads at the pre-simplification tag.
- **Center Stage = the solo fallback** (Appendix A.4 mandate, satisfied
  structurally): a companion-poor run holds the stage herself and
  converts play volume into Fanfare instead of a multiplier. The
  anti-self-buff lever is structural — her own cards receive no numeric
  bonus at all, legible on card text.
- **Held in reserve, schematized but OFF:** per-turn Spotlighted-card cap
  (raiseable via powers as design space). Turns on only if Tier 0 shows
  asymmetry alone fails the §6 acceptance criterion.
- **Texture design space (ratified, uncommon/power tier):** "first
  Spotlighted card each turn costs 1 less," "first Spotlighted card each
  turn draws 1," etc. — cheap riders on top of the flat baseline.

## 4. Resources: Encore & Fanfare

**Supersedes furina-predesign-notes Part 2 where they conflict** (that doc
predates the v1.6 amendment).

- **Encore** (per v1.6): **unbounded per-combat** buffer pool (Osty/
  Regent-star house style — governed by opportunity cost, made safe by
  per-combat reset). Her "healing" effects grant Encore; it absorbs damage
  before HP; potent cards carry "Spend N Encore:" cost lines; **Salon
  overdraw drains true HP when Encore is empty** — greed is legal and
  priced. **True healing is Rare-tier AND Exhausts** (Pneuma/Singer
  identity; tightened from v1.5's disjunctive form by user ruling — see
  m7-rulings R8).
- **Fanfare: capped at %maxHP** — the cap is a high safety rail, not a
  first-order dial: no sheet carries `raise_fanfare_cap` (the uncapper
  clause died with "The Tide Turns", F-A5); cards raise a permanent
  **floor** instead (`gain_fanfare_floor`), and proportional decay means
  nothing reaches the rail. Stacks grant flat power bonuses.
  **Generation is activity-based, never passive** (v1.12): the live legs
  are **HP lost / Encore spent / Encore absorbed / Spotlighted card
  played**. Fanfare prints when Encore goes **down**, never when it goes
  up (ruled 2026-07-28, post-playtest-3; shipped in both engines). No
  per-turn passive accrual, ever — passive accrual = stall payoff, the
  healing policy exists to kill exactly that, and a floor is static value
  that does not grow with time, so stalling still earns nothing.
- **Fanfare is a global pool on Furina** — it survives Spotlight moves.
  Commitment texture comes from throughput (a deep single-partner kit
  plays more Spotlighted cards per turn), not from reset punishment. The
  formerly-proposed "move Ovation" uncommon is unnecessary and cut.
- **Co-op:** partner HP/Encore flux counts toward Fanfare (her Genshin
  identity; first ally-coupled mechanic). **Audit at sheet pass:**
  exclude or discount self-inflicted partner damage (Klee's Hot Hands)
  or Fanfare farms itself.

## 5. Salon Members

Off-field Hydro application engine (her repeatable aura source under
Skill-grade cadence) with HP-drain overdraw per §4.

**Engineering directive — check-if-solved FIRST:** audit Necrobinder/Osty
and BaseLib summon machinery before building anything. Salon ships on
existing rails or it ships smaller. (House norm; 3-for-3 last time.)

## 6. Keyword budget (guardrail 5 amendment — RATIFIED by user)

Census: **Spotlight, Encore, Fanfare = 3 named keywords** vs guardrail 5's
≤2 (Klee: Bombs, Sparks = 2). Ratified as a deliberate, logged exception:
support-protagonists carry structurally more machinery, and Furina pays
for the third keyword with flavor-only stances, zero new reaction content,
and Salon riding existing rails.

**Proposed principles amendment text:** guardrail 5 becomes "≤2 new
keywords per character; support-protagonists (§4.4 High-appetite or
Appendix A lineage) may carry one additional keyword via logged amendment
with compensating cuts." Note for the record: Columbina will pressure even
the amended budget (she touches every Nod-Krai mechanic); the amendment
sanctions *one* extra, not open season — her kickoff fights that fight.

## 7. Archetypes (template §3.3)

1. **Salon** — default plan; off-field Hydro engine + modest personal
   damage; the solo floor.
2. **Spotlight** — draft-gated high-ceiling; commit to companion depth
   (or your co-op partner) and direct the stage. Allowed to be luck-gated;
   that is what the slot means.
3. **Fanfare** — tempo/velocity plan; the drain→refill→spend flux cycle.

Card-slot competition separates them: Salon wants member cards and Encore
generation; Spotlight wants companion depth and selector payoffs; Fanfare
wants flux accelerants and spend payoffs.

## 8. Acceptance criteria (pre-registered, sim-enforceable)

1. **Self-carry is not the median-best plan:** at median draft quality,
   Salon and Spotlight archetype winrates must beat the self-carry
   (Encore/Fanfare-pumping-own-kit) package. Self-carry may own the
   ceiling when the draft hands it cracked Rares — lean-in reward, not
   default plan.
2. **The delete-test applies to her unmodified:** deleting Furina's cards
   from a winning Spotlight deck must gut it (her cards are the mult). If
   companions win anyway, that is SUPPORT_CARRY — real failure, even for
   the support character. **No detector carve-outs.**
3. **Standard floors:** strict relevance ≥35%/archetype; winrate bands at
   ≥1000 fights; identity heuristic on starter+median, never monoculture.

**Pre-registered achievability experiment (blocks Spotlight card tuning,
not sheet drafting):** P(≥N same-character companion cards by act 2)
under nation-weighted rewards, swept over 2-card vs 3-card Fontaine kits,
with and without Encore Performance (§9) in the drafted deck, and with
and without Guest Star generators (§9) — three arms: duplication lifts the
committed archetype's ceiling; generation lifts its floor; the bet is that
neither substitutes for drafted depth.
**Registered prediction:** 3-card kits alone put one-character depth 4+ at
"reachable but luck-gated" (correct for the slot); duplication separates
the archetype's median from its ceiling. Null results binding as usual.

## 9. Personal-pool design space (pre-sheet notes)

- **Encore Performance** (duplication): "add a copy of a Spotlighted card
  to your deck/hand" — the archetype's kit-deepener and Columbina's replay
  grammar on schedule. **Watchlist notes:** duplicating a 5-star Rare is
  copies-of-one-support (does not break banner scarcity); duplicating an
  amp-booster (Durin-analogue) goes on the **amp-cap watchlist** —
  bounded in principle by the iron rule, confirm in provenance logs.
- **Guest Star suite** (generation — user design, ratified this session):
  personal-pool cards that create companion cards for this combat only,
  Discovery-class machinery. Four binding guardrails: this-combat-only;
  generators Exhaust; equal-rarity clause (sub-Rare generators cannot
  create 5-star Rares — banner untouched); pulls from the shared companion
  pool + a purpose-built Guest Star set, NEVER from playable characters'
  pools (cadence rules live on the character dial — generated playable
  cards would have undefined element behavior). Guest Star set v0.1: 2–3
  Neuvillette cards at common/uncommon, support-shaped, explicit
  applies_element flags, personal-scoped and banner-exempt (temporary
  cameos). Generated cards are legal Spotlight targets — this is
  Spotlight's bricking mitigation, the soft form of Appendix A.3.
  Tea-party flavor is the identity: she invites guests; the Iudex always
  comes.
- Selector-payoff cards ("if you moved the Spotlight this turn…" /
  "…if it hasn't moved this combat"), Fanfare spenders, Encore-cost
  potent cards, rare-tier true heals.
- Naming sources per §3.6: constellations for rares, talent names for
  relics, voice lines for flavor. Her material is theatrical; it will
  write itself.


============================================================================
THE PROPOSAL -- review/active/furina-e4-2026-08-29.md, verbatim and entire
============================================================================

# Furina E4 — the redesign packet

> **Lifecycle: ACTIVE.** A design packet, not law. §3 is a DRAFT ruling
> awaiting [USER]'s countersign; §9 is the pick list. Nothing here has moved
> a shipped number, a sheet row, a constant or a register. No code was
> written for it.

**Date:** 2026-08-29. **Branch:** `furina-e4`, cut from `origin/main` at
`fb56095`. **World:** `RT12 / D18 / P11 / C20`, `PILOT_WEIGHTS_VERSION` 6.
**Authority:** R213 E4, amended by R217; the delegation ladder is R212.

---

## 1. The inputs

### 1.1 [USER], verbatim

> "For Furina, we have three mechanics - Salon, Fanfare, Encore - plus a
> desire to make Companion Cards work through Fanfare, and I feel like the
> different aspects are not really cohesive. I think some of this is UI/UX
> legibility - Encore should be a status bar like Regent's stars, Spotlight
> should not require playing a card every single turn - and some of it is
> fundamental."

### 1.2 R213 E4, verbatim (`1050f67`)

> "E4 -- Salon is Furina's provisional spine: YES. Encore, Fanfare and
> Spotlight must prove they function as parts of the Salon loop or be cut
> or merged; the first Furina slice tests all four in that arrangement."

And the sequencing clause from the same ruling:

> "...its findings inform Furina. Then Klee's reaction slice, then Furina
> after the Companion result. Not three explorations in parallel."

### 1.3 GPT's proposal (summarised faithfully; the full text is [USER]'s)

A hierarchy: **"Salon and Companions are Furina's cast. Spotlight directs
the cast. Encore pays for and protects the performance. Successful
performances create Fanfare, which unlocks the finale."**

1. Salon members are the *resident troupe*; Companions are the *guest cast*.
   Both hang off the same performance hooks.
2. Remove Center Stage. Spotlight chooses **Salon** (the front member's next
   performance is Featured) or **Guest Cast** (the first Companion played is
   Featured).
3. Spotlight is a **persistent state**, not a generated selector card:
   chosen at combat start, and moving it costs Encore.
4. Encore is the immediate tactical resource, shown beside HP. Furina cards
   spend it to move Spotlight, command another performance, repeat the
   Featured performance, or add a rider.
5. Fanfare **replaces** the separate 70-point Burst meter. Spotlighted
   performances generate it; full = Burst; casting empties it. The Burst is
   a qualitative **"Grand Performance"** (e.g. both lanes Featured for a
   round), not "+1 per X".

Archetypes stay three: Resident Troupe / Guest Cast / Conductor. The starter
kit teaches the whole sentence — one deterministic Salon deployment, one
generated Companion, one Encore generator, one cheap Encore spend, and the
relic supplies the persistent Spotlight interface. Prototype the starter,
two members and a few Companions first. The decisive test: *"Did the player
plan who would receive Spotlight, and did spending Encore on that
performance feel meaningfully different from keeping it for defense?"*

### 1.4 Claude's read, as given to [USER]

The diagnosis is on the record, not a hunch: Salon 4.70% / Spotlight 1.50% /
Fanfare 1.30% at `RT9/D14/P6/C8`; the selector heuristic in
`tier0/engine/effects.py:1803` *is* the collapse rule; the measured
Spotlight-move share is 0.167; and The Curtain Never Falls already deletes
the mode choice outright. Burst is MOD-defined (a BaseLib `CustomResource`),
not base-game, so merging Fanfare into it removes machinery rather than
adding it — and both meters already observe the same activity. Grand
Performance is the state that relic already expresses.

Reshape: (i) a persistent lane chosen at combat start through the existing
choice-modal machinery, with Encore-priced moves through the `encore_cost`
gate on `spotlight_designate` — click-on-rail is an unverified interaction
and is the second attempt, not the first; (ii) Encore is uncapped, so a fill
bar has no denominator — show a count with pips beside HP and put the runway
arithmetic in the tooltip; (iii) two LAW touches, §2.2a and Burst kit-scope.

Picks given: **P1** go/hold; **P2** (1) combat-start choice + Encore-priced
moves; **P3** (1) Fanfare becomes the Burst meter; **P4** (1) count + pips;
**P5** (1) qualitative within §2.2a / (2) amend the guard.

### 1.5 GPT's review of that read — the binding parts, verbatim

> "P1: Go · P2: Option 1 · P3: Option 1 · P4: Option 1 · P5: Option 2, but
> with a narrow amendment."

**Constraint 1.**

> "For E4, I would initially allow Fanfare from only: A Featured performance
> resolving. Encore deliberately spent through a Furina `Direct` effect,
> potentially enabling another performance. No Fanfare from generic HP loss,
> automatic Salon upkeep, or Encore absorption in the first arm. Those can be
> tested later. This makes Fanfare evidence that the player directed the
> cast, not a general activity odometer. It also resolves the companion
> Burst-law issue: an arbitrary Companion does not generate Burst; one
> specifically Featured by Furina's kit does, under a once-per-turn or
> otherwise bounded hook."

**Constraint 2.**

> "I would retire the blanket +50% from the prototype."

Proposed LAW text:

> "Spotlight is a designation, not a universal numeric multiplier. A Featured
> performance may generate Furina's Burst resource and receive effects
> explicitly authored by Furina's kit. Spotlight alone may not duplicate a
> card, reduce its cost, add draw, or amplify nonnumeric Companion text.
> Explicit bounded Furina cards and her Burst may create additional Featured
> performances."

And:

> "The prototype needs at least two meaningfully different Featured outcomes.
> Otherwise choosing Salon versus Guest still only selects which body
> produces meter."

**Constraint 3.**

> "the first playable experiment cannot remove Center Stage before Salon and
> Companions share a minimal performance hook."

Order:

> "1. Write E4 and its doctrine gate. 2. Implement the smallest shared
> performance hook: one Salon action and one Companion play. 3. Add
> persistent Salon/Guest Spotlight, combat-start choice, and Encore-priced
> movement. 4. Merge Fanfare with Burst and add Grand Performance.
> 5. Construct the starter-deck boards. 6. Migrate nothing else until
> independent playthroughs answer the D2 question."

Closing:

> "I would only insist that E4 explicitly retire 'all Encore loss is
> progress' and 'Spotlight means +50%' rather than carrying those assumptions
> invisibly into the new architecture."

### 1.6 Claude's acceptance

All three constraints accepted. My order had Center Stage removal first;
GPT's order is right, and the reason is that a shared hook is the thing that
makes the two lanes comparable at all — remove the mode before the hook
exists and the "choice" is between one implemented lane and one that is not.

The two Featured outcomes below are **my reading**, built from machinery
that already ships. Alternatives are listed beside each.

---

## 2. Evidence

Every row here is a file:line in this tree at `fb56095`.

### 2.1 The three plans are not one system, and the numbers say so

| arm | winrate | 95% interval | source |
|---|---|---|---|
| `furina / salon` | **4.70%** | [4.00, 5.52] | `review/active/eb81-furina-remedy-options-2026-08-12.md:38` |
| `furina / spotlight` | **1.50%** | [1.12, 2.00] | `…:39` |
| `furina / fanfare` | **1.30%** | [0.95, 1.77] | `…:40` |
| `real_silent / generic` (floor) | 1.37% | [1.01, 1.85] | `…:41` |
| `real_ironclad / generic` (floor) | 6.13% | [5.33, 7.05] | `…:42` |

World stamp `RT9/D14/P6/C8` (`…:44-48`). **Use it for structure, never for
level** — that is the packet's own caveat, and it is three boundaries stale.
The structure that survived the boundary crossing (`…:50-54`): Salon
separates from both other plans, intervals disjoint; Spotlight and Fanfare
cannot be told apart from each other; both overlap the `real_silent` floor.

That is a *structural* finding, which is exactly what the simulator's
authority covers (LAW: "the simulator's authority is relative deltas and
structural findings, not absolute human-play winrates"). It is not a level
claim and nothing below rests on one.

### 2.2 The selector heuristic is the collapse rule

`tier0/engine/effects.py:1803-1829`, `_op_spotlight_designate`:

```python
companion_in_hand = any(c.is_companion and not c.kit_card for c in p.hand)
...
elif companion_in_hand:
    target = C.SPOTLIGHT_GUEST_CAST
else:
    target = p.character_id or (
        C.SPOTLIGHT_GUEST_CAST if companion_anywhere else None)
```

That is, in code, the rule the richness brief asked the table whether the
choice had collapsed to:

> "was the mode choice ever actually contested, or did it collapse to a rule
> — 'Guest Cast if I'm holding a Companion, otherwise Center Stage'? If it
> collapsed to a rule, say which rule."
> (`review/active/richness-playtest-brief-2026-08-26.md:115-120`)

The pilot does not "collapse to" that rule. The pilot **is** that rule. The
selector history in `tier0/constants.py:123-157` records five versions of
the heuristic and the shipped one is v5, described there as exactly this
two-branch pick.

### 2.3 The move share

`tier05/draft.py:79`: `STATIC_SPOTLIGHT_MOVED_SHARE = 0.167`, read at
`tier05/draft.py:176`, described at `tier0/constants.py:2628` as "the
measured spotlight-arm rate". The drafter prices every "if you moved the
Spotlight this turn" payoff against one turn in six.

### 2.4 The relic upgrade already deletes the choice

`klee-mod/KleeCode/Relics/UpgradedStarterRelics.cs:399-405`:

> "[gold]Center Stage[/gold] and [gold]Guest Cast[/gold] are both always
> active, and you always count as having moved the [gold]Spotlight[/gold]
> this turn."

The brief put the same fact to the table
(`review/active/richness-playtest-brief-2026-08-26.md:121-126`): "There is an
upgraded version of that relic that deletes the choice entirely… Whether that
is a relief or a loss is exactly the sort of thing only the table knows."

### 2.5 Two meters, one activity

Fanfare legs (`tier0/constants.py:180-199`):

| constant | value | line |
|---|---|---|
| `FANFARE_PER_HP_LOST` | 1 | `:180` |
| `FANFARE_PER_ENCORE_SPENT` | 1 | `:181` |
| `FANFARE_PER_ENCORE_ABSORBED` | 1 | `:182` |
| `FANFARE_PER_SPOTLIGHT_CARD` | 2 | `:199` |

Burst legs:

| constant | value | line |
|---|---|---|
| `BURST_PER_SKILL_TAG` | 5 | `tier0/constants.py:69` |
| `BURST_PER_REACTION` | 5 | `:70` |
| `SALON_TICK_BURST` | 2 (per member per tick AND bow) | `:319` |
| `BURST_PER_ENCORE_SPENT` | 1 | `:321` |

`burst_max: 70` (`tier0/content/characters/furina.yaml:14`), whose own
comment names the four income sources: "particles from skill tags (5),
reactions (5), Salon ticks (2/member/turn), Encore spend (1/point)."

**Encore spend feeds both meters.** `BURST_PER_ENCORE_SPENT = 1` and
`FANFARE_PER_ENCORE_SPENT = 1` are the same event paying two different
counters, each with its own display, its own tooltip and its own reader set.
That is the concrete form of "the different aspects are not really cohesive".

Burst is mod-defined, not base-game: `let_the_people_rejoice` is
`kit_card: true`, `requires: burst_energy_full`
(`docs/furina-cards.yaml:836-837`), granted by
`tier0/engine/combat.py:156-170` and emptied at `combat.py:380`.

### 2.6 The two assumptions E4 has to name and kill

**"All Encore loss is progress."** `tier0/constants.py:183-198` records it as
a deliberate ruling: after the 2026-07-28 rework "EVERY point of damage that
gets past Block prints exactly 1 Fanfare, via absorption if the buffer eats
it and via `hp_lost` if HP does", and the invariant is asserted by
`test_every_point_past_block_prints_exactly_one_fanfare`. LAW:197 states it
as a design invariant. Under GPT's Constraint 1 it goes: HP loss, upkeep and
absorption stop paying.

**"Spotlight means +50%."** `tier0/constants.py:91`,
`SPOTLIGHT_BASE_MULT = 1.5`, RATIFIED R71. Printed on the face:
`klee-mod/KleeCode/Cards/Furina/SpotlightCards.cs:117-119` — "Their printed
damage and Block are 50% stronger, but their plays do not generate Fanfare."

### 2.7 Encore has a bar and no denominator

Encore is unbounded by design — `tier0/constants.py:165` ("Encore is
unbounded per-combat (v1.6) -- no cap constant by design"), and
`tier0/engine/resources.py:330-352` `gain_encore` has no clamp. The display
lives on the Salon stage ribbon and renders as a **runway**, not a fill bar
(`klee-mod/KleeCode/Vfx/SalonVisualsBridge.cs:572-587`): a segment is one
turn of upkeep at the current stage, because "the old denominator was a
display-only 20 -- a lie twice over, because Encore is uncapped and because
20 meant nothing to the player."

So [USER]'s "Encore should be a status bar like Regent's stars" is
achievable as a **count**, and is not achievable as a *fill fraction*. There
is nothing to be a fraction of.

### 2.8 The Encore spender family does not exist yet

`docs/current/STATE.md` "Active workstreams": "`F3` / the Furina
ENCORE-SPENDER family → `Win11`, which opens only AFTER the pilot's Encore
opportunity-cost repair — spenders cannot be priced against a resource the
pilot values wrongly". Both windows are FROZEN by R213. The richness brief
told the table the same thing in plain words
(`review/active/richness-playtest-brief-2026-08-26.md:107-111`): "Encore now
has a bar but almost nothing to spend it on… Note whether the Encore meter
felt stranded — filling with no door out."

### 2.9 The starter does not teach the default plan

`tier0/content/characters/furina.yaml:49-59` — ten cards, of which exactly
one is a Salon card and it picks its member at random
(`docs/furina-cards.yaml:83-84`, `member: random`). The brief again
(`…:127-131`): "The Salon is her default plan, but the starter barely teaches
it. Only one starter card is a Salon card and it picks its member at random.
The relic teaches the Spotlight instead."

### 2.10 The sheet census

`review/active/eb81-furina-remedy-options-2026-08-12.md:117-131`:

| plan | cards touching it | exclusive to it | exclusive by rarity |
|---|---|---|---|
| salon | 28 | 15 | 7 common / 5 uncommon / 2 rare / 1 basic |
| spotlight | 18 | 15 | 3 common / 8 uncommon / 4 rare |
| fanfare | **31** | 14 | 3 common / 5 uncommon / 6 rare |
| generic | 32 | — | 10 common / 13 uncommon / 5 rare / 4 basic |

Two facts travel with it (`…:128-131`): fanfare's exclusives are rare-heavy,
six of fourteen, so a change confined to them is a change most decks never
draw; and twelve cards are tagged `[fanfare, generic]`, i.e. fanfare cards
the drafter offers to salon decks too.

### 2.11 Blast radius, as already measured

`…:225-229`: "37 test files reference salon, 32 fanfare, 18 spotlight, across
a 190-file suite (of those, 16 / 10 / 6 pin the plan name as a quoted string
literal)." And: "each scorer change is a version bump, and a bump makes every
arm of the table discontinuous — for every character, not just Furina. The
re-baseline is therefore always the whole twelve-arm table, never the three
Furina rows."

### 2.12 R177, which E4 reopens

`020c888`, verbatim:

> "Direction ruled: rebalance the weak Furina plans until they are viable.
> NOT option B1 (fold plans into salon) and NOT B2 (widen the stage). The
> reasons on record: it is the reversible move, it preserves the S4-G7
> comparison series, and the four-slot sweep measured WORSE (16.7% -> 14.7%)
> because half of all upkeeps already arrive unable to fund a member — the
> stage outruns its fuel."

E4 is the fold R177 declined. The stage-outruns-its-fuel finding is not
overturned by E4 and is load-bearing for §4.3's rate arithmetic.

---

## 3. E4 — ruling text, DRAFT for [USER]'s countersign

> **R2xx — E4: Furina's four mechanics become one sentence.**
>
> **The architecture.** Salon members and Companion cards are one cast.
> Spotlight designates which half of the cast is Featured. Encore pays for
> and protects the performance. A Featured performance, and Encore
> deliberately spent to command one, are the only two things that create
> Fanfare. Fanfare **is** Furina's Burst meter; a full meter grants her
> Burst, and the Burst is a Grand Performance rather than a number.
>
> **What this reopens.** R177's ruled direction — "rebalance the weak Furina
> plans… NOT option B1 (fold plans into salon)" — is **overturned on the
> fold**. E4 is B1, done deliberately and under R213's authority rather than
> as a balance move. R177's measured finding that the stage outruns its fuel
> **stands** and constrains §4.3. The kickoff's deliberate separation of
> Encore and Fanfare as two independent counters
> (`docs/current/characters/furina-kickoff-v0.1.md` §4, "Encore is Furina's
> only managed resource" / "Fanfare is a global pool") is **retired**: they
> are one loop with one meter.
>
> **What E4 explicitly RETIRES.** Each of these is named so it cannot ride
> into the new architecture unexamined:
>
> 1. **"All Encore loss is progress."** `FANFARE_PER_HP_LOST`,
>    `FANFARE_PER_ENCORE_ABSORBED` and Salon upkeep stop generating. The
>    design invariant "every point of damage past Block prints exactly 1
>    Fanfare" (LAW; `tier0/constants.py:196`) is retired with them, along
>    with the test that asserts it.
> 2. **"Spotlight means +50%."** `SPOTLIGHT_BASE_MULT = 1.5` is retired as
>    the meaning of a designation. A Featured performance receives what
>    Furina's kit explicitly authors for it and nothing by default.
> 3. **Center Stage as a mode.** The two-mode Spotlight (R41, LAW "Character
>    identity — Furina") is replaced by two lanes of one cast: **Salon** and
>    **Guest Cast**. Furina's own cards stop being a Spotlight target.
> 4. **The Ethereal selector card.** The starting relic stops adding a card
>    to hand each turn and instead supplies the persistent Spotlight
>    interface. "Spotlight should not require playing a card every single
>    turn" ([USER]) is satisfied structurally, not by making the card
>    cheaper.
> 5. **Fanfare decay, floor and cap machinery.** `FANFARE_DECAY_FRACTION`,
>    `gain_fanfare_floor`, `raise_fanfare_cap`, `FANFARE_CAP_FRACTION` and
>    the `Fanfare +X` / `Fanfare Cap +X` keyword pair are retired: a Burst
>    meter does not decay, has no floor, and its ceiling is the meter size.
>    Register lint `R6` and `L12`, which exist to police those keywords,
>    retire with them.
>
> **Two LAW amendments** (exact replacement text at §3.1 and §3.2 below).
>
> **What the animation contract keeps.** All three contracted binding points
> (LAW: "Visuals bind to funnels, never to values") survive E4 unchanged:
> **Salon = 3 slot-index-keyed slots, duplicates legal** — E4 moves no slot
> and adds no member-to-slot assumption; **Encore-absorbs-before-HP** —
> unchanged, only its Fanfare side-effect is removed;
> **Spotlight-is-a-designation-event** — E4 makes this *more* true, since a
> designation stops carrying a multiplier. The centered-overhead
> creature-space slot stays the cross-character Burst indicator (LAW), and
> under E4 it renders the merged meter; that is one gauge where there were
> two, not a new binding point. The Encore ribbon
> (`SalonVisualsBridge.cs:572`) is a D3/D7 display decision, not a contracted
> binding point, so moving Encore beside HP breaches nothing — see §9 P4.
>
> **Its place in R213's sequence.** R213 ordered Kokomi → Klee → Furina,
> "not three explorations in parallel", and R217 left that untouched. Kokomi
> slice 2 and Klee round 3 are both open. Whether E4's build steps start in
> parallel with them or queue behind them is **[USER]'s call** (§9 P1). This
> packet is documentation and a doctrine gate only, which R213 lists as step
> 1 of the order and which consumes no slice capacity either way.

### 3.1 LAW amendment 1 — replaces LAW.md:74-75

Current text:

```
- **Spotlight/empowerment boosts numbers only, never turn-economy effects.**
  (principles §2.2a, v1.10)
```

Replacement (GPT's wording, adopted verbatim):

```
- **Spotlight is a designation, not a universal numeric multiplier.** A
  Featured performance may generate Furina's Burst resource and receive
  effects explicitly authored by Furina's kit. Spotlight alone may not
  duplicate a card, reduce its cost, add draw, or amplify nonnumeric
  Companion text. Explicit bounded Furina cards and her Burst may create
  additional Featured performances. (principles §2.2a, v1.10; R213 E4)
```

Note what the new text is *stricter* about and what it relaxes. It relaxes
"numbers only" — a Featured performance may now receive a Furina-authored
non-numeric rider. It tightens everything else: the four turn-economy verbs
the old guard banned (duplicate, cheapen, draw, amplify companion text) are
now banned **from the designation itself** rather than from empowerment
generally, and they remain reachable only through a printed Furina card.
That is why the old guard cannot simply be kept beside it: "boosts numbers
only" and "receives effects explicitly authored by Furina's kit" contradict
each other, and the second is the one E4 needs.

### 3.2 LAW amendment 2 — Burst kit-scope, amends LAW.md:144-146

Current text:

```
- **Neutral action-energy / draw / thin are legal enabler-grade companion utility**
  (one-shot, Exhaust-gated, costed) — the pool must let any character draft a
  kit-gap fix. **Burst-meter (`burst_energy`) generation stays character-kit-scoped**
  and must never be cheaply repeatable from companions. (principles §4.7, §2.4)
```

Replacement — the first two sentences are unchanged; one clause is appended:

```
- **Neutral action-energy / draw / thin are legal enabler-grade companion utility**
  (one-shot, Exhaust-gated, costed) — the pool must let any character draft a
  kit-gap fix. **Burst-meter (`burst_energy`) generation stays character-kit-scoped**
  and must never be cheaply repeatable from companions. **Furina's meter is
  the narrow case (R213 E4): only a Featured performance generates it, and at
  most once per turn. A Companion card generates none by being played; a
  Companion Featured by Furina's kit generates it under that same
  once-per-turn bound. HP loss, automatic Salon upkeep, Encore absorption and
  Skill tags feed her meter not at all.** (principles §4.7, §2.4; R213 E4)
```

This is the clause GPT's Constraint 1 says resolves the companion Burst-law
question, and it resolves it in the direction the existing law already
points: the meter stays kit-scoped because the *kit* is what Features the
Companion, not the Companion.

**What is NOT amended.** The kit-Burst clause at LAW.md:176-178 — "never
draftable, granted to hand on meter fill, casting empties the whole meter
(overflow lost at cast), re-granted on refill, carries Retain" — is
unchanged and E4 obeys it: the merged meter is granted on fill and emptied
on cast, exactly as `tier0/engine/combat.py:156-170` and `:380` already do.

---

## 4. Architecture

### 4.1 The shared performance hook

**The event.** One new engine event, `performance`, with a payload of
`(lane, source_card, actor)`. It fires in exactly two places, and GPT's step
2 builds exactly these two:

- **Salon lane** — fires once when a Salon member resolves its slot act at
  the start of Furina's turn. Today that path is the tick in
  `SALON_MEMBERS` (`tier0/constants.py:301-310`), priced at
  `SALON_TICK_ENCORE_COST = 1` per member (`:316`) and paying
  `SALON_TICK_BURST = 2` (`:319`). The hook wraps the existing tick; it does
  not replace it.
- **Guest Cast lane** — fires once when a Companion card resolves. That is
  the same predicate the selector already uses,
  `c.is_companion and not c.kit_card` (`tier0/engine/effects.py:1808`).

Everything else in E4 hangs off this one event. That is the point of doing
it first: until both lanes emit the same event, "choose a lane" is a choice
between one implemented thing and one imagined thing, which is GPT's
Constraint 3.

**What "Featured" does, per lane.** A `performance` event whose `lane`
matches the live Spotlight designation is **Featured**. Featured is a
per-performance property, bounded once per turn by §3.2.

**Salon lane, Featured — my reading:** *the Featured member performs its BOW
act in place of its tick, and is not displaced.* The numbers already exist,
ratified, per member (`tier0/constants.py:301-310`, ratified R187):

| member | tick (today) | bow (today) |
|---|---|---|
| crabaletta | 6 damage | 14 damage |
| usher | 3 Block | 9 Block |
| chevalmarin | 2 damage + Hydro | Hydro to ALL + 3 Encore |

So Featuring the Salon lane is a real, legible, *already-priced* upgrade —
roughly 2.3× on Crabaletta, 3× on the Usher, and a categorical change on
Chevalmarin (single-target aura → all-enemy aura plus Encore back). It
invents no breakpoint: every number is lifted off a shipped face, per the
doctrine-seat rule.

**Alternatives, if the seat or [USER] returns it:**
(a) the Featured member acts **twice** (tick, then tick) — simpler, but it is
"+1 per X" wearing a costume and it doubles Chevalmarin's aura, which is a
turn-economy effect in aura clothing;
(b) the Featured member's act gains the Focus bonus at a better rate
(`SALON_FOCUS_PER = 10`, `:311`) — but that is a numeric multiplier, exactly
what §3.1 retires;
(c) the Featured member's act is free of upkeep (`SALON_TICK_ENCORE_COST`
waived) — cheap and legible, but it *reduces* the Encore tension the slice
is trying to create.

**Guest Cast lane, Featured — my reading:** *a Featured Companion play mints
Fanfare and receives a Furina-authored rider printed on the card that
Featured it.* The rider is not the +50% (retired, §3) and is not a fixed
engine constant; it is whatever the Furina card that set or moved the light
printed. The default rider for the prototype, again lifted off a shipped
face, is Chevalmarin's bow rider: **apply Hydro** — which is Furina's
skill-grade cadence problem solved through the cast rather than through her
own tags.

**Alternatives:**
(a) the Featured Companion is played **twice** — explicitly banned by §3.1
("may not duplicate a card");
(b) the Featured Companion costs 1 less — banned by §3.1;
(c) the rider is a fixed engine-level buff (Weak, Vulnerable) rather than
card-printed — legal, but it re-centralises the thing E4 is decentralising,
and D4 wants the consequence readable off the card at the decision point.

**Why these two outcomes are "meaningfully different"** (GPT's test): the
Salon-Featured outcome is a *magnitude* change to a body already on the
board and already paid for; the Guest-Featured outcome is a *cadence and
application* change to a body you are choosing to play this turn. One
rewards having built a stage; the other rewards holding a card. They are not
two ways to produce meter.

### 4.2 Persistent Spotlight

**Combat-start choice.** The lane is chosen once, at combat start, through
machinery that already ships on both sides:

- **Mod (C#):** `CardSelectCmd.FromChooseACardScreen`, which
  `EtherealSpotlight.OnPlay` already drives
  (`klee-mod/KleeCode/Cards/Furina/SpotlightCards.cs:52-70`), with the two
  option models `CenterStageOption` / `GuestCastOption` (`:77`, `:109`)
  re-labelled to the two lanes. Note the comment at `:44-51`: the options
  must be combat-scoped owned instances created through
  `combatState.CreateCard`, or the screen softlocks. That trap is already
  documented and already solved; E4 must not re-derive it.
- **Sim (tier0):** the modal chooser seam, `_chosen_mode` at
  `tier0/engine/effects.py:2571`, the surface R192's Deep Breath prototype
  established and R194 ruled. It is **the only shipped modal card**
  (`effects.py:2558-2563`) and the module says so as a discipline, not an
  accident. E4 is the second use, and the reason it qualifies is the reason
  named there: the pilot must be able to distinguish the modes. Under E4 it
  can — the two Featured outcomes are different ops, not different sizes of
  one op.

Two consequences worth stating out loud. First, the seam's switch is a
`POLICY_VERSION` event when flipped on (`effects.py:2580-2591`), so a lane
choice the pilot actually *makes* renumbers every tier0.5 arm; behind the
E4 flag it stays at the staged fixed index, which is index 0 and is the
degenerate case of the policy. Second, the combat-start choice is a screen
before the first draw, which is a place no Furina screen exists today; it is
the one genuinely new UI surface in the whole architecture.

**Encore-priced moves.** Moving the light mid-combat is a card, and the price
rides the field that already exists: `encore_cost`, a **playability gate,
never an overdraw** — `tier0/engine/combat.py:168` refuses the play when
`state.player.encore < card.encore_cost`, and `combat.py:315-320` spends it
on resolution. `tools/gen_prototype_cards.py`'s field whitelist already
knows `encore_cost` is load-bearing and not an effect
(`docs/current/OPERATIONS.md`: "load-bearing: `encore_cost` changes
playability without being an effect"). So an Encore-priced Spotlight move is
`{op: spotlight_designate, ...}` on a card carrying `encore_cost: N` — no
new engine primitive at all.

**Click-on-rail, and what it would need.** Letting the player re-aim the
light by clicking the stage rail is the interaction [USER]'s "status bar"
language points at, and it is an **unverified interaction**: nothing in the
mod today accepts a click on a VFX node as a game action. It would need
(i) a hit-testable node on the rail — `SalonVisualsBridge` builds
`Node2D` displays with `RemoteTransform2D` tracking, not input-consuming
controls; (ii) a command path from that click into `SpotlightSystem.Designate`
that survives co-op ownership checks; (iii) an answer to how the blind-play
harness drives it, since `understudy/blindplay` resolves **printed names**
only and a rail click has no printed name. That third one is not a nicety:
an interaction the harness cannot express cannot be graded by the funnel, and
would silently exempt itself from the process R213 built. **Attempt it
second**, after the card-priced move has a verdict.

### 4.3 Fanfare becomes the Burst meter

**The two legs, and their rates.**

| leg | fires | rate | bound |
|---|---|---|---|
| L1 — a Featured performance resolves | on the `performance` event when `lane == spotlight` | **2** | once per turn |
| L2 — Encore deliberately spent through a Furina `Direct` effect | on `encore_cost` spend, and on `spend_encore` from a Furina card that commands a performance | **2** | once per turn |

Both rates are **2**, lifted off the shipped face
`FANFARE_PER_SPOTLIGHT_CARD = 2` (`tier0/constants.py:199`) — the existing
"a directed play pays the meter" rate. No breakpoint is invented.

L2 is deliberately narrow: `encore_cost` (the gate) and a Furina card's
explicit `spend_encore`, **not** `spend_encore_or_hp`'s overdraw half and
**not** Salon upkeep, which is automatic and therefore not direction. Per
GPT: "Fanfare evidence that the player directed the cast, not a general
activity odometer."

**The meter size is DERIVED, not picked.** Under R212 a derived-not-picked
number with a one-way error direction, an archive scope and one constant is
Claude's to ship, disclosed. Here is the arithmetic.

*Step 1 — what cadence ships today.* Income per player turn under today's
constants, for a mid-fight Furina with two members on stage:

| source | arithmetic | per turn |
|---|---|---|
| Salon ticks | `SALON_TICK_BURST` 2 × 2 members | 4 |
| Skill tags | `BURST_PER_SKILL_TAG` 5 × ~1 skill-tagged play | 5 |
| Encore spend | `BURST_PER_ENCORE_SPENT` 1 × upkeep `SALON_TICK_ENCORE_COST` 1 × 2 members | 2 |
| Reactions | `BURST_PER_REACTION` 5, intermittent | 0 (floor) – 2.5 (one every other turn) |
| **total** | | **11 – 13.5** |

Against `burst_max: 70` (`tier0/content/characters/furina.yaml:14`) that is
**70 / 11 ≈ 6.4 turns** at the floor and **70 / 13.5 ≈ 5.2 turns** with
occasional reactions. Call the shipped cadence **one Burst per ≈5–6 player
turns**. (Assumption named: two members on stage. R177's finding — "half of
all upkeeps already arrive unable to fund a member" — pushes the true
Salon-tick term *below* 4, so this estimate is if anything fast, and the
error direction is stated below.)

*Step 2 — what the new economy pays.* Exactly two legs, each bounded once
per turn, each paying 2: **4 per turn**, hard ceiling, no variance from
skill tags or reaction luck.

*Step 3 — the meter that preserves the cadence.* 4 per turn × 5 turns =
**20**.

*Step 4 — the sanity check against precedent.* **20 is Kokomi's shipped
meter** (`tier0/content/characters/kokomi.yaml:25`, `burst_max: 20`). So the
derived value is a size the roster already ships and the C# already renders,
not a new breakpoint. Klee ships 40 (`klee.yaml:11`) and Furina 70; 20 is the
low end of the existing range, which is correct for a meter with two bounded
legs instead of four unbounded ones.

**DERIVED: `burst_max: 70 → 20` for Furina, one constant, archive scope =
every Furina Burst number.** *Error direction, one-way:* if 20 is wrong it is
wrong **slow** — a player landing only one leg per turn fills in 10 turns
rather than 5, which under-delivers the Grand Performance rather than
over-delivering it. A meter that arrives too rarely reads as "the finale
never came" in a blind-play record and is caught by the funnel; a meter that
arrives too often reads as "nothing else mattered" and would corrupt the
slice's own question. The safe error is the one the instrument sees.

**Cast empties it.** Unchanged: `tier0/engine/combat.py:380`,
`p.burst_energy = 0`, and the LAW clause at 176-178. Overflow lost at cast,
re-granted on refill, carries Retain. E4 changes what fills the meter and
nothing about what emptying means.

**Grand Performance.** The Burst is not a number. It is: **both lanes are
Featured for one round.** The Salon lane's front member bows in place *and*
the next Companion played is Featured, simultaneously, for the round.

That state already exists in shipped C#. The Curtain Never Falls prints it
(`UpgradedStarterRelics.cs:399-405`): "Center Stage and Guest Cast are both
always active." E4 takes the relic's both-halves-live state and makes it the
Burst's payload — one round of it, on demand, instead of permanently and for
free.

**What The Curtain Never Falls becomes.** Under E4 its current text *is* the
Burst, permanently, which is not a relic upgrade — it is a deletion of the
character's finale. It has to become something else, and that is a design
pick, not a derivation. See §9 **P3**.

**What happens to the ~31 Fanfare-reading cards.** See §7.

### 4.4 Encore display

**Count, not a fill bar** — Encore is uncapped (`tier0/constants.py:165`) so
a bar has no denominator, and the shipped runway exists precisely because
someone already discovered that (`SalonVisualsBridge.cs:572-578`: "the old
denominator was a display-only 20 -- a lie twice over").

**The shape:** an integer beside HP, with **pips** for the low counts where
"how many more upkeeps can I pay" is actually a decision — one pip per point
up to a small ceiling, then the bare number. This is the Regent's-stars
reading of [USER]'s note: stars are a *count* of discrete things, not a
fraction of a maximum.

**The slot.** The centered-overhead creature-space slot is the
cross-character **Burst** indicator by LAW ("gauge skins are unique per
character"), and under E4 the merged meter is what renders there — so
Encore cannot go overhead. It goes on the **HP row**, beside the HP number
and Block shield, which is where the player already looks for "what stands
between me and damage" and is exactly what Encore is
(`resources.absorb_into_encore`, after Block and before HP, LAW:192-194).

**The runway becomes a tooltip.** The arithmetic
`SalonVisualsBridge.RefreshRibbon` already computes —
`segmentCost = members × TickEncoreCost`, `turns = encore / segmentCost`
(`:589-595`) — is the right *answer* in the wrong *place*: it is a
second-order derived quantity displayed as the primary one. It moves to the
hover tooltip on the Encore count: "N turns of upkeep at the current stage."
Whether the ribbon survives beside it is §9 **P4**.

---

## 5. Starter kit

### 5.1 What ships today

`tier0/content/characters/furina.yaml:49-59` — ten cards:

| × | id | face | source |
|---|---|---|---|
| 3 | `soloists_solicitation` | Attack, 1, 6 damage | `docs/furina-cards.yaml:56` |
| 3 | `stage_presence` | Skill, 1, 6 Block | `:66` |
| 1 | `regal_bearing` | Skill, 1, 3 Block + Weak 1 | `:69` |
| 1 | `aria_of_recompense` | Skill, 1, gain 5 Encore | `:72` |
| 1 | `salon_debut` | Skill, 1, deploy a **random** member | `:83` |
| 1 | `an_invitation` | Skill, 0, Exhaust, generate a common Companion | `:361` |

Plus the relic `ethereal_spotlight` (`furina.yaml:19-20`), which adds the
Ethereal selector card to hand each turn, and the kit Burst
`let_the_people_rejoice` (`furina.yaml:17`).

Two of the ten (`soloists_solicitation` ×1, `stage_presence` ×1) are
replaced at run start by the randomized Fontaine ensemble
(`furina.yaml:64-66`).

### 5.2 Delta to GPT's five-item list, card by card

| GPT's item | today | delta |
|---|---|---|
| **one deterministic Salon deployment** | `salon_debut`, `member: random` (`docs/furina-cards.yaml:84`) | **CHANGE.** The randomness was ruled in — A11, 2026-07-28, to separate the starter from the Common Surintendante Chevalmarin (`:85-90`). E4 needs it deterministic, because "which member is Featured" is the Salon lane's whole decision and you cannot plan around a coin flip. The separation A11 was protecting is recovered differently: the Common becomes the *second* copy of a named member, not the only way to name one. |
| **one generated Companion** | `an_invitation` — 0-cost, Exhaust, generates a common from the pool + Guest Star set (`:361-363`) | **NONE.** It is already exactly this card, and its four Guest Star guardrails (LAW:215-217) are untouched by E4. |
| **one Encore generator** | `aria_of_recompense` — gain 5 Encore (`:72-73`) | **NONE** on the face. One note: R130 vetoed giving this card a payoff rider (`:75-78`), and E4 does not reopen that — the generator stays a pure generator. |
| **one cheap Encore spend** | **ABSENT.** No starter card spends Encore. The whole spender family is `F3`, deferred to `Win11`, which is FROZEN by R213 and gated behind the pilot's Encore opportunity-cost repair (`STATE.md`) | **ADD ONE.** This is the largest gap and the one the slice cannot skip: without a spend, the Encore half of GPT's decisive question ("did spending Encore feel meaningfully different from keeping it for defense?") has no card to ask it with. See §9 **P6** — a prototype row bumps no stamp and enters no pool, so it does not open `Win11`, but whether that is a legitimate route around a freeze is [USER]'s call. |
| **relic supplies the persistent Spotlight interface** | `ethereal_spotlight` adds a card to hand each turn (`furina.yaml:19-20`; the card at `SpotlightCards.cs:16-73`) | **CHANGE.** The relic keeps its name, art and slot and changes its job: at combat start it opens the lane choice, and thereafter it *is* the lane display. It grants no card. This is the direct answer to "Spotlight should not require playing a card every single turn." |

### 5.3 What that leaves

Eight of the ten cards are unchanged. The deltas are: one card's parameter
(`salon_debut`, random → named), one card added (a cheap Encore spend), and
one relic's mechanism (card-per-turn → persistent interface). That is a
smaller starter change than the architecture might suggest, and it is
deliberate — the starter is where D2's "reachable early and reliably"
requirement bites, and eight familiar cards is what keeps the slice about
the new sentence rather than about a new deck.

---

## 6. The testable slice

GPT's six-step order, with flags, tests and the pre-registered question.

### 6.1 Flags

The repo's prototype-flag convention is a **compile-time MSBuild property
plus an id prefix**: `dotnet build klee-mod/KleeCode -p:PrototypeCards=true`,
every row's id starting `proto_`, and the classes not compiled at all in a
release build (`docs/current/OPERATIONS.md`, "Prototype surface (`EB-147`)").
E4 follows it and adds one master switch and five sub-flags underneath:

| flag | step | scope | default |
|---|---|---|---|
| `FURINA_E4` | master | C# compile symbol, defined only under `-p:PrototypeCards=true`; sim-side module constant in the E4 module, not `constants.py` | OFF |
| `FURINA_E4_HOOK` | 2 | the `performance` event fires; nothing reads it | OFF |
| `FURINA_E4_LANE` | 3 | persistent lane, combat-start choice, `encore_cost` moves | OFF |
| `FURINA_E4_METER` | 4 | Fanfare and Burst are one meter; Grand Performance | OFF |
| `FURINA_E4_STARTER` | 5 | the §5.2 starter deltas | OFF |
| `FURINA_E4_MIGRATE` | 6 | the ~31 readers move to the merged meter | OFF |

**An honesty note that has to be in the packet, not discovered later.**
Staging a prototype *card row* bumps no stamp, because prototype rows never
enter tier0's card index (`OPERATIONS.md`). Steps 2–4 are **engine**
behaviour, not rows. A default-off flag in the engine bumps no stamp *while
it is off*; **turning any of them on is a `CONSTANTS_VERSION` event** and, for
step 3's modal seam, a `POLICY_VERSION` event as well
(`tier0/engine/effects.py:2580-2591`). Every measured number taken with a
flag on is taken in a new world and is not comparable to the standing
baseline. That is not a reason to avoid the flags; it is a reason to say so
before the first table is quoted.

### 6.2 The six steps

**Step 1 — write E4 and its doctrine gate.** *This packet, plus §10.* Test:
the seat answers FOLLOWS or REQUIRES_MODIFICATION per item and names the
clause. Proves: the architecture does not contradict the charter before
anything is built.

**Step 2 — the smallest shared performance hook.** `FURINA_E4_HOOK`. One
Salon action and one Companion play emit the same `performance` event.
Test: a scenario at the real game (the `EB-146` harness) where one Salon tick
and one Companion play both appear in the event log with the correct `lane`;
plus a tier0 unit test that the event fires exactly once per member act and
exactly once per Companion resolution. **Proves:** the two lanes are the same
kind of thing. Nothing is Featured yet and no number moves — this step is
deliberately invisible in play, which is what makes it safe to land first.

**Step 3 — persistent Spotlight.** `FURINA_E4_LANE`. Combat-start choice
through `FromChooseACardScreen`; the designation persists; a card with
`encore_cost: N` moves it. Test: a staged turn where the lane is set at
combat start and a second staged turn where moving it is affordable but
contested. **Proves:** [USER]'s "should not require playing a card every
single turn" is satisfied, and that a move is a real decision rather than a
formality. Falsified if the Encore price is never worth paying in any staged
board, which would mean the lane choice is de facto permanent.

**Step 4 — merge Fanfare with Burst; add Grand Performance.**
`FURINA_E4_METER`. Two legs at 2, meter 20, cast empties, Burst = both lanes
Featured for one round. Test: a scenario that fills the meter from the two
legs only and confirms no other event moves it (the negative test is the
important one — HP loss, upkeep and absorption must all read zero); a staged
turn on a full meter where the Grand Performance round is the decision.
**Proves:** the finale is a state the player steered into, and the meter is
evidence of direction rather than of activity.

**Step 5 — the starter-deck boards.** `FURINA_E4_STARTER`. The §5.2 deltas
staged as prototype rows. Test: the funnel — blind staged turns, the
four-question form, closeness per R213 F, two graders, replay of every graded
line. **Proves:** the starter teaches the sentence. This is the first step
whose output is a grade rather than an assertion.

**Step 6 — migrate nothing else until independent playthroughs answer the
D2 question.** `FURINA_E4_MIGRATE`, and it stays off until then. Test: none
yet, by construction.

### 6.3 The decisive question, as a prediction slate

Per `EXPERIMENTS.md`: **DRAFTED by Claude** from written design intent,
committed as its own commit labelled DRAFTED **before any run**, [USER]
countersigns in batch or vetoes within five days, grade goes in blind, and
every slot names the decision its outcomes would change.

The question, GPT's, unaltered: *"Did the player plan who would receive
Spotlight, and did spending Encore on that performance feel meaningfully
different from keeping it for defense?"* It splits into two gradeable slots.

**Slot 1 — direction.** *Predicted:* on the step-5 boards, a majority of
blind forms name the lane designation as a reason for the line taken, in
q1's own prose, unprompted. *Falsified if:* no form mentions the lane; **or**
forms mention it only as forced ("there was only one Companion"), which is
the §2.2 collapse rule surviving the redesign under a new name.
*Decision it changes:* falsification returns step 3 — the lane goes back to
being a per-turn card, or the combat-start choice acquires a mid-combat
contest it currently lacks.

**Slot 2 — the Encore tension.** *Predicted:* on a matched pair of boards
differing only in whether Encore is best spent commanding a Featured
performance or held as buffer, `closeness` SURVIVES on both — the gap between
the top two lines on the pilot's own EV surface does not show one line
overwhelmingly dominating (R213 F). *Falsified if:* closeness fails on
either board; **or** both graders take the same line on both boards, which
would mean the two uses of Encore are not actually in tension.
*Decision it changes:* falsification returns §4.3's L2 leg — either the
Direct-spend leg is not carrying its half of the meter, or the cheap spender
of §5.2 is priced wrong.

**Who grades:** the Codex seat, blind, per `EB-149`, two graders per turn.
The reason it is the seat and not fresh Claude is §8.

**Registered blind spot.** Every board in step 5 is staged, and a staged turn
cannot show whether a lane chosen at *combat start* still felt planned at
turn six. That is a whole-fight question, and it belongs to the automatic
post-ADVANCE gate — whole-fight blind play on a dev build, `EB-188`'s door.
The slate does not pretend to answer it.

---

## 7. Blast radius

**Tests.** 37 test files reference salon, 32 fanfare, 18 spotlight, across a
190-file suite; 16 / 10 / 6 of those pin the plan name as a quoted string
literal (`review/active/eb81-furina-remedy-options-2026-08-12.md:227-229`).
E4 touches all three names. The string-literal subset is the expensive half:
a rename or an archetype fold breaks them mechanically, not semantically.
One test dies outright rather than being updated —
`test_every_point_past_block_prints_exactly_one_fanfare`
(`tier0/constants.py:196`), which asserts the invariant §3 retires.

**The re-baseline.** Any landed step is the **whole twelve-arm table**, never
the three Furina rows — "a bump makes every arm of the table discontinuous —
for every character, not just Furina" (`…:225-227`). Steps 2–4 each carry a
`CONSTANTS_VERSION` bump when their flag flips on, and step 3 carries a
`POLICY_VERSION` bump as well if the modal seam is switched on. Under R207
that is one window per step **where attribution matters**, and here it does:
the whole question is which of the four mechanics the merge fixed.

**The ~31 Fanfare-reading cards.** 31 cards touch fanfare, 14 exclusively, 6
of those 14 at rare (`…:120`, `:128-129`). Twelve are tagged
`[fanfare, generic]` and are therefore offered to salon decks too (`:130-131`).

**Prototype default: behind the flag they read the merged meter unchanged.**
The reader grammar does not change — `bonus_formula: 1_per_4_fanfare` and
`if: fanfare_at_least_N` (`docs/furina-cards.yaml:20-21`) both take a
non-negative integer off the player, and the merged meter is a non-negative
integer on the player. Every reader compiles and every reader reads. What
changes is the **rate and the ceiling**: a meter that fills to 20 on two
bounded legs is a different distribution from one that decays 20% a turn and
caps at half of max HP, so a `fanfare_at_least_20` gate that used to open
sometimes may now open only at the finale. That is a real balance change and
it is exactly why migration is step 6 and not step 4: the readers are
*correct* under the merge and *mispriced* by it, and pricing them is a sheet
edit — a `CONSTANTS_VERSION` event under LAW's material-card-sheet-edit rule
— that should happen after the architecture has a verdict, not before.

**What retires with the merge.** `FANFARE_DECAY_FRACTION` and
`decay_fanfare`; `gain_fanfare_floor` / `raise_fanfare_cap` and the two
printed keywords; `FANFARE_CAP_FRACTION`; register lints `R6` and `L12`
(`tools/lint_furina_registers.py`), which exist only to police those
keywords. Nothing on any shipped sheet carries `raise_fanfare_cap` today
(`STATE.md`: "Furina's pool carries **zero** `raise_fanfare_cap` riders"), so
that half is already free.

**Co-op.** Not addressed by this packet and deliberately so. The kickoff's
co-op Fanfare coupling ("partner HP/Encore flux counts toward Fanfare",
kickoff §4) dies with the HP-loss leg, and cross-player selector passing
(kickoff §3.1) has no selector card to pass. Both are consequences of E4 that
need their own pass; there is no automated co-op backstop for a live
`CombatState` (`klee-mod/KleeTests/README.md`), so neither can be settled by
a test. Flagged here, not resolved.

---

## 8. Independence

The direction in §1.3 and §1.5 is **GPT-co-authored with [USER]**. The
specifics in §4–§6 — the two Featured outcomes, the derived meter, the flag
ladder, the slate — are **Claude-authored**. Under R217 C independence is by
model FAMILY, author against grader, and the roles are fixed at two: Claude
authors, GPT grades and reviews (`OPERATIONS.md`, "Doctrine seat protocol").
There is no third family and none is being added.

That splits this packet's independent reader in two, and the split is not
optional:

- **§3's E4 rule rows** — the ruling text, what it retires, the two LAW
  amendments. GPT co-authored the direction and wrote the §3.1 wording, so
  the seat cannot be the independent reader here: it would be reviewing its
  own text. **A fresh Claude session, with no part in this drafting, is the
  independent reader on the E4 rule rows.**
- **§4–§6's specifics** — Claude authored them, so **the Codex seat (GPT) is
  the independent grader there**, and it is the seat that runs the doctrine
  gate at §10 and grades the step-5 forms.

**This is a case-specific allocation, not a general rule.** "Fresh Opus
grades" is never written as the house rule and must not be inferred from
this packet: the house rule is R217 C's model-family separation, and it
happens to point at a fresh Claude here only because GPT authored the rows
in question. Where Claude authors, GPT grades — which is the ordinary case
and the case for everything the slice actually builds.

**`authored_by:` consequences.** Every prototype row this slice stages
records its families (`EB-190`, `docs/prototype-surface.yaml` header). The
§5.2 starter rows are `[claude]`. If the seat's §10 gate returns an item and
Claude re-derives from the named clause, the row stays `[claude]` — a clause
name is explicitly not a contribution. If the seat supplies text, a number or
a mode, its remedy is **discarded** and Claude re-derives; that is the
protocol, and it is what keeps `seat grade` from refusing the turn later.

---

## 9. Picks for [USER]

Only what the packet cannot settle. Everything else is derived, cited, or
already ruled.

**P1 — sequencing.** R213 ordered Kokomi → Klee → Furina, "not three
explorations in parallel"; Kokomi slice 2 round 2 and Klee round 3 are both
open.
 (1) E4's build steps run **in parallel** on their own branch, starting now.
 (2) E4 stays **queued** behind the open Kokomi and Klee rounds; this packet
     and its doctrine gate land now, and step 2 waits.
 *(Claude's read: (2). The packet is R213's own step 1 and consumes no slice
 capacity; starting step 2 in parallel is the thing R213 named and declined.)*

**P2 — the two LAW amendments (§3.1, §3.2).**
 (1) Ratify both as written.
 (2) Ratify §3.1 (Spotlight-is-a-designation) only; hold the Burst kit-scope
     clause until step 4 has a verdict.
 (3) Return both — E4 proceeds behind flags without amending LAW, and the
     amendments land with the slice's acceptance.

**P3 — what The Curtain Never Falls becomes.** Its current text is the Grand
Performance, permanently and free, which under E4 deletes the finale.
 (1) It makes Spotlight moves **free** — the `encore_cost` on lane-move cards
     is waived. Keeps the relic about direction; costs the slice its Encore
     tension on the one run where the boon appears.
 (2) Each combat **opens with one round of Grand Performance**. Keeps the
     both-halves-live flavour, bounded.
 (3) **Both lanes permanently Featured** — today's text, which under E4 is
     enormously stronger than it is today.
 (4) **Retire it.** Furina's Ancient relic upgrade becomes something else
     entirely, authored fresh.
 *(Claude's read: (2). It is the only option that preserves the relic's
 printed identity while leaving the Burst something to be.)*

**P4 — the Encore display, and what happens to the ribbon.**
 (1) Count + pips beside HP; the Salon ribbon **keeps** its runway rendering
     as the stage's own fuel gauge.
 (2) Count + pips beside HP; the ribbon is **retired**, its arithmetic moving
     to the tooltip on the count.
 (3) Keep the ribbon only; add nothing beside HP.
 *(Claude's read: (2). Two displays of one resource is the legibility
 complaint in miniature. But (1) is defensible — the ribbon shows Encore's
 relationship to the stage, which the count cannot — and this is eyes-on
 taste, so it is [USER]'s.)*

**P5 — Grand Performance's body.** §4.3 proposes "both lanes Featured for one
round", reusing the relic's shipped state.
 (1) As written.
 (2) Both lanes Featured **until the meter is next spent** — a state rather
     than a round.
 (3) Something else, on [USER]'s brief.
 *(Claude's read: (1). (2) makes the finale a permanent mode change, which is
 the shape R213 E4 is trying to get away from.)*

**P6 — the starter's missing cheap Encore spend (§5.2).** The whole spender
family is `F3` → `Win11`, FROZEN by R213 and gated behind the pilot's Encore
opportunity-cost repair. A prototype row bumps no stamp and enters no pool.
 (1) Stage one spender as a **prototype row** under `FURINA_E4_STARTER`. The
     freeze is on shipped content and this is not shipped content.
 (2) **Wait on `Win11`.** Step 5 runs without a spender and the slate's Slot 2
     goes ungraded until the family exists.
 (3) Unfreeze `Win11` for this one card, by ruling.
 *(Claude's read: (1), and it is close to a ladder item rather than a pick —
 but "does a prototype row route around a freeze" is a governance question
 with a precedent attached, so it goes to [USER] rather than being assumed.)*

**P7 — migrating the ~31 Fanfare readers (§7).** They compile and read
unchanged behind the flag; they are mispriced by the merge.
 (1) Step 6 migrates them as a sheet edit — `CONSTANTS_VERSION` bump plus a
     twelve-arm re-baseline.
 (2) E4 ships the merged meter with the readers untouched, and repricing is a
     separate later window.
 (3) The readers are cut rather than repriced, folding fanfare's 14
     exclusives into the other two plans.
 *(Claude's read: (1), but not until the D2 question is answered — which is
 GPT's step 6 and is why this is a pick about *when*, not a pick about
 whether.)*

**Not picks, recorded so they are not mistaken for picks.** The meter size
20 is DERIVED under R212 (arithmetic at §4.3, one constant, one-way error
direction, archive scope named) and is Claude's to ship, disclosed. The two
leg rates of 2 are lifted off `FANFARE_PER_SPOTLIGHT_CARD` by the
shipped-face rule and invent no breakpoint. The flag ladder at §6.1 follows
the existing prototype convention and settles nothing new.

---


============================================================================
YOUR OUTPUT

One verdict block per gate, in this order: G1, G2, G3, G4, OVERALL.

Each block:

  <GATE>: FOLLOWS | REQUIRES_MODIFICATION
  Clause: <the LAW line, D-charter item, or kickoff section -- only when
           REQUIRES_MODIFICATION; quote enough of it to be unambiguous>
  Why: <one short paragraph naming what in the packet collides with that
        clause. No remedy. No number. No rewritten text.>

Nothing else. No preamble, no summary of the packet back to me, no advice.
