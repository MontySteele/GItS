# Bag of Tricks, re-asked — the independent seat's doctrine verdict, verbatim

**2026-08-30.** The repo-visible doctrine seat, run under the clause-only
protocol (`docs/current/OPERATIONS.md`, "Doctrine seat protocol"), on the brief
committed beside this file as
`review/qa/bag-of-tricks-reask-doctrine-prompt.txt`.

```
python -m understudy.seat review review/qa/bag-of-tricks-reask-doctrine-prompt.txt \
    --role doctrine --model gpt-5.6-sol \
    --out review/qa/bag-of-tricks-reask-doctrine-codex-gpt-5.6-sol.md
```

**Model observed:** `gpt-5.6-sol`, via OpenAI Codex v0.150.1 — `approval:
never`, `sandbox: read-only`, `--ignore-user-config --ephemeral`, session
`01a050dc-b7a3-75e0-90cd-fa9e1823500a`. The raw stdout and stderr transcripts
sit beside this file and are **gitignored** (`.gitignore:75-76`) for the same
reason every seat transcript is: they carry a third party's system prompt and
raw output. This record and the brief are the committed artifacts.

**Why this read exists.** R224 item 17 took `M47` option (3) — build per-mode
playability (`EB-182`) first, then re-ask the seat. `EB-182` merged (#200,
`ac52e46`, main `ccc8a23`). This is the re-ask, and it is ONE Codex call: one
brief, one reply, no second send.

**What was asked.** One arm (`proto_spark_mode_bombs`, "Bag of Tricks"), its
text reproduced verbatim and not re-authored; the two clauses the seat's
2026-08-29 read held it on, quoted back; what has changed since (the per-option
playability rule as `OPERATIONS.md` now states it, the alternative-cost Spark
economy as LAW now carries it, and the explicit statement that nothing else
moved); and LAW's D-charter and Klee identity bullets. The seat was asked for a
verdict, the clause quoted, and a plain yes/no on whether D4 is closed.

Everything below the line is the seat's own words, **unedited**. No verdict has
been argued with.

---

REQUIRES_MODIFICATION

CLAUSE — TOP-LEVEL COST RULE: “The expensive modal body necessarily puts the expenditure inside one choice rather than at the card’s top level.” The written top-level cost rule would have to change.

D4 — RESOLVED: Per-option playability closes D4 because unaffordable modes are omitted, fully priced-out cards are refused with an explanation, and affordable options visibly declare their price.
