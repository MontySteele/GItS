# proofs-9 lane 1 frames — 2026-09-16

Material for a person to look at, taken off the running game on **lane 1**
(port 15527, the disposable profile at `%LOCALAPPDATA%\gits-lanes\lane1`)
against the installed build `0.2.3541+proto.dirty`, arms
`klee,companion,kokomi,furina-stage,teyvat`, `TeyvatFrame` ON. The capture was
made **by pid** (39476) through `understudy.frames` (`PrintWindow`), never by
image name, so it cannot be lane 0's window — another agent held lane 0
(pid 41464) the whole time.

`frames-manifest.jsonl` is the literal capture log and carries one row per
capture. Its `path` names the full 3841x2160 original; the committed file is
that frame **downscaled to 960px wide**, as 8c committed its shop frame, and
the original was not kept.

**GUARDRAIL.** A frame here is MATERIAL, not evidence. Nothing a bot or an LLM
derives from one is a claim about look, legibility, readability or fun — those
stay [USER]-only instruments (Guardrail-7, and the no-fun rule). The only
claims the record makes off this frame are mechanical: this card region carries
an illustration; this string is printed with its markup showing.

| frame | what it is |
|---|---|
| `eb786-curtain-rise-mode-chooser.png` | `EB-786`. The Curtain Rise mode chooser on a Furina Stage run, act 1 floor 2, seed `64WJ72QL8Z14`. Both mode faces — `..._CURTAIN_RISE_MODE_A` and `..._MODE_B` — draw the parent's portrait (`art_of: aria_of_recompense`), which is what the row asks. The same frame is where the record's off-list finding 1 is read: mode B's **title** prints the raw `[gold]…[/gold]` markup while the body line under it renders the same word in gold. |

The record these belong to is
[`review/records/teyvat-proofs-9-lane1-2026-09-16.md`](../../records/teyvat-proofs-9-lane1-2026-09-16.md).
