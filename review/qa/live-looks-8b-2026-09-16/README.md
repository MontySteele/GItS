# live-looks-8b frames — 2026-09-16

Material for a person to look at, taken with `understudy/frames.py` on the
installed `0.2.3480+proto.dirty` build, lane 0. Every frame was captured by
**pid** (never by image name) so nothing here is the other lane's window; the
one batch that was taken by image name landed on the other lane's game and was
deleted unread from the record.

`frames-manifest.jsonl` carries the capture rows, each with its own guardrail
sentence. The frames were downscaled to 1280px wide for the record and the raw
3842x2160 captures were not kept.

**MATERIAL, NOT EVIDENCE.** Nothing a bot or an LLM derives from a frame here
is a claim about look, legibility, readability or fun. Those are [USER]'s
instruments (Guardrail-7). What this record asserts about a frame is only
mechanical: a pixel changed, a sentence is present, a badge is blank.

| file | row | what it is |
|---|---|---|
| `eb220-meter-cost-badges.png` | `EB-220` | a combat hand holding Pocket Match (1 Spark) and Ka-pow! (0 energy), with the Spark meter beside the energy gem |
| `eb38-restsite-breath-a.png` / `-b.png` | `EB-38` | the same rest site 0.6 s apart; the only pixels that differ between them are inside the portrait |
| `eb686-auto-take-full.png` / `eb686-auto-take-bubble.png` | `EB-686` | Read the Field on a one-card draw pile, and the line it says |
| `eb65-power-badges-and-nope-relic.png` | `EB-65` | four shipped Furina powers live; also the relic row |
| `eb65-power-badges-blank-crop.png` | `EB-65` | the badge row, close |
| `eb652-salon-panel-standing.png` / `-hover.png` | `EB-652` | the shipped Salon panel under the Stage arm, standing and with a Companion under the cursor |
