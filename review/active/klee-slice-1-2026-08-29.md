# Klee slice 1 — what Sparks could be worth, put to the gate before anything is built

**2026-08-29. Branch `klee-slice-1`, cut from `origin/main` at `0f96c4e`.**
Executing R213 E2 under the R213 course-correction and the R217 seat rules.
Everything proposed below would live on the quarantined prototype surface
(`docs/prototype-surface.yaml`); **no shipped Klee row would move by one
character**, and under R215 B no number measured on a prototype row is
quotable in a packet, a register or a commit message.

**I designed these arms, so I am not allowed to say whether any of them is
good.** That is R213's first guard. Tonight [USER] added a second one, and it
is why §1–§3 exist as their own commit before any code: the independent seat
reads this proposal *first*, and rules on each arm against Klee's written
doctrine. An arm the seat says needs the doctrine changed is **held for the
morning**, not authored.

Sections §1–§3 are the proposal as it went to the gate. §4 onward are written
after the gate has spoken.

---

## 1. The question this slice is asking

R213 E2, in full:

> **E2 — Reopen "Sparks mean free Attack": YES. Likely upstream of Klee's
> Attack-spam identity.**

The finding it answers is [USER]'s, from the 2026-08-26 exploratory playtest
(mod `0.2-1159`, raw notes `review/active/kokomi-playtest-notes-2026-08-26.md`).
Two lines carry it. R213's own summary of the run says Klee plays as
**"attack-spam Ironclad with less complexity"**. The run notes themselves say:

> *"Klee's cards mostly strike me as boring flavors of 'attack and/or bomb' —
> compared to Ironclad, who has a similar intended attack-focused playstyle,
> she seems much less interesting."*

### What the engine says, and it is sharper than the complaint

The Spark rule is three lines of `tier0/engine/combat.py`, and they are
**automatic and unconditional**:

```
if (card.type == "attack"
        and state.player.sparks >= spark_threshold(state)):
    return 0
```

…and, at play time, the bank is debited without being asked:

```
if (card.type == "attack" and cost == 0
        and p.sparks >= spark_threshold(state)
        and card.cost != 0 and card.cost != "X"):
    p.sparks -= spark_threshold(state)
```

So the bank has exactly one destination, it is chosen for the player, and it
is chosen the moment they play any Attack. There is no timing choice (the
discount lands on whichever Attack goes first), no targeting choice, no
conversion, and no way to forgo it. Measured against **LAW D2** —

> **D2 — Player-controlled leverage.** Every persistent resource and every
> automatic engine must feed a decision the player can steer: timing,
> targeting, placement, acquisition, conversion, or forgoing. "Watch it rise
> until the number is large" is not a decision.

— the base Spark rule feeds none of the six verbs. It is not even "watch it
rise": it is *watch it rise and then be spent for you*. That is the strongest
statement of E2 the repo can make, and it is a fact about the engine rather
than a taste claim.

### What already exists, so that this slice does not rebuild it

The Spark sink is **already built and already shipped** — this slice is not
inventing expenditure. `spend_spark` resolves in both engines; a short bank
pays nothing (never a partial spend); `combat.spark_cost` derives a
playability gate from the printed op so the price shown and the price paid
cannot drift apart; and the price must sit at top level or it is not a cost
line. Three shipped Uncommons print it (R211's W3 window):

| shipped sink | body | what the bank buys |
|---|---|---|
| `powder_charge` | Spend 2 Sparks. Detonate, +4. | a detonation |
| `hold_the_line` | Spend 2 Sparks. Block 5; +6 if the enemy intends to attack. | defence |
| `smoke_and_sparks` | Spend 2 Sparks. Apply 3 Vulnerable. | a debuff |

All three are **Skills**. That is the gap this slice is standing in: after
W3, Klee's *Skills* have a Spark decision and her *Attacks* still have none —
and "attack spam" is a complaint about her Attack turns. The sinks also charge
**2**, priced against True Spark Knight's reduced threshold ("2 is exactly one
free Attack under True Spark Knight"), not against the base rule E2 reopened.

### The hypothesis under test

> A resource that only subsidises Attacks feeds one decision — attack, or
> don't. A Spark that is **priced, thresholded, chosen against another
> outcome, or spent on something that is not an Attack** feeds a decision the
> player steers. If none of the four shapes changes how a turn plays, the
> answer to E2 is that the automatic free Attack was right and the rule closes
> again.

The three charter questions that bind are **D2** (does the bank feed a
steerable decision), **D3** (does the payoff carry a binding price), and
**D8** (does a Spark line change the *shape* of a turn rather than only its
number). The slice does not settle any of them by argument. It builds four
shapes, puts each on the same board as its shipped twin, and lets the funnel
ask.

### The one thing this slice deliberately does not touch

**The automatic free Attack itself stays exactly as it is.** Making it opt-in
would be a change to Klee's core mechanic (`klee-character-design.md` §3,
"at **3 Sparks**, next Attack costs 0 (consumes 3)"), which is a character
rule and not a card, and it cannot be quarantined on a card surface at all.
Every arm below is a **card**, and every arm is engineered to sit beside the
automatic rule without altering it. Where that took care, §2 says so.

That restraint is itself put to the gate as overall question 4.

---

## 2. The four arms, and how they would print

> **This section is the text the gate read, preserved unchanged, and two
> things in it are wrong.** The seat caught both, I verified both in the
> repository, and both are corrected in §4 — arm 4's shipped twin is
> `clockwork_toy` ("Imaginary Friend"), not `combustion_study`, and arm 2 as
> printed here draws three cards at the bar, not two. Arm 2's body and arm 4's
> Burst figure also changed on the seat's instruction. **§4 is what was
> actually built; this section is the record of what was asked.**

Four rows on `docs/prototype-surface.yaml`, compiled only into a dev build. A
shipped mod does not contain the classes at all, so there is no id a normal
game could be talked into granting.

### Arm 1 — the Attack pays the bank instead of being paid by it

| | |
|---|---|
| id | `proto_spark_priced_strike` |
| name | **Second Helping** |
| cost | 0, Attack, Uncommon |
| prints | *Spend 3 **Sparks**. Deal `{Damage}` damage twice.* (6 × 2) |
| shipped twin | `flame_on_the_wick` — *Flame on the Wick*, 0, Attack, Uncommon, 6 damage, **bank untouched** |

Same cost, type, rarity, target and damage figure as the twin. Two things
differ and they are one idea: the card charges the bank, and it hits twice for
having done so.

**Why this is the arm that speaks to "attack spam".** With three Sparks banked
and this card plus any paid Attack in hand, the turn has an order that
matters and cannot be dodged. Play the paid Attack first and the automatic
rule makes it free and eats all three Sparks — and this card is now unplayable.
Play this card first and it takes the three Sparks itself — and the other
Attack costs full price. **Two Attacks, one bank, and the player picks which
one the bank belongs to.** That is D2's *timing* and *forgoing* on the Attack
half of her kit, which is precisely the half W3 left without a decision.

**Why it needs no rule change, and this is engine-verified rather than
assumed.** A printed-**0**-cost Attack never debits the bank: `play_card`'s
debit branch is guarded by `card.cost != 0`, a guard that has been there since
R39 and R34 for their own reasons. So at bank 3 this card costs 0 (it always
did), takes no automatic debit, and its own top-level `spend_spark 3` is the
only thing that moves the meter. The automatic rule is untouched, and the
card sits beside it rather than against it.

**Doctrine it claims to follow, quoted.**
- **D2**: *"Every persistent resource and every automatic engine must feed a
  decision the player can steer: timing, targeting, placement, acquisition,
  conversion, or forgoing."* — timing and forgoing, on the Attack half.
- **D3**: *"Defence and engine advancement may share a card, but not both at
  full rate without a binding cost: … a card or resource spent …"* — the
  resource is spent, at top level, gated.
- LAW, Klee: *"X-cost attacks are exempt from spark-spend"* — untouched; this
  card is not an X card, and the exemption is not widened.

### Arm 2 — a bar to bank to, and the bank pays for what it used to read for free

| | |
|---|---|
| id | `proto_spark_threshold_draw` |
| name | **Rummage** |
| cost | 1, Skill, Uncommon |
| prints | *Draw 1 card. If you have 3 or more **Sparks**: spend 3 **Sparks** and draw 2 cards.* |
| shipped twin | `eager_to_help` — *Eager to Help*, 1, Skill, Common, draw 2 if you have any Spark, otherwise draw 1, **bank untouched** |

Both of the twin's numbers, 1 and 2, are unmoved. Two things differ and they
are one idea: the bonus now **costs** the Sparks it reads, and the bar moves
from "any Spark at all" to **the free Attack's own threshold**, so that the
bar and the price are the same fact and the card competes with the rule
directly.

`eager_to_help` is the purest "watch it rise" card on the sheet — it looks at
the bank, takes nothing, and pays more for the bank existing. This arm asks
whether the same bonus is more interesting when it is bought.

**Doctrine it claims to follow, quoted.** **D2** — *conversion*, and
*acquisition* on the card the extra draw buys. **D3** — *"a resource spent"*,
and *"a future draw or deck cost"* read the other way. **D5**: *"Any added
line of text must alter a decision."* — the added clause is a price, which is
the only kind of line that always does.

### Arm 3 — two prices for one card

| | |
|---|---|
| id | `proto_spark_mode_bombs` |
| name | **Bag of Tricks** |
| cost | 0, Skill, Uncommon |
| prints | *Choose one: Place 1 Bomb dealing 5 \| Spend 3 **Sparks**: Place 3 Bombs dealing 5.* |
| shipped twin | `pop` — *Pop!*, 0, Skill, basic, place 1 Bomb dealing 5 |

The cheap mode is the shipped twin printed alone, exactly — so the pair asks
what the second mode is worth and nothing else. The expensive mode is
`bomb_voyage`'s body verbatim (3 Bombs dealing 5), which ships at **2 energy**;
here the bank buys those two energy instead. Every number on the face is
lifted off a shipped face; nothing is picked.

**What the bank buys that it has never bought before: placement.** D2 lists
placement as a steerable verb, Klee is the only character in the roster who
has one, and Sparks have never touched it. This is also the arm that most
directly changes the *shape* of a turn rather than its size: three Bombs are
delayed damage on a board the player has to survive to collect, so taking the
expensive mode is a decision about the next turn, not this one.

**Doctrine it claims to follow, quoted.** **D2** — *placement*. **D3** —
*"mutually exclusive outcomes"*, named in the clause as a legal binding price,
and *"a resource spent"*. **D8**: *"Archetypes differ in how turns and drafts
unfold … the route, cadence, constraint, targeting, transformation, control or
economy must differ."* — the bank crosses from the Spark plan into the
Demolition plan, which is a route change, and `powder_charge` is the only
existing bridge in that direction.

### Arm 4 — the bank leaves the Attack economy altogether

| | |
|---|---|
| id | `proto_spark_burst_conversion` |
| name | **Slow Burn** |
| cost | 1, Skill, Uncommon, `skill_tag` |
| prints | *Spend 3 **Sparks**. Gain 5 Block. Gain 10 Burst Energy.* |
| shipped twin | `combustion_study` — *Combustion Study*, 1, Skill, Common, `skill_tag`, Block 5 and **3 Burst Energy**, bank untouched |

Cost, type, tag and the Block figure are unmoved. The Burst rider stops being
free and starts being bought, and it pays more for being bought.

Klee has three meters — Sparks, Bombs, Burst — and **Sparks and Burst have
never interacted in either direction.** Sparks buy Attacks; Burst fills from
Skills and reactions and casts the kit Burst. This arm is the only shape in
the slice where the bank buys something that is not damage, not defence and
not a Bomb: it buys *progress toward the one Klee payoff that is not an
Attack at all*.

**Doctrine it claims to follow, quoted.** **D2** — *conversion*. **D3** —
*"a resource spent"*. **D8** — *"the route, cadence, constraint, targeting,
transformation, control or economy must differ"*; this is economy. LAW, Klee:
*"The Burst (Sparks 'n' Splash) is kit, not loot: never draftable, granted to
hand on meter fill, casting empties the whole meter"* — untouched. This card
moves the meter; it does not grant, alter or draft the Burst.

### The one price, and the one number that is not a shipped number

Every arm that charges Sparks charges **three**. That is an attribution call,
not a balance one: four arms differing in shape *and* in price would leave
nothing about a turn attributable to the shape. Three is also **lifted, not
picked** — it is the free Attack's own threshold (`klee-character-design.md`
§3, "at **3 Sparks**, next Attack costs 0"), and E2's question is precisely
whether the bank is worth more as something other than that. A price of 2 —
the three shipped sinks' price — would have been a trade against True Spark
Knight's reduced threshold rather than against the rule under test. Charging
exactly the rule's price is what makes each pair a clean question.

Every other number on every face is lifted off a shipped face. **The one
exception is arm 4's `10`, and it is derived rather than picked**: the Burst
meter is 40 and a `skill_tag` card pays 5 (klee §3), so 3 Sparks buying 10 is
exactly **two skill-plays of Burst progress** — a rate, not a taste. It is
used once.

### What the engine would have to grow

Almost nothing, which is the point of standing on W3's work.

- **`spend_spark`** — built, shipped, printed by three Uncommons. No change.
- **`spark_at_least_three`** — arm 2 needs a Spark threshold predicate, and
  the sheet has only `has_spark` (`sparks > 0`). The parameterised family
  already exists and already contains the exact sibling: `charge_at_least_`,
  `fanfare_at_least_`, `encore_at_least_`. A `spark_at_least_` prefix on that
  same rail is the smallest possible addition, and it is quarantined the way
  `spend_charge` was — no shipped row uses it, so every drafted number in the
  world is byte-identical with and without this branch.
- **`choose_one`** — arm 3 uses the shipped modal grammar (`ModalChoice`,
  fixed and proven live at EB-150). No change.
- **Everything else** — `damage`, `draw`, `block`, `place_bomb`,
  `burst_energy` — shipped ops, unchanged.

**One known gap, disclosed rather than buried, and it is arm 3's.** The
choose-a-card screen has **no per-mode playability** (found by Kokomi slice 2
and filed there). Arm 3's expensive mode is offered whatever the bank holds.
It does not pay out on a short bank — the emitted body returns and the sim
raises the same refusal at the same point — but the face cannot grey the mode
out. On these boards the bank is set so it never fires. As a shipped shape it
would be a D4 defect, and arm 3's acceptance would inherit that defect as a
prerequisite.

---

## 3. The names, and what they lost to

Provisional names are mine under R212's ladder (R179), and these are all
provisional — they are deleted with the rows when the slice closes.

**Second Helping. Rummage. Bag of Tricks. Slow Burn.** Ordinary card names,
Mondstadt- and Klee-flavoured (she is a child with a satchel and an appetite),
and none of them says spend, threshold, mode, price, conversion or prototype.
The reason is Kokomi slice 1's and has not changed: a blind QA grader reads
printed card titles, and a title that names the experiment tells the grader
which arm they are holding, which is the one thing a blind grade cannot
survive.

Two collisions were checked and both bit. **"Bombs Away"** was the first
choice for arm 3 and already appears in `docs/klee-cards.yaml`. **"Powder
Keg"** was the second and sits one word from the shipped `powder_charge`
(*Powder Charge*), which is one of the three sinks this slice is standing
beside — two Spark-spending cards a syllable apart would be a legibility
defect of our own making. `docs/reserved-card-names.txt` and all three card
sheets plus `mondstadt-companions.yaml` were grepped for the four names that
survived; none appears anywhere.

The **turn ids** (`klee-slice1-t01` … ) are opaque for the same reason — they
are printed into the packet. The filenames name the arm, because only the
tooling and the packet-writer read filenames.
`understudy/turns/klee-slice-1/MANIFEST.md` is the map.

---

## 4. What the gate said, and what was built

> **Independence caveat, and it is now discharged.** Two of the rows below were
> built on content the doctrine seat authored, and the same model family later
> graded and pair-read them. **Both have since been RE-DERIVED Claude-side from
> the seat's clause** and both rows read `authored_by: [claude]`; the derivations
> are in §13.1 and the clean re-run is §13. This section is left as the record of
> what was built at the time — see *Independence* at the end of §11 for the
> lift.

The seat read §1–§3 at commit `c96f027` and ruled on all four arms. Its full
words, both rounds, are in
`review/qa/klee-slice-1-doctrine-review-codex-gpt-5.6-sol.md`. **I did not
argue with a doctrine verdict**, and where it corrected a fact I checked the
repository rather than taking the claim.

| arm | fixes the noted issues? | doctrine | outcome |
|---|---|---|---|
| Second Helping | PARTLY | **FOLLOWS** | built, unchanged |
| Rummage | PARTLY | REQUIRES_MODIFICATION → **FOLLOWS** after alteration | built, re-authored by the seat |
| Bag of Tricks | PARTLY | **REQUIRES_MODIFICATION** + an admitted D4 defect | **held for you — §6** |
| Slow Burn | PARTLY | **FOLLOWS** | built, at the seat's number, twin corrected |

Round 1's overall answer to "does this proposal as a whole require modifying
Klee's design doctrine?" was **YES**. Round 2's answer, on the reduced
three-arm slate, was **NO**. That is why three arms were built and one is
waiting for you.

**No arm was marked NO on fixing the noted issues.** Every one came back
PARTLY, and the seat's reason is the same each time and worth reading straight:
each card gives the bank a decision *where that card is in hand*, and none of
them stops the automatic rule from taking the bank on every other turn. That is
a true and useful answer to E2, and §6 carries what it points at.

### The three rows, as they print

**Arm 1 — Second Helping.** `proto_spark_priced_strike`. 0, Attack, Uncommon.

> *Spend 3 **Sparks**. Deal 6 damage twice.*

Shipped twin `flame_on_the_wick` — *Flame on the Wick*, 0, Attack, Uncommon,
6 damage, bank untouched. Unchanged from the proposal. The seat's note on it:
*"This is the only arm that directly changes an Attack turn … 'play every
Attack' is no longer automatically correct"*, and *"keep the arm as written for
the prototype. I would alter only the claim made for it: test it as 'better
Attack sequencing,' not as a complete cure for Klee's narrow card vocabulary."*
That narrowing is accepted and is why §6 exists.

**Arm 2 — Rummage.** `proto_spark_priced_draw`. 1, Skill, Uncommon.

> *Spend 3 **Sparks**. Draw 3 cards.*

Shipped twin `eager_to_help` — *Eager to Help*, 1, Skill, Common, draw 2 if you
have any Spark and otherwise 1, bank untouched.

**This row is the seat's, not mine.** It was proposed as a threshold arm —
*Draw 1. If you have 3 or more Sparks: spend 3 and draw 2* — and the seat ruled
REQUIRES_MODIFICATION against the rule authored on the shipped sheet at
`powder_charge`:

> *"THE COST MUST STAY AT TOP LEVEL. A `spend_spark` inside a conditional
> branch is invisible to the playability gate, and the payoff would then fire
> unpaid. That is a structural rule about this verb, not a preference about
> this card."*

My threshold guard would have stopped an unpaid payout, but the rule is
categorical and the price still would not have been a cost line — the seat's
words: *"that does not make the spend a top-level cost or make `spark_cost`
recognize it. Admitting this design requires carving an exception into that
categorical structural rule."* It supplied the remedy itself, verbatim, and
confirmed on the second read that the altered row FOLLOWS and that the Klee
generation limb (*"no sub-Rare card is simultaneously a spark source and a draw
enabler"*) does not reach it, because Rummage is a **sink**, not a source.

**The id lost the word `threshold` with the conditional, and so did the slice.
There is no threshold arm any more.** Kokomi slice 2 had one; this one does
not, and it is a real narrowing rather than a relabelling.

**Arm 3 — Slow Burn.** `proto_spark_burst_conversion`. 1, Skill, Uncommon,
`skill_tag`.

> *Spend 3 **Sparks**. Gain 5 Block. Gain 10 Burst Energy.* — and, printed by
> the generator because the tag is real, *Burst +5.*

Shipped twin `clockwork_toy` — *Imaginary Friend*, 1, Skill, Common,
`skill_tag`, Block 5 and 3 Burst Energy, bank untouched.

**Both the twin and the number are the seat's corrections, and both were
verified before use.** I had named `combustion_study` as the twin; it is
actually *Burst 10 + Draw 1* (`docs/klee-cards.yaml` line 160), and the
Block 5 + Burst 3 `skill_tag` Common is `clockwork_toy` (line 195). And
`skill_tag` pays `BURST_PER_SKILL_TAG = 5` automatically
(`tier0/constants.py:69`, `tier0/engine/combat.py:383`), so the printed number
is never the meter movement.

The one thing I put back to the seat rather than accepting: its arithmetic fix
(printed 10 → 5) would have made three Sparks buy **2 Burst** over a Common
that costs no Sparks at all, which leaves the prototype half of the pair with
no second line — the exact thing the funnel exists to refuse. That is a balance
observation on an arm it had already ruled FOLLOWS, not a challenge to a
verdict, so I gave it both numbers and the arithmetic and asked it to choose.
It chose **10**: *"Printed 10 makes those Sparks buy 7 additional Burst Energy,
giving the choice a credible opportunity cost against `kaboom` while using a
shipped Klee value rather than an invented breakpoint."*

### The price, and the numbers

Every row charges **three Sparks**, and three is **lifted, not picked** — it is
the free Attack's own threshold, which is the rule under test. The three
shipped sinks charge 2, but 2 was authored against True Spark Knight's reduced
threshold. The seat was asked about this directly:

> *"Three is the right controlled price for this slice. The experiment is
> explicitly about competition with the base three-Spark free Attack … Using
> two here would confound the question. That does not establish three as the
> eventual universal price."*

**There is no derived number in this slice at all.** 6 damage at cost 0 is
`flame_on_the_wick`'s; `times: 2` is the sheet's standard multi-hit count;
Block 5 is `clockwork_toy`'s; Burst 10 is `combustion_study`'s. The proposal
had one derived number and the seat's arithmetic correction removed the need
for it.

### What the engine grew: nothing

**No new op, no new predicate, no new priced verb, no `DRAFTER_VERSION`
question.** `spend_spark` has been in both engines since EB-118 Phase 2, and
three shipped Uncommons have printed it since R211's W3 window; its playability
gate (`combat.spark_cost`) and its no-partial-spend rule are shipped behaviour
these rows only reuse. The proposal's arm 2 would have needed a
`spark_at_least_` predicate; the seat's re-authoring removed the conditional
and with it the predicate. `gen_roster_cards --check` is clean, so every
shipped generated file is byte-identical with and without this branch.

**And the automatic free-Attack rule is untouched.** Arm 1 sidesteps it rather
than altering it: `play_card`'s debit branch is guarded by `card.cost != 0`
(there since R39/R34 for their own reasons), so a printed-zero Attack takes no
automatic debit and its top-level spend is the only thing that moves the meter.

---

## 5. The boards, and why each is set that way

Six turns in three matched pairs. Within a pair the two files declare the
**same board** — HP, Block, energy, Spark bank, enemy, and the same
alternatives in hand — and differ in exactly one card. Every board carries
`exact_hand: true` (EB-165), so the staged hand is the declared hand and not
the declared hand plus whatever the game dealt.

Every board is **42/62 HP, no Block, turn 3, 2 energy, one enemy telegraphing
an attack**, and a **Spark bank of exactly three**.

| pair | arm | Spark bank | enemy | shipped half | prototype half |
|---|---|---|---|---|---|
| 1 | the priced Attack | 3 | 48 HP, attack 14 | `t01` Flame on the Wick | `t02` Second Helping |
| 2 | the priced draw | 3 | 40 HP, attack 16 | `t03` Eager to Help | `t04` Rummage |
| 3 | the Burst conversion | 3 | 44 HP, attack 18 | `t05` Imaginary Friend | `t06` Slow Burn |

**Why the bank is exactly three everywhere, and never four.** This is the one
place the slice deliberately departs from Kokomi slice 2, and the reason is the
difference between the two questions. There the bank sat *above* the price, so
a spend left a bank behind and the shipped *readers* stayed live on both halves.
Here the shipped competitor is not a card that reads the bank — it is the
**rule**, which takes the whole bank on the first Attack played. A bank that
survives the rule is a bank the rule was not really competing for. At exactly
three the bank buys one of two things and the player has to pick.

**Why Rapid Fire is on every board.** It is the control, not filler: the only
*paid* Attack on the board, and therefore the thing the automatic rule spends
the bank on when the player does nothing. Without a paid Attack the rule has
nothing to fire on and the arm's question cannot be asked at all.

**Why Duck and Cover, and only Duck and Cover.** It is the one standalone Block,
in hand everywhere, because a Spark payoff that nothing competes with is not a
decision. A second flat-Block card would make "defend" the answer by arithmetic
rather than by choice.

**No line on any board is lethal**, and the arithmetic is written per board in
`understudy/turns/klee-slice-1/MANIFEST.md`: the largest total damage each
board can produce at true values is 29 against 48, 28 against 48, 23 against 40
twice, and 23 against 44 twice. "Just kill it" never ends a turn here.

**The one row of that table that is the slice itself:** on `t02` the *order* of
two plays changes the total, and on `t01` it does not.

### The closeness reading

`staged_turn closeness` was run on all six declared boards. **All six
SURVIVE**, against a dominance threshold of 0.5:

| turn | gap | top line | runner-up | lines |
|---|---|---|---|---|
| `t01` | 0.1695 | 34.800 | 28.900 | 15 |
| `t02` | 0.1076 | 28.800 | 25.700 | 12 |
| `t03` | 0.1667 | 28.800 | 24.000 | 14 |
| `t04` | 0.2049 | 28.800 | 22.900 | 10 |
| `t05` | 0.0062 | 28.980 | 28.800 | 14 |
| `t06` | 0.2049 | 28.800 | 22.900 | 10 |

Per-turn detail is in `review/qa/<turn id>/closeness.json`. No line on any of
these boards is worth more than twice the runner-up in the pilot's own
currency, so the falsifier refuses none of them. That is **not** a claim that
any turn is good, and the numbers are not comparable between two rows — R213 F
allows the reading only as a refusal.

**Two things the reading cannot do, and both are disclosed rather than buried.**

1. **On `t04` the mirror has no draw pile.** The tier0 `Board` carries hp,
   block, energy, turn, resources, hand and enemies and nothing else, so both
   halves of pair 2 draw from an empty pile and every draw is worth zero.
   Rummage scores as a card that spends 1 energy and 3 Sparks to do nothing,
   and it appears in none of the top lines.
2. **On `t06` the pilot does not price Burst Energy.** Nothing in
   `tier0/pilot/policy.py` values a meter that pays out in a future turn (it
   has no Spark hold-versus-spend term either — the shipped `powder_charge` row
   says so). Slow Burn therefore scores as Duck and Cover's 5 Block minus a
   free Rapid Fire: strictly worse, correctly, for a pilot that cannot see what
   it bought. It too appears in none of the top lines.

Both errors run **one way** — the arm is *under*-valued, so less likely to
dominate, and dominance is the only thing the falsifier refuses. So a SURVIVE
here is the conservative direction. Both fixes are `POLICY_VERSION` changes
carrying their own re-baseline, which is frozen and not this slice's to make.
**The blind seat is the only reading pairs 2 and 3 have**, and if a human
grader cannot separate those halves either, that is itself the arm's answer.

---

## 6. Held for [USER]

Two things are waiting for you, and they are different in kind: one is an arm
the gate stopped, the other is the question the gate raised on its own.

### 6.1 — Bag of Tricks, held on the seat's verdict

> **RULED (R224, 2026-08-30): option (3).** [USER]: *"17 - I think 3 is fine,
> we can try it."* Build per-mode playability (`EB-182`) first, then re-ask the
> doctrine seat; the top-level-cost clause returns to [USER] then, as ONE
> clause rather than two. `M47` CLOSES. **Fact-check carried with the ruling:**
> the per-mode work is owed regardless of this arm not to Kokomi slice 2 but to
> the SHIPPED Furina card `deep_breath`, whose mode 2 prices *Spend 3 Encore:
> draw 3* (`docs/furina-cards.yaml:707-713`) — so `EB-182` now names two
> consumers.

The arm, as proposed: `proto_spark_mode_bombs`, 0, Skill, Uncommon —
*Choose one: Place 1 Bomb dealing 5 | Spend 3 **Sparks**: Place 3 Bombs dealing
5.* The cheap mode is `pop` printed alone; the expensive mode is
`bomb_voyage`'s body, which ships at 2 energy, with the bank buying the two
energy.

The seat called it *"the strongest arm for widening what Klee does"* — Sparks
buying placement and a future-turn plan instead of an immediate Attack, the one
D2 verb Klee owns that Sparks have never touched — and then held it on two
written clauses:

1. **The top-level cost rule**, again: *"The expensive modal body necessarily
   puts the expenditure inside one choice rather than at the card's top
   level."*
2. **D4**: *"At the decision point the player can perceive and forecast the
   consequences that matter…"* — the choose-a-card screen has no per-mode
   playability, so the expensive mode is offered whatever the bank holds. It
   cannot pay out, but the face cannot grey it out either. The seat's words:
   *"Quarantining the card prevents shipping the defect; it does not make the
   proposed card follow doctrine."*

And its instruction: *"Hold the whole arm tonight … Moving the Spark cost to
the top level would make the free one-Bomb mode inaccessible and destroy the
arm's central comparison."*

**Ways forward, as a numbered pick list.**

1. **Drop the arm.** Accept that a mode-priced resource is not available to
   Klee, and let the slice be three arms. Costs nothing; loses the only arm
   that put Sparks on the bomb board.
2. **Amend the top-level-cost rule to admit a mode price**, narrowly: a
   `spend_*` at the top of a `choose_one` *mode* is a cost line for that mode,
   `spark_cost` becomes mode-aware, and the modal screen must grey an
   unaffordable mode. This is a LAW-adjacent amendment plus real engineering in
   both engines and the modal UI. It is the option that keeps the arm intact,
   and it is the one that costs the most.
3. **Build the per-mode playability first, as its own item**, and re-ask the
   seat afterwards. The D4 half then dies on its own; the top-level-cost half
   still needs your ruling, but you would be ruling on one clause instead of
   two. This is the same gap Kokomi slice 2 filed from the other side, so the
   work is owed regardless of what happens to this arm.
4. **Re-author the arm without a mode.** Two cards instead of one — a free
   `pop` and a top-level *Spend 3 Sparks. Place 3 Bombs dealing 5.* — which
   follows doctrine today and needs no ruling at all, but tests a different
   thing: it removes the choice from inside the card, which was the arm.
5. **Move the price to the card's cost line** (a 3-Spark cost on the whole
   card, both modes). Doctrine-clean, and the seat explicitly says it destroys
   the comparison, because the free mode stops being free.

My reading, for what it is worth and it is not a vote: **(3) then re-ask** is
the only option that buys something the project needs anyway.

### 6.2 — the automatic free-Attack rule, which the seat raised unprompted

I asked whether leaving the rule alone was right for a card-level slice. The
answer was yes *and* a warning, and the warning is the most important sentence
either round produced:

> *"the automatic rule itself remains facially in tension with D2. It chooses
> the first eligible Attack and forces both timing and expenditure. Alternative
> sinks create decisions before that trigger, but they do not make the
> automatic engine itself steerable whenever no sink is available. If the
> project intends D2 literally, the eventual core rule must become opt-in,
> forgoable, or allow the player to choose which Attack receives the discount.
> The character-mechanic clause that would have to move is: 'Sparks: at 3
> Sparks, next Attack costs 0 (consumes 3).' That core change need not precede
> this diagnostic slice, but it must precede any claim that E2 has been
> doctrinally resolved."*

**Nothing was done about this and nothing should be until you rule.** It is a
character mechanic, it cannot be quarantined on a card surface, and it is
exactly the class of call the ladder keeps with you. It is also why every arm
came back PARTLY rather than YES: three Uncommons cannot fix a rule that fires
on every Attack in the deck.

**Ways forward, as a numbered pick list.**

1. **Leave the rule alone.** Judge E2 on whether the sinks make her turns
   better; if they do, that is the answer and the rule stays automatic.
2. **Make it opt-in** — the discount applies only when the player elects it,
   which is a prompt or a toggle on the Attack. Maximum control, maximum
   friction, and a UI question in the live game.
3. **Make it targetable** — the bank is spent on an Attack the player
   *designates*, rather than the first one played. Cheaper than (2): it is a
   choice the player already makes by ordering, made explicit.
4. **Make it forgoable only** — the discount is automatic, but a player may
   decline it, which costs nothing to a player who does not care. The smallest
   change that answers the seat's D2 objection.
5. **Rule that D2 does not reach a starting mechanic** — the charter's
   "reachable early and reliably" clause is about the *control*, and the
   control could live in the sinks rather than the rule. This is the option
   that keeps the rule and amends nothing, but it is an interpretation of D2
   and it is yours to make, not mine.

---

## 7. What is staged, and what is waiting

**Nothing is staged.** Not one of the six, including the three shipped halves.
Every seed is unpinned and every row reads `staged: pending`.

Staging needs the live game, and the live game and the art-bearing main
checkout belong to another session for the duration of this branch. This work
was done in a sibling worktree, which by house rule may not launch the game,
install the bridge or deploy. So what is waiting is, in order:

1. **A dev build** carrying the three prototype rows
   (`klee-mod\build\deploy_proto.ps1`), on the main checkout.
2. **Staging the three pairs**, shipped half first to discover the seed, then
   the other half with `--seed <that value>`. Two halves on two seeds are two
   different fights and the pair has measured the encounter instead of the
   card. **If a seed's fight telegraphs anything but an attack, re-roll.**
3. **Blind grading** — a fresh grader per packet, the four-question form,
   design-blind, and then the independent seat (a different model family,
   R217 C) on all six.
4. **Replay** of every graded line (`staged_turn execute`, EB-170). No card in
   this slice is a modal, so no form needs a `choose` key — that was Bag of
   Tricks, and it is held.
5. **The pair read** — shipped half against prototype half, arm by arm.

And the same seat guardrail the Kokomi slices ran under: seat testimony is
iteration feedback, never validation, never balance evidence, never approval
(R217 G). Tonight's doctrine read is a **gate**, which is a different job from
a grade — it decided what could be built, and it decides nothing about whether
what was built is any good.

---

## 8. Register moves I think should be minted

I did not mint or close anything. Next free ids are **R219 / EB-182 / M47**.
My reading of what should be, in the order I would file them:

- **Nothing closes.** The slice is not run.

- **Two [USER] calls, and they are §6's two pick lists.** The Bag of Tricks
  clause (a mode-level resource price, and whether the top-level-cost rule
  should admit one) and the automatic free-Attack rule (whether D2 reaches a
  starting mechanic). Both are QUEUE rows, both are numbered pick lists rather
  than blanks, and the second one is the larger of the two by a long way.

- **A defect, owed regardless of what happens to Bag of Tricks: the
  choose-a-card screen has no per-mode playability.** Kokomi slice 2 found it
  from the other side and filed it there; this slice is the second arm it has
  blocked, which is the evidence that it gates a shape rather than a card.
  Next action: read `CardSelectCmd`'s choose-a-card path off the decompile for
  a per-option disabled state; if there is none, decide whether a priced mode
  may ship at all. Gate: it gates any shipped modal that prices a resource, and
  it is now half of why an arm is held.

- **Owed instrument work, and the reason `t04`'s reading is thin: the tier0
  mirror `Board` has no draw pile.** `understudy/staged_turn.py`'s `Board`
  carries hp, block, energy, turn, resources, hand and enemies, so every draw
  on a staged board is worth zero and any arm that buys cards is invisible to
  the falsifier. It is a `Board` field plus a line in `_state_from_board`, not
  a policy change, so unlike the two items below it is **not** frozen. Whoever
  picks it up should re-run `closeness` on `t03`/`t04` afterwards; the numbers
  in §5 are the pre-fix reading and should be replaced, not compared.

- **For the slate, not a register row: the pilot prices neither Sparks nor
  Burst.** There is no hold-versus-spend term for Sparks anywhere in
  `tier0/pilot/policy.py` (the shipped `powder_charge` row already says so) and
  nothing values a meter that pays out next turn. Every closeness reading on a
  Spark spender is therefore taken by a pilot that thinks holding is free and
  the Burst is worthless. The error direction is conservative (§5), so this
  invalidates nothing here — but it is a `POLICY_VERSION` change with a
  re-baseline attached, and both are frozen. **This is yours.**

- **A defect fixed in place rather than filed, named here because it touched
  code outside the slice.** `tools/lint_burst_legibility.py` listed the six
  shipped sheets and not `docs/prototype-surface.yaml`, while it already
  scanned the prototype generated tree — so a correctly tagged prototype row
  was reported as a PHANTOM BURST. The lint was not merely blind, it was wrong
  in the accusing direction, and it would have gone on being wrong for every
  future slice that tags a prototype row. Hygiene under the norms, its own
  commit, no generated output moved.

- **No `DRAFTER_VERSION` question arises**, and that is worth one line because
  Kokomi slice 2 had to argue it. This slice adds no op, no predicate and no
  priced verb — `spend_spark` was already priced and already printed by three
  shipped Uncommons — so there is nothing for the drafter to have missed.


## 9. Revert

**Every commit on this branch is confined to the paths listed below.** The
branch's own range reverts cleanly in one command:

```
git revert --no-commit <first-sha>^..<last-sha>
```

where the range is this branch's commits and nothing else. On this branch that
is `c96f027^..` through the last commit; `git log --oneline origin/main..HEAD`
prints the exact pair.

The paths touched, in full:

- `review/active/klee-slice-1-2026-08-29.md` (new)
- `review/qa/klee-slice-1-doctrine-review-codex-gpt-5.6-sol.md` (new)
- `review/qa/klee-slice1-t0[1-6]/closeness.json` (new)
- `docs/prototype-surface.yaml` (appended to; the quarantined surface)
- `klee-mod/KleeCode/Cards/Prototype/Generated/` — three new classes,
  `PrototypeRoster.cs` and `manifest.json` regenerated
- `understudy/turns/klee-slice-1/` (new)
- `tools/lint_burst_legibility.py` — **the only shared file**, and it is its
  own commit (`845eaec`) so it can be kept or dropped independently of the
  slice

**No shipped Klee row moved by one character.** `docs/klee-cards.yaml` and
`docs/klee-upgrades.yaml` are untouched, and not one file under
`klee-mod/KleeCode/Cards/Klee/Generated/` moved — proven rather than asserted
by `python tools/gen_roster_cards.py --check`, which reports *"gen_klee_cards:
up to date"* with the branch applied. The R213 freeze already forbids moving
them; this is the receipt.

**Nothing I believed necessary would have moved a shipped Klee row.** The one
change outside the slice's own paths is the lint fix, and it changes no
generated output at all.

---

## 10. What I could not do

- **Stage anything live** (§7): the live game and the main checkout belong to
  another session, and a worktree may not launch the game, install the bridge
  or deploy.
- **Discover seeds**, for the same reason. The declared boards are therefore
  the only reading that exists.
- **Run either deploy script**, or `build_pck`. A worktree has no
  `local.props`, no `game_ref/` and no art.
- **See a prototype card render.** Prototype rows have no art by design — art
  is commissioned when a slice is accepted and its rows move to a real sheet —
  so they will draw with no portrait. That is correct, not a defect.
- **Let the seat read the repository itself.** Codex's Windows command policy
  rejected every exec form it tried, so it refused twice rather than guess
  (the stderr and its exact words are in the review file). Every file it needed
  was inlined into the prompt instead, through the same
  `understudy.seat review` door and the same argv. It read the proposal, both
  doctrine files, the whole Klee sheet, the upgrades sheet and the engine
  excerpts — nothing was summarised for it — but it could not go looking for a
  file I had not thought to give it. That is the one way tonight's gate is
  narrower than it should have been, and it is worth fixing before the next
  slice: the seat's `--sandbox read-only` is not survivable on this machine.
- **Build a threshold arm.** The slice lost its threshold shape to the gate
  (§4), and nothing replaced it, so this slice asks three of the four questions
  Kokomi slice 2 asked and not the fourth.
- **Grade any of it.** I designed these rows (R213's first guard), and nothing
  in this document says whether an arm is good.


---

## 11. ROUND 1 (2026-08-29) — what actually happened

Everything §7 said was waiting is now done. This section is written after the
fact and by a different session from the one that designed the rows; it records
what the funnel did and settles nothing.

**The build:** `0.2.1314+proto`, built and deployed by `deploy_proto.ps1` on the
art-bearing main checkout, game closed, `validate.ps1` OK. The pin had not
moved: game v0.111.0, commit `41cef1ea`, buildid `24724944`, branch
`public-beta`, BaseLib 3.4.5.0 — all four read off disk before any live work.

### The staging, and the eleven rolls

Each pair's seed was discovered by staging the shipped half, then pinned onto
both halves — in each file's own `seed:` key and in
`understudy/turns/klee-slice-1/MANIFEST.md`.

| pair | rolls | seed | the body it settled on |
|---|---|---|---|
| 1 — the priced Attack | 4 | `N6PCA0C9GCFG` | Seapunk 45/45, attacks for 11 |
| 2 — the priced draw | 4 | `LUMNB3D9GFKD` | Nibbit 40/43, attacks for 12 |
| 3 — the Burst conversion | 3 | `PQUR2MZ6Z1KF` | Nibbit 43/43, attacks for 12 |

Every board settled BELOW its designed telegraph — 11, 12 and 12 against a
design asking 14, 16 and 18 — and pair 1's first roll, a Sludge Spinner
telegraphing **8**, was rejected rather than taken: Duck and Cover is the
deliberate defensive competitor on every board here, and against a small hit it
is not a competitor at all. Two boards also lost HP to `set_hp`'s clamp (48 → 45
on a Seapunk whose maximum is 45; 44 → 43 on this Nibbit); pair 2 got exactly
the 40 it asked for. Each file now declares the LIVE body and its staging step
carries the achieved figure. **Every stated property still holds at the live
figures** — one enemy, an attack telegraphed, a bank of exactly 3, and no lethal
line — and the observed board was checked against the declared one on all six
before any grade.

Closeness was taken BOTH ways on all six. **All twelve SURVIVE**, and every
declared number is byte-identical to §5's pre-run reading, because no telegraph
here crosses a threshold five Block could meet. The observed readings, however,
**cannot see the Spark bank at all** — `EB-185` — so `closeness.json` holds the
DECLARED reading on this slice, the opposite of Kokomi slice 2's choice. The
manifest carries both tables and the reason.

### The grades — twelve forms, two graders on every turn

A fresh Claude grader per packet (`opus-5-fresh`, `claude-opus-5`; one agent,
one turn, never reused, no repo access, the packet inline) and the R217 C
independent seat (`codex-gpt-5.6-sol-fresh`). No seat refused to answer; no
answer was edited on the way in.

**Seven SURVIVES, five REFUSED.**

| turn | Claude grader | independent seat |
|---|---|---|
| `t01` shipped, arm 1 | REFUSED `intent_insensitive` | REFUSED `intent_insensitive` |
| `t02` prototype, arm 1 | SURVIVES | REFUSED `no_second_line` + `intent_insensitive` |
| `t03` shipped, arm 2 | SURVIVES | SURVIVES |
| `t04` prototype, arm 2 | SURVIVES | REFUSED `intent_insensitive` |
| `t05` shipped, arm 3 | SURVIVES | SURVIVES |
| `t06` prototype, arm 3 | REFUSED `intent_insensitive` | SURVIVES |

A refusal is the falsifier firing, never a rating. Notably **both graders
refused `t01`**, the one board where the shipped card neither reads nor touches
the bank — the Claude grader's words: *"there is no damage-versus-defense
tension to resolve."*

### The replays — and this is the round

Every one of the twelve graded lines was replayed live on its own pinned seed
and board. **TEN OF THE TWELVE ARE UNPLAYABLE AS WRITTEN**, every one refused by
the bridge with `EnergyCostTooHigh`, and every one for the same reason — which
is not any prototype card.

**At a bank of three the game prints EVERY Attack in hand at `Cost: 0` and
`can_play: true`, while the rule makes exactly ONE of them free.** Kaboom! is
printed at 1 and Rapid Fire at 2; on a board banking three, both render as 0.
Every grader on every board read *"all my Attacks are free"* off the page,
built a line on it, and the board refused the third or fourth card. This is the
live game's own wire state, not the packet's rendering:
`review/qa/klee-slice1-t01/observed.json` carries Flame on the Wick, Kaboom! and
Rapid Fire all at `cost=0, can_play=true` with the bank at three. It is filed as
**`EB-186`**.

| turn | grader | what the board did |
|---|---|---|
| `t01` | Claude | **completes** — Seapunk 45 → 16, the 29 claimed; Block 0 → 5 |
| `t01` | seat | **completes** — identical, 45 → 16, Block 0 → 5 |
| `t02` | Claude | refused at Rapid Fire; 45 → 26 (**19**, not the 35 claimed) |
| `t02` | seat | refused at Rapid Fire; 45 → 26, identical |
| `t03` | Claude | refused at Rapid Fire; 40 → 33 (**7**, not the 23 claimed) |
| `t03` | seat | refused at Duck and Cover; 40 → 17, the 23 claimed, but no Block |
| `t04` | Claude | refused at Rapid Fire; Rummage resolved, then nothing |
| `t04` | seat | refused at Rapid Fire; identical |
| `t05` | Claude | refused at Duck and Cover; **Burst 20 → 28** |
| `t05` | seat | refused at Rapid Fire; Burst 20 → 28 |
| `t06` | Claude | refused at Rapid Fire; **Burst 20 → 35** |
| `t06` | seat | refused at Rapid Fire; Burst 20 → 35 |

**Only `t01` completed, and it completed by luck of ordering rather than by
foresight.** Both graders happened to lead with the 2-cost Attack, so the one
real discount landed where it paid. That is the sharpest thing the round
produced about E2: on the SHIPPED board the automatic rule's value depends
entirely on an ordering the face gives the player no way to see, and both blind
readers got it right by accident while refusing the turn as intent-insensitive.

**Arm 3's meter difference is real and visible in the replay**, and it is the
one prototype effect the display defect did not eat: the shipped half moved
Burst 20 → 28 and the prototype half 20 → 35, so three Sparks bought **seven
extra Burst**, exactly as §4 said it would.

**Nothing was re-graded and no answer was edited.** A replay that contradicts a
form is the finding, not a correction.

### The pair read

Shipped half against prototype half, arm by arm, on all six packets with the
twelve forms, verdicts and live replay post-states inline, and the display
defect disclosed to the reviewer up front so it could judge reasoning rather
than arithmetic. Reply unedited:
`review/qa/klee-slice-1-pair-review-codex-gpt-5.6-sol.md`; the prompt is kept at
`review/qa/klee-slice-1-review-prompt.txt`, sha256
`76ed7f0a73f059af74e22cc41ab339656325e31f47a8ee3a2ec9c03b0942bbe6`.

**Two ADVANCE, one RETURN, no ESCALATE.** Seat testimony is iteration feedback,
never validation, never balance evidence, never approval (R217 G); ADVANCE means
worth asking again with whole-fight play, not ship.

| arm | card | outcome | the reviewer's own reason |
|---|---|---|---|
| 1 — the priced Attack | Second Helping | **ADVANCE** | *"the shipped half merely discharged Spark automatically, whereas the prototype created a choice about whether Second Helping or another Attack received the bank."* The cost bound: *"Spending Spark on Second Helping removed the automatic discount from later Attacks."* The caution: *"the other's `no_second_line` refusal weakens replication but does not contradict that evidence."* |
| 2 — the priced draw | Rummage | **ADVANCE** | *"Eager to Help only rewarded preserving the bank until it was read, while Rummage explicitly competed with the automatic Attack discount for ownership of it."* And: *"Both graders independently recognized the bank-allocation branch, including the concrete trade between three cards now and an Attack discount now or later."* |
| 3 — the Burst conversion | Slow Burn | **RETURN** | *"Imaginary Friend leaves Spark inside the Attack economy, whereas Slow Burn lets the player divert it into Block and Burst progression"* — but *"Return the board and presentation for rework: the Spark diversion is real, but the displayed-cost failure makes the apparent 'play everything' line fictitious, and the Burst wording produced a replay-disproved reading."* |

The reviewer's closing, verbatim: *"Overall, the round supports the narrow claim
that explicit Spark spenders can turn an automatic bank into a player-steered
allocation, most cleanly in Arms 1 and 2. Nothing warrants ESCALATE:
disagreements about Duck and Cover's intent sensitivity are peripheral and
resolvable, while the graders agree on the prototype-relevant ordering. The
display defect does not erase the qualitative ordering evidence, but it prevents
conclusions about actual line closeness, affordability, or the magnitude of each
prototype's opportunity cost."*

**The Burst misread the reviewer names is a defect in this slice's own staging
text, not in the card**, and it is filed as `EB-187`. Slow Burn's face prints
*Gain 10 Burst Energy. Burst +5* — where the `Burst +5` IS the Skill-tag rider,
printed by the generator — while the board's assumption line told the grader
that a Skill-tagged card adds 5 *"over and above anything the card's own text
grants."* A reader who believes both counts the tag twice, and the Claude grader
did: it predicted 20 → 40 where the board produced 20 → 35.

### Independence — two arms provisional

Two of the three arms in this round carry content the doctrine seat wrote, and
the seat, the blind grader and the pair reviewer are all the same model family
(`gpt-5.6-sol`, the Codex seat).

- **Rummage** (§4, arm 2): its printed text — *Spend 3 Sparks. Draw 3 cards.* —
  is the seat's re-authoring of my threshold row, supplied as a remedy with its
  REQUIRES_MODIFICATION verdict.
- **Slow Burn** (§4, arm 3): its printed Burst number, 10, is the seat's choice
  between the two numbers I put to it.

R217 C separates author from grader **by model family**, and on these two arms
that separation does not hold: the family that wrote the row also graded and
read it. So their outcomes here — Rummage **ADVANCE**, Slow Burn **RETURN** —
were **PROVISIONAL**.

**There is no third model family, and none is being added.** The repair was to
put the roles back where they belong rather than to buy a new grader: both rows
**re-authored Claude-side from the seat's CLAUSE only** — the cost-at-top-level
rule for Rummage, the shipped-face rule for Slow Burn's number — with the seat's
own text and numbers discarded, and then re-run as a **Klee round 3** with fresh
graders. If a derivation lands on the same wording or the same number the seat
gave, that is fine and it is still clean: what makes it clean is that the record
shows the derivation, not that the output differs.

**THE PROVISIONAL FLAG IS LIFTED ON BOTH ARMS. Round 3 ran on 2026-08-29 (§13)
and covers both.** Both rows were re-derived from the clause — the derivations
are on the rows and in §13.1, and both landed on the seat's own wording and
number, which §13 says plainly — and both now read `authored_by: [claude]`, so
`seat grade` ran all four turns without refusing one. The round's turn ids are
**`klee-slice1-r3-t03`/`t04`** (Rummage, on a board re-set to round 2's own
returned specification, fresh seed `8D3369V27Z55`) and
**`klee-slice1-r3-t05`/`t06`** (Slow Burn, round 2's board unchanged on seed
`PQUR2MZ6Z1KF`). Eight forms on two graders, eight clean replays, and the pair
read **ADVANCES both arms**. Those clean outcomes REPLACE the provisional ones;
they are not averaged with them, and the rounds that produced the provisional
ones stand exactly as published (R101b).

**Second Helping is unaffected.** Its row is unchanged from the proposal, the
seat supplied nothing on it beyond a FOLLOWS verdict and a narrowing of the
claim, and its ADVANCE is a clean independent read.

The protocol that keeps this from recurring — a review seat rules FOLLOWS or
REQUIRES_MODIFICATION and names the clause, never supplies text or numbers — is
in `docs/current/OPERATIONS.md` under *Doctrine seat protocol*, and the
mechanism that enforces it (each prototype row records the model families that
authored it, and a seat refuses a turn its own family touched) is `EB-190`.
Two families, fixed roles: Claude authors, GPT grades and reviews.

### What the round means, and what it does not

**R213 E2 is not settled and not refuted.** Two of the three shapes produced a
bank the reviewer describes as steered rather than taken, and its account of why
is the same in both: the prototype had to compete with the automatic rule *for
ownership of the same three Sparks on the same turn*. The one that RETURNED
returned on the instrument — its board and its presentation — rather than on the
shape, and the reviewer says the diversion itself is real.

**The falsifier refused nothing.** Closeness SURVIVES on all six, declared and
observed, which is a refusal that did not fire and never a rating (R213 F).

**And the round's largest finding is not about any arm.** Ten of twelve blind
readers' lines were impossible, because the face lies about which Attack the
bank is going to pay for. §6.2's held question — whether the automatic rule
should become opt-in, forgoable or targetable — now has a live witness on the
shipped surface: not one blind reader could tell, from the page, which Attack
would get the discount, and the arithmetic every one of them did was wrong
because of it. That is evidence for the question, not an answer to it, and the
answer is still [USER]'s.

### What the round could not do

- **Ask arm 3 cleanly.** Its RETURN is the board's, and the reviewer named the
  repair: the display defect and the Burst wording, both now filed.
- **Read a Spark bank on any observed board** (`EB-185`).
- **Separate the graders' arithmetic from the display defect on ten of twelve
  lines.** Their reasoning is readable; their numbers are not.
- **Try Bag of Tricks.** It stays held for [USER] at §6.1, untouched.

---

## 12. ROUND 2 (2026-08-29) — the same six boards, on a page that says what a card costs

Round 1 ended on a caveat in the reviewer's own words: the cost-display defect
*"prevents conclusions about actual line closeness, affordability, or the
magnitude of each prototype's opportunity cost."* Round 2 exists to remove it.
Three instrument fixes landed first and **nothing about any card, any board, any
enemy, any seed or any number was touched.**

**The build:** `0.2.1323+proto`, the dev build the staging ran on, left
installed and reused for the replays rather than rebuilt — a rebuild would have
stamped a different package over the one that wrote the packets. The pin was
re-read off disk before any live work and had not moved: game **v0.111.0**,
commit `41cef1ea`, buildid `24724944`, branch `public-beta`, BaseLib 3.4.5.0.
Found on disk at the start: no game process, no `mods\STS2_MCP`, and that
`+proto` package still installed from the stalled session — `STATE.md` had been
left claiming the release `0.2.1318`, which was wrong until this round's
teardown made it true again.

### What changed between the rounds, and it is only the instrument

| fix | what it does | row |
|---|---|---|
| the honest page | the packet prints Spark's rule **verbatim from the in-game keyword text**, states the bank, and marks each card shown below its printed cost — *"The cost printed on this card is 2; it is showing 0 here."* | `EB-186` (page half) |
| the observed board | `observed.json` now carries the Spark bank, so the two mirrors agree | `EB-185` CLOSED |
| the Burst assumption | the printed rider **is** the tag; `check` refuses a board that says otherwise | `EB-187` CLOSED |

Same six boards, same three seeds (`N6PCA0C9GCFG`, `LUMNB3D9GFKD`,
`PQUR2MZ6Z1KF`), same bodies, new turn ids under
`understudy/turns/klee-slice-1-r2/`. Closeness SURVIVES on all six, declared and
observed, unchanged from round 1.

### The grades — twelve forms, two graders on every turn

A fresh Claude grader per packet (`opus-5-fresh`, `claude-opus-5`; one agent,
one turn, never reused, no repo access, the packet inline) and the R217 C
independent seat (`codex-gpt-5.6-sol-fresh`). No seat refused to answer, no
retry was needed, and no answer was edited on the way in. The three identity
fields a model cannot know about itself — `grader.id`, `grader.kind`,
`grader.model` — were filled by the orchestrator exactly as the seat fills them,
so the ledger's grouping string is stable; the unedited subagent replies sit
beside each filled form as `form-raw-opus-5-fresh.json`.

**Seven SURVIVES, five REFUSED** — the same split as round 1, on different
turns.

| turn | Claude grader | independent seat |
|---|---|---|
| `t01` shipped, arm 1 | REFUSED `intent_insensitive` | REFUSED `intent_insensitive` |
| `t02` prototype, arm 1 | REFUSED `intent_insensitive` | SURVIVES |
| `t03` shipped, arm 2 | SURVIVES | SURVIVES |
| `t04` prototype, arm 2 | REFUSED `intent_insensitive` | SURVIVES |
| `t05` shipped, arm 3 | SURVIVES | REFUSED `intent_insensitive` |
| `t06` prototype, arm 3 | SURVIVES | SURVIVES |

`t01` was refused by both graders in **both** rounds — the one board where the
shipped card neither reads nor touches the bank.

### The replays — and this is what the honest page bought

Every one of the twelve graded lines was replayed live on its own pinned seed
and board. **ALL TWELVE PLAYED TO COMPLETION**, against ten of twelve refused
with `EnergyCostTooHigh` in round 1. No refusal, no `misread`, no unanswered
modal, seed honoured on every committed replay, and `board_check` clean on
every one.

| turn | line both graders played | what the board did |
|---|---|---|
| `t01` | Rapid Fire, Flame on the Wick, Kaboom!, Duck and Cover | Seapunk 45 to **16**, Block 5 |
| `t02` | Rapid Fire, Kaboom!, Duck and Cover | Seapunk 45 to **22**, Block 5 |
| `t03` | Rapid Fire, Kaboom!, Duck and Cover | Nibbit 40 to **17**, Block 5 |
| `t04` | Rapid Fire, Kaboom!, Duck and Cover | Nibbit 40 to **17**, Block 5 |
| `t05` | Rapid Fire, Kaboom!, Imaginary Friend | Nibbit 43 to **20**, Block 5, **Burst 20 to 28** |
| `t06` | Rapid Fire, Kaboom!, Duck and Cover | Nibbit 43 to **20**, Block 5, Burst 20 to 20 |

Both graders wrote the same line on all six boards this round, and every stated
damage total matched what the board dealt. **Eleven of the twelve forms put the
discount on Rapid Fire** and said why in the same words — it prints 2, so the
bank buys the most there. Round 1's `t01` completion was luck of ordering; round
2 is the same ordering reasoned out loud on every board.

`EB-187`'s rule held live: Imaginary Friend moved Burst **20 to 28**, its printed
*Gain 3 Burst Energy* plus its printed *Burst +5* and nothing behind them.

**The draw arm has no replay of its own text.** Not one of the four graded lines
on `t03`/`t04` played Eager to Help or Rummage, so there is no "draw 3" to
record. That is the finding, not a gap: with the printed costs visible, all four
graders priced the draw card out of the turn.

Seven of the twelve replays needed one retry, every one because the harness came
up with the run seed reading back `None` (`seed_not_honoured`, R95's own guard).
That is a launch race, not a turn result; each committed replay ran on the
pinned seed.

### The pair read

Same five questions, arm by arm, all six packets with the twelve forms, verdicts
and live post-states inline; the round-1 display defect is described as removed
rather than hidden. Reply unedited:
`review/qa/klee-slice-1-r2-pair-review-codex-gpt-5.6-sol.md`; the prompt is kept
at `review/qa/klee-slice-1-r2-review-prompt.txt`, sha256
`4eb574038381edcf807bd98551e14667d7afebec1c9e6f8e6eaa4bb327ef5ed0`.

**Two ADVANCE, one RETURN, no ESCALATE — and it is not the same two.** Seat
testimony is iteration feedback, never validation, never balance evidence, never
approval (R217 G); ADVANCE means worth asking again with whole-fight play, not
ship.

| arm | card | round 1 | round 2 | the reviewer's own reason |
|---|---|---|---|---|
| 1 — the priced Attack | Second Helping | ADVANCE | **ADVANCE** | *"The shipped half only asked which Attack should receive the automatic discount. The prototype half added a distinct, player-steered destination: spend the bank to activate Second Helping instead of discounting Rapid Fire."* The cost bound: *"the destination of the bank concretely traded damage against 5 Block."* |
| 2 — the priced draw | Rummage | ADVANCE | **RETURN** | *"The board prices the prototype against 16 guaranteed damage and defense while offering three unknown, mostly unaffordable draws, and one grader consequently constructed an illegal comparison. This arm needs a board that can expose a real draw destination before whole-fight testing."* |
| 3 — the Burst conversion | Slow Burn | RETURN | **ADVANCE** | *"The prototype created a genuine second destination with an explicit immediate cost, while its Burst benefit is inherently forward-looking. Whole-fight play is the appropriate next question, especially because one grader weighed that conversion seriously and the staged-turn replay cannot value its later payoff."* |

**Arm 3's RETURN was the instrument's, and the instrument is fixed.** Round 1
returned it for *"the displayed-cost failure"* and *"the Burst wording"*; with
both repaired the same reviewer ADVANCEs it and says the replay confirms the
meter: *"Opus correctly predicted that declining Slow Burn would leave Burst at
20, and the replay did so."*

**Arm 2's ADVANCE did not survive the honest page.** Round 1's reviewer credited
both graders with recognising *"the bank-allocation branch"* — on a page that
told them the attacks were free. Priced honestly, the branch is not close: the
reviewer's own arithmetic is that Rummage first *"leaves only one energy:
Rummage plus Kaboom! produces 7 damage and no Block, versus the replayed 23
damage and 5 Block"*, so the draw destination *"cost 16 immediate damage and 5
Block on this board."* No grader took it. **This is exactly the conclusion round
1 could not reach**, and it is the one the caveat named: the magnitude of the
prototype's opportunity cost.

The reviewer also names two grader misreads, neither of them a defect in a card:
Opus's rejected-line arithmetic on `t02` (*"'Kaboom! plus nothing' is false"*)
and the seat's on `t04`, where its alternative *"contradicts 'Spend 3 Sparks'"*
because Rapid Fire had already emptied the bank. Both are the graders' own
reasoning about lines they did **not** play; the lines they did play all
replayed clean, and nothing was re-graded.

### What round 2 changed versus round 1

1. **The arithmetic is usable.** Twelve forms, twelve clean replays, every
   claimed damage total confirmed by the board. Round 1 could read reasoning
   only.
2. **The verdicts moved on evidence, not on the cards.** Arm 3 RETURN to
   ADVANCE because its board was repaired; arm 2 ADVANCE to RETURN because its
   cost became visible. Not one character of any card changed between the
   rounds.
3. **§6.2's live witness got sharper, in the opposite direction.** Round 1's
   witness was that no blind reader could tell which Attack the bank would pay
   for. Round 2's is that when the page **does** tell them, eleven of twelve
   independently reach the same answer and call it forced — the seat's `t02`
   words for it are that *"the line is forced by the arithmetic of the discount,
   not by what Seapunk is doing."* An automatic rule whose one real decision is
   unanimous is still evidence for the held question, not an answer to it.
4. **The keyword's own wording is part of `EB-186`.** The in-game text reads
   *"At 3 Sparks, your Attacks cost 0. Playing one consumes 3 Sparks"* — plural
   *Attacks*, which is the very reading that cost round 1 ten lines. It sits
   inside §6.2's held pick and was **not** changed; the scope note is recorded
   on the row.

### Numbered picks this round adds

None. Round 2 changed no design surface and asks for no new decision. §6.1 (Bag
of Tricks) and §6.2 (the automatic free-Attack rule) are unchanged and still
[USER]'s, and §6.2 now carries round 2's witness as well as round 1's.

### What round 2 could not do

- **Watch a "draw 3" resolve.** No graded line played the draw card, so its
  text has never been replayed on a staged board. That is the arm's RETURN, not
  a hole in the instrument.
- **Value Slow Burn's Burst.** A one-turn board cannot; the reviewer says so and
  makes it the reason for whole-fight play.
- **Try Bag of Tricks.** Still held at §6.1, untouched.

---

## 13. ROUND 3 (2026-08-29) — the two provisional arms, re-derived and re-run clean

Rounds 1 and 2 were run on two rows the doctrine seat had supplied content for,
and the same model family then graded and pair-read both. §11's *Independence*
subsection called those outcomes **PROVISIONAL** and named the repair: re-author
Claude-side from the seat's CLAUSE alone, discard its text and its number, and
re-run. This is that round. **Second Helping is not in it** — its row was never
touched by the seat and its round-2 ADVANCE was already clean.

**The build:** `0.2.1353+proto`, built and deployed by `deploy_proto.ps1` on the
art-bearing main checkout, game closed, `validate.ps1` OK, no `+dirty` mark. The
pin was read off disk before any live work and had not moved: game **v0.111.0**,
commit `41cef1ea`, buildid `24724944`, branch `public-beta`, BaseLib 3.4.5.0.
Found on disk at the start: the release build `0.2.1328`, no game process, and no
bridge installed under `mods\STS2_MCP`.

### 13.1 What was re-derived, and how

Both derivations are written onto their rows in `docs/prototype-surface.yaml`,
and both rows now read `authored_by: [claude]` with a one-line comment saying
the seat's text and number were discarded.

**Rummage (`proto_spark_priced_draw`), from the clause.** The clause, quoted and
used as the only input: *"THE COST MUST STAY AT TOP LEVEL. A `spend_spark`
inside a conditional branch is invisible to the playability gate, and the payoff
would then fire unpaid. That is a structural rule about this verb, not a
preference about this card."* Applied to the ORIGINAL proposal in §2 — *"Draw 1
card. If you have 3 or more **Sparks**: spend 3 **Sparks** and draw 2 cards"* —
in three forced steps:

1. **Hoist.** The clause says the `spend_spark 3` may not sit inside the
   conditional branch, so it moves to the top of the effect list. That is the
   whole content of the clause and it is mechanical.
2. **The guard is now dead, and that is arithmetic rather than taste.** With the
   spend at top level, `combat.spark_cost` derives a PLAYABILITY GATE from it —
   the shipped `powder_charge` behaviour these rows reuse — so the card cannot
   be played at all below 3 Sparks. `spark_at_least_3` is therefore true on every
   board where the card resolves, and a predicate that is true whenever it is
   evaluated is not a branch. It is deleted because it cannot change an outcome,
   not because a preference removed it.
3. **The two draws collapse.** `draw 1` at top level plus `draw 2` in a branch
   that always runs is `draw 3`. Both figures are the proposal's own, and both
   were already lifted off the twin (`eager_to_help` draws 2 with a Spark and 1
   without).

**Slow Burn (`proto_spark_burst_conversion`), by the packet's own numbers rule**
(§4: lift the value off a shipped face, never invent a breakpoint). The shape is
fixed before the number: this arm is its twin with exactly one idea changed, so
cost 1, Skill, `skill_tag` and Block 5 are `clockwork_toy`'s and do not move, and
the figure being chosen is a printed `burst_energy` value on a card of that
shape. The sheet offers three: **10** (`combustion_study`, cost 1, Skill, Common,
`skill_tag`), **3** (`clockwork_toy`, the twin), **5** (`study_of_explosions`,
cost **0**, and it pairs its Burst with damage rather than Block). Three is the
twin's own figure and would leave the pair with no second line — the one thing
the funnel exists to refuse. Five is priced at a different cost, and moving a
figure to a different price is not a lift. **One survives: 10.**

**Both derivations land where the seat landed, and that is said here rather than
hidden.** *"Spend 3 Sparks. Draw 3 cards."* is what the seat wrote as its
volunteered remedy, and 10 is the figure it picked between two. A categorical
structural rule applied to a two-line card has one outcome, and a candidate set
of three with two forced eliminations has one survivor. §11's own words are the
test and they are met: *"If a derivation lands on the same wording or the same
number the seat gave, that is fine and it is still clean: what makes it clean is
that the record shows the derivation, not that the output differs."* The seat's
opportunity-cost argument for 10 is **not** the derivation above and is not used.

Nothing in either effect list moved, so `gen_prototype_cards --check` reports the
surface up to date and not one generated C# file changed. The pinned
`-p:PrototypeCards=true` build is clean.

**The lint's debt list was REMOVED, and that was its own test's answer rather
than a choice.** `tools/lint_prototype_authorship.py` carried four entries —
`klee-slice1-t04`/`t06` and `-r2-t04`/`t06` — for grades taken with a
contributing family on the row. With `authored_by: [claude]` back on both rows
there is no contributing family left, so check (2) stops naming those turns at
all, and the debt set's own staleness rule (*an entry that has stopped tripping
FAILS*) then required their deletion; the test that pins `offenders == set(DEBT)`
is exact in both directions. Deleting the carry-list is not deleting a record
(R101b): rounds 1 and 2's forms, verdicts and packets stand exactly as published,
and §11 and §12 still call those outcomes provisional.

### 13.2 The boards

**Pair 3 (`t05`/`t06`, Imaginary Friend against Slow Burn) is UNCHANGED** — the
round-2 files byte-identical but for the turn id and the header, on round 2's own
pinned seed `PQUR2MZ6Z1KF`, Nibbit 43/43 telegraphing 12. Round 2 already
ADVANCED this arm on a repaired board with a clean replay; what made that
outcome provisional was who derived the number, not the board, and re-boarding
would have changed a second thing and made the round unattributable.

**Pair 2 (`t03`/`t04`, Eager to Help against Rummage) is RE-SET, and the
reviewer set it.** Round 2 returned it with a named repair — *"This arm needs a
board that can expose a real draw destination before whole-fight testing"* —
after *"three unknown, mostly unaffordable draws."* Three changes, all of them
that repair:

- **energy 4, not 2**, so playing the card under test leaves THREE energy
  standing after it resolves;
- **a hand of three, not four** — Kaboom! is off the board, because the hand
  alone could otherwise absorb every point of energy; Rapid Fire (the paid Attack
  the automatic rule takes the bank for) and Duck and Cover (the one standalone
  Block) both stay;
- **a declared draw pile** — three copies of Kaboom! (1 energy, 7 damage) granted
  to the pile before the turn, with an assumption line on the grader's page
  saying the pile is real, that they are in it, and that nothing in it costs more
  than 2.

The declared pile was **checked rather than asserted**: `observed.json` reads 13
cards on both halves, identical — 3× Duck and Cover (1), Jumpy Dumpty (2), 6×
Kaboom! (1, three of them the grant), Pop! (0), Kaeya — Frostgnaw (1), Prune —
Little Witch's Hunt (1) — and nothing in it costs more than 2 printed energy.

**The seed is fresh, because a new board is a new fight, and it took nine
staging attempts.** `understudy/turns/klee-slice-1-r3/MANIFEST.md` lists every one
with why it was refused; the honest summary is that the FIRST was operator error
rather than a roll (a `seed: PENDING` placeholder left in the file was accepted as
a literal seed, so nothing was rolled — the key was removed), two came back with
the run seed reading `None` (`EB-191`) and could not be pinned, four telegraphed
too small to make Duck and Cover a competitor (two at 4, one at 8, one at 3 —
round 1 rejected an 8 for the same reason), and two were usable telegraphs on
enemies at or under the board's damage ceiling. The ninth was taken:
**`8D3369V27Z55`, Seapunk 46/46 telegraphing an attack for 11**.

**The lethality arithmetic had to be recomputed, because the draw now reaches
real cards.** The largest immediate total either half can produce is **39** — the
shipped half's free Rapid Fire (16), Eager to Help (1 energy), then a drawn Jumpy
Dumpty (2 energy, 16) and a drawn Kaboom! (1, 7), exactly 4 of 4 energy — and
**45** even counting the Bomb that lands later, against 46 HP. The prototype half
tops out at 23. No line on either half is lethal.

**Closeness was taken BOTH ways on all four turns. All eight readings SURVIVE**,
and declared and observed are byte-identical on every one, because `EB-185` is
closed and the observed board now carries the Spark bank.

| turn | gap | top line | runner-up | lines |
|---|---|---|---|---|
| `t03` | 0.0478 | 23.000 | 21.900 | 7 |
| `t04` | 0.0274 | 21.900 | 21.300 | 7 |
| `t05` | 0.0062 | 28.980 | 28.800 | 14 |
| `t06` | 0.2049 | 28.800 | 22.900 | 10 |

**What the re-set could not fix, disclosed rather than buried.** The tier0 mirror
`Board` still has no draw-pile field, so in the mirror both halves draw from an
empty pile and Rummage appears in none of the top lines. The error runs one way —
the arm is *under*-valued, and dominance is the only thing the falsifier refuses
— so a SURVIVE stays the conservative direction. The live board has the real
deck, so the packet and the replay are unaffected.

### 13.3 The grades — eight forms, two graders on every turn

A fresh Claude grader per packet (`opus-5-fresh`, `claude-opus-5`; one agent, one
turn, never reused, no repo access, the packet inline; the three identity fields
a model cannot know about itself filled by the orchestrator, the unedited reply
kept beside each form as `form-raw-opus-5-fresh.json`) and the R217 C independent
seat (`codex-gpt-5.6-sol-fresh`).

**And the round's first result is a refusal that did NOT happen.** `seat grade`
refuses a turn whose row lists the seat's own family (`EB-190`). With both rows
re-derived it ran all four turns without tripping `seat_authored_row`, so the
separation R217 C asks for now holds structurally on these two arms rather than
being noted as broken after the fact.

**Seven SURVIVES, one REFUSED.**

| turn | Claude grader | independent seat |
|---|---|---|
| `t03` shipped, arm 2 | SURVIVES | SURVIVES |
| `t04` prototype, arm 2 | SURVIVES | SURVIVES |
| `t05` shipped, arm 3 | SURVIVES | SURVIVES |
| `t06` prototype, arm 3 | SURVIVES | REFUSED `intent_insensitive` |

The one refusal is the seat on `t06`, in its own words: *"No. With only one
enemy, this line deals the maximum available attack damage while also adding 5
Block, so a different telegraphed intent would not have changed it."* A refusal
is the falsifier firing, never a rating, and nothing was re-graded.

### 13.4 The replays — all eight, and what the draw actually reached

Every one of the eight graded lines was replayed live on its own pinned seed and
board. **ALL EIGHT PLAYED TO COMPLETION.** Seed honoured on every one,
`board_check` clean on every one, no refusal, no `misread`, no unanswered modal,
and no retry needed on any launch.

| turn | grader | line played | what the board did |
|---|---|---|---|
| `t03` | Claude | Eager to Help, Rapid Fire, Duck and Cover | Seapunk 46 → **30** |
| `t03` | seat | Eager to Help, Rapid Fire, Duck and Cover | identical, 46 → **30** |
| `t04` | Claude | Rummage, Rapid Fire | Seapunk 46 → **30**, 1 energy left unspent |
| `t04` | seat | Rummage, Rapid Fire, Duck and Cover | Seapunk 46 → **30**, 0 energy left |
| `t05` | Claude | Rapid Fire, Kaboom!, Imaginary Friend | Nibbit 43 → **20**, **Burst 20 → 28** |
| `t05` | seat | Rapid Fire, Kaboom!, Imaginary Friend | identical, 43 → **20**, Burst 20 → 28 |
| `t06` | Claude | Rapid Fire, Kaboom!, Duck and Cover | Nibbit 43 → **20**, Burst 20 → 20 |
| `t06` | seat | Rapid Fire, Kaboom!, Duck and Cover | identical, 43 → **20**, Burst 20 → 20 |

`EB-187`'s rule held again: Imaginary Friend moved Burst 20 → 28, its printed
*Gain 3 Burst Energy* plus its printed *Burst +5* and nothing behind them, and
declining Slow Burn left the meter at 20.

**WHAT THE DRAW REACHED, which is the thing the re-set board was for and the
first time this slice has been able to record it.** Read off the live game after
each play:

- **`t04`, both graders — Rummage drew Kaboom! (cost 1), Pop! (cost 0) and
  Jumpy Dumpty (cost 2), with THREE energy standing and `can_play: true` on all
  three.** The draw destination was real, affordable and witnessed. After Rapid
  Fire took the remaining 2 energy, Kaboom! and Pop! were still playable and
  Jumpy Dumpty read `EnergyCostTooHigh`.
- **`t03`, both graders — Eager to Help drew Kaboom! and Pop!, both playable,
  with three energy standing.** Both lines then ended with those two cards still
  in hand and **2 energy unspent**.

**Not one of the four pair-2 lines played a drawn card.** That is the round's
sharpest fact about this arm, and it is now a fact about the graders rather than
about the board: round 2's RETURN said the draws were unaffordable, and on this
board they demonstrably were not.

### 13.5 The pair read

Same five questions, arm by arm, both pairs, with all eight forms, the
falsifier's verdicts, the live post-states and the per-play draw contents inline.
Reply unedited: `review/qa/klee-slice-1-r3-pair-review-codex-gpt-5.6-sol.md`; the
prompt is kept at `review/qa/klee-slice-1-r3-review-prompt.txt`, sha256
`880ca410bd95174fa55f54cdbbcd79838dc55014eccea6741557f7c7a73b748d`.

**Two things went wrong on the way to this read, and both are on the record
rather than tidied away.**

1. **The seat's protocol silenced it.** The first attempt came back as two lines
   — *"PAIR A: FOLLOWS"*, *"PAIR B: FOLLOWS"* — with no reading and no verdict.
   The seat was obeying: `EB-190` shipped ONE `REVIEW_PROTOCOL`, the DOCTRINE
   GATE's, and prepended it to every `seat review`; its own words are *"It
   overrides anything below that conflicts with it"* and *"That is the whole
   output."* A pair read asks five numbered questions and a
   RETURN/ADVANCE/ESCALATE, which is exactly the conflict that clause resolves
   against the brief. Round 3 was the first pair read since that door landed.
   Fixed in place as hygiene: `seat review --role {doctrine,pair}` splits the
   OUTPUT SHAPE only, the remedy ban is identical and verbatim in both, the
   doctrine text is unchanged to the byte, and an unknown role raises rather than
   falling back.
2. **The prompt builder mis-rendered every verdict.** The second attempt ran on a
   prompt whose builder read a verdict key that does not exist (`survives`; the
   file's keys are `verdict` and `refused_by`), so all eight forms printed as
   REFUSED and the seat ESCALATED both arms on that false premise — its stated
   reason was that *"Both forms are marked `intent_insensitive` refusals even
   though both explicitly answered that a different intent would change their
   line."* That is a correct reading of a corrupted page. The builder is a
   scratch artifact, not committed code; it was fixed, the prompt rebuilt to the
   true 7 SURVIVES / 1 REFUSED, and the read re-run. **Only the corrected read is
   committed**, and this paragraph is why it is the second one.

**Both arms ADVANCE. No RETURN, no ESCALATE.** Seat testimony is iteration
feedback, never validation, never balance evidence, never approval (R217 G);
ADVANCE means worth asking again with whole-fight play, not ship.

| arm | card | round 1 | round 2 | round 3 | the reviewer's own reason |
|---|---|---|---|---|---|
| 2 — the priced draw | Rummage | ADVANCE (provisional) | RETURN | **ADVANCE** | *"The board exposed a real draw destination, both surviving graders used the prototype, and the bank's competing destinations produced a steerable choice worth testing in whole-fight play."* On the kind of choice: *"The shipped half preserved the automatic Attack discount and used Spark only as a draw condition. The prototype half made the bank's destination steerable: spend it on Rummage for cards, or preserve it for the discounted Attack."* The cost bound: *"Spending the bank on Rummage made Rapid Fire cost its printed energy, constraining whether the remaining energy went to defense or a drawn card."* |
| 3 — the Burst conversion | Slow Burn | RETURN | ADVANCE (provisional) | **ADVANCE** | *"One surviving grader seriously weighed the prototype's distinct Spark destination and named its concrete opportunity cost. That is enough to justify asking the question again in whole-fight play, despite the second prototype form's refusal."* The cost bound: *"Spending the bank on the prototype would forfeit the free Rapid Fire and the damage line. Conversely, spending it on Rapid Fire made Slow Burn unplayable and forfeited its Burst progress and fuller guard."* |

The reviewer names one form/replay conflict and it is not a printed face: on
`t04` the Claude grader wrote *"That leaves 1 energy, which I spend on one of the
drawn 1-energy 7-damage Attacks"* and then listed only two plays, so the replay
*"recorded only Rummage and Rapid Fire, and the actual draws were not the
guaranteed set that assertion assumed."* Nothing was re-graded. Its other note is
smaller: on `t05` the Claude grader left the meter *"partly unresolved — '25 plus
whatever the 3 Burst Energy is worth' — while the replay resolved the printed
effects to the final meter value; that imprecision did not alter the played
line."*

### 13.6 What round 3 changed

1. **The independence breach is repaired, and by role separation rather than by
   a new grader.** Both rows are Claude-derived from the clause, both are
   `authored_by: [claude]`, `seat grade` no longer refuses either turn, and the
   lint's carry-list is empty. There is still no third model family and none is
   being added.
2. **Both provisional outcomes are replaced by clean ones, and they are not the
   same outcomes.** Rummage's round-1 ADVANCE was provisional AND was overturned
   by round 2 on the honest page; its round-3 ADVANCE is on a board built to the
   reviewer's own specification and is the first clean reading this arm has had.
   Slow Burn's round-2 ADVANCE is confirmed on an unchanged board with the number
   re-derived.
3. **The draw arm finally has a replay of its own text.** Round 2 could not watch
   a "draw 3" resolve because no graded line played the card. Round 3's did, on
   both graders, and the cards it reached are recorded card by card.
4. **A shipped instrument defect was found by running the round.** `seat review`
   could not perform one of its two documented jobs, and only a pair read could
   have found it.

### 13.7 What round 3 could not do

- **Get a grader to spend energy on a drawn card.** Four lines, three affordable
  draws each time, and none was played. The board can offer a destination; it
  cannot make a blind reader take one.
- **Ask arm 1 anything.** Second Helping was not in this round — its row was
  never provisional — and its whole-fight gate is §14.
- **Try Bag of Tricks.** Still held for [USER] at §6.1, untouched.
- **Read a `t06` seat form that engages the prototype.** Its refusal is
  `intent_insensitive` on a line that ignored Slow Burn, so the arm's evidence
  here rests on one surviving form, and the reviewer says so.

### 13.8 A round-1 finding, recharacterised on [USER]'s ruling

Round 1 recorded *"ten of twelve blind readers' lines were impossible"* and filed
it as a shipped display defect. **[USER] ruled on 2026-08-29 that it is not a
defect at all**, and the ruling is quoted rather than paraphrased:

> *"that 0-cost 'bug' was the intended mechanic. The sparks make your next attack
> free, whatever it may be, so they are supposed to all show 0 cost. I think the
> playtest agent just misunderstood."*

Every Attack rendering `Cost: 0` at a bank of 3 is the mechanic working: at the
moment the hand is read every Attack **is** free, whichever one is played first
takes the discount, and the bank then drops and the rest snap back to their
printed cost. Round 1's ten refused lines were readers failing to chain the
keyword's second sentence — *"Playing one consumes 3 Sparks"* — which is a reader
misread and not a display or D4 fault. The register row is removed outright as a
false positive; the keyword's plural wording is unchanged and sits inside `M48`,
still [USER]'s. **§11 and §12 are NOT rewritten** — a published round stands as
published — and the honest-page staging that tells the reader which Attack gets
the discount stays valid as staging, because a page that does the reader's
chaining for them is a better instrument whether or not the game is at fault.

---

## 14. Whole-fight play — Second Helping

The gate after ADVANCE is whole-fight blind play on a dev build, and it is
automatic: nobody is asked for a form to start it (`OPERATIONS.md`, *After
ADVANCE*). Until now it was **blocked, not skipped** — prototype rows are
quarantined out of every pool, so a blind run could not draw one. `EB-188` built
the door (`embark --arm`), and this is its first live run.

**This section rates nothing** (R217 G). Seat testimony is iteration feedback,
never validation, never balance evidence, never approval. The tester's words are
reproduced verbatim and nothing is concluded from them here.

**The session.** `review/qa/blindplay/20260829-181718/record.md`, one sealed
Codex thread from a run already open.

| | |
|---|---|
| model | `gpt-5.6-sol` requested, `gpt-5.6-sol` observed |
| mod build | **`0.2.1353+proto`**, read off `mods\klee\manifest.json` |
| game build | **v0.111.0**, read off the game's own `release_info.json` |
| run seed | `71D8JS1VSKRN`, read back off the wire (R95) |
| **arms granted** | **`KLEEMOD-PROTO_SPARK_PRICED_STRIKE`** — Second Helping, into the starting deck |
| arms-granted source | the embark sidecar, matched by run seed |
| actions | **120**, termination `max_actions` |
| leak audit | 127 observations scanned, **1 hit** — `pilot-vocabulary-score`, the harness's own guide line saying there is *"no score, no recommendation"*, the identical benign hit the previous sealed run recorded |

Six fights were recorded and the run was still alive when the action budget ran
out.

**The granted card was drawn AND played.** The tester names it in five of the
seven sections without ever being told what it was for, and the trajectory is
its own record:

> *"Second Helping was dead throughout because I never reached three Sparks"*
> (fight 1)

> *"Second Helping was dead on turn one, but would have become strong once three
> Sparks were available if it had been drawn then."* (fight 2)

> *"On the final turn, Second Helping reduced the healthier slug, Kaboom killed
> the other, and Kaeya's Melt finished the survivor."* (fight 4, question 1)

> *"Second Helping finally became excellent at three Sparks."* (fight 4,
> question 4)

> *"Second Helping was dead until Sparks reached three and then was not drawn at
> the useful moment."* (fight 6)

### The run-end record, verbatim

> **1.** The character builds Sparks and Burst Energy through attacks, elemental
> skills, and reactions, then turns Sparks into free attacks. Bombs provide
> delayed or attack-triggered damage and soften one enemy attack, while Pyro
> setup plus Kaeya's Cryo creates strong Melt finishes.
>
> **2.** The recurring tension was whether to detonate Bombs immediately for
> damage or leave them attached for delayed damage and attack reduction. I also
> repeatedly chose between spending energy on defense now or racing toward a kill
> that would prevent more damage later.
>
> **3.** Pop!, Sharp-enhanced Jumpy Dumpty, Kaeya — Frostgnaw, and Bombs Away!
> defined the run. Pop! made many turns start automatically, Jumpy Dumpty
> supplied efficient multi-hit damage, Kaeya converted Pyro auras into lethal
> Melt hits, and Bombs Away! enabled huge detonations. Run Away! and Trip Wire
> were the most useful supporting additions.
>
> **4.** Play became repetitive once the pattern settled into free Bomb, attack
> to detonate, then block with leftover energy. Later, Bombs Away! followed by a
> free Spark-discounted attack became an even more dominant version of the same
> sequence. Reward collection also required the same repeated Gold, Potion or
> Relic, Card, proceed inputs.
>
> **5.** I would avoid Chain Fuse unless its effect became clearer or was fixed;
> the displayed Bomb totals never showed its promised damage bonus. I would also
> avoid adding more ordinary one-cost attacks or basic block cards because the
> deck already had enough, and Second Helping was often dead until three Sparks
> happened to align with its draw.

### What this section is, and is not

`EB-188`'s acceptance was *"a sealed blind run on a dev build draws or is granted
the arm, and the record names it."* It is met: the record's identity block names
`arms_granted` and both builds, the run went to its action budget, the leak audit
ran, and the arm was not merely granted but drawn and played. **The row is
CLOSED.**

Everything above the acceptance line is testimony and stays testimony. Nothing
here says Second Helping is good, bad, correctly costed or ready; nothing here is
compared with any other run; and the arm's status is unchanged — ADVANCE, which
means worth asking again, not ship.
