# Furina E4 — the redesign packet

> **Lifecycle: ACTIVE.** A design packet, not law. §3 is a DRAFT ruling
> awaiting [USER]'s countersign; §9 is what returns to him. Nothing here has
> moved a shipped number, a sheet row, a constant or a register. No code was
> written for it.
>
> **Revision 2 (2026-08-29).** Revised against the doctrine seat's
> implementation return, recorded verbatim at **§11**, which binds this
> revision. Its four points — the Salon payoff is not already priced;
> collapse the two Fanfare legs into one cause; the Guest rider needs one
> owner; 20 is a seed, not a derived value — plus the elevation of
> whole-fight blind play into a mandatory gate, are folded in place. Closed
> text left rather than being struck.

**Date:** 2026-08-29. **Branch:** `furina-e4-r2`, cut from `origin/main` at
`48a2273`, which carries revision 1. **World:** `RT12 / D18 / P11 / C20`,
`PILOT_WEIGHTS_VERSION` 6. **Authority:** R213 E4, amended by R217; the
delegation ladder is R212.

---

## 1. The inputs

### 1.1 [USER], verbatim

> "For Furina, we have three mechanics - Salon, Fanfare, Encore - plus a
> desire to make Companion Cards work through Fanfare, and I feel like the
> different aspects are not really cohesive. I think some of this is UI/UX
> legibility - Encore should be a status bar like Regent's stars, Spotlight
> should not require playing a card every single turn - and some of it is
> fundamental."

### 1.2 R213 E4, verbatim (`1050f67`)

> "E4 -- Salon is Furina's provisional spine: YES. Encore, Fanfare and
> Spotlight must prove they function as parts of the Salon loop or be cut
> or merged; the first Furina slice tests all four in that arrangement."

And the sequencing clause from the same ruling:

> "...its findings inform Furina. Then Klee's reaction slice, then Furina
> after the Companion result. Not three explorations in parallel."

### 1.3 GPT's proposal (summarised faithfully; the full text is [USER]'s)

A hierarchy: **"Salon and Companions are Furina's cast. Spotlight directs
the cast. Encore pays for and protects the performance. Successful
performances create Fanfare, which unlocks the finale."**

1. Salon members are the *resident troupe*; Companions are the *guest cast*.
   Both hang off the same performance hooks.
2. Remove Center Stage. Spotlight chooses **Salon** (the front member's next
   performance is Featured) or **Guest Cast** (the first Companion played is
   Featured).
3. Spotlight is a **persistent state**, not a generated selector card:
   chosen at combat start, and moving it costs Encore.
4. Encore is the immediate tactical resource, shown beside HP. Furina cards
   spend it to move Spotlight, command another performance, repeat the
   Featured performance, or add a rider.
5. Fanfare **replaces** the separate 70-point Burst meter. Spotlighted
   performances generate it; full = Burst; casting empties it. The Burst is
   a qualitative **"Grand Performance"** (e.g. both lanes Featured for a
   round), not "+1 per X".

Archetypes stay three: Resident Troupe / Guest Cast / Conductor. The starter
kit teaches the whole sentence — one deterministic Salon deployment, one
generated Companion, one Encore generator, one cheap Encore spend, and the
relic supplies the persistent Spotlight interface. Prototype the starter,
two members and a few Companions first. The decisive test: *"Did the player
plan who would receive Spotlight, and did spending Encore on that
performance feel meaningfully different from keeping it for defense?"*

### 1.4 Claude's read, as given to [USER]

The diagnosis is on the record, not a hunch: Salon 4.70% / Spotlight 1.50% /
Fanfare 1.30% at `RT9/D14/P6/C8`; the selector heuristic in
`tier0/engine/effects.py:1803` *is* the collapse rule; the measured
Spotlight-move share is 0.167; and The Curtain Never Falls already deletes
the mode choice outright. Burst is MOD-defined (a BaseLib `CustomResource`),
not base-game, so merging Fanfare into it removes machinery rather than
adding it — and both meters already observe the same activity. Grand
Performance is the state that relic already expresses.

Reshape: (i) a persistent lane chosen at combat start through the existing
choice-modal machinery, with Encore-priced moves through the `encore_cost`
gate on `spotlight_designate` — click-on-rail is an unverified interaction
and is the second attempt, not the first; (ii) Encore is uncapped, so a fill
bar has no denominator — show a count with pips beside HP and put the runway
arithmetic in the tooltip; (iii) two LAW touches, §2.2a and Burst kit-scope.

Picks given: **P1** go/hold; **P2** (1) combat-start choice + Encore-priced
moves; **P3** (1) Fanfare becomes the Burst meter; **P4** (1) count + pips;
**P5** (1) qualitative within §2.2a / (2) amend the guard.

### 1.5 GPT's review of that read — the binding parts, verbatim

> "P1: Go · P2: Option 1 · P3: Option 1 · P4: Option 1 · P5: Option 2, but
> with a narrow amendment."

**Constraint 1.**

> "For E4, I would initially allow Fanfare from only: A Featured performance
> resolving. Encore deliberately spent through a Furina `Direct` effect,
> potentially enabling another performance. No Fanfare from generic HP loss,
> automatic Salon upkeep, or Encore absorption in the first arm. Those can be
> tested later. This makes Fanfare evidence that the player directed the
> cast, not a general activity odometer. It also resolves the companion
> Burst-law issue: an arbitrary Companion does not generate Burst; one
> specifically Featured by Furina's kit does, under a once-per-turn or
> otherwise bounded hook."

**Constraint 2.**

> "I would retire the blanket +50% from the prototype."

Proposed LAW text:

> "Spotlight is a designation, not a universal numeric multiplier. A Featured
> performance may generate Furina's Burst resource and receive effects
> explicitly authored by Furina's kit. Spotlight alone may not duplicate a
> card, reduce its cost, add draw, or amplify nonnumeric Companion text.
> Explicit bounded Furina cards and her Burst may create additional Featured
> performances."

And:

> "The prototype needs at least two meaningfully different Featured outcomes.
> Otherwise choosing Salon versus Guest still only selects which body
> produces meter."

**Constraint 3.**

> "the first playable experiment cannot remove Center Stage before Salon and
> Companions share a minimal performance hook."

Order:

> "1. Write E4 and its doctrine gate. 2. Implement the smallest shared
> performance hook: one Salon action and one Companion play. 3. Add
> persistent Salon/Guest Spotlight, combat-start choice, and Encore-priced
> movement. 4. Merge Fanfare with Burst and add Grand Performance.
> 5. Construct the starter-deck boards. 6. Migrate nothing else until
> independent playthroughs answer the D2 question."

Closing:

> "I would only insist that E4 explicitly retire 'all Encore loss is
> progress' and 'Spotlight means +50%' rather than carrying those assumptions
> invisibly into the new architecture."

### 1.6 Claude's acceptance

All three constraints accepted. My order had Center Stage removal first;
GPT's order is right, and the reason is that a shared hook is the thing that
makes the two lanes comparable at all — remove the mode before the hook
exists and the "choice" is between one implemented lane and one that is not.

The two Featured outcomes below are **my reading**, built from machinery
that already ships. Alternatives are listed beside each.

---

## 2. Evidence

Every row here is a file:line in this tree at `fb56095`.

### 2.1 The three plans are not one system, and the numbers say so

| arm | winrate | 95% interval | source |
|---|---|---|---|
| `furina / salon` | **4.70%** | [4.00, 5.52] | `review/active/eb81-furina-remedy-options-2026-08-12.md:38` |
| `furina / spotlight` | **1.50%** | [1.12, 2.00] | `…:39` |
| `furina / fanfare` | **1.30%** | [0.95, 1.77] | `…:40` |
| `real_silent / generic` (floor) | 1.37% | [1.01, 1.85] | `…:41` |
| `real_ironclad / generic` (floor) | 6.13% | [5.33, 7.05] | `…:42` |

World stamp `RT9/D14/P6/C8` (`…:44-48`). **Use it for structure, never for
level** — that is the packet's own caveat, and it is three boundaries stale.
The structure that survived the boundary crossing (`…:50-54`): Salon
separates from both other plans, intervals disjoint; Spotlight and Fanfare
cannot be told apart from each other; both overlap the `real_silent` floor.

That is a *structural* finding, which is exactly what the simulator's
authority covers (LAW: "the simulator's authority is relative deltas and
structural findings, not absolute human-play winrates"). It is not a level
claim and nothing below rests on one.

### 2.2 The selector heuristic is the collapse rule

`tier0/engine/effects.py:1803-1829`, `_op_spotlight_designate`:

```python
companion_in_hand = any(c.is_companion and not c.kit_card for c in p.hand)
...
elif companion_in_hand:
    target = C.SPOTLIGHT_GUEST_CAST
else:
    target = p.character_id or (
        C.SPOTLIGHT_GUEST_CAST if companion_anywhere else None)
```

That is, in code, the rule the richness brief asked the table whether the
choice had collapsed to:

> "was the mode choice ever actually contested, or did it collapse to a rule
> — 'Guest Cast if I'm holding a Companion, otherwise Center Stage'? If it
> collapsed to a rule, say which rule."
> (`review/active/richness-playtest-brief-2026-08-26.md:115-120`)

The pilot does not "collapse to" that rule. The pilot **is** that rule. The
selector history in `tier0/constants.py:123-157` records five versions of
the heuristic and the shipped one is v5, described there as exactly this
two-branch pick.

### 2.3 The move share

`tier05/draft.py:79`: `STATIC_SPOTLIGHT_MOVED_SHARE = 0.167`, read at
`tier05/draft.py:176`, described at `tier0/constants.py:2628` as "the
measured spotlight-arm rate". The drafter prices every "if you moved the
Spotlight this turn" payoff against one turn in six.

### 2.4 The relic upgrade already deletes the choice

`klee-mod/KleeCode/Relics/UpgradedStarterRelics.cs:399-405`:

> "[gold]Center Stage[/gold] and [gold]Guest Cast[/gold] are both always
> active, and you always count as having moved the [gold]Spotlight[/gold]
> this turn."

The brief put the same fact to the table
(`review/active/richness-playtest-brief-2026-08-26.md:121-126`): "There is an
upgraded version of that relic that deletes the choice entirely… Whether that
is a relief or a loss is exactly the sort of thing only the table knows."

### 2.5 Two meters, one activity

Fanfare legs (`tier0/constants.py:180-199`):

| constant | value | line |
|---|---|---|
| `FANFARE_PER_HP_LOST` | 1 | `:180` |
| `FANFARE_PER_ENCORE_SPENT` | 1 | `:181` |
| `FANFARE_PER_ENCORE_ABSORBED` | 1 | `:182` |
| `FANFARE_PER_SPOTLIGHT_CARD` | 2 | `:199` |

Burst legs:

| constant | value | line |
|---|---|---|
| `BURST_PER_SKILL_TAG` | 5 | `tier0/constants.py:69` |
| `BURST_PER_REACTION` | 5 | `:70` |
| `SALON_TICK_BURST` | 2 (per member per tick AND bow) | `:319` |
| `BURST_PER_ENCORE_SPENT` | 1 | `:321` |

`burst_max: 70` (`tier0/content/characters/furina.yaml:14`), whose own
comment names the four income sources: "particles from skill tags (5),
reactions (5), Salon ticks (2/member/turn), Encore spend (1/point)."

**Encore spend feeds both meters.** `BURST_PER_ENCORE_SPENT = 1` and
`FANFARE_PER_ENCORE_SPENT = 1` are the same event paying two different
counters, each with its own display, its own tooltip and its own reader set.
That is the concrete form of "the different aspects are not really cohesive".

Burst is mod-defined, not base-game: `let_the_people_rejoice` is
`kit_card: true`, `requires: burst_energy_full`
(`docs/furina-cards.yaml:836-837`), granted by
`tier0/engine/combat.py:156-170` and emptied at `combat.py:380`.

### 2.6 The two assumptions E4 has to name and kill

**"All Encore loss is progress."** `tier0/constants.py:183-198` records it as
a deliberate ruling: after the 2026-07-28 rework "EVERY point of damage that
gets past Block prints exactly 1 Fanfare, via absorption if the buffer eats
it and via `hp_lost` if HP does", and the invariant is asserted by
`test_every_point_past_block_prints_exactly_one_fanfare`. LAW:197 states it
as a design invariant. Under GPT's Constraint 1 it goes: HP loss, upkeep and
absorption stop paying.

**"Spotlight means +50%."** `tier0/constants.py:91`,
`SPOTLIGHT_BASE_MULT = 1.5`, RATIFIED R71. Printed on the face:
`klee-mod/KleeCode/Cards/Furina/SpotlightCards.cs:117-119` — "Their printed
damage and Block are 50% stronger, but their plays do not generate Fanfare."

### 2.7 Encore has a bar and no denominator

Encore is unbounded by design — `tier0/constants.py:165` ("Encore is
unbounded per-combat (v1.6) -- no cap constant by design"), and
`tier0/engine/resources.py:330-352` `gain_encore` has no clamp. The display
lives on the Salon stage ribbon and renders as a **runway**, not a fill bar
(`klee-mod/KleeCode/Vfx/SalonVisualsBridge.cs:572-587`): a segment is one
turn of upkeep at the current stage, because "the old denominator was a
display-only 20 -- a lie twice over, because Encore is uncapped and because
20 meant nothing to the player."

So [USER]'s "Encore should be a status bar like Regent's stars" is
achievable as a **count**, and is not achievable as a *fill fraction*. There
is nothing to be a fraction of.

### 2.8 The Encore spender family does not exist yet

`docs/current/STATE.md` "Active workstreams": "`F3` / the Furina
ENCORE-SPENDER family → `Win11`, which opens only AFTER the pilot's Encore
opportunity-cost repair — spenders cannot be priced against a resource the
pilot values wrongly". Both windows are FROZEN by R213. The richness brief
told the table the same thing in plain words
(`review/active/richness-playtest-brief-2026-08-26.md:107-111`): "Encore now
has a bar but almost nothing to spend it on… Note whether the Encore meter
felt stranded — filling with no door out."

### 2.9 The starter does not teach the default plan

`tier0/content/characters/furina.yaml:49-59` — ten cards, of which exactly
one is a Salon card and it picks its member at random
(`docs/furina-cards.yaml:83-84`, `member: random`). The brief again
(`…:127-131`): "The Salon is her default plan, but the starter barely teaches
it. Only one starter card is a Salon card and it picks its member at random.
The relic teaches the Spotlight instead."

### 2.10 The sheet census

`review/active/eb81-furina-remedy-options-2026-08-12.md:117-131`:

| plan | cards touching it | exclusive to it | exclusive by rarity |
|---|---|---|---|
| salon | 28 | 15 | 7 common / 5 uncommon / 2 rare / 1 basic |
| spotlight | 18 | 15 | 3 common / 8 uncommon / 4 rare |
| fanfare | **31** | 14 | 3 common / 5 uncommon / 6 rare |
| generic | 32 | — | 10 common / 13 uncommon / 5 rare / 4 basic |

Two facts travel with it (`…:128-131`): fanfare's exclusives are rare-heavy,
six of fourteen, so a change confined to them is a change most decks never
draw; and twelve cards are tagged `[fanfare, generic]`, i.e. fanfare cards
the drafter offers to salon decks too.

### 2.11 Blast radius, as already measured

`…:225-229`: "37 test files reference salon, 32 fanfare, 18 spotlight, across
a 190-file suite (of those, 16 / 10 / 6 pin the plan name as a quoted string
literal)." And: "each scorer change is a version bump, and a bump makes every
arm of the table discontinuous — for every character, not just Furina. The
re-baseline is therefore always the whole twelve-arm table, never the three
Furina rows."

### 2.12 R177, which E4 reopens

`020c888`, verbatim:

> "Direction ruled: rebalance the weak Furina plans until they are viable.
> NOT option B1 (fold plans into salon) and NOT B2 (widen the stage). The
> reasons on record: it is the reversible move, it preserves the S4-G7
> comparison series, and the four-slot sweep measured WORSE (16.7% -> 14.7%)
> because half of all upkeeps already arrive unable to fund a member — the
> stage outruns its fuel."

E4 is the fold R177 declined. The stage-outruns-its-fuel finding is not
overturned by E4 and is load-bearing for §4.3's rate arithmetic.

---

## 3. E4 — ruling text, DRAFT for [USER]'s countersign

> **R2xx — E4: Furina's four mechanics become one sentence.**
>
> **The architecture.** Salon members and Companion cards are one cast.
> Spotlight designates which half of the cast is Featured. Encore pays for
> and protects the performance. **Only a successful Featured performance
> creates Fanfare. One natural and one Encore-commanded Featured performance
> may pay each turn.** Fanfare **is** Furina's Burst meter; a full meter
> grants her Burst, and the Burst is a Grand Performance rather than a
> number.
>
> Fanfare therefore has exactly one cause, not two. Encore creates
> additional Fanfare by successfully commanding another performance, never
> by leaving the counter: a command that finds no eligible performer pays
> nothing.
>
> **What this reopens.** R177's ruled direction — "rebalance the weak Furina
> plans… NOT option B1 (fold plans into salon)" — is **overturned on the
> fold**. E4 is B1, done deliberately and under R213's authority rather than
> as a balance move. R177's measured finding that the stage outruns its fuel
> **stands** and constrains §4.3. The kickoff's deliberate separation of
> Encore and Fanfare as two independent counters
> (`docs/current/characters/furina-kickoff-v0.1.md` §4, "Encore is Furina's
> only managed resource" / "Fanfare is a global pool") is **retired**: they
> are one loop with one meter.
>
> **What E4 explicitly RETIRES.** Each of these is named so it cannot ride
> into the new architecture unexamined:
>
> 1. **"All Encore loss is progress."** `FANFARE_PER_HP_LOST`,
>    `FANFARE_PER_ENCORE_ABSORBED` and Salon upkeep stop generating. The
>    design invariant "every point of damage past Block prints exactly 1
>    Fanfare" (LAW; `tier0/constants.py:196`) is retired with them, along
>    with the test that asserts it. **`FANFARE_PER_ENCORE_SPENT` retires as
>    an independent leg too**: under the single cause above, Encore spent
>    pays only through the performance it successfully commands, and a spend
>    that commands nothing pays nothing.
> 2. **"Spotlight means +50%."** `SPOTLIGHT_BASE_MULT = 1.5` is retired as
>    the meaning of a designation. A Featured performance receives what
>    Furina's kit explicitly authors for it and nothing by default.
> 3. **Center Stage as a mode.** The two-mode Spotlight (R41, LAW "Character
>    identity — Furina") is replaced by two lanes of one cast: **Salon** and
>    **Guest Cast**. Furina's own cards stop being a Spotlight target.
> 4. **The Ethereal selector card.** The starting relic stops adding a card
>    to hand each turn and instead supplies the persistent Spotlight
>    interface. "Spotlight should not require playing a card every single
>    turn" ([USER]) is satisfied structurally, not by making the card
>    cheaper.
> 5. **Fanfare decay, floor and cap machinery.** `FANFARE_DECAY_FRACTION`,
>    `gain_fanfare_floor`, `raise_fanfare_cap`, `FANFARE_CAP_FRACTION` and
>    the `Fanfare +X` / `Fanfare Cap +X` keyword pair are retired: a Burst
>    meter does not decay, has no floor, and its ceiling is the meter size.
>    Register lint `R6` and `L12`, which exist to police those keywords,
>    retire with them.
> 6. **The two co-op provisions that depend on the retired machinery.**
>    Cross-player selector passing (kickoff §3.1: "In co-op, the selector may
>    instead be passed to a teammate, who applies it to one of their own
>    cards") is retired, because there is no selector card to pass — the
>    lane is a persistent state on Furina. Partner-coupled Fanfare (kickoff
>    §4: "partner HP/Encore flux counts toward Fanfare") is retired, because
>    HP loss and Encore flux stop generating for Furina herself under item 1
>    and cannot survive as a partner-only leg. **Both are retired, not
>    deferred**, and neither is replaced by this packet. Furina's co-op
>    identity is thereby left with no ally-coupled mechanic at all, which is
>    a real loss against Appendix A.4's mandate and is called out as such;
>    what replaces it is future design space and is not E4's to author.
>

> **Two LAW amendments** (exact replacement text at §3.1 and §3.2 below).
>
> **What the animation contract keeps.** All three contracted binding points
> (LAW: "Visuals bind to funnels, never to values") survive E4 unchanged:
> **Salon = 3 slot-index-keyed slots, duplicates legal** — E4 moves no slot
> and adds no member-to-slot assumption, and the Featured rotation of §4.1 is
> a **re-render of the same three slot indices, not a contract change**: the
> slots are keyed by index and the members are re-drawn into them, which is
> exactly what the shipped `salon_rotate` already does today
> (`tier0/engine/effects.py:1935-1956`); **Encore-absorbs-before-HP** —
> unchanged, only its Fanfare side-effect is removed;
> **Spotlight-is-a-designation-event** — E4 makes this *more* true, since a
> designation stops carrying a multiplier. The centered-overhead
> creature-space slot stays the cross-character Burst indicator (LAW), and
> under E4 it renders the merged meter; that is one gauge where there were
> two, not a new binding point. The Encore ribbon
> (`SalonVisualsBridge.cs:572`) is a D3/D7 display decision, not a contracted
> binding point, so moving Encore beside HP breaches nothing — see §4.4.
>
> **Its place in R213's sequence.** R213 ordered Kokomi → Klee → Furina,
> "not three explorations in parallel", and R217 left that untouched. Kokomi
> slice 2 and Klee round 3 are both open. Whether E4's build steps start in
> parallel with them or queue behind them is **[USER]'s call**; the seat's read is at §9.3 P1. This
> packet is documentation and a doctrine gate only, which R213 lists as step
> 1 of the order and which consumes no slice capacity either way.

### 3.1 LAW amendment 1 — replaces LAW.md:74-75

Current text:

```
- **Spotlight/empowerment boosts numbers only, never turn-economy effects.**
  (principles §2.2a, v1.10)
```

Replacement (GPT's wording, adopted verbatim):

```
- **Spotlight is a designation, not a universal numeric multiplier.** A
  Featured performance may generate Furina's Burst resource and receive
  effects explicitly authored by Furina's kit. Spotlight alone may not
  duplicate a card, reduce its cost, add draw, or amplify nonnumeric
  Companion text. Explicit bounded Furina cards and her Burst may create
  additional Featured performances. (principles §2.2a, v1.10; R213 E4)
```

Note what the new text is *stricter* about and what it relaxes. It relaxes
"numbers only" — a Featured performance may now receive a Furina-authored
non-numeric rider. It tightens everything else: the four turn-economy verbs
the old guard banned (duplicate, cheapen, draw, amplify companion text) are
now banned **from the designation itself** rather than from empowerment
generally, and they remain reachable only through a printed Furina card.
That is why the old guard cannot simply be kept beside it: "boosts numbers
only" and "receives effects explicitly authored by Furina's kit" contradict
each other, and the second is the one E4 needs.

### 3.2 LAW amendment 2 — Burst kit-scope, amends LAW.md:144-146

Current text:

```
- **Neutral action-energy / draw / thin are legal enabler-grade companion utility**
  (one-shot, Exhaust-gated, costed) — the pool must let any character draft a
  kit-gap fix. **Burst-meter (`burst_energy`) generation stays character-kit-scoped**
  and must never be cheaply repeatable from companions. (principles §4.7, §2.4)
```

Replacement — the first two sentences are unchanged; one clause is appended:

```
- **Neutral action-energy / draw / thin are legal enabler-grade companion utility**
  (one-shot, Exhaust-gated, costed) — the pool must let any character draft a
  kit-gap fix. **Burst-meter (`burst_energy`) generation stays character-kit-scoped**
  and must never be cheaply repeatable from companions. **Furina's meter is
  the narrow case (R213 E4): only a successful Featured performance generates
  it. One natural and one Encore-commanded Featured performance may pay each
  turn. A Companion card generates none by being played; a Companion Featured
  by Furina's kit generates it as a Featured performance. Encore spend, HP
  loss, automatic Salon upkeep, Encore absorption and Skill tags feed her
  meter not at all.** (principles §4.7, §2.4; R213 E4)
```

This is the clause GPT's Constraint 1 says resolves the companion Burst-law
question, and it resolves it in the direction the existing law already
points: the meter stays kit-scoped because the *kit* is what Features the
Companion, not the Companion.

**Why this wording and not the previous one.** The version this replaces said
the meter "is generated by exactly two things — a Featured performance
resolving, and Encore deliberately spent through a Furina card that commands
a performance". The doctrine seat's G2 verdict caught the contradiction
between that and the ruling's own prose; the first revision fixed it by
making both legs explicit, and the seat's return then closed it properly by
**collapsing the two legs semantically**. There is one cause. The
Encore-commanded leg is not a second source: it is the same Featured
performance, arrived at by paying Encore instead of by the turn's natural
cadence. The 2 + 2 per-turn ceiling is preserved exactly; what is removed is
the possibility of Encore paying the meter without a performance happening.
That makes the LAW sentence **literally true as written**, which is what G2
asked for.

**What is NOT amended.** The kit-Burst clause at LAW.md:176-178 — "never
draftable, granted to hand on meter fill, casting empties the whole meter
(overflow lost at cast), re-granted on refill, carries Retain" — is
unchanged and E4 obeys it: the merged meter is granted on fill and emptied
on cast, exactly as `tier0/engine/combat.py:156-170` and `:380` already do.

---

## 4. Architecture

### 4.1 The shared performance hook

**The event.** One new engine event, `performance`, with a payload of
`(lane, source_card, actor)`. It fires in exactly two places, and GPT's step
2 builds exactly these two:

- **Salon lane** — fires once when a Salon member resolves its slot act at
  the start of Furina's turn. Today that path is the tick in
  `SALON_MEMBERS` (`tier0/constants.py:301-310`), priced at
  `SALON_TICK_ENCORE_COST = 1` per member (`:316`) and paying
  `SALON_TICK_BURST = 2` (`:319`). The hook wraps the existing tick; it does
  not replace it.
- **Guest Cast lane** — fires once when a Companion card resolves. That is
  the same predicate the selector already uses,
  `c.is_companion and not c.kit_card` (`tier0/engine/effects.py:1808`).

Everything else in E4 hangs off this one event. That is the point of doing
it first: until both lanes emit the same event, "choose a lane" is a choice
between one implemented thing and one imagined thing, which is GPT's
Constraint 3.

**What "Featured" does, per lane.** A `performance` event whose `lane`
matches the live Spotlight designation is **Featured**. Featured is a
per-performance property, bounded once per turn by §3.2.

**Which body is Featured, exactly.** The lane names a half of the cast, and
each half resolves to one body by a rule that already ships:

- **Salon lane → the FRONT member**, `p.salon[0]`, index 0 of the single
  company order defined under "the clarification the rule needs" below. That
  is the same position `salon_bow` takes, the same position a deploy landing
  on a full stage displaces, and the same position the
  `leftmost_salon_member_*` predicates read
  (`tier0/engine/effects.py:1928-1949`). With `SALON_MEMBER_SLOTS = 3`
  (`tier0/constants.py:313`) up to three members act each turn; exactly one
  of them — the front one — is Featured, and the other two tick normally.
- **Guest Cast lane → the FIRST Companion played this turn.** Same
  once-per-turn bound, and it is the wording GPT's proposal already uses.

**This is what finally gives `change_the_bill` a reader.** The card rotates
the front member to the back (`docs/furina-cards.yaml:472-473`,
`{op: salon_rotate}` + `{op: salon_perform}`), and the richness brief's own
standing complaint about it is that nothing cares:
"No card anywhere on her sheet cares which member is at the front. So 'why
would I play this' is the correct reaction, and it is a hole we already know
about" (`review/active/richness-playtest-brief-2026-08-26.md:100-105`).
Under E4, which member is at the front decides which act gets Featured, and
the rotate becomes the Salon lane's aiming verb. That is a hole E4 closes
without adding a card, and it is evidence for the architecture rather than a
convenience of it.

**Salon lane, Featured — the seat's rule, adopted verbatim in substance:**
*the Featured front member performs its special act and **rotates to the
back**.* Three things travel with it and none of them is optional:

- **It is NOT a `final_bow` event.** The member stays on stage. The engine's
  `salon_final_bow` emit (`tier0/engine/effects.py:1404`) does not fire, and
  **bow-triggered Powers do not fire**: `salon_bow_block` and
  `salon_bow_encore` (Stagehands, Curtain Call B / R85,
  `effects.py:1392-1402`) read the departure, not the performance, and a
  member who did not depart has not paid their price.
- **The act bodies are TEMPORARY PROTOTYPE INPUTS, labelled UNPRICED in this
  cadence.** The seat's return is exact about why, and it is the reason this
  section was returned:

  > "'Bow in place' is not already priced. The current bow is explicitly a
  > final displaced payoff. It removes the oldest member and triggers every
  > bow-dependent Power. … Calling the existing numbers 'ratified' does not
  > make them transferable to a free, repeatable, once-per-turn action that
  > leaves the member in play. In particular: Crabaletta becomes a repeatable
  > 14-damage engine. Usher becomes repeatable 9 Block. Chevalmarin refunds 3
  > Encore repeatedly, potentially erasing the intended Encore tension."

  So the table below is **input, not price**. The R187 signing
  (`tier0/constants.py:280-292`) signed 6/14, 3/9 and 2/+3 **as a tick-and-
  displaced-bow pair**, and nothing in it transfers to a repeatable
  in-place act.

| member | tick (shipped) | bow body (shipped) | as a Featured act |
|---|---|---|---|
| crabaletta | 6 damage | 14 damage | **UNPRICED** in this cadence |
| usher | 3 Block | 9 Block | **UNPRICED** in this cadence |
| chevalmarin | 2 damage + Hydro | Hydro to ALL + 3 Encore | **UNPRICED**; the Encore refund is the specific hazard — a repeatable +3 erases the tension Slot 2 exists to grade |

Pricing them is step-4 work under the flag and its numbers are graded by the
funnel, not asserted here. The one thing the packet does assert is the
**shape**: the Featured act is categorically different from a tick, and it
costs the front slot.

**The clarification the rule needs and the return did not give (Claude's,
and it is a real fork).** Once a Featured performance rotates, "front" and
"oldest-by-deploy" **diverge**. `_deploy_salon_members`'s docstring says
deploying into a full stage "bows the OLDEST member OUT"
(`effects.py:1418-1425`) while its code pops `p.salon[0]`; today those are
the same thing only because nothing but `salon_rotate` ever reorders the
queue. Under E4 a rotation happens every turn the Salon lane is Featured, so
the two orders come apart on turn one and every positional reader has to be
told which one it reads.

**My reading: there is ONE order, and it is the rotation order.**

1. The Salon is a single ordered company. **FRONT = index 0** of that order.
2. The Featured act is performed by the front member, who then goes to the
   back.
3. **Deploying into a full stage bows out the FRONT** — so the rotation is
   also the player choosing who leaves next.
4. `change_the_bill`, `salon_bow`, `salon_perform` and every
   `leftmost_salon_member_*` predicate read **that same order**, unchanged.

This is not a new invariant — **it is the shipped one**, stated out loud.
`_op_salon_rotate`'s own docstring already says the rotate "buys exactly one
thing — which performer the FIFO end offers next, to `salon_bow`, to a deploy
landing on a full stage, and to the `leftmost_salon_member_*` reads"
(`effects.py:1935-1946`). "Oldest" in the deploy docstring is a description of
the un-rotated case, not a second key; there is no deploy-age field on the
queue and E4 does not add one. **This is therefore a documentation fix
inside E4's scope, not a rule change** — and it turns the Featured rotation
into a second copy of a verb the player already learned from
`change_the_bill`, which is the D4 argument for it.

*The alternative, and why it is worse:* keep displacement keyed to **deploy
age** and let the two orders diverge — add an age key, let the front be
whoever the rotations left there, and let deploys still evict the eldest.
That fails D4 ("at the decision point the player can perceive and forecast
the consequences that matter") on its face: the stage would render one
order while resolving another, so "who leaves when I deploy" becomes
unreadable off the board exactly when Featured rotations are churning it.
It also splits `salon_bow` from deploy displacement, which the shipped
`_op_salon_bow` docstring names as the whole reason it takes leftmost —
"the player already knows which member is next out, because the deploy rule
taught them". Two orders means the deploy rule teaches nothing.

*Against the animation contract:* **a re-render, not a contract change.**
The contracted point is "Salon = 3 slot-index-keyed slots (duplicates
legal)" — the slots are keyed by **index**, not by member identity, so
re-drawing which member occupies which index moves no slot, adds no slot and
adds no member-to-slot assumption. The shipped `salon_rotate` already does
precisely this and the bridge already renders it. No breach, and no
stop-work.

**Alternatives to the Featured act itself, if [USER] returns it:**
(a) the Featured member acts **twice** (tick, then tick) and does not rotate
— simpler, but it is "+1 per X" wearing a costume, it doubles Chevalmarin's
aura, and it costs the player nothing positional, so the lane stops being a
place where order matters;
(b) the Featured member's act gains the Focus bonus at a better rate
(`SALON_FOCUS_PER = 10`, `:311`) — a numeric multiplier, exactly what §3.1
retires;
(c) the Featured member's act is free of upkeep (`SALON_TICK_ENCORE_COST`
waived) — cheap and legible, but it *reduces* the Encore tension the slice
is trying to create;
(d) the Featured member performs its act and **is displaced** — i.e. the old
"bow" reading. This is the one the return rejected, and the rejection is
about repeatability: a payoff that removes its own source is priced as a
final payoff and cannot be the lane's every-turn body.

**Guest Cast lane, Featured — the seat's rule:** *a Featured Companion play
mints Fanfare and applies **Hydro**, and the rider has exactly one owner —
the **relic**.* The return is explicit that the rider needed an owner and
that the first prototype is not where that gets to be flexible:

> "The Guest rider needs one owner. … For the first prototype, make Hydro a
> fixed relic-authored Guest rider. Spotlight-movement cards should only
> move Spotlight."

So, for the prototype: **the rider is a fixed, relic-authored Hydro
application.** `ethereal_spotlight` — which under §5.2 already becomes the
persistent Spotlight interface — is the single place the Guest rider is
printed and the single place a reader looks for it. Hydro is lifted off a
shipped face (Chevalmarin's aura rider, `tier0/constants.py:308-310`) and is
Furina's skill-grade cadence problem solved through the cast rather than
through her own tags.

**Spotlight-movement cards only move Spotlight.** A lane-move card carries
`{op: spotlight_designate, ...}` and an `encore_cost`, and it prints no
rider. The packet's earlier proposal — *"the rider is whatever the Furina
card that set or moved the light printed"*, a per-moving-card payload — is
**recorded as later design space and is not built**. Its problem is that it
gives one effect several owners: the rider would depend on which card last
touched the light, which is a fact the board does not show at the moment the
Companion is played, and the slice would then be grading a rider-selection
minigame instead of the lane choice.

**Alternatives:**
(a) the Featured Companion is played **twice** — explicitly banned by §3.1
("may not duplicate a card");
(b) the Featured Companion costs 1 less — banned by §3.1;
(c) per-moving-card rider payloads — the deferred design space above; not the
first prototype, by the return.

**Why these two outcomes are "meaningfully different"** (GPT's test): the
Salon-Featured outcome is a *magnitude* change to a body already on the
board and already paid for; the Guest-Featured outcome is a *cadence and
application* change to a body you are choosing to play this turn. One
rewards having built a stage; the other rewards holding a card. They are not
two ways to produce meter.

### 4.2 Persistent Spotlight

**Combat-start choice.** The lane is chosen once, at combat start, through
machinery that already ships on both sides:

- **Mod (C#):** `CardSelectCmd.FromChooseACardScreen`, which
  `EtherealSpotlight.OnPlay` already drives
  (`klee-mod/KleeCode/Cards/Furina/SpotlightCards.cs:52-70`), with the two
  option models `CenterStageOption` / `GuestCastOption` (`:77`, `:109`)
  re-labelled to the two lanes. Note the comment at `:44-51`: the options
  must be combat-scoped owned instances created through
  `combatState.CreateCard`, or the screen softlocks. That trap is already
  documented and already solved; E4 must not re-derive it.
- **Sim (tier0):** the modal chooser seam, `_chosen_mode` at
  `tier0/engine/effects.py:2571`, the surface R192's Deep Breath prototype
  established and R194 ruled. It is **the only shipped modal card**
  (`effects.py:2558-2563`) and the module says so as a discipline, not an
  accident. E4 is the second use, and the reason it qualifies is the reason
  named there: the pilot must be able to distinguish the modes. Under E4 it
  can — the two Featured outcomes are different ops, not different sizes of
  one op.

Two consequences worth stating out loud. First, the seam's switch is a
`POLICY_VERSION` event when flipped on (`effects.py:2580-2591`), so a lane
choice the pilot actually *makes* renumbers every tier0.5 arm; behind the
E4 flag it stays at the staged fixed index, which is index 0 and is the
degenerate case of the policy. Second, the combat-start choice is a screen
before the first draw, which is a place no Furina screen exists today; it is
the one genuinely new UI surface in the whole architecture.

**Encore-priced moves.** Moving the light mid-combat is a card, and the price
rides the field that already exists: `encore_cost`, a **playability gate,
never an overdraw** — `tier0/engine/combat.py:168` refuses the play when
`state.player.encore < card.encore_cost`, and `combat.py:315-320` spends it
on resolution. `tools/gen_prototype_cards.py`'s field whitelist already
knows `encore_cost` is load-bearing and not an effect
(`docs/current/OPERATIONS.md`: "load-bearing: `encore_cost` changes
playability without being an effect"). So an Encore-priced Spotlight move is
`{op: spotlight_designate, ...}` on a card carrying `encore_cost: N` — no
new engine primitive at all.

**Click-on-rail, and what it would need.** Letting the player re-aim the
light by clicking the stage rail is the interaction [USER]'s "status bar"
language points at, and it is an **unverified interaction**: nothing in the
mod today accepts a click on a VFX node as a game action. It would need
(i) a hit-testable node on the rail — `SalonVisualsBridge` builds
`Node2D` displays with `RemoteTransform2D` tracking, not input-consuming
controls; (ii) a command path from that click into `SpotlightSystem.Designate`
that survives co-op ownership checks; (iii) an answer to how the blind-play
harness drives it, since `understudy/blindplay` resolves **printed names**
only and a rail click has no printed name. That third one is not a nicety:
an interaction the harness cannot express cannot be graded by the funnel, and
would silently exempt itself from the process R213 built. **Attempt it
second**, after the card-priced move has a verdict.

### 4.3 Fanfare becomes the Burst meter

**One cause, two payments per turn.**

| what pays | fires | rate | bound |
|---|---|---|---|
| a successful Featured performance | on the `performance` event when `lane == spotlight` and the performance actually resolves | **2** | the turn's **natural** Featured performance: once |
| a successful Featured performance **commanded by Encore** | the same event, on a performance a Furina card paid Encore to cause | **2** | the turn's **commanded** Featured performance: once |

Both rows are the **same event**. The table has two rows because the ceiling
is 2 + 2, not because there are two sources: the second row is what the first
row looks like when the player paid for it. The rate is **2**, lifted off the
shipped face `FANFARE_PER_SPOTLIGHT_CARD = 2` (`tier0/constants.py:199`) —
the existing "a directed play pays the meter" rate. No breakpoint is
invented.

**Encore never pays the counter directly.** A Furina card that spends Encore
to command a performance pays the meter *through the performance it caused*.
If the command finds no eligible performer — an empty stage, no Companion in
hand, the once-per-turn commanded bound already consumed — **it pays
nothing**, and that is the intended feel: the Encore is spent on the cast,
not on the gauge. `encore_cost` on a card that does not command a performance
(a lane move, for instance) mints nothing at all. Neither does
`spend_encore_or_hp`'s overdraw half, nor Salon upkeep, which is automatic
and therefore not direction.

This is the seat's collapse, and its whole point is legibility:

> "Only a successful Featured performance creates Fanfare. One natural and
> one Encore-commanded Featured performance may pay each turn. That preserves
> the intended 2 + 2 ceiling while giving Fanfare exactly one comprehensible
> cause. Encore creates additional Fanfare by successfully commanding another
> performance, not merely by leaving the counter."

**The meter size: `20 — declared prototype seed; cadence unresolved until
whole-fight play.`** That is the seat's label and it is the one the packet
carries. The arithmetic below is real and is kept, but it computes a
**ceiling, not expected income**, and the return says why that matters:

> "Twenty is a prototype seed, not a derived shipping value. The arithmetic
> uses the hard ceiling of both legs firing every turn. … The staged boards
> can test the Burst decision by starting near full. Only complete MCP
> combats can determine whether it arrives every five turns or every ten."

So 20 is **not** claimed as a derived-under-R212 shipping constant. It is the
seed a prototype starts from, and the number that replaces it — if one does —
comes out of whole-fight play, not out of this section. Here is the
arithmetic that produced the seed.

*Step 1 — what cadence ships today.* Income per player turn under today's
constants, for a mid-fight Furina with two members on stage:

| source | arithmetic | per turn |
|---|---|---|
| Salon ticks | `SALON_TICK_BURST` 2 × 2 members | 4 |
| Skill tags | `BURST_PER_SKILL_TAG` 5 × ~1 skill-tagged play | 5 |
| Encore spend | `BURST_PER_ENCORE_SPENT` 1 × upkeep `SALON_TICK_ENCORE_COST` 1 × 2 members | 2 |
| Reactions | `BURST_PER_REACTION` 5, intermittent | 0 (floor) – 2.5 (one every other turn) |
| **total** | | **11 – 13.5** |

Against `burst_max: 70` (`tier0/content/characters/furina.yaml:14`) that is
**70 / 11 ≈ 6.4 turns** at the floor and **70 / 13.5 ≈ 5.2 turns** with
occasional reactions. Call the shipped cadence **one Burst per ≈5–6 player
turns**. (Assumption named: two members on stage. R177's finding — "half of
all upkeeps already arrive unable to fund a member" — pushes the true
Salon-tick term *below* 4, so this estimate is if anything fast, and the
error direction is stated below.)

*Step 2 — what the new economy pays at its CEILING.* One cause, two payments
per turn, each paying 2: **4 per turn**, and that is the **hard ceiling** —
what a player collects on a turn where the natural Featured performance
resolves *and* a second one is successfully commanded. It is not expected
income. A turn with no eligible performer, no Companion in hand, or no Encore
to spend pays 2, or 0.

*Step 3 — the meter that preserves the cadence at that ceiling.* 4 per turn ×
5 turns = **20**.

*Step 4 — the sanity check against precedent.* **20 is Kokomi's shipped
meter** (`tier0/content/characters/kokomi.yaml:25`, `burst_max: 20`). So the
seed is a size the roster already ships and the C# already renders, not a new
breakpoint. Klee ships 40 (`klee.yaml:11`) and Furina 70; 20 is the low end
of the existing range, which is correct for a meter with one bounded cause
instead of four unbounded legs.

**PROTOTYPE SEED: `burst_max: 70 → 20` for Furina, behind
`FURINA_E4_METER`, one constant, archive scope = every Furina Burst number.**
*Error direction, one-way:* if 20 is wrong it is wrong **slow** — a player
landing only the natural performance fills in 10 turns rather than 5, which
under-delivers the Grand Performance rather than over-delivering it. A meter
that arrives too rarely reads as "the finale never came" in a blind-play
record and is caught by the funnel; a meter that arrives too often reads as
"nothing else mattered" and would corrupt the slice's own question. The safe
error is the one the instrument sees.

**Cadence is a whole-fight question and this packet does not answer it.**
Staged boards cannot: a staged turn has no turn one to have started from, so
"every five turns or every ten" is unanswerable on the surface step 5
measures. What the staged boards *can* do — and what they are for — is start
the meter **near full** and test the **Burst decision** itself: is spending
the Grand Performance round the interesting call, and against what. The
arrival rate is graded by the mandatory whole-fight gate at §6.2, and 20
moves or stays on that evidence.

**Cast empties it.** Unchanged: `tier0/engine/combat.py:380`,
`p.burst_energy = 0`, and the LAW clause at 176-178. Overflow lost at cast,
re-granted on refill, carries Retain. E4 changes what fills the meter and
nothing about what emptying means.

**Grand Performance — the seat's exact wording (P5 option 3), adopted:**

> **"Gain one Featured entitlement for each lane; each is consumed by the
> next eligible performance and expires at the end of Furina's next turn."**

Two entitlements, one per lane. Each is spent by the **next eligible
performance in its lane** — the Salon entitlement by the front member's next
act, the Guest entitlement by the next Companion played — and whatever is
unspent **expires at the end of Furina's next turn**.

**Why the timing had to be made exact.** The packet's earlier wording was
"both lanes are Featured for one round", and the return names the defect:
"'One round' is ambiguous because Salon acts before Furina can cast the
Burst." Salon members resolve at the **start** of Furina's turn, so a
Burst cast on the turn it fills arrives after the Salon lane has already
acted; "for one round" leaves it undecidable whether that lane's round was
the one just spent or the one to come. An **entitlement consumed by the next
eligible performance** has no such gap: the Salon half is simply held until
the members next act, which is the following turn's start, and both halves
expire together at the end of that turn.

The both-halves-live *state* already exists in shipped C# — The Curtain Never
Falls prints it (`UpgradedStarterRelics.cs:399-405`): "Center Stage and Guest
Cast are both always active." E4 takes that state and makes it the Burst's
payload: two entitlements, on demand, instead of permanently and for free.

**What The Curtain Never Falls becomes — retired under the flag (P3, the
seat's pick 4).** Under E4 its current text *is* the Grand Performance,
permanently, which is not a relic upgrade — it is a deletion of the
character's finale. The seat's pick, and the reason:

> "P3: 4 — retire the upgraded relic's current effect and author its
> replacement after the base loop passes. 'Opening Grand Performance' may
> waste the Salon half before a member is deployed."

That objection kills the packet's own earlier read (option 2, "each combat
opens with one round of Grand Performance") on the same timing fact the
entitlement wording fixes: at combat start the stage is **empty**, so the
Salon entitlement has no eligible performance to be consumed by and expires
having done nothing. So: **behind `FURINA_E4_METER` the relic's current
effect is retired, and its replacement is authored after the base loop
passes** — not in this packet, and not before there is a loop to build a
relic on top of. [USER] countersigns the retirement; the replacement is a
later authoring step with its own pick list.

**What happens to the ~31 Fanfare-reading cards.** See §7, and the triage plan at §7.1.

### 4.4 Encore display

**Count, not a fill bar** — Encore is uncapped (`tier0/constants.py:165`) so
a bar has no denominator, and the shipped runway exists precisely because
someone already discovered that (`SalonVisualsBridge.cs:572-578`: "the old
denominator was a display-only 20 -- a lie twice over").

**The shape:** an integer beside HP, with **pips** for the low counts where
"how many more upkeeps can I pay" is actually a decision — one pip per point
up to a small ceiling, then the bare number. This is the Regent's-stars
reading of [USER]'s note: stars are a *count* of discrete things, not a
fraction of a maximum.

**The slot.** The centered-overhead creature-space slot is the
cross-character **Burst** indicator by LAW ("gauge skins are unique per
character"), and under E4 the merged meter is what renders there — so
Encore cannot go overhead. It goes on the **HP row**, beside the HP number
and Block shield, which is where the player already looks for "what stands
between me and damage" and is exactly what Encore is
(`resources.absorb_into_encore`, after Block and before HP, LAW:192-194).

**The runway becomes a tooltip.** The arithmetic
`SalonVisualsBridge.RefreshRibbon` already computes —
`segmentCost = members × TickEncoreCost`, `turns = encore / segmentCost`
(`:589-595`) — is the right *answer* in the wrong *place*: it is a
second-order derived quantity displayed as the primary one. It moves to the
hover tooltip on the Encore count: "N turns of upkeep at the current stage."

**The ribbon retires as a primary display** — the seat's P4 pick 2: "count
and pips beside HP; runway in the tooltip; retire the second primary
display." Two primary displays of one resource is [USER]'s legibility
complaint in miniature, and the runway is the derived half, so it is the half
that becomes a hover. This is eyes-on taste and therefore still [USER]'s to
countersign (§9); it is recorded here as the shape the slice builds unless
[USER] says otherwise.

---

## 5. Starter kit

### 5.1 What ships today

`tier0/content/characters/furina.yaml:49-59` — ten cards:

| × | id | face | source |
|---|---|---|---|
| 3 | `soloists_solicitation` | Attack, 1, 6 damage | `docs/furina-cards.yaml:56` |
| 3 | `stage_presence` | Skill, 1, 6 Block | `:66` |
| 1 | `regal_bearing` | Skill, 1, 3 Block + Weak 1 | `:69` |
| 1 | `aria_of_recompense` | Skill, 1, gain 5 Encore | `:72` |
| 1 | `salon_debut` | Skill, 1, deploy a **random** member | `:83` |
| 1 | `an_invitation` | Skill, 0, Exhaust, generate a common Companion | `:361` |

Plus the relic `ethereal_spotlight` (`furina.yaml:19-20`), which adds the
Ethereal selector card to hand each turn, and the kit Burst
`let_the_people_rejoice` (`furina.yaml:17`).

Two of the ten (`soloists_solicitation` ×1, `stage_presence` ×1) are
replaced at run start by the randomized Fontaine ensemble
(`furina.yaml:64-66`).

### 5.2 Delta to GPT's five-item list, card by card

| GPT's item | today | delta |
|---|---|---|
| **one deterministic Salon deployment** | `salon_debut`, `member: random` (`docs/furina-cards.yaml:84`) | **CHANGE.** The randomness was ruled in — A11, 2026-07-28, to separate the starter from the Common Surintendante Chevalmarin (`:85-90`). E4 needs it deterministic, because "which member is Featured" is the Salon lane's whole decision and you cannot plan around a coin flip. The separation A11 was protecting is recovered differently: the Common becomes the *second* copy of a named member, not the only way to name one. |
| **one generated Companion** | `an_invitation` — 0-cost, Exhaust, generates a common from the pool + Guest Star set (`:361-363`) | **NONE.** It is already exactly this card, and its four Guest Star guardrails (LAW:215-217) are untouched by E4. |
| **one Encore generator** | `aria_of_recompense` — gain 5 Encore (`:72-73`) | **NONE** on the face. One note: R130 vetoed giving this card a payoff rider (`:75-78`), and E4 does not reopen that — the generator stays a pure generator. |
| **one cheap Encore spend** | **ABSENT.** No starter card spends Encore. The whole spender family is `F3`, deferred to `Win11`, which is FROZEN by R213 and gated behind the pilot's Encore opportunity-cost repair (`STATE.md`) | **ADD TWO, and they must be separate cards** — see below. Without a spend, the Encore half of GPT's decisive question ("did spending Encore feel meaningfully different from keeping it for defense?") has no card to ask it with. The seat's P6: "1 — prototype row. The starter slice also needs access to a distinct Encore-priced lane move; don't bundle movement and commanding into the same test card." |
| **relic supplies the persistent Spotlight interface** | `ethereal_spotlight` adds a card to hand each turn (`furina.yaml:19-20`; the card at `SpotlightCards.cs:16-73`) | **CHANGE.** The relic keeps its name, art and slot and changes its job: at combat start it opens the lane choice, thereafter it *is* the lane display, and it is the **single owner of the Guest lane's fixed Hydro rider** (§4.1). It grants no card. This is the direct answer to "Spotlight should not require playing a card every single turn." |

### 5.2a The two Encore spenders, and why they cannot be one card

The seat returned the single-spender plan, and the reason is attribution, not
economy: bundling movement and commanding into one test card makes every
graded line ambiguous about which verb the player was buying. So the slice
stages **two** prototype rows under `FURINA_E4_STARTER`, both `authored_by:
[claude, gpt]` (the split is GPT's, and a mode is a contribution — see §8):

| row | verb | shape | what it isolates |
|---|---|---|---|
| **the lane move** | move the Spotlight | `{op: spotlight_designate, ...}` + `encore_cost: N`. **It prints no rider** (§4.1) and commands no performance, so it mints **no** Fanfare | "was aiming the light worth Encore this turn?" — Slot 1's verb |
| **the command** | cause an extra Featured performance | Encore-priced; on resolution the front member acts (and rotates) or the next Companion is Featured, per the live lane | "was commanding a second performance worth Encore against holding it as buffer?" — Slot 2's verb |

Both are cheap, both are prototype rows, and neither enters a pool. The
command card is the one that can pay the meter, and only by succeeding: on an
empty stage with no Companion in hand it commands nothing and mints nothing
(§4.3). That failure mode is deliberate and is itself a graded reading — a
command the player cannot aim is a card that teaches them to build a stage
first.

### 5.3 What that leaves

Eight of the ten cards are unchanged. The deltas are: one card's parameter
(`salon_debut`, random → named), **two** cards added (a lane move and a
command, §5.2a), one relic's mechanism (card-per-turn → persistent interface
carrying the fixed Guest Hydro rider), and the upgraded relic's current
effect retired under the flag (§4.3). That is a smaller starter change than
the architecture might suggest, and it is deliberate — the starter is where
D2's "reachable early and reliably" requirement bites, and eight familiar
cards is what keeps the slice about the new sentence rather than about a new
deck.

---

## 6. The testable slice

GPT's six-step order, with flags, tests and the pre-registered question.

### 6.1 Flags

The repo's prototype-flag convention is a **compile-time MSBuild property
plus an id prefix**: `dotnet build klee-mod/KleeCode -p:PrototypeCards=true`,
every row's id starting `proto_`, and the classes not compiled at all in a
release build (`docs/current/OPERATIONS.md`, "Prototype surface (`EB-147`)").
E4 follows it and adds one master switch and five sub-flags underneath:

| flag | step | scope | default |
|---|---|---|---|
| `FURINA_E4` | master | C# compile symbol, defined only under `-p:PrototypeCards=true`; sim-side module constant in the E4 module, not `constants.py` | OFF |
| `FURINA_E4_HOOK` | 2 | the `performance` event fires; nothing reads it | OFF |
| `FURINA_E4_LANE` | 3 | persistent lane, combat-start choice, `encore_cost` moves | OFF |
| `FURINA_E4_METER` | 4 | Fanfare and Burst are one meter; Grand Performance | OFF |
| `FURINA_E4_STARTER` | 5 | the §5.2 starter deltas | OFF |
| `FURINA_E4_MIGRATE` | 6 | the ~31 readers move to the merged meter | OFF |

**An honesty note that has to be in the packet, not discovered later.**
Staging a prototype *card row* bumps no stamp, because prototype rows never
enter tier0's card index (`OPERATIONS.md`). Steps 2–4 are **engine**
behaviour, not rows. A default-off flag in the engine bumps no stamp *while
it is off*; **turning any of them on is a `CONSTANTS_VERSION` event** and, for
step 3's modal seam, a `POLICY_VERSION` event as well
(`tier0/engine/effects.py:2580-2591`). Every measured number taken with a
flag on is taken in a new world and is not comparable to the standing
baseline. That is not a reason to avoid the flags; it is a reason to say so
before the first table is quoted.

### 6.2 The six steps

**Step 1 — write E4 and its doctrine gate.** *This packet, plus §10.* Test:
the seat answers FOLLOWS or REQUIRES_MODIFICATION per item and names the
clause. Proves: the architecture does not contradict the charter before
anything is built.

**Step 2 — the smallest shared performance hook.** `FURINA_E4_HOOK`. One
Salon action and one Companion play emit the same `performance` event.
Test: a scenario at the real game (the `EB-146` harness) where one Salon tick
and one Companion play both appear in the event log with the correct `lane`;
plus a tier0 unit test that the event fires exactly once per member act and
exactly once per Companion resolution. **Proves:** the two lanes are the same
kind of thing. Nothing is Featured yet and no number moves — this step is
deliberately invisible in play, which is what makes it safe to land first.

**Step 3 — persistent Spotlight.** `FURINA_E4_LANE`. Combat-start choice
through `FromChooseACardScreen`; the designation persists; a card with
`encore_cost: N` moves it. Test: a staged turn where the lane is set at
combat start and a second staged turn where moving it is affordable but
contested. **Proves:** [USER]'s "should not require playing a card every
single turn" is satisfied, and that a move is a real decision rather than a
formality. Falsified if the Encore price is never worth paying in any staged
board, which would mean the lane choice is de facto permanent.

**Step 4 — merge Fanfare with Burst; add Grand Performance.**
`FURINA_E4_METER`. One cause paying 2, at most twice a turn (natural +
commanded); meter seeded at 20; cast empties; Burst = one Featured
entitlement per lane, each consumed by the next eligible performance and
expiring at the end of Furina's next turn. The Curtain Never Falls' current
effect is retired under this flag. Test: a scenario that fills the meter from
successful Featured performances only and confirms **no other event moves
it** (the negative test is the important one — HP loss, upkeep, absorption
and a bare Encore spend that commands nothing must all read zero); a staged
turn on a full meter where spending the entitlements is the decision.
**Proves:** the finale is a state the player steered into, and the meter is
evidence of direction rather than of activity.

**Step 5 — the starter-deck boards.** `FURINA_E4_STARTER`. The §5.2 deltas
staged as prototype rows. Test: the funnel — blind staged turns, the
four-question form, closeness per R213 F, two graders, replay of every graded
line. **Proves:** the starter teaches the sentence. This is the first step
whose output is a grade rather than an assertion.

**Step 5b — WHOLE-FIGHT BLIND PLAY. A MANDATORY GATE, not a note.** The
seat's return: *"elevate the packet's whole-fight blind-play note into a
mandatory gate before step 6."* Nothing in step 6 may begin until this has
run and been graded.

It is the gate the house already has, applied here rather than invented:
whole-fight blind play on a dev build is the **automatic** gate after an arm
ADVANCES (`OPERATIONS.md`, "After ADVANCE" — "It is the next gate, not a
pick, and nobody is asked for a form to start it"), and the door is
`EB-188`'s `embark --arm`, which grants a named prototype row into the
starting deck of a `+proto` build. E4 does not get an exemption from it and
does not need a new mechanism for it.

**What it inherits from `EXPERIMENTS.md`, named explicitly:**

- **Pre-registration from design intent.** The whole-fight questions are
  written and committed **before the first run**, never revised against the
  run that grades them.
- **The slate is DRAFTED by Claude (R212)**, committed as its own commit
  labelled DRAFTED before any seed run; [USER] countersigns in batch or
  vetoes within five days. Countersign once — a moved world means re-drafting
  the affected slots and disclosing the diff, never re-signing.
- **The grade goes in blind**, and the tester's model family is not the
  author's (R217 C).
- **Decision linkage (R206):** every slot names the decision its outcomes
  would change, and if every plausible outcome leaves the next action
  unchanged the run is not made.
- **Stamp law:** the run is taken with `FURINA_E4_METER` on, which is a
  `CONSTANTS_VERSION` world; the record carries that cell's `RT/D/P/C` and
  is never compared unlabeled to a pre-flag number (§6.1).

**The two things only this gate can answer**, both of which the staged boards
are structurally unable to reach:

1. **The meter's cadence.** 20 is a declared prototype seed and the return is
   explicit that "only complete MCP combats can determine whether it arrives
   every five turns or every ten." The staged boards test the Burst
   *decision* by starting near full; they cannot test *arrival*.
2. **Whether a lane chosen at combat start still felt planned at turn six.**
   A staged turn has no turn one to have been planned from.

**Falsification returns real things, not notes.** A meter that arrives once
per fight returns §4.3's seed; a lane nobody re-aimed after turn two returns
step 3's combat-start choice; a Featured act that reads as a repeatable
engine returns §4.1's unpriced bodies to pricing.

**Step 6 — migrate nothing else until the whole-fight gate and independent
playthroughs answer the D2 question.** `FURINA_E4_MIGRATE`, and it stays off
until then. Its content is the P7 triage plan at §7.1, which is executed
**only after** step 5b reports. Test: none yet, by construction.

### 6.3 The decisive question, as a prediction slate

Per `EXPERIMENTS.md`: **DRAFTED by Claude** from written design intent,
committed as its own commit labelled DRAFTED **before any run**, [USER]
countersigns in batch or vetoes within five days, grade goes in blind, and
every slot names the decision its outcomes would change.

The question, GPT's, unaltered: *"Did the player plan who would receive
Spotlight, and did spending Encore on that performance feel meaningfully
different from keeping it for defense?"* It splits into two gradeable slots.

**Slot 1 — direction.** *Predicted:* on the step-5 boards, a majority of
blind forms name the lane designation as a reason for the line taken, in
q1's own prose, unprompted. *Falsified if:* no form mentions the lane; **or**
forms mention it only as forced ("there was only one Companion"), which is
the §2.2 collapse rule surviving the redesign under a new name.
*Decision it changes:* falsification returns step 3 — the lane goes back to
being a per-turn card, or the combat-start choice acquires a mid-combat
contest it currently lacks.

**Slot 2 — the Encore tension.** *Predicted:* on a matched pair of boards
differing only in whether Encore is best spent commanding a Featured
performance or held as buffer, `closeness` SURVIVES on both — the gap between
the top two lines on the pilot's own EV surface does not show one line
overwhelmingly dominating (R213 F). *Falsified if:* closeness fails on
either board; **or** both graders take the same line on both boards, which
would mean the two uses of Encore are not actually in tension.
*Decision it changes:* falsification returns §4.3's **commanded** payment —
either commanding a second Featured performance is not carrying its half of
the meter, or one of §5.2a's two spenders is priced wrong. The two-card split
is what makes that attributable: a failure on the move card and a failure on
the command card return different things.

**Who grades:** the Codex seat, blind, per `EB-149`, two graders per turn.
The reason it is the seat and not fresh Claude is §8.

**Registered blind spot.** Every board in step 5 is staged, and a staged turn
cannot show whether a lane chosen at *combat start* still felt planned at
turn six. That is a whole-fight question, and it belongs to **step 5b, the
mandatory gate** — whole-fight blind play on a dev build, `EB-188`'s door.
The slate does not pretend to answer it, and step 6 does not begin until 5b
has.

---

## 7. Blast radius

**Tests.** 37 test files reference salon, 32 fanfare, 18 spotlight, across a
190-file suite; 16 / 10 / 6 of those pin the plan name as a quoted string
literal (`review/active/eb81-furina-remedy-options-2026-08-12.md:227-229`).
E4 touches all three names. The string-literal subset is the expensive half:
a rename or an archetype fold breaks them mechanically, not semantically.
One test dies outright rather than being updated —
`test_every_point_past_block_prints_exactly_one_fanfare`
(`tier0/constants.py:196`), which asserts the invariant §3 retires.

**The re-baseline.** Any landed step is the **whole twelve-arm table**, never
the three Furina rows — "a bump makes every arm of the table discontinuous —
for every character, not just Furina" (`…:225-227`). Steps 2–4 each carry a
`CONSTANTS_VERSION` bump when their flag flips on, and step 3 carries a
`POLICY_VERSION` bump as well if the modal seam is switched on. Under R207
that is one window per step **where attribution matters**, and here it does:
the whole question is which of the four mechanics the merge fixed.

**The ~31 Fanfare-reading cards.** 31 cards touch fanfare, 14 exclusively, 6
of those 14 at rare (`…:120`, `:128-129`). Twelve are tagged
`[fanfare, generic]` and are therefore offered to salon decks too (`:130-131`).

**Prototype default: behind the flag they read the merged meter unchanged.**
The reader grammar does not change — `bonus_formula: 1_per_4_fanfare` and
`if: fanfare_at_least_N` (`docs/furina-cards.yaml:20-21`) both take a
non-negative integer off the player, and the merged meter is a non-negative
integer on the player. Every reader compiles and every reader reads. What
changes is the **rate and the ceiling**: a meter that fills to 20 on two
bounded legs is a different distribution from one that decays 20% a turn and
caps at half of max HP, so a `fanfare_at_least_20` gate that used to open
sometimes may now open only at the finale. That is a real balance change and
it is exactly why migration is step 6 and not step 4: the readers are
*correct* under the merge and *mispriced* by it, and pricing them is a sheet
edit — a `CONSTANTS_VERSION` event under LAW's material-card-sheet-edit rule
— that should happen after the architecture has a verdict, not before.

### 7.1 The reader triage — a PLAN, executed at step 6

The packet's earlier P7 offered "migrate them as a sheet edit" as an option
and the seat returned it:

> "P7: Return and recut. The answer should not be 'reprice 31 scalar
> readers.' Triage all 31: cut scalar `per Fanfare` bodies by default,
> re-author survivors around Salon/Guest/Direct/Grand Performance, and
> convert the old Fanfare plan into the Conductor emphasis. Otherwise the
> rejected delayed-scaling archetype survives inside the new meter."

That is the reason repricing is not on the table: a scalar `per Fanfare`
body *is* the delayed-scaling archetype, and repricing it preserves it.
Under a two-payment, cast-empties meter that tops out at 20, "the longer you
wait the bigger this gets" is no longer a plan the game supports — the meter
is a countdown to a finale, not a stockpile.

**Three buckets, the seat's:**

1. **CUT** — a body whose whole payoff is a scalar `bonus_formula:
   N_per_M_fanfare` read. Cut by default; a survivor needs a reason.
2. **RE-AUTHOR** — bodies worth keeping, re-authored around the four things
   the new architecture actually has: **Salon**, **Guest**, **Direct** (an
   Encore-priced command), and **Grand Performance**.
3. **CONDUCTOR** — cards tagged `fanfare` that never read the meter. The old
   Fanfare *plan* becomes the **Conductor** emphasis, and these are its
   membership: direction, commanding and Encore management rather than
   accumulation.

**The 31 rows, with a PROVISIONAL bucket each.** Read by `grep fanfare
docs/furina-cards.yaml` with context, never by loading the sheet. The 31 are
the 30 rows carrying `fanfare` in `archetypes:` plus `let_the_people_rejoice`,
the kit Burst, whose body reads `1_per_4_fanfare` without carrying the tag.
**These buckets are Claude's and are gated by the seat at step 6** — the plan
is what is being countersigned now, not the assignments.

**CUT — 6.** Pure scalar payoffs; nothing survives the cut but the card name.

| id | rarity | body |
|---|---|---|
| `applause_line` | common | 3 damage, `1_per_4_fanfare` |
| `held_breath` | common | 4 Block, `1_per_4_fanfare` |
| `crescendo` | uncommon | 6 damage, `1_per_2_fanfare` + `grow_damage` |
| `high_tide` | rare | 15 damage, `1_per_2_fanfare` |
| `thunderous_ovation` | rare | 6 Block, `1_per_2_fanfare` |
| `the_final_verdict` | rare | 0 damage, `1_per_1_fanfare`, `crash_fanfare 30` — doubly dead: a meter of 20 cannot pay a crash of 30, and crashing a Burst meter is what casting already does |

**RE-AUTHOR — 8.** Threshold gates, meter machinery retired by §3 item 5, and
the kit Burst. Each is re-bodied on Salon / Guest / Direct / Grand
Performance; the ids and their hooks:

| id | rarity | why it survives the cut | re-author onto |
|---|---|---|---|
| `florid_cadenza` | uncommon | `fanfare_at_least_12` gate, not a slope | **Direct** — draw on a commanded performance |
| `dramatic_entrance` | uncommon | `fanfare_at_least_12` gate on a damage line; the bar was measured, not picked | **Guest** — the entrance is a Companion's |
| `universal_revelry` | rare | scalar **and** `fanfare_at_least_15`; the gate half is the keepable half | **Grand Performance** — an AoE finale reads as one |
| `flood_of_emotion` | rare | `fanfare_at_least_20` doubling — at a meter of 20 this is literally "on the finale" | **Grand Performance** |
| `unheard_confession` | rare | `gain_fanfare_floor` + `fanfare_delta_block` power; both ops retire | **Salon** or **Direct** — a Power that pays per performance, not per point |
| `the_sea_is_my_stage` | rare | `gain_fanfare_floor 15`; the op retires | **Grand Performance** — a Focalors-tagged finale enabler |
| `rapturous_applause` | rare | `gain_fanfare_floor` + `fanfare_attack_per10` power; both ops retire | **Direct** |
| `let_the_people_rejoice` | rare (kit) | **cannot be cut** — LAW 176-178 makes the kit Burst never-draftable and mandatory | **Grand Performance** — its body becomes §4.3's two entitlements; its `1_per_4_fanfare` scalar goes, since casting empties the meter it reads |

**CONDUCTOR — 17.** No meter read at all; the tag moves and the body does
not. `aria_of_recompense` (basic), `suffering_for_art`, `lasting_impression`,
`warmup_act`, `ebb_and_flow`, `breathless`, `macaron_break` (commons),
`curtain_cue`, `dress_rehearsal`, `hearts_swelling`, `deep_breath`,
`audience_participation`, `tempo_change`, `curtain_up`,
`standing_room_only`, `crowd_work` (uncommons), `reginas_mercy` (rare).

Two notes on that bucket. It is **17 of 31**, which is the finding: most of
the "Fanfare plan" was never a meter plan — it was a tempo-and-Encore plan
wearing the meter's tag, which is why the arm measured indistinguishable from
Spotlight at `RT9/D14/P6/C8` (§2.1). And `deep_breath` is the repo's only
shipped modal card (`effects.py:2558-2563`), so it is a Conductor card by
construction: it is already the sheet's one "the player directs" body.

**Counts: 6 cut / 8 re-author / 17 Conductor = 31.** Rarity of the cut set:
2 common, 1 uncommon, 3 rare — so the cut lands hardest on rares, which is
the census's own warning (fanfare's exclusives are rare-heavy, "a change
confined to them is a change most decks never draw") pointing the other way
for once: cutting rares is cheap in draft-frequency terms and expensive in
ceiling terms, and the ceiling is what the Grand Performance is now for.

**When.** Step 6, and step 6 opens only after the mandatory whole-fight gate
at §6.2 step 5b reports. The triage is a sheet edit and therefore a
`CONSTANTS_VERSION` event under LAW's material-card-sheet-edit rule, carrying
the twelve-arm re-baseline; doing it before the architecture has a verdict
would spend that re-baseline on an architecture that might return.

**What retires with the merge.** `FANFARE_DECAY_FRACTION` and
`decay_fanfare`; `gain_fanfare_floor` / `raise_fanfare_cap` and the two
printed keywords; `FANFARE_CAP_FRACTION`; register lints `R6` and `L12`
(`tools/lint_furina_registers.py`), which exist only to police those
keywords. Nothing on any shipped sheet carries `raise_fanfare_cap` today
(`STATE.md`: "Furina's pool carries **zero** `raise_fanfare_cap` riders"), so
that half is already free.

**Co-op.** Both of Furina's ally-coupled mechanics are **retired by §3 item
6**, not deferred: partner-coupled Fanfare dies with the HP-loss leg, and
cross-player selector passing has no selector card to pass. Nothing replaces
them in E4, so after this she has no co-op-specific mechanic at all — a real
loss against the kickoff's Appendix A.4 lineage, stated plainly rather than
left to be discovered. It cannot be settled by a test either way: there is no
automated co-op backstop for anything needing a live `CombatState`
(`klee-mod/KleeTests/README.md`), so every co-op defect here would be
play-derived. Authoring a replacement is future design space and is
deliberately outside this packet.

---

## 8. Independence

The direction in §1.3 and §1.5 is **GPT-co-authored with [USER]**, and the
seat's return at §11 **authored four more revisions plus one exact card
wording**. What remains Claude's is narrower than it was before that return,
and the allocation has to follow authorship row by row, not section by
section. Under R217 C independence is by model FAMILY, author against
grader, and the roles are fixed at two: Claude authors, GPT grades and
reviews (`OPERATIONS.md`, "Doctrine seat protocol"). There is no third family
and none is being added.

**GPT-authored — a fresh Claude session, with no part in this drafting, is
the independent reader.** The seat cannot review its own text.

| row | where |
|---|---|
| the Featured act = *perform and rotate to the back*, not a `final_bow`, bodies labelled UNPRICED | §4.1 |
| the Fanfare collapse — "only a successful Featured performance creates Fanfare; one natural and one Encore-commanded may pay each turn" — and the §3 / §3.2 text that carries it | §3, §3.2, §4.3 |
| the Guest rider has one owner: a fixed relic-authored Hydro; movement cards only move | §4.1, §5.2 |
| the 20 relabelled "declared prototype seed; cadence unresolved until whole-fight play" | §4.3 |
| **the exact Grand Performance wording** — "Gain one Featured entitlement for each lane; each is consumed by the next eligible performance and expires at the end of Furina's next turn" | §4.3 |
| the split of movement from commanding into two starter cards | §5.2a |
| the three triage buckets, as buckets | §7.1 |
| whole-fight blind play elevated to a mandatory gate | §6.2 |
| the §3.1 Spotlight-is-a-designation LAW wording (from the earlier review) | §3.1 |

**Claude-authored — the Codex seat (GPT) is the independent grader**, and it
is the seat that grades the step-5 forms.

| row | where |
|---|---|
| **the one-order clarification** — a single company order, front = index 0, deploy bows the FRONT, every positional reader on that order; and the finding that this is the shipped invariant rather than a new one | §4.1 |
| the animation-contract reading: a Featured rotation is a re-render of slot indices, not a contract change | §3, §4.1 |
| **the P7 bucket assignments** — which of the 31 rows is CUT, RE-AUTHOR or CONDUCTOR, and the re-author hooks | §7.1 |
| the derivation arithmetic behind the seed, and its one-way error direction | §4.3 |
| the flag ladder and the stamp-honesty note | §6.1 |
| the prediction slate's two slots and their decision linkage | §6.3 |
| the blast-radius reading | §7 |

**Two consequences of that table.** The Grand Performance body and the
starter's card split are now GPT-authored **modes**, so the prototype rows
that carry them record `authored_by: [claude, gpt]` — a mode is a
contribution beyond a clause name (`docs/prototype-surface.yaml` header,
`EB-190`), and recording it is what lets `understudy/seat.py` refuse a seat
that would grade its own work. And the seat is **not** eligible to grade the
Fanfare-collapse rows or the entitlement wording; a fresh Claude is.

**This is a case-specific allocation, not a general rule.** "Fresh Opus
grades" is never written as the house rule and must not be inferred from
this packet: the house rule is R217 C's model-family separation, and it
happens to point at a fresh Claude here only because GPT authored the rows
in question. Where Claude authors, GPT grades — which is the ordinary case
and the case for everything the slice actually builds.

**`authored_by:` consequences.** Every prototype row this slice stages
records its families (`EB-190`, `docs/prototype-surface.yaml` header). After
the return, the §5.2a spender rows and any row carrying the Grand
Performance body are `[claude, gpt]`; rows the return did not touch stay
`[claude]`. If the seat's §10 gate returns an item and
Claude re-derives from the named clause, the row stays `[claude]` — a clause
name is explicitly not a contribution. If the seat supplies text, a number or
a mode, its remedy is **discarded** and Claude re-derives; that is the
protocol, and it is what keeps `seat grade` from refusing the turn later.

---

## 9. What returns to [USER]

The seat answered the whole pick list in its return. So this section is no
longer a pick list of Claude's — it is two things: what [USER] countersigns,
and what the seat picked while waiting for him.

### 9.1 What [USER] countersigns

**C1 — the §3 ruling text, as revised.** In particular the single Fanfare
cause ("Only a successful Featured performance creates Fanfare. One natural
and one Encore-commanded Featured performance may pay each turn."), the six
retirements including the co-op pair, and the added retirement of
`FANFARE_PER_ENCORE_SPENT` as an independent leg.

**C2 — the prospective LAW text (§3.1, §3.2), as prospective.** Not as live
law. The mechanism is at §9.2 and it is R213's, not a new device.

**C3 — the P7 triage PLAN (§7.1).** The three buckets and the rule that the
triage executes only after the whole-fight gate. Not the bucket assignments —
those are Claude's provisional read and the seat grades them at step 6.

Nothing else in this packet is [USER]'s to settle. The seat's five picks
below are recorded as the seat's read and are open to his veto, but they are
answers, not blanks.

### 9.2 P2 is answered by a mechanism, not by a choice

P2 asked whether to ratify the two LAW amendments now, hold one, or return
both. The seat's answer:

> "P2: Ratify both conceptually, but make them prospective E4 law. They
> should not replace the live shipped contract until E4 advances. If the
> choices cannot express that, return P2 for recutting."

**The repo can express exactly that, and already does — it is R213's
quarantine.** A prototype surface exists so that a rule being *tried* is not
a rule in force: `docs/prototype-surface.yaml`'s header states that a row
there "is not a card in the game: it is a question, put in front of the real
engine", that it is in no pool, no release build, no pck and no version
stamp, and that its rows **leave the surface** on ACCEPT or REJECT — accepted
rows re-authored onto the real sheet with their numbers ruled and their
stamps bumped, rejected rows deleted outright. The same quarantine covers a
prospective rule as covers a prospective card:

1. **Now:** §3.1 and §3.2's replacement text lives in **this packet** and, for
   the rows that print it, in the prototype surface's header prose. It
   governs the flagged E4 build and nothing else. `docs/current/LAW.md` is
   **not edited**.
2. **The live contract stands untouched** until then — "Spotlight/empowerment
   boosts numbers only" and the unamended Burst kit-scope clause remain the
   law of the shipped game, and a shipped-build question is answered from
   them.
3. **On ADVANCE** — the pair read ADVANCES, and then the mandatory
   whole-fight gate (§6.2 step 5b) reports — the two amendments are applied
   to `LAW.md` in the same commit that re-authors the accepted rows onto the
   real sheet and bumps the stamps. That commit is the amendment.
4. **On REJECT** the prospective text is deleted with the rows, and `LAW.md`
   was never touched.

So P2 is **not a pick**. It is satisfied by the mechanism that already
governs everything else in this slice, and the only thing left for [USER] is
C2: countersigning the prospective text so the flagged build has something to
be built against. It stays in this section for that reason and no other.

One thing this mechanism does *not* dissolve: **a LAW amendment is [USER]'s
under the delegation ladder**, and it stays his — the quarantine changes
*when* he signs the amendment into LAW, not *whether*. Step 3 above is the
signature, and it is owed then.

### 9.3 The seat's picks — the seat's read, awaiting [USER]

Recorded verbatim where the seat gave a reason. These are **not rulings** and
none of them is Claude's.

| pick | the seat's answer | the seat's reason, verbatim where given |
|---|---|---|
| **P1** — when the queued slice opens | **1** — E4 step 2 opens when Klee round 3 reports, whatever it says | "P1: 1 — open after Klee round 3." |
| **P3** — what The Curtain Never Falls becomes | **4** — retire its current effect; author the replacement after the base loop passes | "retire the upgraded relic's current effect and author its replacement after the base loop passes. 'Opening Grand Performance' may waste the Salon half before a member is deployed." |
| **P4** — the Encore display | **2** — count + pips beside HP, runway in the tooltip, the ribbon retired as a primary display | "count and pips beside HP; runway in the tooltip; retire the second primary display." |
| **P5** — Grand Performance's body | **3** — the exact entitlement wording, now in §4.3 | "make the timing exact: 'Gain one Featured entitlement for each lane; each is consumed by the next eligible performance and expires at the end of Furina's next turn.' 'One round' is ambiguous because Salon acts before Furina can cast the Burst." |
| **P6** — the starter's Encore spend | **1** — stage it as a prototype row, **and** stage a distinct Encore-priced lane move beside it | "prototype row. The starter slice also needs access to a distinct Encore-priced lane move; don't bundle movement and commanding into the same test card." |
| **P7** — the ~31 Fanfare readers | **Return and recut** — now the triage plan at §7.1 | "The answer should not be 'reprice 31 scalar readers.' … Otherwise the rejected delayed-scaling archetype survives inside the new meter." |

**P4 and P6 carry a [USER] question inside them even so.** P4 is eyes-on
taste, which the ladder reserves to [USER] whoever proposed it. P6's "does a
prototype row route around the `Win11` freeze" is a governance question with
a precedent attached, and the seat's pick answers the design half, not the
governance half. Both are recorded above as the seat's read; both are open.

**Not picks, recorded so they are not mistaken for picks.** The meter size 20
is a **declared prototype seed** (§4.3), not a derived shipping value, and its
cadence is unresolved until whole-fight play. The rate of 2 is lifted off
`FANFARE_PER_SPOTLIGHT_CARD` by the shipped-face rule and invents no
breakpoint. The flag ladder at §6.1 follows the existing prototype convention.
The one-order clarification at §4.1 is a **documentation fix**, not a rule
change: it states the invariant `_op_salon_rotate` already implements.

---

## 10. The doctrine gate

Run 2026-08-29 against this packet as committed at `b25485d`, before the
edits recorded in the last column. Seat: OpenAI Codex CLI `0.150.1`, model
`gpt-5.6-sol`, through `understudy/seat.py`'s repo-visible review door.
Verbatim output and full provenance:
`review/qa/furina-e4-doctrine-review-codex-gpt-5.6-sol.md`; the prompt is
`review/qa/furina-e4-review-prompt.txt`, sha256
`5538382fc7142598e6715618c53f8844b9ba8e3461557cf28e777ddd2c661c71`.

The seat was told, in the prompt and in those words, that the direction is
settled and co-authored by its own family, and that it was gating §3's ruling
text, §4's specifics, the LAW wording and §9's picks against D2/D4, LAW and
the kickoff — not re-litigating the direction. It stayed inside that scope,
named clauses, and **volunteered no remedy**, so nothing is marked DISCARDED
and its family stays eligible to grade these rows later. Every quotation
below is the seat's, unedited.

| item | verdict | clause cited (verbatim) | why (verbatim) | what Claude re-derived |
|---|---|---|---|---|
| **G1** — the E4 ruling text and its retirements (§3) | `REQUIRES_MODIFICATION` | "Furina kickoff §3.1, "In co-op, the selector may instead be passed to a teammate"; and §4, "partner HP/Encore flux counts toward Fanfare."" | "E4 removes the selector and the flux-based Fanfare legs while leaving these binding co-op provisions neither operative nor explicitly retired." | **Fixed.** §3 gains a sixth retirement naming both co-op provisions, retiring them explicitly rather than letting them lapse, and stating the cost: Furina is left with no ally-coupled mechanic. §7's co-op paragraph is rewritten from "flagged, not resolved" to point at that retirement. |
| **G2** — the two LAW amendment wordings (§3.1, §3.2) | `REQUIRES_MODIFICATION` | "D4 — "Text that cannot bind in the shipped world… [is a defect]."" | "The Burst-scope amendment says only a Featured performance generates Furina's meter, while the ruling and architecture also make deliberate Encore spending an independent generation leg." | **Fixed, and it was a real contradiction.** §3.2's replacement text said "only a Featured performance" while §3's prose and §4.3's table both carry two legs. The amendment now reads "generated by exactly two things — a Featured performance resolving, and Encore deliberately spent through a Furina card that commands a performance — each bounded to at most once per turn." §3.1 drew no verdict and is unchanged. **That wording was in turn superseded** by the seat's later implementation return, which collapsed the two legs into one cause and made the sentence literally true — §11.2, point 2. |
| **G3** — the §4 architecture specifics | `REQUIRES_MODIFICATION` | "D4 — "At the decision point the player can perceive and forecast the consequences that matter."" | "The Salon lane designates a lane whose hook fires for each member act, but the Featured outcome refers to a particular member without defining how that member is selected." | **Fixed.** §4.1 gains "Which body is Featured, exactly": Salon lane → the FRONT member, `p.salon[0]`, the FIFO head that `salon_bow`, full-stage displacement and the `leftmost_salon_member_*` predicates already read (`tier0/engine/effects.py:1928-1949`); Guest Cast lane → the first Companion played this turn. The seat did not rule the two Featured outcomes insufficiently different, which was the other question put to it inside G3. |
| **G4** — the §9 pick list | `REQUIRES_MODIFICATION` | "R213 sequencing — "Then Klee's reaction slice, then Furina after the Companion result. Not three explorations in parallel."" | "P1 presents parallel Furina implementation as an unsettled choice even though the governing sequence already excludes it." | **Fixed.** P1's option (1) offered a parallel start R213 forecloses, which is a settled thing dressed as a pick — exactly the failure G4 was scoped to catch. P1 is re-cut as *when the queued slice opens*, with the queue stated as not-a-pick and the three options now naming which report opens step 2; the "start now" option survives only as an explicit, labelled amendment to R213's sequence. |
| **OVERALL** | `REQUIRES_MODIFICATION` | "D4 — "Text that cannot bind in the shipped world… [is a defect]."" | "The unresolved rule contradiction and undefined Featured-member selection prevent the proposed architecture from binding unambiguously." | Both named causes are the G2 and G3 rows above, and both are fixed. The packet has **not** been re-gated; a second gate on the revised text is available and is not owed by the protocol, which asks the seat to name clauses and Claude to re-derive from them. |

**What the gate did not touch, and what that does and does not mean.** The
seat ruled on four items and returned no verdict on the meter size, the
payment rates, the flag ladder, the prediction slate's shape, or the
blast-radius reading. Under this protocol silence is not endorsement — the
seat answers what it is asked and stops — so those remained Claude-authored
and ungated at the close of this gate.

**Three of them did not stay that way.** The seat's later implementation
return (§11) reached the meter size, the two legs and the Salon Featured
body directly, and authored the revisions itself. §8's allocation is
therefore **re-cut against §11**, row by row rather than section by section:
what the return authored is read by a fresh Claude, and what Claude authored
— the one-order clarification, the P7 bucket assignments, the seed's
arithmetic and the slate — is graded by the seat.

---

## 11. The doctrine seat's return, and what it changed

§10 records the **doctrine gate** the seat ran on the packet as committed at
`b25485d`. This section records something later and different: after [USER]
forwarded the revised packet, the seat **returned it once more** — not on
doctrine, but on implementation, before anything is built. Its words are
below, verbatim, and they bind this revision.

### 11.1 The verdict

> "The E4 architecture is good enough to countersign. I would return the
> packet for one narrow implementation revision before building, chiefly
> because the proposed Salon payoff silently removes its existing price."

**Claude accepts all four points and all seven picks as the seat's read.**
[USER] has **not** countersigned. The §3 ruling text, P2's prospective-law
shape and P7's recut are his; the picks at §9.3 are recorded as the seat's,
not as rulings.

**The packet is not re-gated for this revision, and a second pass is not
owed** — the seat authored these revisions itself. What the revisions *do*
owe is a reallocation of who reads them independently, and §8 pays it: the
four points and the exact P5 wording are GPT-authored, so a fresh Claude is
the independent reader on those rows.

### 11.2 The four points, verbatim, and what changed

**Point 1 — the Salon payoff is not already priced.**

> "'Bow in place' is not already priced. The current bow is explicitly a
> final displaced payoff. It removes the oldest member and triggers every
> bow-dependent Power. … Calling the existing numbers 'ratified' does not
> make them transferable to a free, repeatable, once-per-turn action that
> leaves the member in play. In particular: Crabaletta becomes a repeatable
> 14-damage engine. Usher becomes repeatable 9 Block. Chevalmarin refunds 3
> Encore repeatedly, potentially erasing the intended Encore tension.
> Existing bow-triggered Powers may fire without the member paying the
> departure price. I would prototype: The Featured front member performs its
> special act and rotates to the back. This is not a `final_bow` event and
> does not trigger final-bow Powers. The raw bow bodies can be temporary
> prototype inputs, but must be labelled unpriced in this new cadence—not
> inherited as ratified values."

*Changed:* §4.1's Salon-Featured outcome is now *perform and rotate to the
back*. Not a `final_bow`; `salon_bow_block` and `salon_bow_encore` do not
fire; the member table is relabelled **UNPRICED in this cadence** with the
R187 signing explicitly stated as covering a tick-and-displaced-bow pair
only. The old reading survives as alternative (d), marked rejected and why.

*Added, Claude's, because the seat's rule needs it and the return did not
give it:* the **one-order clarification**. After a rotation "front" and
"oldest-by-deploy" diverge; §4.1 defines a single company order (front =
index 0; deploy bows the FRONT; every positional reader on that order),
states the alternative (two orders keyed by deploy age) and why it fails D4,
and shows this is the **shipped** invariant — `_op_salon_rotate`'s own
docstring already says the rotate decides "which performer the FIFO end
offers next, to `salon_bow`, to a deploy landing on a full stage, and to the
`leftmost_salon_member_*` reads". Against the animation contract it is a
**re-render of three slot-index-keyed slots, not a contract change**.

**Point 2 — collapse the two Fanfare legs semantically.**

> "A cleaner rule is: Only a successful Featured performance creates Fanfare.
> One natural and one Encore-commanded Featured performance may pay each
> turn. That preserves the intended 2 + 2 ceiling while giving Fanfare
> exactly one comprehensible cause. Encore creates additional Fanfare by
> successfully commanding another performance, not merely by leaving the
> counter."

*Changed:* §3's ruling text and §3.2's LAW replacement now carry that
sentence verbatim; §4.3's table is one cause with two payments rather than
two legs; a command with no eligible performer **pays nothing**; and
`FANFARE_PER_ENCORE_SPENT` joins §3's retirement list as an independent leg.
This is what finally resolves **G2** cleanly — the LAW sentence is now
literally true rather than true-with-an-exception.

**Point 3 — the Guest rider needs one owner.**

> "For the first prototype, make Hydro a fixed relic-authored Guest rider.
> Spotlight-movement cards should only move Spotlight."

*Changed:* §4.1's Guest-Featured outcome is a fixed relic-authored Hydro
application, owned by `ethereal_spotlight`; §5.2's relic row records it.
Movement cards print no rider. The per-moving-card payload idea is recorded
as later design space and is **not built**.

**Point 4 — twenty is a seed, not a derived value.**

> "The arithmetic uses the hard ceiling of both legs firing every turn. …
> record it as: `20 — declared prototype seed; cadence unresolved until
> whole-fight play.` The staged boards can test the Burst decision by
> starting near full. Only complete MCP combats can determine whether it
> arrives every five turns or every ten."

*Changed:* §4.3 carries that label verbatim. The arithmetic stays and is
labelled a **ceiling, not expected income**; the R212 "DERIVED" claim is
withdrawn; staged boards start near full to test the **Burst decision**, and
**cadence** is routed to the whole-fight gate.

**And the gate elevation:**

> "elevate the packet's whole-fight blind-play note into a mandatory gate
> before step 6."

*Changed:* §6.2 gains **step 5b — whole-fight blind play, a mandatory gate**,
with the `EXPERIMENTS.md` requirements it inherits named one by one
(pre-registration from design intent; the slate DRAFTED and committed before
any run, countersigned in batch; blind grading by a non-author family;
R206 decision linkage; stamp law across the flag's `CONSTANTS_VERSION`
world). §6.3's registered blind spot now points at it, and step 6 does not
open until it reports.

### 11.3 The picks, and where each landed

| pick | seat | where it landed |
|---|---|---|
| P1 | 1 — open after Klee round 3 | §9.3 |
| P2 | prospective E4 law, or return for recutting | **Answered by mechanism** — R213's quarantine expresses it; §9.2 |
| P3 | 4 — retire the relic's current effect | §4.3, §9.3 |
| P4 | 2 — count + pips, runway in tooltip, ribbon retired | §4.4, §9.3 |
| P5 | 3 — the exact entitlement wording | §4.3 (adopted verbatim), §9.3 |
| P6 | 1 — prototype row, **plus** a distinct lane-move card | §5.2a, §9.3 |
| P7 | Return and recut — triage, do not reprice | §7.1, as a **plan**; §9.1 C3 |

### 11.4 What this revision did NOT do

No code, no sheet row, no constant, no id, no register entry. `LAW.md` is
untouched and stays untouched until an arm ADVANCES (§9.2). The bucket
assignments at §7.1 are Claude's provisional read and are gated by the seat
at step 6, not now. And the packet was **not** re-gated: the seat authored
these revisions, so a second doctrine pass on its own text would be the
independence breach the whole protocol exists to prevent.
