# Sitting 2026-08-30 — the R224 slate, DRAFTED

> **What this is.** One drafted slate. A single countersign closes `M47`, `M49`,
> `M50`, `M52`, `M54`, `M55`, `M56`, `M57`, `M59`, `M60`, `M64`, the Klee round-2
> picks and the §14 direction, as **R224** (R206: one batch per sitting; R212:
> Claude drafts, you countersign).
> **The audit was fact-checked.** GPT's slate was read against the registers and
> the packets line by line; where its reason was wrong but its call right, the
> reason is corrected here, not laundered. The appendix carries the claim table.
> **"Countersign as-is"** means: every item below takes the **Claude** column,
> which is the default. Nothing implements before Klee closes (R213).
> **How to answer.** Either write "R224 = Claude's column", or write
> "R224 = Claude's column except 7 = (1), 12 = (2)" — per-item overrides by the
> item number in the left margin. No item is a blank; every one is a numbered pick.

---

## A. Kokomi

**Item 1 — `M49`.** Close the pilot's obsolete Charge hold-versus-spend term?
Options (`QUEUE.md:79`): "(1) close it, defer a *tempo* term to the re-authoring
— **default**, `POLICY_VERSION` stays frozen; (2) build it anyway, version bump
and re-baseline."
**GPT: (1). Claude: (1) — DEFAULT.** The packet supersedes the term in its own
words; the number is frozen either way.

**Item 2 — `M50` pick 1, the *Bake-Kurage* card.** What happens to a Basic that
can no longer be reached in a run? Option (2), verbatim (memory packet:1372):
"**Retire the row.** Honest — the card's job is gone. Costs her a Basic and a
name."
**GPT: (2). Claude: (2) — DEFAULT**, with one word added: retire *under the
re-authoring*. With `C.KURAGE_MEMORY` off the row is still the shipped Basic, so
this is not a deletion today.

**Item 3 — `M50` pick 2, the upgrade delta.** Option (2), verbatim (memory
packet:1383): "**Retire the delta** with the row, under pick 1." The delta is
already inert — an upgraded copy is identical to a base one.
**GPT: (2). Claude: (2) — DEFAULT.** The only answer that leaves no dead upgrade
row behind item 2.

**Item 4 — `M50` pick 3, the Casket refresh link.** Answered through `M60` `KO2`
(item 27). Option (1), verbatim (burst-retirement:703-706): "**Fold it into the
answer to KO1** … Under (a), (c) or (d), re-key the refresh to an immediate
extra pulse."
**GPT: KO2(1), presented as settled. Claude: (1), recorded as CONDITIONAL.** It
is not an answer until `KO1` is ruled: under `KO1`(b) it lands on "leave it
live"; under (a) — item 25's default — it becomes the immediate extra pulse.

**Item 5 — `M50` pick 4, `KURAGE_MEMORY_KEYWORD_NEEDS_SUMMON`.** Option (2),
verbatim (memory packet:1476-1478): "**Delete the constant** and the branch with
it, accepting that the v3 arm is then no longer one flip away."
**GPT: (2). Claude: (2) — DEFAULT.** Under the base kit there is never no
jellyfish, so both branches read the same; the stated cost is that the v3
fallback stops being one flip away, and that fallback is dead once the base kit
is the design.

**Item 6 — `M54`.** The base kit does not teach Rule 1 (0 of 10 turns, 0 of six
Musters). Option (1), verbatim (`QUEUE.md:82`): "print Rule 1 on the Muster's
face or as a keyword, then re-run the gate".
**GPT: (1), "in the Muster keyword/tooltip". Claude: (1) — DEFAULT**, printed as
the **Muster KEYWORD** (hover text is that keyword's detail; "tooltip" is not a
third surface), then re-run on `KURAGEMEM002`, which is unspent. The diagnosed
failure is wording, not dose.

**Item 7 — `M55`, where the Charge-source line goes.** ***A disagreement.***
GPT takes (5) as printed (`QUEUE.md:83`): "surface 'Gain 1 Charge when a card of
yours Exhausts' in the persistent Charge display — **the bar's list**, the strip
being replaced". That surface no longer exists. You cut the persistent display
to one card, one ring, one number — "Hide everything but the charge count" —
and that is what shipped as `M61` option 3. There is no bar and no always-on
list to hold a sentence.
**Claude: (5) RE-SCOPED — DEFAULT.** Put the line at the head of the
click-through **pile view**, the only text-bearing memory surface left; do not
build "Stir". **Fallback: (4) neither**, plus item 6's rerun, if you would
rather see whether the missing explanation was the whole problem.
Pick: **(i) (5) re-scoped to the pile view [Claude]** / (ii) (4) neither /
(iii) (5) as printed [GPT — aimed at a retired surface].

**Item 8 — `M56`, the instrument gap.** Option (1), verbatim (`QUEUE.md:84`):
"carry a per-turn wire snapshot in future records, these grades stand —
**default**".
**GPT: (1). Claude: (1) — DEFAULT.** Published grades stand (R101b). Note the
free ride GPT missed: item 6's rerun on `KURAGEMEM002` carries the snapshot
anyway, so nothing is re-graded and option (2)'s re-registration is **not**
taken.

**Item 9 — `M57`, the prototype card's face.** Option (2), verbatim (memory
packet:2060-2064): "**Take the `Localization` channel** — add an optional
`description:` to the prototype surface row, let `gen_prototype_cards.py` emit
it, and delete the merge."
**GPT: (2), to "remove the risky boot-time merge". Claude: (2) — DEFAULT, on
DUPLICATION grounds.** The risk GPT names is not live: `EB-194` is closed and
the merge already moved to pool-build time behind two locks seen to FAIL first.
The surviving reason is that two channels describe one card, and (2) leaves one
fewer moving part.

**Item 10 — `ephemeral` and the Exhausting copy** (no register row; memory
packet:1183-1190, "**Still open, and [USER]'s**"). Should a copy whose original
printed Exhaust behave differently from one that did not? The packet's own
finding: "Every option that distinguishes them either pays Charge (forbidden) or
files the copy in a pile (which 'remove from combat' forbids), so the build
chose uniform removal."
**GPT: unaddressed. Claude: (1) keep uniform removal, `ephemeral` stays recorded
and behaviour-free — DEFAULT** (the packet's own lean, and the reversible
branch). / (2) author a distinguishing rule, which needs a new answer to the
Charge clause first.

**Item 11 — the pulse's untested type branch** (no register row; memory
packet:1194-1196). "The Skill pulse's 5 and the Power pulse's derived 1 are now
ruled, but the *pulse as a whole* is still keyed to a type branch that no sim
arm has moved."
**GPT: unaddressed. Claude: (1) leave the branch as built and let the next sweep
touch it after the price — DEFAULT** (the packet says it "is the first thing a
sweep should touch after the price"). / (2) collapse the branch now, which is a
design change with no measurement behind it.

---

## B. Klee — round 2 and §14

**Item 12 — `EB-208`, the fix shape** (sparks §13.6 pick 1). (c) verbatim: "A
SEED LEDGER PER CHARACTER: record the encounter each seed drew per character and
let a turn file pin from it." (a) verbatim: "A LIVE-COUNT PREFLIGHT: after
staging, compare the live enemy count with the declared one and mark the slot
UNREACHED on that board automatically…"
**GPT: (c) guarded by (a). Claude: (c)+(a), SEQUENCED — DEFAULT.** Ship (a)
first: it costs nothing, it is hygiene Claude ships under the ladder, and it
alone closes `EB-208`'s acceptance line. (c) waits on a Klee three-body seed
hunt, which is game time either way.

**Item 13 — `P4`** (sparks §13.6 pick 2). (a) verbatim: "Nothing: §12.8 item 1
is answered by the `t01` replay as an out-of-slot observation, `P4` stays
UNREACHED in the published record, and the arithmetic question closes."
**GPT: (a). Claude: (a) — DEFAULT.** R101b-clean; the published record is not
rewritten.

**Item 14 — the next gate** (sparks §13.6 pick 3; options are (a), (b), (c),
(e) — there is no (d)). (e) verbatim: "accept `P2` as the round's answer; record
`P1` only as 'alternative sinks are legible' and NOT as a spend-versus-hold
result … and resolve the two open questions about the SINK SET … BEFORE the arm
advances unchanged."
**GPT: (e). Claude: (e) — DEFAULT.** The only option consistent with the §13.1
erratum; (c) is explicitly ruled out on it.

**Item 15 — §14 direction.** (5) verbatim (sparks:3009-3011): "Migrate before
you duplicate: rule on the three existing non-damage spenders under the
alternative-cost economy first, test the real mixed sink pool, and create no new
rows until that read is in."
**GPT: (5). Claude: (5) — DEFAULT.**

**Item 16 — §14's open migration branch.** ***An extra pick, because (5) does
not settle it.*** (5) leaves it open in its own words (sparks:3016-3021): the
three hybrids "either … stay **hybrid** (1 Energy *and* a Spark price…), or they
migrate to **Spark-only** (0 Energy, price paid entirely in Sparks…)".
Options: **(i) Spark-only, FLAG-GATED** as a dev-only substitution
(`loader._pool_substitutions`' Klee half, under `SPARK_ALT_COST_ENABLED`) /
(ii) stay hybrid.
**GPT: Spark-only under the prototype flag (it presented this as (5) itself; it
is one of (5)'s two branches, plus an addition). Claude: (i) — DEFAULT.**
Energy-gating would make a null read uninterpretable, and the flag removes the
one cost (5) records against itself, that a migration is "a shipped-pool edit
and therefore a heavier, less reversible act than a dev-only prototype row".

**Item 17 — `M47`, *Bag of Tricks*.** ***A disagreement.*** Options
(`QUEUE.md:78`): "(1) drop it; (2) amend the top-level-cost rule to admit a mode
price; (3) build per-mode playability (`EB-182`) first, then re-ask — my
reading; (4) re-author as two cards; (5) price the whole card."
GPT chooses (1) because "its modal-price UI and LAW cost are no longer justified
when the mixed shipped pool can answer the same qualitative-sink question."
Three parts of that fail against the packets: the modal-price UI and LAW cost
belong to **option (2)**, not (1); the per-mode playability engineering is owed
to Kokomi slice 2 **regardless** of this arm (option (3)'s own text); and the
mixed shipped pool contains **no Bomb-placement sink**, so it cannot answer this
arm's question — Bag of Tricks is "the only arm that put Sparks on the bomb
board", the one D2 verb Klee owns that Sparks have never touched.
**Claude: (3) — DEFAULT.** The D4 half of the hold dies for free on work already
owed, leaving one clause to rule instead of two. **Fallback (4)** (two cards, no
mode) if you want the arm gone anyway — it keeps the bomb-board question at zero
LAW cost, which (1) throws away. **GPT: (1).**

**Item 18 — `M64`, the author-disjoint deciding read.** ***A disagreement, in
degree.*** Options (`QUEUE.md:88`): "(1) Codex decides every board (**~7 calls,
not 3**); (2) fresh-Opus rounds are INSTRUMENT rounds, no ADVANCE rests on one;
(3) a third author-disjoint model. **Gate:** no default".
GPT takes (1) flat and prices it as "model calls, not human QA" — right in kind,
but the register prices it at **~7 Codex calls per round against the standing
3**, on the scarcest budget in the project. And §14.4 condition 5 makes the
mixed-pool fight **wait** on this pick, so it is on the critical path.
**Claude: SPLIT — DEFAULT.** (1) for any round that can ADVANCE an arm; (2) for
INSTRUMENT rounds — repairs and benches, where no ADVANCE rests on the read —
which stay fresh-Opus. **GPT: (1) flat.**

**Item 19 — Rummage and Slow Burn's owed whole fights.** ***A disagreement.***
GPT: run both, separately from the §14 batch, so the questions do not
contaminate each other. Neither card is in the eight-row prototype set, so
accidental contamination is not the live risk. The live facts are different:
Rummage is the **same shape** as the three hybrids — 1 Energy plus a top-level
Spark price, paying in draw — i.e. exactly (5)'s hybrid-price question; and Slow
Burn's payoff is "Gain 10 Burst Energy" while Burst is being retired roster-wide
(`EB-199`).
**Claude: (i) FOLD Rummage into the §14 mixed-pool deck** rather than staging a
separate fight for the same question, and **(ii) HOLD Slow Burn's whole fight
until `M60`/`EB-199` settle what its Burst rider becomes** — **DEFAULT**.
Alternative: (iii) GPT's two separate fights.

**Item 20 — the Spark drain's sequencing.** ***A disagreement about a missing
word.*** Both columns agree the drain (*Sparks 'n' Splash* under `K1`) stays out
of the §14 batch: a repeatable spend-all Attack invalidates every small Spark
spender on the turns it is drawn. (GPT's stated mechanism — a hoard incentive —
only exists because R220 F moved `spark` to unbounded; under the old bounded-at-3
reading it would not.) But the burst packet's ship order currently schedules the
drain into "the Sparks arm's next round".
**Claude: rule the sequencing word — DEFAULT: the drain lands only AFTER the
mixed-pool read is in.** Alternative: leave the ship order as written.

---

## C. Furina

**Item 21 — `M52`, the reframe countersign.** Three picks (`QUEUE.md:81`):
(1) "the packet's §3 ruling text — **default**"; (2) "its §3.1 LAW text AS
PROSPECTIVE (R213)"; (3) "LAW:145's clause — (a) the packet's, (b) the relayed
rewrite keeping Furina's Companion→Fanfare loop legal."
**GPT: (1), (2), and (3)(b). Claude: (1), (2), (3)(b) WITH THE PACKET'S OWN
TIGHTENING — DEFAULT.** Signing (2) explicitly adopts **amendment 3** (the Salon
overdraw hygiene sentence) and **amendment 4** (the scaling invariant) — worth
saying out loud, since amendment 3 is otherwise hygiene. On (3)(b): the rewrite
reads "Companion cards may not directly grant signature resources. A
character-owned engine may respond to a Companion play and generate its resource
where explicitly declared by that character's kit." The packet flags that
"directly" is undefined and carries the whole prohibition, and that the rewrite
drops "cheaply repeatable", so the rate guard survives only in what the kit
declaration bounds. Countersign (b) **plus** those two repairs, not bare (b).

**Item 22 — `M59`, the sixteen reframe picks `F1`–`F16`.** Every one has a
marked default and it is always option (1).
**GPT: F1(2), F13(2), F14(2), the rest defaults. Claude: identical — DEFAULT.**
- **F1 = (2)** "Four: the three existing plus a scaling member. Energy deferred
  until the scaling member has been played…" — taken on the packet's own ground:
  a per-turn energy faucet on a board is the most dangerous thing in the packet.
  *Not* on the scaling objection GPT restates, which amendment 4 already answers.
- **F13 = (2)** "The trigger line in full, the Evoke line as a keyword with
  detail in the member's board tooltip."
- **F14 = (2)** "As (1), plus the Evoke effect resolved at current Focus."
- **F2–F12, F15, F16 = (1)**, the marked defaults.
- No implementation before Klee closes (R213; Furina's fold lands last, R220 B).

---

## D. Burst retirement — `M60`

**Item 23 — `K1`, *Sparks 'n' Splash*'s rarity.** (3) verbatim
(burst-retirement:556-562): "**Rare, once per combat — Exhaust on the body.**"
**GPT: (3), "Exhaust is sufficient only if recovery cannot trivially replay it".
Claude: (3) — DEFAULT**, with Exhaust as the concrete mechanism: no
Exhaust-recovery op exists anywhere in `tier0/` or the sheets, so the caveat is
moot today and should be re-checked if one is ever added.
**Flag for you:** (3) is a packet-added third shape, not one of the two you
named ("either let Klee's go to Uncommon or make it stronger as a Rare payoff").

**Item 24 — `K2`, the five feeds.** (1) verbatim (burst-retirement:597-599):
"**The table as written** … Two retire, Catalytic Converter and Blazing Delight
convert to Sparks at derived rates, Durin retires under LAW:145."
**GPT: (1). Claude: (1) — DEFAULT.** Durin retires under either LAW:145 reading.

**Item 25 — `KO1`, the Kokomi fold shape.** (1) = (a) verbatim
(burst-retirement:687-691): "**(a) State length paid from Charge** … It keeps
R74's ruling intact (the card is still a state, not a nuke)".
**GPT: (1). Claude: (1) — DEFAULT.**

**Item 26 — `KO1a`, the payment rule.** (1) verbatim (burst-retirement:845-852):
"**Fixed N, printed on the card.** *'Spend 4 Charge. Gain Ceremonial Garment
(2 turns).'* … Least agency, least to explain, zero new UI, and it is the only
option where a co-op seat needs no prompt."
**GPT: (1), on "avoid another modal, preserve Charge for potency". Claude: (1) —
DEFAULT, on the zero-new-UI and co-op-no-prompt grounds.** The potency argument
rules out (2), not (3); (3) is the modal, and that is what (1) beats.

**Item 27 — `KO2`, the Casket link.** See item 4. **(1), fold into `KO1`** —
which under item 25's (a) means re-key the refresh to an immediate extra pulse.
Recorded as the conditional, not as a flat "immediate pulse".

**Item 28 — the other seven LAW blocks and the architecture paragraph.**
***GPT did not address these.*** §6 item 1 returns "§3.1's architecture paragraph
and the **eight** replacement blocks in §3.2" — LAW:64, :72, :145, :176, :248,
:266, :481, :508. GPT ruled only LAW:145 (item 21).
Options: **(1) countersign all eight blocks and the architecture paragraph AS
PROSPECTIVE**, same standing as reframe §3.1, implemented last — **DEFAULT** /
(2) LAW:145 only now, the other seven return next sitting.

---

## E. Process

**Item 29 — the local Qwen seat.** Nothing to rule: it stays shadow until
`EB-211`/`EB-212` land positive cost evidence and matched-intent pairs, which is
already law (R222 B + R223).

**Item 30 — the decision queue itself.** GPT's largest process worry is that ten
granular rulings have made you the bottleneck. This packet is the answer: one
countersign closes all of the above together, as R224 (R206/R212).

---

## F. No-ops and things already taken — nothing to answer here

- **Powder Keg's repair is already taken** as hygiene: (i) place-then-modify, the
  **+12** version, "matches the sentence the candidate was written to make". GPT
  endorses the shipped state, not an open pick. The **+9** branch remains
  available to you at a stroke. (GPT's "+9 risks a dead top rung" is not the
  packet's reason, though it is consistent with §14.4 condition 4.)
- **Minefield** ("don't develop; price at 3 if retained") — a **no-op under
  (5)**: Minefield lives only inside option (2), which item 15 does not take.
- **Powder Trail** ("reject") — a **no-op**: it appears in no numbered option,
  only as an alternative body inside (1)/(2). The identity objection is right and
  is already the packet's (it is a statline amendment under the identity rail).
- **"Run the amended mixed-pool proof before creating another card"** — a
  **no-op**: §14.4's five conditions are already ADOPTED as instrument conditions
  under R212.
- **Kurage's Oath 3/5** — not a pick. It stays as you ruled it, a placeholder
  with no measurement attached. It carries one debt (item under §G).
- **Slot 6** (Furina) — keep it, but it is an instruction to Claude, not an `M59`
  answer, and it is not countersignable until `F1`–`F16` land.
- **The two-slot model server** — GPT's caution is moot; it merged as #194 and is
  on `main` (`cdc2d68`). GPT read `main` before that merge.

---

## G. What the countersign does

**Rows that CLOSE:** `M47`, `M49`, `M50` (all four picks), `M52`, `M54`, `M55`,
`M56`, `M57`, `M59`, `M60` (`K1`/`K2`/`KO1`/`KO1a`/`KO2` plus the eight LAW
blocks), `M64`.

**LAW:** nothing moves in `LAW.md` on the day. Reframe §3.1 and the burst
packet's eight §3.2 blocks are countersigned **as PROSPECTIVE text** (R213) —
they bind the folds when the folds land, and `LAW.md` is edited then, not now.

**Register rows minted on landing:**
- **`EB-213`** (next free id; no existing row covers it — checked) — the
  prototype surface has **no upgrade channel**, so the substituted Kurage's Oath
  cannot be upgraded at a campfire; any rerun that wants to see the upgraded 5
  needs that fixed first.
- **`M16`** gets a hygiene re-point, not an answer: item 2 drops its premise from
  four end-of-turn sources to three. It stays OPEN.

**What RUNS next, in order:**
1. `EB-208`'s **(a) live-count preflight** — cheap, closes the acceptance line.
2. The **§14 hybrid migration** to Spark-only behind `SPARK_ALT_COST_ENABLED`.
3. The **mixed-pool whole fight**, with Rummage folded into the deck — waits on
   `M64` (§14.4 condition 5).
4. `M54`'s **rerun on `KURAGEMEM002`**, with `M55`'s line at the head of the pile
   view.
5. **Slow Burn's whole fight**, after `M60`/`EB-199` settle its Burst rider.
6. **Furina: nothing**, until Klee closes.

**Stays OPEN, untouched:** `M45`(6) and (7), `S4-G6`, `S4-G14`, `M16`, and the
older rows above section 6 of the QUEUE.

---

## Appendix — the audit, fact-checked

RIGHT = call and reason both stand. PARTLY = call stands, reason corrected.
WRONG = the claim fails.

### Kokomi

| GPT's claim | verdict | evidence |
|---|---|---|
| M49(1) closes an obsolete term | RIGHT | memory packet:414-418; QUEUE.md:79 |
| Bake-Kurage is an unreachable Basic | RIGHT | memory packet:1366-1369 |
| Upgrade delta retires with the row | RIGHT | memory packet:1380-1383 |
| Casket answered by KO2(1) | PARTLY — conditional on KO1 | burst-retirement:703-705; QUEUE.md:80 |
| The no-jellyfish state no longer exists | RIGHT (cost omitted) | memory packet:1471-1478 |
| M54: 0/10 turns, 0/6 Musters | RIGHT | memory packet:2234-2239 |
| M54 "tooltip" as a surface | PARTLY — option (1) names face or keyword | QUEUE.md:82 |
| M55(5) "the persistent Charge display — the bar's list" | WRONG — that surface is retired | memory packet:2337-2341, 2392-2400, 2823-2860 |
| M56: published grades stand | RIGHT | memory packet:2265-2272 |
| M56(2) is a rival to (1) | PARTLY — it rides along free on M54's rerun | QUEUE.md:84 |
| M57: "risky boot-time merge" | PARTLY — EB-194 closed, merge moved | BACKLOG.md:105; memory packet:1930-1940 |
| "Rerun once" as a closing step | PARTLY — the rerun is M55's precondition | QUEUE.md:82 |
| Oath stays provisional 3/5 | PARTLY — right, but not an open pick | memory packet:1386-1391, 1411-1413 |
| (unmentioned) Oath has no upgrade channel | debt | memory packet:1432, 1458-1459 |
| M60 as a whole, M45(6)/(7), S4-G6, S4-G14, M16, the two rowless questions | unaddressed | QUEUE.md:32-33, 48, 64, 87; memory packet:1183-1196 |

### Klee

| GPT's claim | verdict | evidence |
|---|---|---|
| EB-208: (c) guarded by (a) | RIGHT (restates its own prior column) | sparks:2235-2247, 2251-2258 |
| P4 (a) | RIGHT | sparks:2265-2273 |
| Next gate (e) | RIGHT | sparks:2288-2298; §13.1 erratum |
| §14 option (5) exists and says migrate-first | RIGHT | sparks:3009-3011 |
| "(5) means Spark-only" | PARTLY — (5) leaves the branch open | sparks:3016-3021 |
| "under the prototype flag" | addition, and a good one | sparks:3031-3033; BACKLOG EB-205:112 |
| "Run the amended proof first" | RIGHT but already binding | sparks:3062-3066, 3080-3084 |
| Price-3 rung → prototype Powder Keg alone | RIGHT, verbatim inside (5) | sparks:3026-3029 |
| Powder Keg +12 | PARTLY — already taken; GPT's reason is not the packet's | sparks:2681-2691 |
| Minefield: don't develop, price 3 | RIGHT, and a no-op under (5) | sparks:2588-2591 |
| Powder Trail: reject | RIGHT, and a no-op | sparks:2856-2867; klee-character-design.md:15 |
| M47(1)'s "modal UI and LAW cost" | WRONG — that is option (2)'s cost | slice1:602-608 |
| M47(1) saves the per-mode work | WRONG — owed to Kokomi slice 2 anyway | slice1:612-616 |
| The mixed pool answers the same question | WRONG — no Bomb-placement sink in it | slice1:577-580, 599; sparks:2450-2453 |
| M64(1) "costs model calls, not human QA" | PARTLY — ~7 calls vs the standing 3 | QUEUE.md:88 |
| M64: author-disjointness is real | RIGHT | sparks:2342-2352; understudy/seat.py:275-279 |
| Rummage/Slow Burn contaminate §14 | PARTLY — they are not in the eight rows | prototype-surface.yaml:459, 525 vs 757-861 |
| Rummage is a different question from §14 | WRONG — same hybrid price shape | slice1:184-186 |
| Slow Burn's fight can run now | WRONG — its Burst rider is being deleted | BACKLOG.md:107, EB-199 |
| Drain out of §14 | RIGHT (mechanism backwards: hoarding follows R220 F's unbounded bank) | burst-retirement:571-575; LAW.md:481 |

### Furina and M60

| GPT's claim | verdict | evidence |
|---|---|---|
| Every F-pick has a default | RIGHT — always (1) | reframe:738, 1543 |
| F1(2) verbatim | RIGHT | reframe:833-835 |
| F1's reason: the scaling hole is still open | WRONG — amendment 4 answers it | reframe:1574-1580; surviving ground at :806-808 |
| F13(2) / F14(2) | RIGHT | reframe:1163-1164, 1172-1173, 1585-1588 |
| Keep Slot 6 | RIGHT, but not an M59 answer | reframe:1355-1386, 1376-1377 |
| No implementation before Klee closes | RIGHT | reframe:621, 687-693; burst:449 |
| M52 §3.1 is prospective | RIGHT (signs amendments 3 and 4) | reframe:634-637, 695-715 |
| LAW:145 (b) | RIGHT, but the tightening is unaddressed | burst:332-345, 350-353 |
| K1(3) once per combat | RIGHT; no Exhaust-recovery exists to check | burst:556-580 |
| K1(3) is one of [USER]'s two shapes | WRONG — a packet-added third | burst:45-46 |
| K2(1) | RIGHT | burst:597-599 |
| KO1(1) = (a) | RIGHT | burst:687-691 |
| KO1a: potency argument | PARTLY — rules out (2), not (3) | burst:859-867 |
| KO2(1) = immediate pulse | PARTLY — a consequence of KO1(a), not the label | burst:703-706 |
| The other seven §3.2 LAW blocks + architecture paragraph | unaddressed | burst:820-825 |
| Two-slot server not on main | WRONG as of today — merged #194 | `main` cdc2d68 |
