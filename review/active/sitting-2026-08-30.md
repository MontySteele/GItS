# Sitting 2026-08-30 — the R224 slate

> **R224 LANDED THE SIGNED ITEMS on 2026-08-30** — the commit is named by hash
> in `docs/current/RULINGS.md`. `M47`, `M49`, `M50`, `M52`, `M54`, `M56`,
> `M57`, `M59` and `M60` are CLOSED and have left `QUEUE.md`; the Klee round-2
> picks and the §14 direction are answered, Ceremonial Garment is ruled LOOT,
> and LAW:145's replacement is countersigned as tightened.
> **NOTHING REMAINS OPEN ON THIS PACKET.** The seven flagged items were
> reviewed and then signed at the Claude column too — 7, 16, 18, 19, 20 and 28
> on *"Yes - those 6 at the default"*, and 17 earlier — and item **31**, which
> R224's own LAW:145 text opened after the countersign, is ruled at (2),
> re-author Prune. Every `M` row this packet covered has CLOSED: `M47`, `M49`,
> `M50`, `M52`, `M54`, `M55`, `M56`, `M57`, `M59`, `M60`, `M64`.
> Every signed item below carries a short **RULED (R224)** marker under it, and
> **nothing has been deleted from this packet**: the columns stand as the
> argument they were.

> **What this was.** One drafted slate. A single countersign closed `M47`,
> `M49`, `M50`, `M52`, `M54`, `M56`, `M57`, `M59`, `M60`, the Klee round-2
> picks and the §14 direction, as **R224** (R206: one batch per sitting; R212:
> Claude drafts, you countersign).
> **The audit was fact-checked.** GPT's slate was read against the registers and
> the packets line by line; where its reason was wrong but its call right, the
> reason is corrected here, not laundered. The appendix carries the claim table.
> **"Countersign as-is"** means: every item below takes the **Claude** column,
> which is the default. Nothing implements before Klee closes (R213).
> **How to answer.** Either write "R224 = Claude's column", or write
> "R224 = Claude's column except 7 = (1), 12 = (2)" — per-item overrides by the
> item number in the left margin. No item is a blank; every one is a numbered pick.

---

## A. Kokomi

**Item 1 — `M49`.** Close the pilot's obsolete Charge hold-versus-spend term?
Options (`QUEUE.md:79`): "(1) close it, defer a *tempo* term to the re-authoring
— **default**, `POLICY_VERSION` stays frozen; (2) build it anyway, version bump
and re-baseline."
**GPT: (1). Claude: (1) — DEFAULT.** The packet supersedes the term in its own
words; the number is frozen either way.

**RULED (R224): (1).** The row closes; `POLICY_VERSION` stays frozen.

**Item 2 — `M50` pick 1, the *Bake-Kurage* card.** What happens to a Basic that
can no longer be reached in a run? Option (2), verbatim (memory packet:1372):
"**Retire the row.** Honest — the card's job is gone. Costs her a Basic and a
name."
**GPT: (2). Claude: (2) — DEFAULT**, with one word added: retire *under the
re-authoring*. With `C.KURAGE_MEMORY` off the row is still the shipped Basic, so
this is not a deletion today.

**RULED (R224): (2)**, retire the row **under the re-authoring** — with `C.KURAGE_MEMORY` off it is still the shipped Basic, so this is not a deletion today. Carried on `EB-199`.

**Item 3 — `M50` pick 2, the upgrade delta.** Option (2), verbatim (memory
packet:1383): "**Retire the delta** with the row, under pick 1." The delta is
already inert — an upgraded copy is identical to a base one.
**GPT: (2). Claude: (2) — DEFAULT.** The only answer that leaves no dead upgrade
row behind item 2.

**RULED (R224): (2).** The delta retires with the row.

**Item 4 — `M50` pick 3, the Casket refresh link.** Answered through `M60` `KO2`
(item 27). Option (1), verbatim (burst-retirement:703-706): "**Fold it into the
answer to KO1** … Under (a), (c) or (d), re-key the refresh to an immediate
extra pulse."
**GPT: KO2(1), presented as settled. Claude: (1), recorded as CONDITIONAL.** It
is not an answer until `KO1` is ruled: under `KO1`(b) it lands on "leave it
live"; under (a) — item 25's default — it becomes the immediate extra pulse.

**RULED (R224): (1)**, and the condition resolved — `KO1` = (a) (item 25), so the Casket refresh **re-keys to an immediate extra pulse**.

**Item 5 — `M50` pick 4, `KURAGE_MEMORY_KEYWORD_NEEDS_SUMMON`.** Option (2),
verbatim (memory packet:1476-1478): "**Delete the constant** and the branch with
it, accepting that the v3 arm is then no longer one flip away."
**GPT: (2). Claude: (2) — DEFAULT.** Under the base kit there is never no
jellyfish, so both branches read the same; the stated cost is that the v3
fallback stops being one flip away, and that fallback is dead once the base kit
is the design.

**RULED (R224): (2).** The constant and its branch are deleted; engineering `EB-217`.

**Item 6 — `M54`.** The base kit does not teach Rule 1 (0 of 10 turns, 0 of six
Musters). Option (1), verbatim (`QUEUE.md:82`): "print Rule 1 on the Muster's
face or as a keyword, then re-run the gate".
**GPT: (1), "in the Muster keyword/tooltip". Claude: (1) — DEFAULT**, printed as
the **Muster KEYWORD** (hover text is that keyword's detail; "tooltip" is not a
third surface), then re-run on `KURAGEMEM002`, which is unspent. The diagnosed
failure is wording, not dose.

**RULED (R224): (1)**, printed as the **Muster KEYWORD**, then re-run on `KURAGEMEM002`; engineering `EB-214`.

**Item 7 — `M55`, where the Charge-source line goes.** ***A disagreement.***
GPT takes (5) as printed (`QUEUE.md:83`): "surface 'Gain 1 Charge when a card of
yours Exhausts' in the persistent Charge display — **the bar's list**, the strip
being replaced". That surface no longer exists. You cut the persistent display
to one card, one ring, one number — "Hide everything but the charge count" —
and that is what shipped as `M61` option 3. There is no bar and no always-on
list to hold a sentence.
**Claude: (5) RE-SCOPED — DEFAULT.** Put the line at the head of the
click-through **pile view**, the only text-bearing memory surface left; do not
build "Stir". **Fallback: (4) neither**, plus item 6's rerun, if you would
rather see whether the missing explanation was the whole problem.
Pick: **(i) (5) re-scoped to the pile view [Claude]** / (ii) (4) neither /
(iii) (5) as printed [GPT — aimed at a retired surface].

**RULED (R224): (i), (5) re-scoped to the pile view.** The line *"Gain 1 Charge
when a card of yours Exhausts"* goes at the head of the click-through pile
view, the only text-bearing memory surface left; "Stir" is not built. `M55`
CLOSES, and the work folds into `EB-214`, since it rides the same rerun.

**Item 8 — `M56`, the instrument gap.** Option (1), verbatim (`QUEUE.md:84`):
"carry a per-turn wire snapshot in future records, these grades stand —
**default**".
**GPT: (1). Claude: (1) — DEFAULT.** Published grades stand (R101b). Note the
free ride GPT missed: item 6's rerun on `KURAGEMEM002` carries the snapshot
anyway, so nothing is re-graded and option (2)'s re-registration is **not**
taken.

**RULED (R224): (1).** Published grades stand (R101b); option (2) is not taken, since item 6's rerun carries the snapshot free. Engineering `EB-216`.

**Item 9 — `M57`, the prototype card's face.** Option (2), verbatim (memory
packet:2060-2064): "**Take the `Localization` channel** — add an optional
`description:` to the prototype surface row, let `gen_prototype_cards.py` emit
it, and delete the merge."
**GPT: (2), to "remove the risky boot-time merge". Claude: (2) — DEFAULT, on
DUPLICATION grounds.** The risk GPT names is not live: `EB-194` is closed and
the merge already moved to pool-build time behind two locks seen to FAIL first.
The surviving reason is that two channels describe one card, and (2) leaves one
fewer moving part.

**RULED (R224): (2)**, on the duplication ground; engineering `EB-215`.

**Item 10 — `ephemeral` and the Exhausting copy** (no register row; memory
packet:1183-1190, "**Still open, and [USER]'s**"). Should a copy whose original
printed Exhaust behave differently from one that did not? The packet's own
finding: "Every option that distinguishes them either pays Charge (forbidden) or
files the copy in a pile (which 'remove from combat' forbids), so the build
chose uniform removal."
**GPT: unaddressed. Claude: (1) keep uniform removal, `ephemeral` stays recorded
and behaviour-free — DEFAULT** (the packet's own lean, and the reversible
branch). / (2) author a distinguishing rule, which needs a new answer to the
Charge clause first.

**RULED (R224): (1).** Uniform removal stands; `ephemeral` stays recorded and behaviour-free.

**Item 11 — the pulse's untested type branch** (no register row; memory
packet:1194-1196). "The Skill pulse's 5 and the Power pulse's derived 1 are now
ruled, but the *pulse as a whole* is still keyed to a type branch that no sim
arm has moved."
**GPT: unaddressed. Claude: (1) leave the branch as built and let the next sweep
touch it after the price — DEFAULT** (the packet says it "is the first thing a
sweep should touch after the price"). / (2) collapse the branch now, which is a
design change with no measurement behind it.

**RULED (R224): (1).** The type branch stays as built until the next sweep, after the price.

---

## B. Klee — round 2 and §14

**Item 12 — `EB-208`, the fix shape** (sparks §13.6 pick 1). (c) verbatim: "A
SEED LEDGER PER CHARACTER: record the encounter each seed drew per character and
let a turn file pin from it." (a) verbatim: "A LIVE-COUNT PREFLIGHT: after
staging, compare the live enemy count with the declared one and mark the slot
UNREACHED on that board automatically…"
**GPT: (c) guarded by (a). Claude: (c)+(a), SEQUENCED — DEFAULT.** Ship (a)
first: it costs nothing, it is hygiene Claude ships under the ladder, and it
alone closes `EB-208`'s acceptance line. (c) waits on a Klee three-body seed
hunt, which is game time either way.

**RULED (R224): (c) + (a), SEQUENCED** — (a) first. `EB-208`'s row carries the shape.

**Item 13 — `P4`** (sparks §13.6 pick 2). (a) verbatim: "Nothing: §12.8 item 1
is answered by the `t01` replay as an out-of-slot observation, `P4` stays
UNREACHED in the published record, and the arithmetic question closes."
**GPT: (a). Claude: (a) — DEFAULT.** R101b-clean; the published record is not
rewritten.

**RULED (R224): (a).** `P4` stays UNREACHED as published; §12.8 item 1 closes out of slot.

**Item 14 — the next gate** (sparks §13.6 pick 3; options are (a), (b), (c),
(e) — there is no (d)). (e) verbatim: "accept `P2` as the round's answer; record
`P1` only as 'alternative sinks are legible' and NOT as a spend-versus-hold
result … and resolve the two open questions about the SINK SET … BEFORE the arm
advances unchanged."
**GPT: (e). Claude: (e) — DEFAULT.** The only option consistent with the §13.1
erratum; (c) is explicitly ruled out on it.

**RULED (R224): (e).**

**Item 15 — §14 direction.** (5) verbatim (sparks:3009-3011): "Migrate before
you duplicate: rule on the three existing non-damage spenders under the
alternative-cost economy first, test the real mixed sink pool, and create no new
rows until that read is in."
**GPT: (5). Claude: (5) — DEFAULT.**

**RULED (R224): (5).**

**Item 16 — §14's open migration branch.** ***An extra pick, because (5) does
not settle it.*** (5) leaves it open in its own words (sparks:3016-3021): the
three hybrids "either … stay **hybrid** (1 Energy *and* a Spark price…), or they
migrate to **Spark-only** (0 Energy, price paid entirely in Sparks…)".
Options: **(i) Spark-only, FLAG-GATED** as a dev-only substitution
(`loader._pool_substitutions`' Klee half, under `SPARK_ALT_COST_ENABLED`) /
(ii) stay hybrid.
**GPT: Spark-only under the prototype flag (it presented this as (5) itself; it
is one of (5)'s two branches, plus an addition). Claude: (i) — DEFAULT.**
Energy-gating would make a null read uninterpretable, and the flag removes the
one cost (5) records against itself, that a migration is "a shipped-pool edit
and therefore a heavier, less reversible act than a dev-only prototype row".

**RULED (R224): (i) Spark-only, FLAG-GATED.** The three hybrids go to 0 Energy
with the price paid wholly in Sparks, as a dev-only substitution under
`SPARK_ALT_COST_ENABLED` (sim) and `-p:PrototypeCards=true` (C#). Energy-gating
would make a null read uninterpretable, and the flag removes the one cost (5)
records against itself. Flag off, the pool is byte-identical. Engineering:
`EB-218`.

**Item 17 — `M47`, *Bag of Tricks*.** ***A disagreement.*** Options
(`QUEUE.md:78`): "(1) drop it; (2) amend the top-level-cost rule to admit a mode
price; (3) build per-mode playability (`EB-182`) first, then re-ask — my
reading; (4) re-author as two cards; (5) price the whole card."
GPT chooses (1) because "its modal-price UI and LAW cost are no longer justified
when the mixed shipped pool can answer the same qualitative-sink question."
Three parts of that fail against the packets: the modal-price UI and LAW cost
belong to **option (2)**, not (1); the per-mode playability engineering is owed
**regardless** of this arm — to the SHIPPED Furina card `deep_breath`, whose
mode 2 prices *Spend 3 Encore: draw 3* (`docs/furina-cards.yaml:707-713`).
(Fact-check 2026-08-30: the slate's own first reason, "owed to Kokomi slice 2",
was wrong; the conclusion it supported stands, on this card instead); and the
mixed shipped pool contains **no Bomb-placement sink**, so it cannot answer this
arm's question — Bag of Tricks is "the only arm that put Sparks on the bomb
board", the one D2 verb Klee owns that Sparks have never touched.
**Claude: (3) — DEFAULT.** The D4 half of the hold dies for free on work already
owed, leaving one clause to rule instead of two. **Fallback (4)** (two cards, no
mode) if you want the arm gone anyway — it keeps the bomb-board question at zero
LAW cost, which (1) throws away. **GPT: (1).**

**RULED (R224): (3).** [USER]: *"17 - I think 3 is fine, we can try it."*
Build per-mode playability (`EB-182`, now carrying its two consumers) first,
then re-ask the doctrine seat; the top-level-cost clause returns to [USER]
then, as ONE clause rather than two. `M47` CLOSES.

**Item 18 — `M64`, the author-disjoint deciding read.** ***A disagreement, in
degree.*** Options (`QUEUE.md:88`): "(1) Codex decides every board (**~7 calls,
not 3**); (2) fresh-Opus rounds are INSTRUMENT rounds, no ADVANCE rests on one;
(3) a third author-disjoint model. **Gate:** no default".
GPT takes (1) flat and prices it as "model calls, not human QA" — right in kind,
but the register prices it at **~7 Codex calls per round against the standing
3**, on the scarcest budget in the project. And §14.4 condition 5 makes the
mixed-pool fight **wait** on this pick, so it is on the critical path.
**Claude: SPLIT — DEFAULT.** (1) for any round that can ADVANCE an arm; (2) for
INSTRUMENT rounds — repairs and benches, where no ADVANCE rests on the read —
which stay fresh-Opus. **GPT: (1) flat.**

**RULED (R224): the SPLIT.** (1) Codex decides every board on any round that
can ADVANCE an arm — ~7 Codex calls, not 3; (2) fresh-Opus rounds are
INSTRUMENT rounds, repairs and benches, on which no ADVANCE rests. `M64`
CLOSES; the rule is written into OPERATIONS' *Local tester seat* section.

**Item 19 — Rummage and Slow Burn's owed whole fights.** ***A disagreement.***
GPT: run both, separately from the §14 batch, so the questions do not
contaminate each other. Neither card is in the eight-row prototype set, so
accidental contamination is not the live risk. The live facts are different:
Rummage is the **same shape** as the three hybrids — 1 Energy plus a top-level
Spark price, paying in draw — i.e. exactly (5)'s hybrid-price question; and Slow
Burn's payoff is "Gain 10 Burst Energy" while Burst is being retired roster-wide
(`EB-199`).
**Claude: (i) FOLD Rummage into the §14 mixed-pool deck** rather than staging a
separate fight for the same question, and **(ii) HOLD Slow Burn's whole fight
until `M60`/`EB-199` settle what its Burst rider becomes** — **DEFAULT**.
Alternative: (iii) GPT's two separate fights.

**RULED (R224): (i) and (ii).** Rummage FOLDS into the §14 mixed-pool deck —
it is the same hybrid-price shape as the three hybrids, so a separate fight
would ask this fight's question twice (§14.4 condition 3 now names it). Slow
Burn's whole fight is HELD until `M60`/`EB-199` settle what its Burst rider
becomes.

**Item 20 — the Spark drain's sequencing.** ***A disagreement about a missing
word.*** Both columns agree the drain (*Sparks 'n' Splash* under `K1`) stays out
of the §14 batch: a repeatable spend-all Attack invalidates every small Spark
spender on the turns it is drawn. (GPT's stated mechanism — a hoard incentive —
only exists because R220 F moved `spark` to unbounded; under the old bounded-at-3
reading it would not.) But the burst packet's ship order currently schedules the
drain into "the Sparks arm's next round".
**Claude: rule the sequencing word — DEFAULT: the drain lands only AFTER the
mixed-pool read is in.** Alternative: leave the ship order as written.

**RULED (R224): the drain lands only AFTER the mixed-pool read is in.** The
burst packet's ship order is amended in place: a repeatable spend-all Attack
invalidates every small Spark spender on the turns it is drawn, so it may not
be in the deck the mixed-pool fight reads.

---

## C. Furina

**Item 21 — `M52`, the reframe countersign.** Three picks (`QUEUE.md:81`):
(1) "the packet's §3 ruling text — **default**"; (2) "its §3.1 LAW text AS
PROSPECTIVE (R213)"; (3) "LAW:145's clause — (a) the packet's, (b) the relayed
rewrite keeping Furina's Companion→Fanfare loop legal."
**GPT: (1), (2), and (3)(b). Claude: (1), (2), (3)(b) WITH THE PACKET'S OWN
TIGHTENING — DEFAULT.** Signing (2) explicitly adopts **amendment 3** (the Salon
overdraw hygiene sentence) and **amendment 4** (the scaling invariant) — worth
saying out loud, since amendment 3 is otherwise hygiene. On (3)(b): the rewrite
reads "Companion cards may not directly grant signature resources. A
character-owned engine may respond to a Companion play and generate its resource
where explicitly declared by that character's kit." The packet flags that
"directly" is undefined and carries the whole prohibition, and that the rewrite
drops "cheaply repeatable", so the rate guard survives only in what the kit
declaration bounds. Countersign (b) **plus** those two repairs, not bare (b).

**COUNTERSIGNED (R224): (1), (2) AS PROSPECTIVE — explicitly adopting amendments 3 and 4 — and (3)(b) WITH the packet's tightening** ("directly" defined, "cheaply repeatable" kept), also prospective. No `LAW.md` line moves.

**Item 22 — `M59`, the sixteen reframe picks `F1`–`F16`.** Every one has a
marked default and it is always option (1).
**GPT: F1(2), F13(2), F14(2), the rest defaults. Claude: identical — DEFAULT.**
- **F1 = (2)** "Four: the three existing plus a scaling member. Energy deferred
  until the scaling member has been played…" — taken on the packet's own ground:
  a per-turn energy faucet on a board is the most dangerous thing in the packet.
  *Not* on the scaling objection GPT restates, which amendment 4 already answers.
- **F13 = (2)** "The trigger line in full, the Evoke line as a keyword with
  detail in the member's board tooltip."
- **F14 = (2)** "As (1), plus the Evoke effect resolved at current Focus."
- **F2–F12, F15, F16 = (1)**, the marked defaults.
- No implementation before Klee closes (R213; Furina's fold lands last, R220 B).

**COUNTERSIGNED (R224):** `F1` = (2), `F13` = (2), `F14` = (2), all others (1). Slot 6 stays. Nothing implements before Klee closes.

---

## D. Burst retirement — `M60`

**Item 23 — `K1`, *Sparks 'n' Splash*'s rarity.** (3) verbatim
(burst-retirement:556-562): "**Rare, once per combat — Exhaust on the body.**"
**GPT: (3), "Exhaust is sufficient only if recovery cannot trivially replay it".
Claude: (3) — DEFAULT**, with Exhaust as the concrete mechanism: no
Exhaust-recovery op exists anywhere in `tier0/` or the sheets, so the caveat is
moot today and should be re-checked if one is ever added.
**Flag for you:** (3) is a packet-added third shape, not one of the two you
named ("either let Klee's go to Uncommon or make it stronger as a Rare payoff").

**RULED (R224): (3)**, the gate being **Exhaust on the body**.

**Item 24 — `K2`, the five feeds.** (1) verbatim (burst-retirement:597-599):
"**The table as written** … Two retire, Catalytic Converter and Blazing Delight
convert to Sparks at derived rates, Durin retires under LAW:145."
**GPT: (1). Claude: (1) — DEFAULT.** Durin retires under either LAW:145 reading.

**RULED (R224): (1).**

**Item 25 — `KO1`, the Kokomi fold shape.** (1) = (a) verbatim
(burst-retirement:687-691): "**(a) State length paid from Charge** … It keeps
R74's ruling intact (the card is still a state, not a nuke)".
**GPT: (1). Claude: (1) — DEFAULT.**

**RULED (R224): (1) = (a).**

**Item 26 — `KO1a`, the payment rule.** (1) verbatim (burst-retirement:845-852):
"**Fixed N, printed on the card.** *'Spend 4 Charge. Gain Ceremonial Garment
(2 turns).'* … Least agency, least to explain, zero new UI, and it is the only
option where a co-op seat needs no prompt."
**GPT: (1), on "avoid another modal, preserve Charge for potency". Claude: (1) —
DEFAULT, on the zero-new-UI and co-op-no-prompt grounds.** The potency argument
rules out (2), not (3); (3) is the modal, and that is what (1) beats.

**RULED (R224): (1)**, on the zero-new-UI and co-op-no-prompt grounds.

**Item 27 — `KO2`, the Casket link.** See item 4. **(1), fold into `KO1`** —
which under item 25's (a) means re-key the refresh to an immediate extra pulse.
Recorded as the conditional, not as a flat "immediate pulse".

**RULED (R224): (1)**, and under item 25's (a) that is the **immediate extra pulse**.

**Item 28 — the other seven LAW blocks and the architecture paragraph.**
***GPT did not address these.*** §6 item 1 returns "§3.1's architecture paragraph
and the **eight** replacement blocks in §3.2" — LAW:64, :72, :145, :176, :248,
:266, :481, :508. GPT ruled only LAW:145 (item 21); R224 also signed LAW:176
with the Garment ruling, leaving **six**.
Options: **(1) countersign all eight blocks and the architecture paragraph AS
PROSPECTIVE**, same standing as reframe §3.1, implemented last — **DEFAULT** /
(2) LAW:145 only now, the other seven return next sitting.

**COUNTERSIGNED (R224): (1).** §3.1's architecture paragraph and **all eight**
§3.2 replacement blocks — LAW:64, :72, :145, :176, :248, :266, :481, :508 —
are countersigned AS PROSPECTIVE, the same standing as reframe §3.1,
implemented last. LAW:145 is the tightened sentence; LAW:176 names Ceremonial
Garment loot. **Nothing in `LAW.md` moves today.**

---

## E. Process

**Item 29 — the local Qwen seat.** Nothing to rule: it stays shadow until
`EB-211`/`EB-212` land positive cost evidence and matched-intent pairs, which is
already law (R222 B + R223).

**Nothing to rule (R224).**

**Item 30 — the decision queue itself.** GPT's largest process worry is that ten
granular rulings have made you the bottleneck. This packet is the answer: one
countersign closes all of the above together, as R224 (R206/R212).

**Nothing to rule (R224).** R224 is that answer: one countersign, one slate.

---

## E2. Opened by R224 itself — one new pick

**Item 31 — Prune, illegal under the new LAW:145 text.** ***ADDED 2026-08-30,
after the countersign.*** R224 C countersigns LAW:145's replacement as
tightened: *"Companion cards may not themselves grant signature resources. A
character-owned engine may respond to a Companion play and generate its resource
where that character's kit explicitly declares the trigger and bounds the amount
generated per Companion play."* **Prune — Little Witch's Hunt**
(`docs/mondstadt-companions.yaml:102-103`) grants Sparks off her own face — one
on the body, a second on a successful Swirl — and does not Exhaust. She is a
Companion granting a signature resource herself, so she is **illegal under the
clause as signed**, and the clause is prospective, so nothing is broken today.
Options, **no default marked**: **(1)** re-author the grant as a declared
Klee-engine response — her face stops granting, Klee's kit declares the trigger
and bounds the amount per Companion play; **(2)** strike the Spark line from her
face and leave the Swirl body; **(3)** retire her.
**RULED (R224): (2), re-author her.** [USER]: *"Let's try to re-author her. As
a Personal Companion, she should tie in directly with Klee's engine."* Her
face stops granting Sparks; the grant becomes a declared KLEE-ENGINE response,
with the kit declaring the trigger — a Personal Companion's Swirl on a Pyro
aura — and bounding the amount per Companion play, which is exactly the shape
the signed clause legalises. Engineering: `EB-219`, gated ahead of the
mixed-pool fight so that fight is not run on an illegal companion.

---

## F. No-ops and things already taken — nothing to answer here

- **Powder Keg's repair is already taken** as hygiene: (i) place-then-modify, the
  **+12** version, "matches the sentence the candidate was written to make". GPT
  endorses the shipped state, not an open pick. The **+9** branch remains
  available to you at a stroke. (GPT's "+9 risks a dead top rung" is not the
  packet's reason, though it is consistent with §14.4 condition 4.)
- **Minefield** ("don't develop; price at 3 if retained") — a **no-op under
  (5)**: Minefield lives only inside option (2), which item 15 does not take.
- **Powder Trail** ("reject") — a **no-op**: it appears in no numbered option,
  only as an alternative body inside (1)/(2). The identity objection is right and
  is already the packet's (it is a statline amendment under the identity rail).
- **"Run the amended mixed-pool proof before creating another card"** — a
  **no-op**: §14.4's five conditions are already ADOPTED as instrument conditions
  under R212.
- **Kurage's Oath 3/5** — not a pick. It stays as you ruled it, a placeholder
  with no measurement attached. It carries one debt (item under §G).
- **Slot 6** (Furina) — keep it, but it is an instruction to Claude, not an `M59`
  answer, and it is not countersignable until `F1`–`F16` land.
- **The two-slot model server** — GPT's caution is moot; it merged as #194 and is
  on `main` (`cdc2d68`). GPT read `main` before that merge.

---

## G. What the countersign does

**Rows that CLOSED (R224):** `M47`, `M49`, `M50` (all four picks), `M52`, `M54`,
`M55`, `M56`, `M57`, `M59`, `M60` (`K1`/`K2`/`KO1`/`KO1a`/`KO2`), `M64`.
**Ceremonial Garment's ACQUISITION is separately RULED (R224): LOOT** —
draftable Rare, `kit_card: true` dropped, `requires: burst_energy_full` gone,
and the v1.9 kit-grant machinery DELETED outright rather than retained for her.
Prospective like the rest; the sheet edit rides `EB-199`.
**Rows that STAYED OPEN:** none. Items 16, 19, 20, 28 and 31 never had rows and
are ruled here; their engineering is `EB-214`, `EB-218` and `EB-219`.

**LAW:** nothing moved in `LAW.md`. Reframe §3.1, the burst packet's §3.1
architecture paragraph and **all eight** of its §3.2 blocks — LAW:145 as
tightened and LAW:176 naming every signature payoff LOOT among them — are
countersigned **as PROSPECTIVE text** (R213). They bind the folds when the
folds land, and `LAW.md` is edited then, not now. LAW:145 as signed is what
opened **item 31**, Prune.

**Register rows minted on landing (R224):**
- **`EB-213`** — the prototype surface has **no upgrade channel**, so the
  substituted Kurage's Oath cannot be upgraded at a campfire; any rerun that
  wants to see the upgraded 5 needs that fixed first.
- **`EB-214`** — print Rule 1 as the Muster keyword and re-run the memory gate
  on `KURAGEMEM002` (`M54`).
- **`EB-215`** — the prototype surface's per-row `description:` channel,
  emitted by `gen_prototype_cards.py`, and the loc merge deleted (`M57`).
- **`EB-216`** — a per-turn wire snapshot in every future blind-run record
  (`M56`); nothing re-graded.
- **`EB-217`** — delete `KURAGE_MEMORY_KEYWORD_NEEDS_SUMMON` and its branch
  (`M50` pick 4).
- **`EB-218`** — migrate the three hybrids to Spark-only behind
  `SPARK_ALT_COST_ENABLED` (item 16).
- **`EB-219`** — re-author Prune under the signed LAW:145 (item 31).
- **Not minted, deliberately:** the Bake-Kurage row's retirement and its
  upgrade delta, and the five `K1`/`K2`/`KO1`/`KO1a`/`KO2` shapes, attach to
  the EXISTING Burst-fold rows `EB-199` / `EB-200`; `EB-208` carries its ruled
  fix shape in place. Held items mint nothing.
- **`M16`** got a hygiene re-point, not an answer: item 2 drops its premise from
  four end-of-turn sources to three (and the reachable maximum from three to
  two). It stays OPEN.

**What RUNS next, in order:**
1. `EB-208`'s **(a) live-count preflight** — cheap, closes the acceptance line.
2. The **§14 hybrid migration to Spark-only** behind `SPARK_ALT_COST_ENABLED`
   (`EB-218`), and **Prune's re-author** (`EB-219`) before it, so the fight is
   not run on a companion illegal under the signed LAW:145.
3. The **mixed-pool whole fight**, with Rummage folded into the deck — its
   §14.4 condition 5 is satisfied: `M64` is answered, and a round that can
   ADVANCE an arm gets Codex on every board.
4. `M54`'s **rerun on `KURAGEMEM002`** (`EB-214`), carrying `M55`'s line at the
   head of the pile view — both ride the same rerun.
5. **Slow Burn's whole fight** — HELD by item 19 until `M60`/`EB-199` settle
   what its Burst rider becomes.
6. **Furina: nothing**, until Klee closes.

**Stays OPEN, untouched:** `M45`(6) and (7), `S4-G6`, `S4-G14`, `M16`, and the
older rows above section 6 of the QUEUE.

---

## Appendix — the audit, fact-checked

RIGHT = call and reason both stand. PARTLY = call stands, reason corrected.
WRONG = the claim fails.

### Kokomi

| GPT's claim | verdict | evidence |
|---|---|---|
| M49(1) closes an obsolete term | RIGHT | memory packet:414-418; QUEUE.md:79 |
| Bake-Kurage is an unreachable Basic | RIGHT | memory packet:1366-1369 |
| Upgrade delta retires with the row | RIGHT | memory packet:1380-1383 |
| Casket answered by KO2(1) | PARTLY — conditional on KO1 | burst-retirement:703-705; QUEUE.md:80 |
| The no-jellyfish state no longer exists | RIGHT (cost omitted) | memory packet:1471-1478 |
| M54: 0/10 turns, 0/6 Musters | RIGHT | memory packet:2234-2239 |
| M54 "tooltip" as a surface | PARTLY — option (1) names face or keyword | QUEUE.md:82 |
| M55(5) "the persistent Charge display — the bar's list" | WRONG — that surface is retired | memory packet:2337-2341, 2392-2400, 2823-2860 |
| M56: published grades stand | RIGHT | memory packet:2265-2272 |
| M56(2) is a rival to (1) | PARTLY — it rides along free on M54's rerun | QUEUE.md:84 |
| M57: "risky boot-time merge" | PARTLY — EB-194 closed, merge moved | BACKLOG.md:105; memory packet:1930-1940 |
| "Rerun once" as a closing step | PARTLY — the rerun is M55's precondition | QUEUE.md:82 |
| Oath stays provisional 3/5 | PARTLY — right, but not an open pick | memory packet:1386-1391, 1411-1413 |
| (unmentioned) Oath has no upgrade channel | debt | memory packet:1432, 1458-1459 |
| M60 as a whole, M45(6)/(7), S4-G6, S4-G14, M16, the two rowless questions | unaddressed | QUEUE.md:32-33, 48, 64, 87; memory packet:1183-1196 |

### Klee

| GPT's claim | verdict | evidence |
|---|---|---|
| EB-208: (c) guarded by (a) | RIGHT (restates its own prior column) | sparks:2235-2247, 2251-2258 |
| P4 (a) | RIGHT | sparks:2265-2273 |
| Next gate (e) | RIGHT | sparks:2288-2298; §13.1 erratum |
| §14 option (5) exists and says migrate-first | RIGHT | sparks:3009-3011 |
| "(5) means Spark-only" | PARTLY — (5) leaves the branch open | sparks:3016-3021 |
| "under the prototype flag" | addition, and a good one | sparks:3031-3033; BACKLOG EB-205:112 |
| "Run the amended proof first" | RIGHT but already binding | sparks:3062-3066, 3080-3084 |
| Price-3 rung → prototype Powder Keg alone | RIGHT, verbatim inside (5) | sparks:3026-3029 |
| Powder Keg +12 | PARTLY — already taken; GPT's reason is not the packet's | sparks:2681-2691 |
| Minefield: don't develop, price 3 | RIGHT, and a no-op under (5) | sparks:2588-2591 |
| Powder Trail: reject | RIGHT, and a no-op | sparks:2856-2867; klee-character-design.md:15 |
| M47(1)'s "modal UI and LAW cost" | WRONG — that is option (2)'s cost | slice1:602-608 |
| M47(1) saves the per-mode work | WRONG — owed regardless, to shipped `deep_breath` | furina-cards.yaml:707-713 |
| The mixed pool answers the same question | WRONG — no Bomb-placement sink in it | slice1:577-580, 599; sparks:2450-2453 |
| M64(1) "costs model calls, not human QA" | PARTLY — ~7 calls vs the standing 3 | QUEUE.md:88 |
| M64: author-disjointness is real | RIGHT | sparks:2342-2352; understudy/seat.py:275-279 |
| Rummage/Slow Burn contaminate §14 | PARTLY — they are not in the eight rows | prototype-surface.yaml:459, 525 vs 757-861 |
| Rummage is a different question from §14 | WRONG — same hybrid price shape | slice1:184-186 |
| Slow Burn's fight can run now | WRONG — its Burst rider is being deleted | BACKLOG.md:107, EB-199 |
| Drain out of §14 | RIGHT (mechanism backwards: hoarding follows R220 F's unbounded bank) | burst-retirement:571-575; LAW.md:481 |

### Furina and M60

| GPT's claim | verdict | evidence |
|---|---|---|
| Every F-pick has a default | RIGHT — always (1) | reframe:738, 1543 |
| F1(2) verbatim | RIGHT | reframe:833-835 |
| F1's reason: the scaling hole is still open | WRONG — amendment 4 answers it | reframe:1574-1580; surviving ground at :806-808 |
| F13(2) / F14(2) | RIGHT | reframe:1163-1164, 1172-1173, 1585-1588 |
| Keep Slot 6 | RIGHT, but not an M59 answer | reframe:1355-1386, 1376-1377 |
| No implementation before Klee closes | RIGHT | reframe:621, 687-693; burst:449 |
| M52 §3.1 is prospective | RIGHT (signs amendments 3 and 4) | reframe:634-637, 695-715 |
| LAW:145 (b) | RIGHT, but the tightening is unaddressed | burst:332-345, 350-353 |
| K1(3) once per combat | RIGHT; no Exhaust-recovery exists to check | burst:556-580 |
| K1(3) is one of [USER]'s two shapes | WRONG — a packet-added third | burst:45-46 |
| K2(1) | RIGHT | burst:597-599 |
| KO1(1) = (a) | RIGHT | burst:687-691 |
| KO1a: potency argument | PARTLY — rules out (2), not (3) | burst:859-867 |
| KO2(1) = immediate pulse | PARTLY — a consequence of KO1(a), not the label | burst:703-706 |
| The other seven §3.2 LAW blocks + architecture paragraph | unaddressed | burst:820-825 |
| Two-slot server not on main | WRONG as of today — merged #194 | `main` cdc2d68 |

---

# The R225 slate — the open items (2026-08-30)

> **R225 LANDED on 2026-08-30** — the commit is named by hash in
> `docs/current/RULINGS.md`. Six items went to [USER] as one numbered list with
> GPT's column beside Claude's, and all six are ruled. `M65` and `M66` are
> CLOSED and have left `QUEUE.md`; `EB-224` and `EB-225` are minted in
> `BACKLOG.md`; the written top-level-cost clause is amended; `EB-220` gets its
> glyphs now. **NOTHING REMAINS OPEN ON THIS SLATE.**

> **What this was.** A relayed outside review of the open items, fact-checked
> against the registers and the packets, then drafted as one slate under R206
> (one batch per sitting) and R212 (Claude drafts, [USER] countersigns). Each
> item below carries the same four fields the R224 slate used: what was asked,
> **GPT**'s column, **Claude**'s column, and the **RULED** line.

**Corrections landed.** Two errata went in ahead of the slate, at `f729c09`,
under R101b — grades stand, records are struck rather than rewritten: the
shipped automatic Spark rule was **not** live on either `+proto` build
(`SparkPower.BaseRuleActive` is the compile-time constant `false` under
`PROTOTYPE_CARDS`), so §16.4 contamination 1 and its three back-references are
struck; and the strict Rare Power does not "stay [USER]'s" — §11.7 pick 3 was
answered by R222 (a), so the whole-fight read it ordered is owed WORK and is
now `EB-223`.

**Where the review was right:** the two errata, `W2`'s deck ratio (two Spark
makers against eleven Spark-priced sinks), `#207`'s diagnosis of the two
lifecycle throws, every EB id it cited and its scope, `EB-182`'s three
behaviours (unaffordable mode omitted, fully priced-out card refused with a
reason, the face declaring the price), and the two badge colours being
cyan-adjacent. **Where it was wrong or half right:** "each fold behind its own
flag" is not R220's text (it is `STATE.md` and the burst packet, and it governs
the three unbuilt Burst folds); `EB-195` was filed under Furina and is the
roster-wide twelve-arm re-baseline; `EB-211` is the wrong surface for a wire
instrument, being a tester-form schema; and "two decks said the same thing" is
a paraphrase — the packet says *"twice over now, on two different decks"*, and
both decks were granted, neither drafted.

---

## Item 1 — §16.11 pick 1: what the generator null buys next

The `W1'` MISS says the null is the GENERATOR's and not the sink set's, and
that no re-price of §4.2's table may be taken off the run. Options at
`review/active/klee-sparks-2026-08-29.md` §16.11: (a) generation first;
(b) re-run the same registration as the funded batch; (c) re-price the migrated
twins down; (d) stage a board instead.

**GPT: (a), narrowed** — the granted deck is not a drafted economy, so read the
decision more narrowly and make the next evidence a representative deck.
**Claude: (a).** The decision text was registered before the run and fires as
written; the deck composition was published card for card and contamination 4
discloses the grant, so the narrowing is a reading of the same registered
facts, not a defect in the registration.

**RULED (R225): (a′)** — (a) with GPT's qualification adopted. `W2`'s granted
deck ran **2 makers : 11 sinks**, both starter rows granted a second time, so
the run shows THAT deck could not fund its pool, not yet that the one-for-one
draft economy needs more generation. The generation question still goes first:
no new sink row, no re-price. The next evidence is a **representative deck, NOT
a `W2` re-grant** — first `EB-205`'s sim-drafted arm, which costs no game time
and is now the next Klee read ahead of every sink question, then a live
controlled-ratio deck under a fresh registration Claude drafts. The `W1'`
decision text stands as registered (R101b); this is its reading.

## Item 2 — §16.11 pick 2: the batch that was funded and did not happen

Two whole-fight sessions in a row ended at the first frame after the first
fight. Options: (a) file and fix the driver defect first; (b) re-write the
batch unit as one fight repeated on N seeds; (c) accept single fights.

**GPT: (a)**, and it names the mechanism correctly — a teardown throwing on a
seatless combat, then a null VFX container. **Claude: (a)**, already done.

**RULED (R225): (a).** `EB-221` and `EB-222` are the two throws and both are
fixed; the soak reads `fights=3 defects=0` on `0.2.1608+proto.dirty`. The batch
re-run waits on item 1's deck composition.

## Item 3 — §16.11 pick 3: the Spark-arithmetic candidate, a second time

Options: (a) stage Board C and settle it once; (b) treat round 2's out-of-slot
finding as settling the class and record nothing further; (c) instrument it on
the wire.

**GPT: (b) plus a four-field ledger** — before / price paid / gains by source /
after — hosted on `EB-211`. **Claude: (b)**, and the ledger is instrument work,
but `EB-211` is a tester-form schema and pick 3(c) asked the WIRE for a delta;
the right host is `EB-216`'s per-turn wire snapshot.

**RULED (R225): (b).** Round 2's out-of-slot replay settles the class — bank 3
→ Fwoosh! → Bang Bang! at exactly 2 → 0 with no Bomb — and `W2`'s instance is
the starter relic Pounding Surprise's +1 per detonation. Nothing further is
recorded on the arm. The four-field ledger is adopted as INSTRUMENT work folded
into `EB-216`; it does not gate the arm, and it is not `EB-211`.

## Item 4 — `M65`: Bag of Tricks and the top-level-cost clause

The doctrine seat returned REQUIRES_MODIFICATION with D4 resolved and one
clause left: the written top-level-cost rule must admit a mode price. Options:
(1) amend the clause; (2) drop the arm.

**GPT: (1), narrowly** — admit card-top-level costs and mode-head costs, not
arbitrary nested conditional spends. **Claude: no default** — (1) is
LAW-adjacent and (2) a design direction, both on [USER]'s side of R212. The
fact-check adds that GPT's narrow rule is word for word the engine rule
`EB-182` already shipped at `OPERATIONS.md:373-383`, so the amendment is
text-only.

**RULED (R225): (1), narrowly.** A Spark spend may sit at the card's TOP LEVEL
or at the HEAD of a `choose_one` MODE; nothing nested or conditional. The
clause is amended where it is written — the `powder_charge` comment in
`docs/klee-cards.yaml` and the Spark-cost preface in
`docs/prototype-surface.yaml`. **No `LAW.md` line moved**, because the rule was
never in `LAW.md`. `M65` CLOSES and leaves `QUEUE.md`. The arm proceeds as
**`EB-224`**: build it on `EB-182`'s mode-price machinery, stage, grade,
replay, whole fight — sequenced after `EB-205`'s read.

## Item 5 — `EB-220`: Encore and Charge glyphs

Neither meter owns a glyph anywhere in the mod: the badge draws a coloured
number and hides the icon, and the two colours are ~13° of hue apart.

**GPT: draw glyphs** — the two numbers are hard to tell apart at a glance.
**Claude: agreed**, and it is an art pick, so R212's ladder applies: Claude
ships rank 1, [USER] vetoes on sight.

**RULED (R225): glyphs for both meters, NOW**, under R212's art ladder. They
are being built in parallel on branch `eb220-meter-glyphs`. `EB-220`'s next
action is the glyphs landing there, then eyes-on at the next dev deploy; the
row does NOT close.

## Item 6 — `M66`: the C# prototype gate shape

One `PROTOTYPE_CARDS` symbol gates three `Compile Remove` directories while the
sim has per-fold flags, and cross-arm breakage has been witnessed twice
(`EB-194`, `EB-221`). Options: (1) split per arm; (2) one switch plus a
scope-and-seat lint, soak per deploy; (3) one switch, soak only.

**GPT: split the switch** — Kokomi UI code was present strongly enough to kill
a Klee run. **Claude: (2).** Splitting would revise Furina §6.1's countersigned
flag plan, which deliberately keys `FURINA_REFRAME` to the same symbol; and
`d217b4f` shipped the actual repair, which is a scope-and-seat guard, not a
compile gate.

**RULED (R225): (2).** The single `PROTOTYPE_CARDS` switch stands. The lint is
**`EB-225`**: every prototype Harmony patch is character-scoped and
seat-guarded, the `d217b4f` shape generalised, seen to FAIL first. The
three-fight soak runs on every dev deploy, written into `OPERATIONS.md` beside
`deploy_proto.ps1`. `M66` CLOSES and leaves `QUEUE.md`.

---

## What the countersign did

`M65` and `M66` left `QUEUE.md`; §6 of that register now carries no open row.
`EB-224` and `EB-225` are minted; `EB-205` becomes the next Klee read;
`EB-216` gains the four-field ledger clause; `EB-220` gains its glyph action.
The top-level-cost clause is amended in the two sheets that state it. The
ceilings moved with the rows: `R_CEILING` 224 → 225, the EB ceiling 223 → 225,
the M ceiling unchanged at 66.

---

# The R226 slate — the three whole-project forks (2026-08-30)

> **R226 LANDED on 2026-08-30** — the commit is named by hash in
> `docs/current/RULINGS.md`. Three forks went to [USER] as one numbered list
> under R206 (one batch per sitting) and R212 (Claude drafts, [USER]
> countersigns), and all three are ruled. **NOTHING REMAINS OPEN ON THIS
> SLATE.** No `EB` or `M` id is minted here; the only ceiling that moved is
> `R_CEILING` 225 → 226.

> **What this was.** A read-only inventory of every decision that returns to
> [USER] between now and SHIPPED found five class-A blocking design picks and
> one un-countersigned LAW slate. Three of them could be settled without a
> single new run, and they are the three below. The other two — Klee `PICK 1`
> and `M45` — wait on evidence and on table time, and the "return with
> evidence" list at the foot of this section is the whole remaining surface.

| item | options | RULED (R226) |
|---|---|---|
| **A — the Kokomi Charge LAW slate** (`review/active/kokomi-kurage-memory-2026-08-29.md` §4 deltas i–iv, amended by §11.7 v3). Amend `LAW.md`'s "Charge is never spent"; rewrite `docs/kokomi-cards.yaml`'s R80 standing-law header; add v3's two new clauses; the Companion-exclusion rotation clause and the relic-face edit | (1) sign the §4 slate as written; (2) **sign as amended by §11.7 v3**; (3) hold it until the flag flips | **(2), SIGN AS AMENDED — applied now as PROSPECTIVE text (R213).** The Charge bullet takes v3's wording (accrued at 1 per Exhaust of one of her own cards, **Companions included**, Status and Curse excluded); the R80 yaml header is rewritten to state the new rule, name the one door — the kit's threshold fire — and keep the anti-convergence clause (no card prints a Charge cost, no card reads the bank proportionally); and **two NEW LAW clauses** join the same section: the memory can hold one of her own non-Companion cards, and a memory copy is removed from combat and is **not** an Exhaust. **§4(iii)'s Companion-exclusion rotation clause and §4(iv)'s relic-face edit are NOT applied — v3 withdrew both**, so the accrual funnel does not narrow and `PearlOfWisdomRelic`'s printed face does not change. Every line carries an explicit PROSPECTIVE marker and binds when `C.KURAGE_MEMORY` flips; until the flip the shipped rule is still "Charge is never spent". `SHEET_DIGEST` moved on the comment edit and is re-pinned in `tier0/constants.py` as a **comment-only change, no `CONSTANTS_VERSION` bump** (R225 precedent) |
| **B — Klee `PICK 2`**, how many free Spark generators survive by rarity (`review/active/klee-sparks-2026-08-29.md` §3, `:206-221`). Klee is `0/6/5/5` against Regent's `1/4/4/2` | (1) **match Regent exactly**; (2) match at Basic + Common only; (3) keep all sixteen, add sinks only; (4) go past Regent to `1/3/3/1` | **(1), match Regent** — `Basic 1 / Common 4 / Uncommon 4 / Rare 2`, converting 2 Commons, 1 Uncommon and 3 Rares. **One condition:** *unless* `EB-205`'s sim-drafted arm (`KLEESPARK-S1`) shows a drafted-deck maker:sink ratio that CONTRADICTS Regent's shape — in which case PICK 2 **RETURNS to [USER] with that number** and is **not** re-decided by Claude. The shipped-pool edit takes Regent's shape at the `SPARK_ALT_COST_ENABLED` flip, not before. `PICK 1` is untouched and still first (R225) |
| **C — Furina's Spotlight**, deliberately unresolved with no options drafted (`review/active/furina-reframe-2026-08-29.md` §3 `:600-607`, §9.3) | (1) draft an options packet now; (2) **draft it when Klee closes, or close enough to be worth doing in parallel**; (3) leave the hole named and unowned | **(2). An options packet is OWED and it is Claude's to draft** under the R212 ladder — a numbered pick list, never a blank to fill. **Claude judges the timing**: when Klee closes, or gets close enough that drafting in parallel is worth doing. **The pick itself stays [USER]'s.** The packet is the correct home for `M45`(4) and must carry the two modes, `SPOTLIGHT_BASE_MULT = 1.5`, the selector card, the relic and the 18 `spotlight` sheet rows. Appended as a dated line on a design packet — no strike, the original paragraph stands |

## What returns to [USER] with evidence — the whole remaining decision surface

Nothing below is open tonight. Each row names what has to exist before it can
be asked, so the morning reader can see the surface in one place.

1. **Klee `PICK 1` — the starter-kit generator** (`klee-sparks:183-204`).
   REOPENS only if `EB-205`'s sim-drafted read or a live controlled-ratio deck
   (`W3`) shows the income is short. R225 put it ahead of every sink question:
   no new sink row and no re-price until income is answered.
2. **`M45`'s five surviving calls** — (1) `salon_rotate`, (2)
   `smoke_and_sparks`, (4) the Spotlight selector, (5) `depths_judgment`,
   (6) the Kokomi Block cluster. They return **after `S4-G14`**, the
   confirmatory protocol run; item (4) migrates into item C's packet.
3. **Slot 6's outcome fork** (`furina-reframe:1355-1386`). Returns ONLY if the
   Furina slice's staged round CONFIRMS that a full-stage deploy's free Evoke
   dominates a dedicated Evoke card — then `F7` and §4.2's full-stage rule
   return together. Falsification retires it.
4. **`S4-G6`, Kokomi's HP stability band.** **Claude drafts it** DRAFTED from
   written design intent under R212(2), batch-countersigned — sequenced AFTER
   the Kokomi fold lands and BEFORE `S4-G14` grades it, because her HP moved
   70 → 80 and her kit is mid-re-authoring.
5. **`S4-G14`, the confirmatory protocol run.** Table time, [USER]'s only —
   nothing else can discharge it, and it gates `M45` entirely.

---

## R227 — RULED 2026-08-30 (the post-merge review)

> **R227 LANDED on 2026-08-30 — all four picks at option (1).** The commit is
> named by hash in `docs/current/RULINGS.md`. The four picks went to [USER] as
> one numbered list under R206 (one batch per sitting) and R212 (Claude drafts,
> [USER] countersigns), and the countersign is a BATCH one under R212 — signed
> once, never re-signed. The **GPT seat independently recommended 1 / 1 / 1 /
> 1** before the countersign landed, and [USER] signed the agreed column.
> **NOTHING REMAINS OPEN ON THIS SLATE.** `R_CEILING` moves 226 → 227 in the
> landing commit, which is the precedent in `tools/lint_r_numbers.py`: the
> ceiling moves with the ruling, not with the draft.

**What this was.** After `#211` merged, [USER] relayed GPT's post-merge review
of the three character redesigns. Claude fact-checked it against `main` at
`21a078c4` in two read-only passes. GPT was right on every material claim, and
three of its framings were adjusted rather than disputed. First, its pointer at
`STATE.md:659` names a paraphrase: the "income is not the governor" sentence is
`EXPERIMENTS.md`'s `KLEESPARK-S1` row, not `STATE.md`'s. Second, "test slot 6
early" was not the Furina packet's instruction — the packet had no ordering
note at all. It does now. Third, its "retrospective process acknowledgment" is
not a signature class this house has: R212 item 2 locates pre-registration in
committing the slate before the run, not in when the countersign lands, so the
`KURAGEMEM002` cell is countersigned normally like any other. One thing GPT did
not see, because it is in R226's own text and not in the registers: R226 put a
condition on Klee `PICK 2`, and `S1` read the number the condition names. It is
pick 2 below.

### What Claude shipped under the ladder, no ask

- **Register hygiene, on this branch.** `STATE.md` said `eb214-muster-keyword`
  and `eb216-wire-snapshot` were not on `main`; both landed in `#211`. A sweep
  for other stale unmerged claims found none.
- **`EB-233` and `EB-234` minted.** `EB-233` is the `KLEESPARK-S1` `S3` miss
  that both the registration and the result routed to BACKLOG and that reached
  no row. `EB-234` is the memory-cadence question the kurage packet defers at
  `:2225` and that was registered nowhere.
- **`EB-230` is fixed, on this branch.** Not one face but seventeen: every
  Bomb face rode an attack-family var that resolves against Strength and Weak,
  while the body banks the literal. Each now prints its own amount; the lock
  was seen to FAIL on all seventeen first. `W4` runs on a build carrying it.
- **`W4` now carries an `EB-230` build note**, disclosed before the run: if the
  run happens on a build without the fix, the face/body disagreement is a
  pre-declared confound on the tester's play and not a Spark quantity.
- **Furina slot 6 is staged first**, with a dated ordering line in the packet.
- **`M67` option (3) is retired** as dead — the rerun it waited on has graded.
- **`EB-220`'s next action** now says what is actually left: the eyes-on.

### What returns to [USER]

**PICK 1 — `M67`, the consequence of R226 on Kokomi slice 2.** R226 answered
how Charge accrues. What it did not answer is what happens to the four slice-2
arms, all of which price Charge and so contradict the architecture that was
signed.

1. **It stands** — the four priced arms and their round-2 boards delete, the
   plumbing stays, and the Charge question moves whole to the memory program
   (`EB-229`, then whole fights). **Default**, and GPT's recommendation. The
   two ADVANCED arms showed that priced-card shapes can create decisions; they
   did not show that Charge should become a generic card currency. **← RULED
   (R227).**
2. **A carve-out for the priced arms.** This is a LAW amendment, and it is
   [USER]'s alone.

**RULED (R227): (1), M67 STANDS.** Slice 2 is RETIRED: the four Charge-priced
arms and their round-2 boards delete, the spend plumbing stays, and the Charge
question moves whole to the memory program — `EB-229` first, then whole fights.
`M67` CLOSES and leaves `QUEUE.md`.

**PICK 2 — Klee `PICK 2` returns with its number, as R226 said it must.** R226
matched Klee's generator count to Regent's `1/4/4/2` with one condition: if
`KLEESPARK-S1` read a drafted-deck maker-to-sink ratio that CONTRADICTS
Regent's shape, the pick returns to [USER] with that number and is not
re-decided by Claude. `S4` MISSED. The floor-15 median came in at **1.500** —
three makers to two sinks — and it **ROSE** from floor 5 rather than falling as
the registered arithmetic predicted. That is the shape the condition names, and
whether it contradicts Regent is [USER]'s reading, not Claude's — so the number
is returned rather than read past.

1. **Regent stands, `1/4/4/2`.** **Default.** The 1.500 is what the sim's
   drafter TAKES once a generator is finally worth 4.00 to it — that is, the
   pool is generator-rich, which is the reason to cut to Regent's shape and not
   an argument against it. The exact cut set — two Commons, one Uncommon and
   three Rares, of which `true_spark_knight` is already re-authored — gets
   named in the fold packet with `W4` in hand. **← RULED (R227).**
2. **The ratio contradicts the shape** — keep one more generator, `1/4/4/3` or
   `1/5/4/2`, decided at the fold.
3. **Name the cuts now** — Claude returns a card-by-card list next.

**One criticism accepted, and recorded because it is right:** option (2) as
drafted did not follow from a generator-rich ratio, and a more complete
alternative list would have offered *"hold the exact distribution pending
`EB-233`"* or *"cut further"* rather than only the softer cut. Noted; it is
moot under (1), and the exact cuts are named in the fold packet with `W4` in
hand.

**RULED (R227): (1), REGENT STANDS — `1/4/4/2`.** The 1.500 is returned and
read, not read past: its sink denominator is contaminated by `EB-233`, so it is
evidence for neither a softer nor a deeper cut. The card-by-card cut set is
named at the fold, with `W4` in hand.

**PICK 3 — the batch countersign** (R212, signed once). Three registrations
wait on one signature: `KOKOMI-SLICE1-WF`; `KLEESPARK-W4`, now carrying the
`EB-230` build note above; and `KURAGEMEM002` §13.9, which has already run —
its registration is legitimate because it was committed before the run, which
is where R212 locates pre-registration.

1. **Countersign all three.** **Default.** **← RULED (R227).**
2. **Veto any by name.**

**RULED (R227): (1), all three COUNTERSIGNED**, in one batch under R212 —
signed once, and none re-signed. Each packet carries its marker:
`KOKOMI-SLICE1-WF`, `KLEESPARK-W4` (with the `EB-230` build note standing) and
`KURAGEMEM002` §13.9.

**PICK 4 — when the Furina paper work starts.** R226 left this timing to
Claude. Claude's judgment is that it is close enough now: Klee's remaining
gates are one Codex batch (`W4`, whose start gate is satisfied today) and
`EB-224`'s staging, and the Furina work costs no game time and no Codex. What
would start: filling the five blank predictions from R224's `F`-picks, adding a
co-op rotation slot (the ally-rotates-the-queue cost named at reframe `:622`
has no slot today), and drafting the Spotlight options packet — the home for
`M45`(4), carrying the two modes, `SPOTLIGHT_BASE_MULT = 1.5`, the selector
card, the relic and the 18 spotlight rows. No implementation.

1. **Proceed now, in parallel with `W4`.** **Default.** **← RULED (R227).**
2. **Hold until `W4` reads.**

**RULED (R227): (1), PROCEED NOW.** The Furina paper work starts 2026-08-30, in
parallel with `W4`: the five blank predictions, the co-op rotation slot and the
Spotlight options packet. **No implementation** — no code, no constant, no
sheet row.

### Not a pick — an instruction

`EB-220`'s eyes-on is owed on the installed `0.2.1627+proto.dirty` build, which
already carries the glyphs. Judge the silhouettes on real card faces and on the
mode-selection surfaces at small size — especially whether Encore and Charge
stay apart there — not on the contact sheet.

Next free ids as of this landing: R228 / `EB-235` / `M68`.

## R228 — RULED 2026-08-30 (after `KLEESPARK-W4`)

> **R228 LANDED on 2026-08-30 — pick 1 at options 4 + 6, pick 2 at option
> (1).** The commit is named by hash in `docs/current/RULINGS.md`. The two
> picks went to [USER] as one numbered list under R206 (one batch per sitting)
> and R212 (Claude drafts, [USER] countersigns), and the countersign is a BATCH
> one under R212: signed once, never re-signed, and **[USER]'s five-day veto
> runs from this landing** — any item of it may be struck inside that window
> and nothing here is beyond recall until it closes. The **GPT seat
> independently agreed on both picks** before the countersign landed, so the
> column [USER] signed is an agreed one. **NOTHING REMAINS OPEN ON THIS
> SLATE.** `R_CEILING` moves 227 → 228 in the landing commit, which is the
> precedent in `tools/lint_r_numbers.py`: the ceiling moves with the ruling,
> not with the draft. One id is minted, `EB-235`; `M68` CLOSES and leaves
> `QUEUE.md`.

**What this was.** `KLEESPARK-W4` ran the same day R227 countersigned it: six
fights, 80 of 80 funded actions, 86 of 90 Codex calls, seed `KGU5LKM77PB5`, on
`0.2.1676+proto.dirty` with the `EB-230` fix aboard. The strict Rare Power was
drawn on seven pages and played on none, so its three slots are UNREACHED —
not a pass, not a fail, and no row opens on it. The sink slot `W1'''` MISSED on
a denominator of 50: the two-destination choice was posed zero times, against
`W3`'s one in nineteen. §19.4 registered that a MISS on thirty or more pages
returns to [USER] as a pick between §14.3's options, and that is pick 1.

**PICK 1 — the sink set, now that the longer batch has reported.** The
evidence to weigh: the deck's one damage sink (Ka-pow!) reached the hand once
in fifty pages; an affordable non-damage sink sat in hand on nine; income was
never the binding constraint; two batches at derived ratios now read 1-in-19
and 0-in-50. The tester's unprompted line on the Power — *"sat dead while
costing too much tempo"* — is R217 G feedback, not a grade, and it waits for
the fold.

1. **Re-author two** (§14.3 option 1): `Fwoosh!` → Behind the Barrel and
   `Firework Finale` → Powder Keg, building the missing price-1 and price-3
   rungs.
2. **The alternative pair** (§14.3 option 2): `Bang Bang!` → Slow Fuse and its
   partner.
3. **One only** (§14.3 option 3): `Fwoosh!` → Behind the Barrel.
4. **Keep the set as built** (§14.3 option 4) — read the zero as a DRAW fact:
   one damage rung in a five-sink deck, in hand once in fifty. **Claude's
   recommendation, taken together with option 6.** **← RULED (R228).**
5. **Change the deck, not the set:** re-derive the granted deck with more than
   one damage rung and re-pose `W1'''` under a fresh registration — another
   batch of game time and ~90 Codex calls.
6. **Stop buying batch length on this question:** bank the two readings and
   let the sink set be settled by the fold's mixed-pool work, where the drafter
   composes the deck instead of a grant. **Claude's recommendation, with
   option 4.** The two are compatible: 4 is the set, 6 is whether more live
   pages are bought for it. **← RULED (R228).**

Why 4 + 6: both batches granted a deck a rule derived, and both put exactly
one damage rung in it; a decision that needs two destinations in hand cannot
be posed by a deck that draws its second destination once in fifty. Income is
settled on both engines, so the open question is composition, and composition
is what the fold's drafted mixed-pool read measures directly. What would
change it: if [USER] wants the live reading anyway, option 5 is the clean
version and its cost is stated.

**RULED (R228): 4 + 6 — recorded as two orthogonal decisions, the SET and the
EXPERIMENT BUDGET, and in GPT's wording, which Claude accepted.** The current
sink set is kept as the **FOLD BASELINE** — not "proven fine", merely not
condemned by an artificial granted deck. **No third granted-deck batch is
bought.** The `EB-233`-corrected drafted mixed-pool read **may RETURN the set**
if natural decks still fail to present multiple destinations, and that return
is the registered way this decision reopens.

**And GPT's correction of the draft's "DRAW fact" framing is taken, because it
is right.** The W4 record (Klee packet §20.4 item 2) reads: *"Two or more
distinct Spark-priced titles were in hand on **four** of the 50 combat pages,
all four in the last fight and all four at a printed bank of **0**."* So the
null is **composition AND timing** — the bank and the sink draw never aligned
on one page — and that is a real composition/cadence finding rather than luck.
The set is baselined on a finding, not on a shrug.

**PICK 2 — `M68`, the Spotlight pick R226 owed.** The options packet is
`review/active/furina-spotlight-options-2026-08-30.md`; the register row
carries the five options and the default. This pick can wait for a sitting
when the packet has been read; it gates nothing before the reframe's own
whole-fight read.

1. **One mode, priced** — Center Stage retires, Guest Cast and the 1.5 stay,
   the selector aims a Companion and costs Encore. **Claude's recommendation
   (the packet's §4).** **← RULED (R228).**
2. **Ship as-is.**
3. **Fold into Fanfare** — one scaling number.
4. **Fold into the stage** — the front member is the spotlit one.
5. **Retire Spotlight entirely.**

**RULED (R228): (1), ONE MODE, PRICED.** Center Stage retires, Guest Cast and
`SPOTLIGHT_BASE_MULT = 1.5` stay, and the selector aims a Companion and costs
Encore. The packet's own biggest risk — a **third consumer of Encore**, beside
the deferred Block and the Evoke price — is **tested, not assumed away**: the
new slate slot is staged as a MATCHED PAIR against slot 2, and **the price
returns with evidence** if Encore proves over-subscribed. `M68` CLOSES and
`M45`(4) is answered with it. **Nothing migrates before the reframe's own
whole-fight read** — the M68 row's gate stands as a sequencing fact even though
the row leaves the register. The packet's option numbering was normalized to
the register's before this was recorded, which was GPT's stated precondition.

**Not a pick — owed work Claude carries:** the Rare Power's whole-fight read
(R222 (a)) is UNREACHED and is carried by **`EB-235`**, minted by this ruling —
a NEW registration with an uptake slot, not `W4` repeated, sequenced after
`EB-224`'s staging and riding the fold's mixed-pool read; `EB-224`'s staging
(Bag of Tricks) is next at game time; `EB-229`'s forecast field precedes any
memory rerun.

Next free ids as of this landing: R229 / `EB-236` / `M69`.

## R229 — DRAFT slate (2026-08-30, after `KLEESPARK-BT1`)

> **R229 is a DRAFT and nothing on it is ruled.** It carries ONE pick, drafted
> by Claude under R212 and committed before it goes to [USER], who countersigns
> in batch or vetoes. `R_CEILING` does NOT move on a draft — it moves with the
> ruling, as R228 did. Three ids are minted by the round itself and not by this
> slate: `EB-236`, `EB-237` and `EB-238`, all three engineering rather than
> decisions. **Nothing here re-prices anything**: the round registered in
> advance that no result of its own licenses a price change, and it produced no
> MISS to argue from.

**What this was.** `KLEESPARK-BT1` staged *Bag of Tricks* — the first Klee row
whose price sits at the head of a mode rather than at the top of the card — on
four boards that held everything constant but the Spark bank and one card in
hand. It cost 5 Codex calls of a cap of 15 and moved the five-hour meter from
40% to 48%. Two slots came in PREDICTED: `P1`, the priced mode taken on the
board where three Bombs plainly pay, with the reader naming the cheaper option
it declined; and `P5`, the mode-head price proven live on the wire — both mode
texts offered at a bank of 3, the bank exactly 0 after the play, and three
Bombs at 5 on the target. Three slots came in UNREACHED, because 7 of the 8
forms refused the boards for `intent_insensitive`: every board gave one enemy
on a fixed telegraph, 3 Energy and at most two Energy-costed cards, so the
whole hand was always playable and the telegraph never forced a trade. **The
round's RETURN is therefore on the INSTRUMENT and not on the arm's design**, and
the instrument's three repairs are minted as `EB-236` (a board-design rule for
resource rounds), `EB-237` (`slot_plan` cannot see a mode-head price) and
`EB-238` (the blind page prints no relics).

**The finding that returns.** The priced mode's own price came back inside the
same turn. On `t01` the bank read 3, then 0 when the mode was taken, then **3
again** after Quick Fuse — because Klee's starter relic *Pounding Surprise*
pays +1 Spark for every Bomb detonated, and the mode places exactly three. So
on any turn that also holds a detonator, "spend 3 Sparks for 3 Bombs" is
Spark-NEUTRAL, and the decision the mode exists to create — *one Bomb now, or
three bought out of the bank* — quietly becomes *do I hold a detonator*. This
is **not a defect**: the relic behaves exactly as built, and it changes no
grade (`P5` reads the bank immediately after the play, when it was 0). It is a
design question about what a Spark price MEANS on Bomb placement under the
starter relic, and that kind of call is [USER]'s.

**PICK 1 — what to do about the refund.** The options are the packet's §22.6,
unchanged:

1. **Accept the refund and re-pose the round with it PRINTED.** Say in the
   arm's own terms that the mode is net-free whenever its Bombs detonate the
   same turn, put the relic on the record, and re-run a repaired staged round
   whose boards separate *"the Bombs detonate now"* from *"the Bombs sit until
   next turn"*. Cheapest option; changes no number. **Claude's recommendation.**
2. **Re-pose the round WITHOUT the starter relic**, if the harness can suppress
   it, so the price is measured before the refund. The more honest instrument
   and the less honest world.
3. **Treat the refund as the finding and go to the whole fight unchanged.** The
   refund is a real property of a real deck; let a whole fight say whether a
   self-refunding price is a decision, and skip a second staged round.
4. **Hold the arm.** Bank `P1` and `P5` and let the mode-head machinery's live
   proof wait for other consumers, since two of the four boards were refused by
   their own construction.

Why (1): the relic is the shipped world and every Bomb line in Klee's kit lives
with it, so the instrument should SHOW it (`EB-238`) rather than hide it; the
repaired boards (`EB-236`) are owed anyway before any deciding form can
survive, so option 1 buys them on work already owed; and the whole fight cannot
isolate one-versus-three on its own — it is the step AFTER (1), not instead of
it. What would change it: if [USER] reads a self-refunding price as a design
FAULT rather than a fact of the world, then (2) or (4) is the honest choice and
the price itself returns to design. **No price change is licensed by this round
either way — that was registered before it ran.**

Next free ids as of this draft: R230 / `EB-239` / `M69`.
