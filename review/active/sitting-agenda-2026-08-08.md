# Sitting agenda — 2026-08-08

> **Lifecycle: ACTIVE — a walking order, not a register.** `docs/current/QUEUE.md`
> stays the single source of truth: every item below **points at** its row and
> restates nothing. This packet decides nothing, recommends nothing on any
> design / taste / money call, and invents no number. Where a number appears it
> is quoted from the file named beside it, stamped as that file stamps it.

**What this is.** One consolidated sitting: all 35 QUEUE rows plus one
BACKLOG-side countersign (`EB-17p`), sorted into what can close today and what
cannot, in an order that keeps related material in front of the pen at once.
World stamp for every fresh number quoted here: **`RT9/D14/P6/C8`**, taken
**after** the two instrument fixes `EB-57` (reaction amp counter re-settled as
realized uplift) and `EB-58` (aura-uptime intervals bounded at target death).

**State of play, in one paragraph.** The three rows whose percentages sat two
declared-uncomparable stamp boundaries back were re-read this morning under the
current world, same n, same seed, same recipe
(`review/active/sitting-reads-2026-08-08.md`). **All three structures HELD** —
Furina's salon still separates from spotlight and fanfare with non-overlapping
intervals and fanfare still overlaps the `real_silent` floor; all three Kokomi
plans still sit below `real_ironclad` with assist below even `real_silent`; and
Kokomi's meter-20 act-1 band membership is intact, with priest having moved
*into* the band. Magnitudes fell across the boundary, and the two reference
anchors fell with them. **One ordering fact MOVED:** `furina/salon` was
**above** `real_ironclad` on the prior table (11.20% vs 8.53%) and is now
**below** it (4.70% [4.00, 5.52] vs 6.13% [5.33, 7.05]), non-overlapping —
the whole Furina table fell under the current world. Nothing else about any
row's structural claim changed.

**Counts:** 33 items ready to close · 3 explicitly out.

---

## A. Ready now

### A1 — measurement-backed calls (take these while the fresh numbers are open)

The fence runs `S4-G5`/`B-G1` first; `S4-G7` is fenced behind it.

| # | Row | The call, in one line | Attached / ready | What moved |
|---|---|---|---|---|
| 1 | `S4-G5` / `B-G1` | **Dispose** the Fanfare axis per-axis, and as part of the same call **identify which of A1–A7 "Fanfare axis" names** — it was never written down. | Charter at `git show pre-simplification-2026-08-06:docs/axis-validity-session-charter.md` §4/§7 | — |
| 2 | `S4-G7` | **Choose** three viable Furina plans or one (dead-archetype). | Fresh twelve-arm read → `review/active/sitting-reads-2026-08-08.md` §1; prior table `docs/current/roster/roster-anchor-v14-v6-2026-08-06.md` | Structure HELD. Salon **4.70%** [4.00, 5.52] / spotlight **1.50%** [1.12, 2.00] / fanfare **1.30%** [0.95, 1.77]; fanfare still overlaps `real_silent` **1.37%** [1.01, 1.85]. **Salon now sits below `real_ironclad` 6.13% [5.33, 7.05]**, where it sat above at 11.20%. |
| 3 | `S4-G13` | **Pick one of three levers** for Kokomi below the Ironclad-anchored floor (Code may build any, may pull none); plus the `NT-G5` weak-or-fine fork, whose only accepted input is `OT-1`. | `review/active/sitting-reads-2026-08-08.md` §2 | Structure HELD. Priest **1.10%** [0.78, 1.54] / commander **2.20%** [1.73, 2.79] / assist **0.57%** [0.35, 0.91] vs `real_ironclad` 6.13% [5.33, 7.05]; assist still below `real_silent` with no overlap. Act-1 clear within a point of the prior read on all three (42.20 / 51.83 / 35.37). The `NT-G5` half still waits on `OT-1` — see §B. |
| 4 | `M9` | **Ratify** the meter-20 number on the 500-run confirm; **keep or drop** the commander Garment-uptime watch. | `review/active/sitting-reads-2026-08-08.md` §3; source report `git show pre-simplification-2026-08-06:docs/archive/kokomi-v0.4-report.md` §6 | Structure HELD; priest moved **into** the 35–50% band (30% → 41%), commander 50% at the upper edge, every run winrate ≤ 2.2%. The §2.3 "vs W1 baseline" comparator is **dead and was not replaced** — rebuilding it, or meter 15, means editing ratified `kokomi.yaml` `burst_max`, a content change; so §6 ask 2's named trade cannot be re-read. Ratifying against this read is one-sided; whether that matters is the call. |
| 5 | `M5` | **Amend or accept** A6's debuff term (the only live line; (a) and (c) are closed by the descriptive-only axis ruling; zero balance consequence either way). | Read in the row itself (`A6v2 · RT9/D14/P6/C8 · fights=1000 · seed=20260719`) | Row's own FRESH READ: archetype median 3.5 vs declared 4.2, recomposed (salon 3.53 / spotlight 4.19 / fanfare 3.24); the debuff term reads 0.05–0.46 across all three archetype decks against a 0.90 anchor baseline. Call is unblocked. |
| 6 | `Q-C` (+ the `EB-63` gate question) | **Ratify** the payoff rubric, **aim** each roster archetype high/medium/low inside the census bands (that aim IS the Q-C answer), and **state** direction + threshold for Q-A/Q-B — all one predictions commit, before any sprint number is read. | `review/active/payoff-census-2026-08-08.md` (its §7 states the three asks); `review/active/payoff-reach-reregistration.md` §5 | §7 asks exactly three things: (1) rubric §2 RATIFY/AMEND/REJECT incl. the open **LOOSE or STRICT** P2/P3 attribution choice; (2) CONFIRM or OVERRULE the §6.1 second-hand-mention exclusion; (3) whether **`EB-63`** gates the aim, or the aim is taken against a floor while 60% of payoff-shaped cards sit unattributed. |

### A2 — countersigns

| # | Row | The call | Attached / ready |
|---|---|---|---|
| 7 | `EB-17p` (BACKLOG row, sitting item) | **Countersign** the force-first-copy paired-winrate pre-registration, and settle the three things it leaves open: the filler negative-control card, `N` + cost ceiling, and the per-card predictions (their own commit, before any run). | `review/active/eb17p-registration-draft-2026-08-08.md`; BACKLOG row `EB-17p`. Mechanism settled as deck-injection at run start, so **the R121 `DRAFTER 14` pin does not gate it**. |
| 8 | `N + O countersign` | **Countersign** the PROVISIONAL reactions-corpus banner (the R101-precedent countersign) — the only open item left in the two audit ledgers. | `git show pre-simplification-2026-08-06:docs/archive/lore-fidelity-audit-2026-08-05.md`, `…/instrument-redteam-2026-08-05.md`; the top-5 triage ran as `EB-51` and landed `EB-57`–`EB-62` plus the titles row below. |

### A3 — paste-ready ratifications

| # | Row | The call | Attached / ready |
|---|---|---|---|
| 9 | `EB-22` | **RATIFY, REVISE, or DROP** the 15-card Kokomi pool fill (assist-weighted, 10 of 15; machine-checked; paste-ready), plus the four sub-decisions the brief names. | `git show pre-simplification-2026-08-06:docs/archive/brief-kokomi-pool-fill.md` |
| 10 | `EB-26` | **RATIFY, REVISE, or DROP** `watch_of_the_shallows` (uncommon 1-cost `prevent_exhaust_ward` 3, complete upgrade delta, green on every sheet gate against a shadow tree, **not** in the live sheet), plus the seven sub-decisions. | `review/active/eb26-lesser-ward-draft.md` §5 — D1 name, D2 `max_stacks`, D3 magnitude/cost, D4 lane, D5 tempo band, D6 upgrade slope, D7 scope (re-baseline now or wait; and P3 is still unowned and out of `EB-26`'s scope as written). |

### A4 — money

| # | Row | The call | Attached / ready |
|---|---|---|---|
| 11 | `EB-42q` | **Approve or decline $379** for a Spine licence, vs staying on Path C layered and investing in Godot-native rigs. | `tools/skeleton2d_spike/PROBE-2026-08-08.md` — the one remaining unknown, live-combat seating, answered **YES on every leg** (seats as creature Visuals, RIDs valid under the real renderer, AnimationTree drives the bones, deformation reaches the screen, no engine error). Nothing engineering-side is left to wait for; **the probe rig is spike-grade art and proves nothing about how the motion looks** — that half is the call. |
| 12 | `X10` | **Price at a sitting** the `gorou_heart_of_the_clan` power adjustment (a CANDIDATE, explicitly not ratified; the 81-hit exploit lines target the power, not the price). | `git show pre-simplification-2026-08-06:docs/dockets/companion-pricing.md` §1 and `…/review/redteam/exploit-ledger.md` X10 |
| 13 | `S4-G10` | The companion-shop close-out session: **grade** `SHOP-P1…P3`, **settle** whether money is ever the constraint, the 1.15× surcharge, Track A pool migration, R60 phase-2 fantasy-leak grading; plus the slot-2 rarity-floor agenda item and the §7.7 Track D fallback taste check (merged from `M11`). | R60/R63; principles §4.7 via the row's provenance. The slot-2 item wants empirical Common offer/pick/skip data, which the row records as wanted, not held. |

### A5 — taste batches

| # | Row | The call | Attached / ready |
|---|---|---|---|
| 14 | `N3 + N4 titles` | **Rename to canon, ratify as authored flavor, or amend case by case** — six shipped companion titles naming talents Genshin does not have. | All six re-verified still shipped 2026-08-08 in `docs/{fontaine,mondstadt,inazuma}-companions.yaml` + the matching `Cards/Generated/*.cs` title strings. A rename moves ids, art keys and the C# roster. |
| 15 | `S4-G11` | **Read by eye** card names and lore text before they ship (R29d Furina pass; Kokomi's 20 authored-but-unaudited fill cards; kickoff ask 10) — ruled to have no substitute. | `git show pre-simplification-2026-08-06:tier0/DECISIONS.md` entry 75 + R29d |
| 16 | `S4-G12` / `CC-G1` / `CC-G2` | Art contact-sheet **eyes-on** (re-hunt candidates incl. `grand_gala`; confirm the `standing_room_only` crop by eye) + in-game screenshot review of the twelve Curtain Call cards and the A0 smoke run. | Frozen bundle `git show pre-simplification-2026-08-06:docs/archive/g12-review-2026-08-05.md`. Sheet regenerated in the primary checkout: `art/contact_sheet_eb54_s4g12.html`, 6 gate rows / 25 candidates (`review/active/art-runs-2026-08-08.md` §3). **The `standing_room_only` overturn has been executed** (`Opera Epiclese.png` is now its r1), which is what left `grand_gala` pickless. |
| 17 | `S4-G17` | **Taste** on three running-game looks: `AS2-D5` salon look, `AS2-B5` motion/facing; `AS2-E2` icon picks are canonical in the Art-debt row. | Captures staged, manifest `review/active/livegame-captures-2026-08-08.md` §1 (B5) and §2 (D5), files under gitignored `art/eb52_captures/`, package `0.2-589`. Row's own status: urgency reduced. |
| 18 | `S4-G20` | A bundle: **accept or fix** the sim-vs-C# salon RNG divergence; one taste pass (`kaboom == spark_knight_style`); two infra toggles (branch protection / `gh`; manifest MAJOR bump, dormant by design). | Row + backlog §1 P3-cluster / §5 |
| 19 | `S8 + S10 galleries` | **Taste**: eight flagged potions/relics (S8); RESKIN or REDESIGN per enemy (S10). | `docs/current/dossiers/content/potion-relic-conversion-gallery.md`, `docs/current/dossiers/remap/reskin-gallery.md` (both HEAD) |
| 20 | `Art debt` | The art **picks** — Kokomi 58 faces + 15 companions, `AS2-E2` icons, `grand_gala`, the two `ART-L12` duplicate pairs, the three EB-36 placeholder cards, `curtain_cue`, `breathless`, A7 + six Curtain Call power sigils. **Plus one one-line answer:** which **three** re-hunt rows the `S4-G12` row means (the frozen bundle describes four REHUNT rows plus the overturn; the sheet shows all six rather than guessing). | `review/active/art-runs-2026-08-08.md` — `EB-54` **production DONE**: 37 candidate rows, 34 new PNGs, all **rank ≥ 2 by design** so nothing shipped a pick; per-run contact sheets named there, gitignored in the primary checkout. Facts the packet records: promoting any of the seven power sigils to rank 1 must delete its `$pckDeferred` entry in the same change or S12 fails; `docs/art-claimed-sources.tsv` reads **271 claimed / 180 free**, and three of eight runs had to reach outside the free Furina pool to produce a third candidate; the "four missing Kokomi portraits" phrase is **stale, not blocked** (`art_coverage` bills Kokomi 61/61, Inazuma 15/15). |
| 21 | `M8` | Three Kokomi card-art rulings: crop-reuse budget (a number or eyes-on per card); whether Watatsumi/shrine environment art counts as a card face; whether to hand-crop the banned `Character Details 1` for a Rare. | `docs/current/art/kokomi-art-pass-requirements.md` §6 (HEAD) |
| 22 | `M10` | Four Fontaine Rares close-out items: companion art picks (Navia/Clorinde/Neuvillette/Arlecchino), the v1.7 lore/naming eyes-on audit (non-delegable), the C2 grading countersign, close-out ratification; Neuvillette's "different facet" question is parked with them. | Row + `git show aa09b97:docs/current/backlog/missed-requirements.md` §4.4 |

### A6 — structural rulings

| # | Row | The call | Attached / ready |
|---|---|---|---|
| 23 | `S4-G6` | **Declare** Kokomi's HP stability band from design intent, before her confirmatory playtest (may not be revised against it). | `DEC-D5` clauses 2–4; re-anchored to the post-rework build. Gates her protocol playtest. |
| 24 | `S4-G18` | **Choose one of three branches** for the 15–20 archetype-size band: restore to LAW / restore amended (e.g. to the shipped 28/22/14) / rule it was never law — the branch choice and the count question are one call. | Row (counts recounted 2026-08-07: 28/22/14) |
| 25 | `S4-G19` | **Say whether** the two near-duplicate mechanics become one (Sly unification). | `git show pre-simplification-2026-08-06:docs/archive/tech-debt-audit-2026-07-26.md` §5 |
| 26 | `M7` | **BUILD the Enchant op or leave the fields as-is.** | `docs/current/dossiers/content/event-conversion-gallery.md` (HEAD) carries the live `FLAG — [USER] decision needed` on Stone of All Time; R82 settled only the data-model half. |
| 27 | `M12(a)` | **Rule which statement governs** convergence-cell membership — three, four, or two — before the cell is built. | `docs/furina-cards.yaml:127` / `:391–395` / `:647`; the 2026-08-06 Class-P re-attestation marked it DOWNGRADED TO DOUBT and never closed it. Sub-item (b) was found PREMISE FALSE and needs nothing. |
| 28 | `M13` | **Rule** `ROUTE_REGRET_MARGIN` (and `draft_regret`'s `+ 1.0`) — or pre-register a measurement for it. | `tier05/run_metrics.py` margin note (HEAD); until then only the margin-free reads are quotable. |
| 29 | `shared_billing` | **Rule "no cost upgrades on Commons" into LAW** (the card then needs a re-price) **or allow the card as-is**. | Row: the cited "delta-grammar convention" exists in no LAW and nowhere at the tag beyond the row itself; both engines ship the card. |
| 30 | `EB-27p` | The **personal-pool companion design sweep** — settle for the class how personal-pool cards enter a run, ahead of any single placement. | Row lists the three inherited options (fixed floor / act-1 event pool / run-start Neow-shaped offer), the dead "node 2" anchor, and the LAW-vs-spec tension. Nothing built pending the sweep. |
| 31 | klee-rework `X7` | Rule the Klee spark-economy violations (`skip_and_hop`, `sparkly_treasure`, `crackle`) on R109's disjunction. | dockets/klee-rework |
| 32 | klee-rework `X8` | **Promote, re-price, or accept** the two non-exhaust **Common** bomb-damage writers (`chain_fuse`, `careful_arrangement`) — the audit ran and falsified the "fine at higher rarity" premise. | dockets/klee-rework §3 |
| 33 | kokomi-workshop `X9` | The Kokomi charge bank ("probably too strong, parse carefully") — the next kit workshop. | dockets/kokomi-workshop |

---

## B. Not ready / explicitly out

| Row | Why it cannot close at this sitting |
|---|---|
| `S4-G14` / `OT-1` | Table time, and blocked on **both** `S4-G6` (item 23 above) and the `EB-53` N1 attribution pass. The pass's two engineering legs are built but **not run-verified**, so the playtest is not yet askable. |
| `EB-53` capture review (BACKLOG-side) | **The captures have NOT been taken.** `review/active/n1-kokomi-burst-legs-2026-08-08.md` §6 lists nine owed captures (C1–C9) and states the game was owned by another agent this session, so `deploy.ps1` was never invoked and the game was never launched. The build+deploy+`godot.log` verification steps are owed first. Its **R89 half** (Furina summon damage numbers) is also out: it is a countersign with **no draft in HEAD** to countersign — the BACKLOG row names "the R89 draft" without a path. Its Klee bomb-variety leg is rework-scoped design and untouched by design. |
| `M1` | Furina charter co-op Fanfare mechanic, BUILD or WAIVE. No packet was assembled for it this pass, and the row records that the chartered mechanism cites the `encore_gained` leg Track A deleted in both engines — a BUILD is a re-specification, not an implementation. |
| `M2` | Second charter co-op mechanic (pass a Spotlight between co-op players), BUILD or WAIVE. No packet assembled this pass. |

Also carried but not blocking anything below: `EB-52`(a), the fourth Fanfare
evidence shape, is still owed and its obstacle is **acquisition, not
instrumentation** (`docs/current/BACKLOG.md` `EB-52`). It is not a QUEUE row and
is not on this agenda.

---

## C. What unblocks on your signatures

- **`EB-17p` countersign** → the experiment's engineering prerequisites (packet §10) land, then the run executes. The `DRAFTER 14` pin does not gate it.
- **`Q-C` — ratify + confirm + the `EB-63` answer, then the aim** → R137's six-step order proceeds; `EB-63` (the extraction change that attributes the 24 unattributed payoff cards) either goes first or is deferred, and `EB-43`/D15 remains held as step (5), after blind-first grading.
- **`S4-G6`** → Kokomi's protocol playtest (`S4-G14`/`OT-1`) becomes askable *once `EB-53` also run-verifies*; `OT-1` is in turn the only accepted input to `S4-G13`'s `NT-G5` fork.
- **`EB-22` / `EB-26` ratify** → sheet edits (`docs/kokomi-cards.yaml`, `docs/kokomi-upgrades.yaml`), codegen, and whatever re-measurement `EB-26` D7(a) decides; both packets are paste-ready and neither is in the measured pool today.
- **`S4-G10` session** → consumes the `EB-33/34/35` repricing exhibits, which are filed as inputs awaiting that session.
- **`Art debt` + `S4-G12` picks** → `art_process` promotions to rank 1; the four `art_coverage` misses (`grand_gala`, `confiscated`, `spotlight_center_stage`, `spotlight_guest_cast`) can close, and each power-sigil promotion must delete its `$pckDeferred` entry in the same change.
- **`EB-42q`** → either a purchase, or Path C layered stays the shipped fallback and Godot-native rig work is what gets invested in.
- **`S4-G5`/`B-G1`** → unfences `S4-G7` and the fanfare STOP via the R107/F1 fence.
- **`N + O countersign`** → closes the last open item in both audit ledgers.
