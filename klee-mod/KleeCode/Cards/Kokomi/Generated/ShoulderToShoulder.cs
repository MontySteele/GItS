// <auto-generated>
//     Generated by tools/gen_roster_cards.py from docs/kokomi-cards.yaml.
//     DO NOT EDIT. Edits are lost on the next regen -- change the sheet instead.
//
//     Sheet entry: id=shoulder_to_shoulder rarity=common cost=1
//     Upgrade deltas come from docs/kokomi-upgrades.yaml; unexpressible deltas block the upgrade path.
// </auto-generated>

// Roslyn treats <auto-generated> files as outside the project's nullable
// context, so the ? annotations below need it re-enabled explicitly (CS8669).
#nullable enable

using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using BaseLib.Abstracts;
using Godot;
using KleeMod.Elements;
using KleeMod.Powers;
using KleeMod;
using KleeMod.Cards;
using MegaCrit.Sts2.Core.CardSelection;
using MegaCrit.Sts2.Core.Commands;
using MegaCrit.Sts2.Core.Entities.Cards;
using MegaCrit.Sts2.Core.GameActions.Multiplayer;
using MegaCrit.Sts2.Core.Localization.DynamicVars;
using MegaCrit.Sts2.Core.Models;
using MegaCrit.Sts2.Core.Models.Powers;
using MegaCrit.Sts2.Core.ValueProps;

namespace KleeMod.Cards.Kokomi.Generated;

public sealed class ShoulderToShoulder : CustomCardModel, ICharacterCard
{
    /// <summary>Roster identity used by character-aware mechanics such as Spotlight.</summary>
    public string CharacterId => "kokomi";

    public override Texture2D? CustomPortrait => RosterArt.CardPortrait("shoulder_to_shoulder");

    public override List<(string, string)>? Localization => new()
    {
        ("title", "Shoulder to Shoulder"),
        ("description", "[gold]Exhaust[/gold] 1 card from your hand. {IfUpgraded:show:Add a copy of a random [gold]Companion[/gold] card in your hand to your hand. The copy costs 0.|Add a copy of a random [gold]Companion[/gold] card in your hand to your hand.}"),
    };

    protected override IEnumerable<DynamicVar> CanonicalVars =>
        new List<DynamicVar>
        {

        };

    // autoAdd: false -- the character-aware roster pool owns membership.
    // Partially generated character sheets must never auto-register cards.
    public ShoulderToShoulder()
        : base(1, CardType.Skill, CardRarity.Common, TargetType.Self, autoAdd: false)
    {
    }

    protected override async Task OnPlay(PlayerChoiceContext choiceContext, CardPlay cardPlay)
    {
        {
            var toExhaust = (await CardSelectCmd.FromHand(
                choiceContext, Owner,
                new CardSelectorPrefs(
                    CardSelectorPrefs.ExhaustSelectionPrompt, 1),
                KitGrant.NotKitCard, this)).ToList();
            foreach (var victim in toExhaust)
            {
                await CardCmd.Exhaust(choiceContext, victim);
            }
        }
        {
            var companionsInHand = CardPile.Get(PileType.Hand, Owner)?
                .Cards.Where(c => c is ICompanionCard).ToList();
            if (companionsInHand != null && companionsInHand.Count > 0)
            {
                var pickedCompanion = Owner.RunState.Rng.CombatTargets.NextItem(companionsInHand);
                if (pickedCompanion != null)
                {
                    var copyToken = CombatState!.CreateCard(
                        ModelDb.GetById<CardModel>(pickedCompanion.Id), Owner);
                    if (IsUpgraded)
                    {
                        copyToken.EnergyCost.SetThisCombat(0);
                    }
                    await CardPileCmd.AddGeneratedCardToCombat(copyToken, PileType.Hand, Owner);
                }
            }
        }
    }

    protected override void OnUpgrade()
    {
        // copy_cost_override: expressed at play time as an IsUpgraded read in OnPlay; the text swaps via {IfUpgraded:show:...}.
    }
}
