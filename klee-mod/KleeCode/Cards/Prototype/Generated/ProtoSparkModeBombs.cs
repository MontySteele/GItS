// <auto-generated>
//     Generated by tools/gen_prototype_cards.py from docs/prototype-surface.yaml.
//     DO NOT EDIT. Edits are lost on the next regen -- change the sheet instead.
//
//     Sheet entry: id=proto_spark_mode_bombs rarity=uncommon cost=0
//     Upgrade deltas come from docs/klee-upgrades.yaml; unexpressible deltas block the upgrade path.
// </auto-generated>

// Roslyn treats <auto-generated> files as outside the project's nullable
// context, so the ? annotations below need it re-enabled explicitly (CS8669).
#nullable enable

using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using BaseLib.Abstracts;
using Godot;
using KleeMod.Elements;
using KleeMod.Powers;
using KleeMod;
using KleeMod.Cards;
using MegaCrit.Sts2.Core.CardSelection;
using MegaCrit.Sts2.Core.Commands;
using MegaCrit.Sts2.Core.Entities.Cards;
using MegaCrit.Sts2.Core.GameActions.Multiplayer;
using MegaCrit.Sts2.Core.HoverTips;
using MegaCrit.Sts2.Core.Localization.DynamicVars;
using MegaCrit.Sts2.Core.Models;
using MegaCrit.Sts2.Core.Models.Powers;
using MegaCrit.Sts2.Core.ValueProps;

namespace KleeMod.Cards.Prototype.Generated;

public sealed class ProtoSparkModeBombs : CustomCardModel, IModalCard
{
    protected override IEnumerable<IHoverTip> ExtraHoverTips =>
        ArmKeywordTips.ForSpark(KleeCardTooltips.ForCard(base.ExtraHoverTips, this, Element.None, includesBombRules: true), this);

    public override Texture2D? CustomPortrait => KleeArt.CardPortrait("proto_spark_mode_bombs");

    public override List<(string, string)>? Localization => new()
    {
        ("title", "Bag of Tricks"),
        ("description", "Choose one: Place 1 [gold]Bomb[/gold] dealing 5 | Spend 3 [gold]Sparks[/gold]: place 3 [gold]Bombs[/gold] dealing 5."),
    };

    // EB-184: what each mode does about AIMING, in sheet order.
    // The card's own TargetType is fixed before a mode is chosen (the
    // game aims first), so it answers for the card and not for the
    // play -- an Attack-typed modal declares AnyEnemy for the mode
    // that aims, and the bridge then demanded a target on the mode
    // that attacks nothing. These two rows are what it reads instead.
    public IReadOnlyList<string> ModeLabels =>
        new[] { "Place 1 [gold]Bomb[/gold] dealing 5", "Spend 3 [gold]Sparks[/gold]: place 3 [gold]Bombs[/gold] dealing 5" };

    public IReadOnlyList<bool> ModeAimsAtChosenEnemy =>
        new[] { true, true };

    // EB-182: what each mode PRINTS as its price, and the meter
    // to read it against. One declaration, read by the screen filter,
    // by the playability gate below and -- EB-220 -- by the mode
    // FACE's own cost badge, so the price offered, the price charged
    // and the price DRAWN cannot drift. `null` is an unpriced mode.
    // INTERNAL rather than private: the faces below read their own
    // row out of this table instead of carrying a second copy.
    internal static readonly ModePrice?[] ModePrices =
    {
        null,
        new ModePrice(Meter.Sparks, 3),
    };

    // The per-mode cost lines (EB-182): unplayable when NO mode is
    // affordable, and the unaffordable modes are not offered on the
    // choose-a-card screen either.
    protected override bool IsPlayable =>
        ModalChoice.AnyAffordable(Owner, ModePrices);

    protected override IEnumerable<DynamicVar> CanonicalVars =>
        new List<DynamicVar>
        {

        };

    // autoAdd: false -- the character-aware roster pool owns membership.
    // Partially generated character sheets must never auto-register cards.
    public ProtoSparkModeBombs()
        : base(0, CardType.Skill, CardRarity.Uncommon, TargetType.AnyEnemy, autoAdd: false)
    {
    }

    protected override async Task OnPlay(PlayerChoiceContext choiceContext, CardPlay cardPlay)
    {
        var modeOptions = new List<CardModel>
        {
            ModalChoice.CreateOption<ProtoSparkModeBombsModeA>(Owner),
            ModalChoice.CreateOption<ProtoSparkModeBombsModeB>(Owner),
        };
        var modeIndex = await ModalChoice.SelectAffordableMode(choiceContext, Owner, modeOptions, ModePrices);
        ModalChoice.RecordChoice(this, modeIndex, new[] { "Place 1 [gold]Bomb[/gold] dealing 5", "Spend 3 [gold]Sparks[/gold]: place 3 [gold]Bombs[/gold] dealing 5" }[modeIndex]);
        if (modeIndex == 0)
        {
            ArgumentNullException.ThrowIfNull(cardPlay.Target, "cardPlay.Target");
            await BombPower.Place(choiceContext, cardPlay.Target, 5, Owner.Creature, this);
        }
        else
        {
            if (!await SparkPower.Spend(choiceContext, Owner.Creature, 3, this)) return;
            for (var i = 0; i < 3; i++)
            {
                await BombPower.Place(choiceContext, cardPlay.Target, 5, Owner.Creature, this);
            }
        }
    }

    protected override void OnUpgrade()
    {
        // R24: NO upgrade path -- no ratified delta in klee-upgrades.yaml. Flagged in manifest.
    }
}

/// <summary>Mode 0 of proto_spark_mode_bombs. A face for the choose-a-card screen;
/// never played, never in a pile, never a reward -- but a POOL MEMBER, via
/// the generated ModalOptions roster the character's off-pool list carries.
/// EB-150: a card in no pool takes CardModel.Pool through MockCardPool, which
/// throws inside the screen's _Ready and soft-locks the turn.</summary>
public sealed class ProtoSparkModeBombsModeA : ModalOptionCard
{
    public override List<(string, string)>? Localization => new()
    {
        ("title", "Place 1 [gold]Bomb[/gold] dealing 5"),
        ("description", "Place 1 [gold]Bomb[/gold] dealing 5"),
    };
}

/// <summary>Mode 1 of proto_spark_mode_bombs. A face for the choose-a-card screen;
/// never played, never in a pile, never a reward -- but a POOL MEMBER, via
/// the generated ModalOptions roster the character's off-pool list carries.
/// EB-150: a card in no pool takes CardModel.Pool through MockCardPool, which
/// throws inside the screen's _Ready and soft-locks the turn.</summary>
public sealed class ProtoSparkModeBombsModeB : ModalOptionCard, IMeterPricedCard
{
    public override List<(string, string)>? Localization => new()
    {
        ("title", "Spend 3 [gold]Sparks[/gold]: place 3 [gold]Bombs[/gold] dealing 5"),
        ("description", "Spend 3 [gold]Sparks[/gold]: place 3 [gold]Bombs[/gold] dealing 5"),
    };

    /// <summary>EB-220: the price this MODE prints, read off
    /// the card's own ModePrices row so the badge, the filter and
    /// the gate share one literal.</summary>
    public Meter PricedMeter =>
        ProtoSparkModeBombs.ModePrices[1]!.Value.Meter;

    public int PrintedMeterPrice =>
        ProtoSparkModeBombs.ModePrices[1]!.Value.Amount;
}
