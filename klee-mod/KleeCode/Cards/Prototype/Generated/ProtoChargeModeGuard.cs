// <auto-generated>
//     Generated by tools/gen_prototype_cards.py from docs/prototype-surface.yaml.
//     DO NOT EDIT. Edits are lost on the next regen -- change the sheet instead.
//
//     Sheet entry: id=proto_charge_mode_guard rarity=uncommon cost=1
//     Upgrade deltas come from docs/kokomi-upgrades.yaml; unexpressible deltas block the upgrade path.
// </auto-generated>

// Roslyn treats <auto-generated> files as outside the project's nullable
// context, so the ? annotations below need it re-enabled explicitly (CS8669).
#nullable enable

using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using BaseLib.Abstracts;
using Godot;
using KleeMod.Elements;
using KleeMod.Powers;
using KleeMod;
using KleeMod.Cards;
using MegaCrit.Sts2.Core.CardSelection;
using MegaCrit.Sts2.Core.Commands;
using MegaCrit.Sts2.Core.Entities.Cards;
using MegaCrit.Sts2.Core.GameActions.Multiplayer;
using MegaCrit.Sts2.Core.HoverTips;
using MegaCrit.Sts2.Core.Localization.DynamicVars;
using MegaCrit.Sts2.Core.Models;
using MegaCrit.Sts2.Core.Models.Powers;
using MegaCrit.Sts2.Core.ValueProps;

namespace KleeMod.Cards.Prototype.Generated;

public sealed class ProtoChargeModeGuard : CustomCardModel, ICharacterCard
{
    /// <summary>Block arrives from a conditional row, so this card declares no
    /// BlockVar and BaseLib's auto-detect cannot see it (EB-84).</summary>
    public override bool GainsBlock => true;

    /// <summary>Roster identity used by character-aware mechanics such as Spotlight.</summary>
    public string CharacterId => "kokomi";

    protected override IEnumerable<IHoverTip> ExtraHoverTips =>
        KokomiRiderTips.ForCharge(base.ExtraHoverTips, this);

    public override Texture2D? CustomPortrait => RosterArt.CardPortrait("proto_charge_mode_guard");

    public override List<(string, string)>? Localization => new()
    {
        ("title", "Twin Tides"),
        ("description", "Choose one: Gain 5 Block | Spend 6 [gold]Charge[/gold]: gain 12 Block."),
    };

    // EB-182: what each mode PRINTS as its price, and the bank to
    // read it against. One declaration, read by the screen filter and
    // by the playability gate below, so the price offered and the
    // price charged cannot drift. `null` is an unpriced mode.
    private static readonly ModePrice?[] ModePrices =
    {
        null,
        new ModePrice("Charge", 6, p => KokomiResources.GetCharge(p.Creature)),
    };

    // The per-mode cost lines (EB-182): unplayable when NO mode is
    // affordable, and the unaffordable modes are not offered on the
    // choose-a-card screen either.
    protected override bool IsPlayable =>
        ModalChoice.AnyAffordable(Owner, ModePrices);

    protected override IEnumerable<DynamicVar> CanonicalVars =>
        new List<DynamicVar>
        {

        };

    // autoAdd: false -- the character-aware roster pool owns membership.
    // Partially generated character sheets must never auto-register cards.
    public ProtoChargeModeGuard()
        : base(1, CardType.Skill, CardRarity.Uncommon, TargetType.Self, autoAdd: false)
    {
    }

    protected override async Task OnPlay(PlayerChoiceContext choiceContext, CardPlay cardPlay)
    {
        var modeOptions = new List<CardModel>
        {
            ModalChoice.CreateOption<ProtoChargeModeGuardModeA>(Owner),
            ModalChoice.CreateOption<ProtoChargeModeGuardModeB>(Owner),
        };
        var modeIndex = await ModalChoice.SelectAffordableMode(choiceContext, Owner, modeOptions, ModePrices);
        ModalChoice.RecordChoice(this, modeIndex, new[] { "Gain 5 Block", "Spend 6 [gold]Charge[/gold]: gain 12 Block" }[modeIndex]);
        if (modeIndex == 0)
        {
            await CreatureCmd.GainBlock(Owner.Creature, new BlockVar(5m, ValueProp.Move), cardPlay);
        }
        else
        {
            if (!await KokomiResources.SpendCharge(choiceContext, Owner.Creature, 6, this)) return;
            await CreatureCmd.GainBlock(Owner.Creature, new BlockVar(12m, ValueProp.Move), cardPlay);
        }
    }

    protected override void OnUpgrade()
    {
        // R24: NO upgrade path -- no ratified delta in klee-upgrades.yaml. Flagged in manifest.
    }
}

/// <summary>Mode 0 of proto_charge_mode_guard. A face for the choose-a-card screen;
/// never played, never in a pile, never a reward -- but a POOL MEMBER, via
/// the generated ModalOptions roster the character's off-pool list carries.
/// EB-150: a card in no pool takes CardModel.Pool through MockCardPool, which
/// throws inside the screen's _Ready and soft-locks the turn.</summary>
public sealed class ProtoChargeModeGuardModeA : ModalOptionCard
{
    public override List<(string, string)>? Localization => new()
    {
        ("title", "Gain 5 Block"),
        ("description", "Gain 5 Block"),
    };
}

/// <summary>Mode 1 of proto_charge_mode_guard. A face for the choose-a-card screen;
/// never played, never in a pile, never a reward -- but a POOL MEMBER, via
/// the generated ModalOptions roster the character's off-pool list carries.
/// EB-150: a card in no pool takes CardModel.Pool through MockCardPool, which
/// throws inside the screen's _Ready and soft-locks the turn.</summary>
public sealed class ProtoChargeModeGuardModeB : ModalOptionCard
{
    public override List<(string, string)>? Localization => new()
    {
        ("title", "Spend 6 [gold]Charge[/gold]: gain 12 Block"),
        ("description", "Spend 6 [gold]Charge[/gold]: gain 12 Block"),
    };
}
