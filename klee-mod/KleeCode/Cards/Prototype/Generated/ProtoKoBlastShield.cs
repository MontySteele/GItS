// <auto-generated>
//     Generated by tools/gen_prototype_cards.py from docs/prototype-surface.yaml.
//     DO NOT EDIT. Edits are lost on the next regen -- change the sheet instead.
//
//     Sheet entry: id=proto_ko_blast_shield rarity=uncommon cost=0
//     Upgrade deltas come from docs/klee-upgrades.yaml; unexpressible deltas block the upgrade path.
// </auto-generated>

// Roslyn treats <auto-generated> files as outside the project's nullable
// context, so the ? annotations below need it re-enabled explicitly (CS8669).
#nullable enable

using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using BaseLib.Abstracts;
using Godot;
using KleeMod.Elements;
using KleeMod.Powers;
using KleeMod;
using KleeMod.Cards;
using MegaCrit.Sts2.Core.CardSelection;
using MegaCrit.Sts2.Core.Commands;
using MegaCrit.Sts2.Core.Entities.Cards;
using MegaCrit.Sts2.Core.GameActions.Multiplayer;
using MegaCrit.Sts2.Core.HoverTips;
using MegaCrit.Sts2.Core.Localization.DynamicVars;
using MegaCrit.Sts2.Core.Models;
using MegaCrit.Sts2.Core.Models.Powers;
using MegaCrit.Sts2.Core.ValueProps;

namespace KleeMod.Cards.Prototype.Generated;

public sealed class ProtoKoBlastShield : CustomCardModel, ISparkPricedCard
{
    protected override IEnumerable<IHoverTip> ExtraHoverTips =>
        ArmKeywordTips.ForSpark(base.ExtraHoverTips, this);

    public override Texture2D? CustomPortrait => KleeArt.CardPortrait("proto_ko_blast_shield");

    public override List<(string, string)>? Localization => new()
    {
        ("title", "Blast Shield"),
        ("description", "Gain {Block:diff()} [gold]Block[/gold]. Return this card to your hand."),
    };

    // `EB-730`, Blast Shield: the played card goes back to the HAND
    // instead of the discard pile. `GetResultLocationForCardPlay` is
    // the game's own seam for where a played card lands (see
    // `SparksNSplash` for the kit rule and the v0.111.0 port), and the
    // TOP is `KokomiPlan.Replay`'s position: the card handed back is
    // the one the player is looking at. It gains no Retain, so it
    // discards at end of turn like any card in hand. tier0 twin:
    // `klee_overhaul.mark_return_to_hand` read at `_finish_play`.
    protected override CardLocation GetResultLocationForCardPlay() =>
        new CardLocation(Owner, PileType.Hand, CardPilePosition.Top);

    // The Spark cost line (EB-118): unplayable below the price,
    // which is how the cost is shown rather than silently failing.
    // The printed price is declared ONCE here, on ISparkPricedCard,
    // and the gate reads it back through SparkCost.PriceOf -- the
    // same sum the Spark cost BADGE renders (PICK 8 option 2), so
    // the price shown and the price charged cannot drift. A card
    // that already prints a price is unaffected by the strict Rare
    // Power, which is why PriceOf returns this number unchanged
    // here (tier0 twin: combat.spark_price, sub-pick (a)).
    public int PrintedSparkPrice => 2;

    protected override bool IsPlayable =>
        SparkPower.CanSpend(Owner.Creature, SparkCost.PriceOf(this));

    protected override IEnumerable<DynamicVar> CanonicalVars =>
        new List<DynamicVar>
        {
            new BlockVar(6m, ValueProp.Move)
        };

    // autoAdd: false -- the character-aware roster pool owns membership.
    // Partially generated character sheets must never auto-register cards.
    public ProtoKoBlastShield()
        : base(0, CardType.Skill, CardRarity.Uncommon, TargetType.Self, autoAdd: false)
    {
    }

    protected override async Task OnPlay(PlayerChoiceContext choiceContext, CardPlay cardPlay)
    {
        await SparkPower.Spend(choiceContext, Owner.Creature, 2, this);
        await CreatureCmd.GainBlock(Owner.Creature, DynamicVars.Block, cardPlay);
    }

    protected override void OnUpgrade()
    {
        DynamicVars.Block.UpgradeValueBy(2m);
    }
}
