// <auto-generated>
//     Generated by tools/gen_prototype_cards.py from docs/prototype-surface.yaml.
//     DO NOT EDIT. Edits are lost on the next regen -- change the sheet instead.
//
//     Sheet entry: id=proto_itto_superlative_superstrength_either rarity=rare cost=2
//     Upgrade deltas come from docs/klee-upgrades.yaml; unexpressible deltas block the upgrade path.
// </auto-generated>

// Roslyn treats <auto-generated> files as outside the project's nullable
// context, so the ? annotations below need it re-enabled explicitly (CS8669).
#nullable enable

using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using BaseLib.Abstracts;
using Godot;
using KleeMod.Elements;
using KleeMod.Powers;
using KleeMod;
using KleeMod.Cards;
using MegaCrit.Sts2.Core.CardSelection;
using MegaCrit.Sts2.Core.Commands;
using MegaCrit.Sts2.Core.Entities.Cards;
using MegaCrit.Sts2.Core.GameActions.Multiplayer;
using MegaCrit.Sts2.Core.Localization.DynamicVars;
using MegaCrit.Sts2.Core.Models;
using MegaCrit.Sts2.Core.Models.Powers;
using MegaCrit.Sts2.Core.ValueProps;

namespace KleeMod.Cards.Prototype.Generated;

public sealed class ProtoIttoSuperlativeSuperstrengthEither : CustomCardModel, ICompanionCard, IModalCard
{
    /// <summary>Block arrives from a conditional row, so this card declares no
    /// BlockVar and BaseLib's auto-detect cannot see it (EB-84).</summary>
    public override bool GainsBlock => true;

    /// <summary>Companion identity (companion sheet): star drives the
    /// reward slot's rarity tier; PersonalPool gates per-character
    /// offers; Nation drives SAME_NATION_REWARD_SHARE weighting.</summary>
    public int Star => 5;

    public Element CompanionElement => Element.Geo;

    public string? PersonalPool => null;

    public string? Nation => "inazuma";

    public override Texture2D? CustomPortrait => KleeArt.CardPortrait("proto_itto_superlative_superstrength_either");

    public override List<(string, string)>? Localization => new()
    {
        ("title", "Itto - Oni Rush"),
        ("description", "Choose one: Deal 14 damage | Gain 6 Block."),
    };

    // EB-184: what each mode does about AIMING, in sheet order.
    // The card's own TargetType is fixed before a mode is chosen (the
    // game aims first), so it answers for the card and not for the
    // play -- an Attack-typed modal declares AnyEnemy for the mode
    // that aims, and the bridge then demanded a target on the mode
    // that attacks nothing. These two rows are what it reads instead.
    public IReadOnlyList<string> ModeLabels =>
        new[] { "Deal 14 damage", "Gain 6 Block" };

    public IReadOnlyList<bool> ModeAimsAtChosenEnemy =>
        new[] { true, false };

    protected override IEnumerable<DynamicVar> CanonicalVars =>
        new List<DynamicVar>
        {

        };

    // autoAdd: false -- the character-aware roster pool owns membership.
    // Partially generated character sheets must never auto-register cards.
    public ProtoIttoSuperlativeSuperstrengthEither()
        : base(2, CardType.Attack, CardRarity.Rare, TargetType.AnyEnemy, autoAdd: false)
    {
    }

    protected override async Task OnPlay(PlayerChoiceContext choiceContext, CardPlay cardPlay)
    {
        var modeOptions = new List<CardModel>
        {
            ModalChoice.CreateOption<ProtoIttoSuperlativeSuperstrengthEitherModeA>(Owner),
            ModalChoice.CreateOption<ProtoIttoSuperlativeSuperstrengthEitherModeB>(Owner),
        };
        var modeIndex = await ModalChoice.SelectMode(choiceContext, Owner, modeOptions);
        ModalChoice.RecordChoice(this, modeIndex, new[] { "Deal 14 damage", "Gain 6 Block" }[modeIndex]);
        if (modeIndex == 0)
        {
            ArgumentNullException.ThrowIfNull(cardPlay.Target, "cardPlay.Target");
            await DamageCmd.Attack(SpotlightSystem.PrintedDamage(this, 14m))
                .FromCard(this, cardPlay)
                .Targeting(cardPlay.Target)
                .WithHitFx("vfx/vfx_attack_slash")
                .Execute(choiceContext);
        }
        else
        {
            await CreatureCmd.GainBlock(Owner.Creature, new BlockVar(SpotlightSystem.PrintedBlock(this, 6m), ValueProp.Move), cardPlay);
        }
    }

    protected override void OnUpgrade()
    {
        // R24: NO upgrade path -- no ratified delta in klee-upgrades.yaml. Flagged in manifest.
    }
}

/// <summary>Mode 0 of proto_itto_superlative_superstrength_either. A face for the choose-a-card screen;
/// never played, never in a pile, never a reward -- but a POOL MEMBER, via
/// the generated ModalOptions roster the character's off-pool list carries.
/// EB-150: a card in no pool takes CardModel.Pool through MockCardPool, which
/// throws inside the screen's _Ready and soft-locks the turn.</summary>
public sealed class ProtoIttoSuperlativeSuperstrengthEitherModeA : ModalOptionCard
{
    public override List<(string, string)>? Localization => new()
    {
        ("title", "Deal 14 damage"),
        ("description", "Deal 14 damage"),
    };
}

/// <summary>Mode 1 of proto_itto_superlative_superstrength_either. A face for the choose-a-card screen;
/// never played, never in a pile, never a reward -- but a POOL MEMBER, via
/// the generated ModalOptions roster the character's off-pool list carries.
/// EB-150: a card in no pool takes CardModel.Pool through MockCardPool, which
/// throws inside the screen's _Ready and soft-locks the turn.</summary>
public sealed class ProtoIttoSuperlativeSuperstrengthEitherModeB : ModalOptionCard
{
    public override List<(string, string)>? Localization => new()
    {
        ("title", "Gain 6 Block"),
        ("description", "Gain 6 Block"),
    };
}
